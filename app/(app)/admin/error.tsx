'use client';

// Keeps the app chrome mounted when a page in this section throws — see
// components/app/section-error.tsx for why each chrome-mounting section needs
// its own boundary.
import { SectionError } from '@/components/app/section-error';

export default SectionError;
