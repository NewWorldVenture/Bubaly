# Market Domination Strategy — Verified Audit (2026-09-07)

Audit of `Bubaly_Market_Domination_Strategy_2026.docx` (40 moves, 8 public-site weaknesses, 12 north-star metrics) against the code at `main` `90627d5`.
Method: 7 self-verifying finders (each item records reachability, persistence, tests and mock risk with `path:line` evidence), refuting verifiers over every claim of `exists`, and a completeness critic. Status vocabulary: **exists** = reachable UI + persisted data + server logic + a test, all four; **partial** = some; **mock** = present but inert/static/error-swallowing; **missing**.

Status histogram: ?: 1, exists: 6, missing: 9, mock: 1, partial: 43

## Verifier corrections

- **M13** — stands: All four conditions confirmed by reading the code. Reachable: app/(app)/dashboard/inventory/page.tsx:8 calls requireFeature('/dashboard/inventory') and renders InventoryModule; navigation.ts:147 lists 'Home Inventory' (minLevel 0) while feature-catalog.ts:68 sets 'basic', so a free-tier user sees the nav entry but lib/supabase/auth.ts:222-224 redirects them to /dashboard/billing?upgrade=1 — reachable for Basic+, a nav/tier mismatch for Free (not a mock). Persisted: 0242_home_inventory.sql:11-68 creates home_locations/inventory_items/inventory_moves with is_family_member RLS FOR ALL; inventory-module.tsx:45-57 reads all three via useRealtimeQuery, :470-478 updates inventory_items.location_id then inserts inventory_moves (moved_by, reason, created_by), :327-335 uploads to storage bucket family-media. Server logic: app/api/ai/insights/route.ts:151-157 loads home_locations/inventory_items/inventory_moves server-side and lib/ai/insights.ts:659-684 builds the 'where is X' prompt with the room › container path; route wraps calls in withAiRequest (lib/ai/observability) so ai_requests logging is via that helper, not a direct insert. Pure NL search: lib/inventory/finder.ts:96-131 strips request phrasing and requires every token to match. Tests: tests/inventory-finder.test.ts:41-56 exercises searchItems (name/brand/serial/tags/location-path matching, multi-token, empty), plus locationPath/tree; tests/inventory-module-write-boundary.test.ts:38-45 is a source-text guard (move ordering + history insert + photo upload error surfacing), not a behavioural test. Mock risk is low: reads go through useRealtimeQuery with an ErrorState on failure (inventory-module.tsx:115), not swallowed. Auditor's gap stands: no last_confirmed signal, no lib/ai/tools inventory tool (grep inventory_items in lib → only insights.ts, published-tables.ts, database.types.ts), photos never analysed.
- **M14** — stands: All four conditions confirmed. Reachable: app/(app)/dashboard/moving/page.tsx:8 requireFeature('/dashboard/moving') → MovingModule; navigation.ts:149 'Move Planner' minLevel 1, feature-catalog.ts:71 'basic'; the date-change UI is mounted at moving-module.tsx:352 (<MoveDateRecalculation>) and components/modules/move-date-recalculation.tsx:78 fetches POST /api/moving/recalculate. Persisted: 0245_move_planner.sql:13/39/60 create moves, move_tasks, move_boxes with RLS FOR ALL is_family_member (lines 89-91); 0271 creates move_date_recalculations (line 9) and the SECURITY-scoped RPC move_recalculate_date (line 79, grants at 390-392); moving-module.tsx:50-143 reads/writes all three tables (task generation insert at :98 from planTasks). Server logic: app/api/moving/recalculate/route.ts validates body, role, membership, feature tier from app_settings and calls db.rpc('move_recalculate_date') with preview/apply + idempotency; lib/moving/planner.ts:49-139 derives every dated task from move_date across utilities/address/school/pets/finance/packing/settling with has_kids/has_pets/is_renting_out/move_kind filters; insights.ts:731-740 + insights route case 'moving' (route.ts:175) ground the AI on real rows. Tests: tests/moving-planner.test.ts:13-104 (template keys, filtering, dating/dedupe, timeline, budget, boxes), tests/move-date-recalculation-api.test.ts:111-317 (method, preview/apply contract, idempotent replay, body limits, auth, feature gating), tests/move-date-recalculation-ui.test.ts:230+, tests/move-date-recalculation.test.ts, tests/moving-module-write-boundary.test.ts. Mock risk noted by auditor is accurate: it is a deterministic checklist; the 'AI' is a read-only insight; no move intent in lib/ai/planner/templates and no moving.* tools (grep in lib/ai/tools hits only a comment in calendar.ts:11); life-events 'moving' template (lib/life-events/templates.ts:53) is a separate smaller checklist. The strategy's core — move date orchestrating the plan — works end to end, so 'exists' holds.
- **M18** — stands: All four conditions confirmed. Reachable: navigation.ts:166 '/display' Kitchen Display minLevel 1; feature-catalog.ts:105 F('kitchen', …, 'basic', '/display'); page.tsx:214 requireFeature('/display') with unstable_rethrow so redirects propagate and transient failures render DisplayReconnect (215-225). Persisted: page.tsx:58-80 reads family_members, calendar_events (3 windows), chore_assignments, meal_plans, grocery_items (+count), reminders, notes, family_recipes, display_layouts (tiles, settings), family_photos; display-grid.tsx:541-547 upserts display_layouts {family_id, tiles, settings, updated_by} onConflict family_id with the error toasted (not swallowed); 0026_display_layouts.sql:8-35 table + four RLS policies; 0200_display_settings.sql:15-16 settings jsonb. Server logic: force-dynamic server page loadDisplay with per-query error logging (page.tsx:84-90), serialization firewall and emptyDisplay backstop (231-237), AutoRefresh 120s. Tests: tests/display-render.test.ts (SSR harness rendering DisplayShell with bad data shapes), tests/display-tiles.test.ts:10+ (resolveTiles on saved layouts), tests/display-ambient.test.ts:10-68 (day-part + normalizeSettings), tests/display-server-safety.test.ts:63-76 (RSC boundary), plus display-recover/imagery/name-firstname. Mock risk accurately stated by auditor: widget reads are best-effort (console.error then empty tile), so an RLS/table failure looks like an empty family; no test exercises the display_layouts save path (grep display_layouts tests/display-* → none); no e2e touches /display (grep tests/e2e → none). Layout save error is surfaced via toastError, so it is not a silent no-op. Status 'exists' holds.
- **M1** — stands: All four conditions confirmed by reading the code. REACHABLE: /home is in PRIMARY nav (lib/constants/navigation.ts:260, minLevel 0) and renders <AskBar/> (app/(app)/home/page.tsx:408) -> AskBubaly (components/home/ask-bar.tsx:9-13) which posts via submitAIRequest to /api/ai/requests (components/concierge/ask-bubaly.tsx:21); /dashboard/concierge is in nav at navigation.ts:85 (minLevel 1); the run page /dashboard/concierge/runs/[id] is reached via runPagePath (lib/ai/chat-request.ts:145, pushed by clarification-card.tsx:61) and Home Working-on links (components/concierge/working-on.tsx:75-76). Access gating is assertAIAccess + isAIConfigured (run-actions.ts:83-88; requests/route.ts:45-57). PERSISTED: the migration is supabase/migrations/0250_ai_runtime_core.sql (not '0250_ai_runtime.sql' as cited) — ai_requests (:43), ai_plans (:98, unique request/version :115), ai_plan_steps (:127), family_automation_runs idempotency index (:238-239), ai_run_events (:254), ai_tool_calls with uq (family_id, idempotency_key) (:289-320), claim_ai_runs RPC (:426, service_role only :471-472). intake.ts:194-208 creates ai_requests via caller client and derives runIdempotencyKey from clientRequestId. SERVER LOGIC: planner index.ts:1-27 flow; schema.ts:56-66 dependency graph fields; PLAN_STEP_TYPES excludes replan (schema.ts:32); validate.ts:536-545 per-step approvalRequired from dryRunGate/prepare/model flag; executor runGraphWith blocks dependents (:430-441), batches with Promise.allSettled (:456-457), parks awaiting_approval (:495-502); approval gate opens approval_requests row before work (:753-764); replan blocked when port.replan null (:1050-1058) and createExecutorPort sets replan: null (:1233), while replanRun exists unwired in planner/index.ts:509. Cron worker app/api/cron/ai-runs/route.ts claimRuns->runGraph (:5-6, 51-57), dispatched by scripts/cron-dispatch.mjs:48 and vercel.json:8. TESTED: run-executor 35 cases via fake port (tests/run-executor.test.ts:171), planner-validation 41, planner-schema 12, planner-templates 14, run-states 24, api-ai-runs 17, intake-answer-cas 7, run-controls 13; run-actions-gates has 5 cases (auditor said 6 — trivial miscount). MOCK lens: the scripted provider only engages when AI_PROVIDER_STUB=1 and never on VERCEL_ENV=production (lib/ai/provider-stub.ts:100-104), so the stub risk is confined to dev/CI. The claimed gap is accurate.
- **M35** — stands: All four conditions confirmed. REACHABLE: cron /api/cron/ai-runs gated by hasCronAuthorization (route.ts:36-39), scheduled in scripts/cron-dispatch.mjs:48 and vercel.json:8; run detail page (concierge/runs/[id]/page.tsx:56-75 via loadRunDetail); /dashboard/family-automation lists pending/finished family_automation_runs (page.tsx:56-58); Home Working-on (components/concierge/working-on.tsx:26-36); /dashboard/sync/conflicts reads sync_conflicts (page.tsx:26). PERSISTED: ai_tool_calls unique (family_id, idempotency_key) at 0250_ai_runtime_core.sql:289-320 with reserve/duplicate/takeover semantics in lib/ai/tools/execute.ts:227-277 (succeeded => return prior outputs :246; stale takeover with state-guarded update :255-268); 0256_idempotency_keys.sql:30-48 adds idempotency_key + partial unique indexes to calendar_events, family_reminders, todo_items, chore_assignments, meal_plans, grocery_items; makeKey stable sha256 (lib/services/idempotency.ts:55-57); claim_ai_runs lease RPC (store.ts:566-587), claimRun with lease_owner (:601-635), heartbeatRun (:655); 0263_dead_letter_reconcile.sql:27-56 rewrites claim_ai_runs recovery to reconcile run, legacy status, steps, ai_requests and timeline; sync_conflicts inserted by lib/sync/engine/generic.ts:174,332 and google.ts:164. SERVER LOGIC: failStep bounded by retry_count < max_retries and only when retryable (executor.ts:839-868); jittered backoff with out_of_budget parking (:943-953); verify=false downgrades step to partially_completed while dependents proceed (:900-907); provider fail-loud on schema fallback (provider.ts:303-306) and retry only when executed===0 (:466); lib/ai/retry.ts read-only policy (:1-25); briefing deterministic fallback gated on isAIConfigured (briefing/route.ts:297). TESTED: tests/run-executor.test.ts 35 (fake port), tests/tool-execute.test.ts exercises the ai_tool_calls duplicate-key path with a fake ledger returning 23505 (:85-92, 232), tests/ai-retry.test.ts 12, tests/sync-conflict.test.ts 10, tests/run-controls.test.ts 13, tests/run-states.test.ts 24, tests/allowance-cron-idempotency.test.ts 3, tests/onboarding-idempotency.test.ts 3. Counts corrected: tests/0256-idempotency.test.ts has 3 cases (not 4) and tests/0263-dead-letter-reconcile.test.ts has 9 (not 10) and is a SQL-text assertion over readFileSync (test:14-16), not a behavioural test — the behaviour of the dead-letter path is therefore only text-pinned, which is the one honest caveat. The claimed gap (no single family-wide run history list; app/(app)/dashboard/concierge/ has only page.tsx and runs/[id], no runs index) is accurate.
- **M22** — stands: All four conditions confirmed. REACHABLE: /dashboard/knowledge and /dashboard/playbook are in the nav catalogue at lib/constants/navigation.ts:212-213 (minLevel 0); Settings (:176) mounts AI settings via components/modules/settings-module.tsx -> components/settings/ai-settings.tsx which renders <AIMemoryPanel canManage/> at :258; /dashboard/family-automation at :217 (minLevel 2); /dashboard/family-memory is a redirect to /dashboard/memories (family-memory/page.tsx:9-11) as claimed. PERSISTED: KnowledgeBaseModule reads family_facts live (knowledge-base-module.tsx:53-54) and calls saveFactAction/forgetFactAction/setFactPinnedAction (:15,75,91) which route to updateFact/rememberFact(source:'user')/forgetFact (knowledge/actions.ts:47-110); settings ai-actions.ts:14,111,140 call forgetFact and clearAiMemory; playbook-actions.ts:136,173 write family_playbook_suggestions and accept writes family_facts (:143-156); routines panel reads/deletes routine_templates and routine_template_items (routines-panel.tsx:67-72,175,300-305). SERVER LOGIC: lib/services/memory/index.ts two-table policy (:1-45), rememberFact :130, memoryEnabled gate :170, recallFacts :399, confirmFact :452, forgetFact :543, clearAiMemory :599, updateFact :673; lib/ai/tools/memory.ts refuses sensitive (:105), routes to inbox unless asked_for (:123-127), recall respects memoryEnabled (:176), forget (:212). TESTED: tests/service-memory.test.ts, memory-write-path, memory-opt-out, memory-expiry-lifecycle exercise clearAiMemory/forgetFact/updateFact/confirmFact (grep confirmed); family-facts, family-knowledge, memory-expiry-review, playbook-learn, tool-meals-memory-messages all exist. UNDERCLAIM on the residual gap: the digital twin is NOT without a UI — app/(app)/dashboard/family-digital-twin/page.tsx:27 reads family_digital_twin_profiles and lib/family/actions.ts:20,37 whitelists strengths/notes/ai_insights/stress_baseline/preferences/responsibilities as user-writable. What autopilot infers is written into the `metadata` column (lib/autopilot/scan.ts:67,163-164 via computeMemberTraits in lib/autopilot/twin.ts:25), and `metadata` is absent from that WRITABLE list, so the true residual is narrower: the inferred trait blob specifically cannot be viewed or reset.

## Critic

**Suspicious claims (spot-checked in code):**
- **M16** → missing (adjacent care_log + read-only grandparent portal only): Read components/modules/care-module.tsx:68-136 — every read/write is care_log only (an elder-care timeline). lib/constants/roles.ts caregiver/guest are enum labels with no RLS or assigned-areas storage (audit's own grep confirms). Nothing implements 'permissioned care circles' for appointments/tasks/documents/transport/handoffs: no circle entity, no scoped access, no handoff record. The adjacent care_log does not exercise any part of the strategy behaviour, so 'partial' overstates it.
- **X1** → partial for the definition; the family-facing surface is mock-pattern until safeCount surfaces errors and loadTimeSaved has a test: lib/metric/time-saved-server.ts:8-11 safeCount returns 0 on ANY read error and the banner hides at 0 (computeTimeSaved.show). That is exactly the vocabulary's mock pattern ('a read that swallows its error and renders empty'); the loader has no test (only the pure compute is tested) and the metric is never stored. The definition + pure compute are real, but the shown surface cannot be distinguished from a broken read.
- **M38** → partial (pipeline real) — /dashboard/intelligence surface is mock-pattern; cron path untested: app/(app)/dashboard/intelligence/page.tsx:12-13 cnt() swallows read errors to 0 and the page reads network_aggregates which, per the audit's own evidence, is empty below the 100-family launch gate. So the only customer-visible surface renders 'no insights' identically for (a) not enough families, (b) opted-out, (c) RLS/read failure. The pipeline (consent, contributions, cron) is real; the visible surface is mock-pattern.
- **M26** → partial overall; concierge-calls placement = mock (fake 'calling' state): app/api/concierge-calls/place/route.ts:55-66 flips queued rows to status 'calling', increments attempts and returns {ok:true, placed:N, providerReady:true} while the comment admits the provider dial is not triggered. This is a persisted claim that a real-world action (a phone call) was placed when nothing happened — a live violation of 'never claim a real-world action succeeded without provider confirmation'. The marketplace and contractor book are real, so M26 stays partial, but the concierge-calls sub-surface should be labelled mock and the proposed book_service outreach step must not reuse it as-is.
- **M15** → partial (unchanged) — migration confirmed required, not conditional: Audit hedged 'verify in 0070 before adding disruption_status'. Verified: supabase/migrations/0070_vacations.sql:134-155 (vacation_flights) and :183-203 (vacation_lodging) have no status/delay column — only booked boolean, times, cost. So the disruption build definitely needs the migration; needs_migration=true is correct and should not be treated as optional.
- **M4** → partial (confirmed); W3's 'first brief saved you N minutes' from home_briefs is not backed by persisted data: Confirmed: lib/briefing/store.ts:19-45 saveBrief always returns denied, loadBrief always ok(null), markDelivered false (0262 quarantine); app/api/ai/briefing/route.ts has no role/isManager gating (only member roles interpolated into the prompt at :90,:165). The buildBrief pipeline and tests are real, so 'partial' holds, but note home_briefs.time_saved_minutes is therefore never written — X1/W3 proposals that cite home_briefs.timeSavedMinutes as a data source are citing a dead column.
- **M21** → partial (confirmed; actionable half is entirely absent): Confirmed components/modules/notifications-module.tsx:114-138: a row click only marks read; related_type/related_id never become a link or action; notification-bell.tsx links only to /dashboard/notifications. notify() plumbing, dedupe and quiet hours are real and tested, so partial is honest — but the strategy behaviour ('every notification is actionable') is 0% implemented, so the effort should be treated as building the feature, not finishing it.
- **M2** → partial (confirmed); /dashboard/inbox alone would be 'mock' for this strategy item: Confirmed: grep submitRequest|todo_items|calendar_events|paperwork_items|reminders over lib/contact-center and app/api/contact-center returns nothing; app/(app)/dashboard/inbox/page.tsx renders InboxModule (manual family_communications log). Contact-center ingestion is real but Family+ only and storage-only. Partial is fair, but the /dashboard/inbox surface itself is a manual log wearing an inbox name (mock-risk correctly noted).
- **M10** → partial (confirmed): Confirmed: groceries.addFromMealPlan is referenced only from lib/ai/tools/meals.ts and the plan-meals/plan-week planner templates; components/modules/meals-module.tsx has no add-from-plan action (only toggling existing grocery_items via setGroceryItemCheckedAction :196-201). The loop closes only through an AI run, so a non-AI family never sees it.
- **M34** → partial (confirmed): Confirmed: components/modules/life-events-module.tsx and app/(app)/dashboard/life-event-actions.ts contain no suggest/recommend/todo/reminder/calendar logic (only icon maps for item kinds); six templates persist to life_event_plans/_items. Checklist persistence is real; 'intelligence' and orchestration are absent.
- **M3** → partial (confirmed): Confirmed lib/family/actions.ts:24-25 still lists family_knowledge_nodes/edges write columns, so two graphs coexist. Canonical graph_entities projection is real and tested. Partial holds.
- **M6** → partial (confirmed); migration scope can shrink: Confirmed lib/home/today.ts:294-318 lists only completed/partially_completed runs plus agent_activity rows (capped at 6) and components/concierge/completed-by-bubaly.tsx renders title/detail/href only. Note ai_tool_calls ALREADY has resource_table/resource_id (0250:25-26), so the proposed created_refs jsonb column is partly redundant — undo can key off existing columns for single-row writes.
- **W7** → partial (confirmed): Confirmed: lib/marketing/reputation.ts is imported only by app/(app)/admin/marketing/reputation/actions.ts; lib/marketing/platform.ts merely mentions testimonials in prompt guardrails. No public reader of testimonials/case_studies exists; the families count is the only live social proof.
- **M18** → exists (with documented error-swallowing; add a visible 'data unavailable' tile before marketing it as W6): Claimed 'exists'. app/(app)/display/page.tsx:84-92 logs and continues on any widget query error, and :232-240 renders an EMPTY display on fatal load error (only requireFeature failure gets the reconnect screen). That is the vocabulary's error-swallowing pattern, though deliberate for a kiosk and covered by display-server-safety tests. Keep 'exists' but the audit should state that an RLS regression on any of 12 tables is invisible on the wall, and there is no e2e.

**Cross-cutting primitives to build once:**
- HANDLED / TIME-SAVED ACCOUNTING — build ONCE: a single countHandledThisWeek + loadTimeSaved (lib/metric/time-saved-server.ts) with error-aware reads, consumed by M4 (brief handled count), M6 (ledger), X1, X4, X11, M40 (UpgradeModal numbers), W2/W3 (public + pricing proof), M18 (handled tile), M30 (first-session card). Today there are three inconsistent definitions (time-saved-server, autonomy/loop autopilotStats, home/today mergeCompletedByBubaly) plus the first-brief opportunity estimate.
- family_metric_weeks SNAPSHOT TABLE — one migration serving X1 (time_saved_minutes), X2 (auto_captured_pct), X4 (handled_actions), X6 (signal_precision), X9 (weekly-active flag), X11 (value_ratio), written by the existing weekly-digest cron; do not let each X item ship its own column migration.
- public_stats() RPC EXTENSION — one CREATE OR REPLACE for W2 (ai_handled_30d), W7 (avg_first_brief_minutes, handled_30d) and W3's optional aggregate; must exclude the demo family in the function body.
- family_inbox_messages LINK COLUMNS — one migration reconciling M2 (request_id, handled_at, channel 'share'/'scan'/'paste'), M20 (request_id, paperwork_item_id, channel 'form') and M9 (member_id, linked_type, linked_id, sub_intent). Three specs today, one table.
- INBOUND → BUBALY ADAPTER — one lib/inbox/handle.ts (message body → submitRequest intent 'capture' / runAction with trust gating, stamping request_id) reused by M2 'Handle it', M20 route-into-bubaly, M9 school/sports front desk, M27 partner events (should land as inbox rows needing approval, not direct service writes), and W8 entity resolution (lib/graph/resolve.ts called from the same adapter).
- OUTCOME / WORKFLOW TEMPLATE RUNNER — one 'register template + intent + launch from a UI button → ai_requests' primitive (M25's outcomes-launcher POST) reused by M1 chief_of_staff, M14 plan_move, M34 life_event, M26 book_service, M15 disruption follow-up, M30 'Do one thing now', W1/W5 hero outcomes. lib/outcomes/launcher.ts + lib/ai/planner/templates/index.ts are the seams; build the launch path once.
- NEEDS/DECISIONS SOURCE REGISTRY — lib/home/needs-build.ts buildHomeNeeds is the single ranked-decisions engine for M5 (needs-you page), M4 (brief decisions slice), M21 (notification → action mapping should reuse the same href/action resolvers), M20/M9 (inbox rows as needs). Add sources once, not per item.
- GRAPH PROJECTOR + PROVENANCE — one change to lib/twin/project.ts / project-server.ts (+ 0134 dirty triggers) covers M3 (events/obligations/preferences + source/confidence), M12 (home assets/warranties/projects), X7 (completeness score computed from the same TwinSnapshot), W8 (resolver over graph_entities names). Retire the legacy family_knowledge_nodes writes in the same PR.
- SAVINGS LEDGER — M32's savings_ledger is the money-side twin of the handled ledger and is the ONLY data source for M17 (duplicate_avoided), X11 (realizedSavingsCents), W3 ('savings found'), M11 (CFO 'saved so far'), M10 (grocery swap). Build it before any surface quotes a money figure.
- HOUSEHOLD SEARCH — M33's search_household RPC + lib/services/search should also back M13 inventory.find, M17 owned-duplicate check, and the command bar 'record' results; don't add per-module searches.
- HASHED-CREDENTIAL HELPER — M27 partner_api_keys, M18(c) display_devices pairing codes, and the existing lib/sync/feed-token.ts share one prefix+sha256+timingSafeEqual pattern; extract once.
- TIME-LIMITED DELEGATION PRESETS — trust_delegations presets (babysitter tonight / grandparent this week) serve M23 (access & sharing panel), M28 (caregiver invite → delegation on accept), M16 (care circle scopes could be delegations + subject member rather than a new RLS model).
- MARKETING CLAIMS-CONTRACT SOURCES — one extension of tests/marketing-claims-contract.test.ts to cover security/page.tsx, trust/page.tsx, ai-showcase.tsx, hero-outcomes.ts, display/page.tsx, display-compare.ts serves W1, W2, W4, W6, W8, M36.
- ERROR-AWARE COUNT HELPER — safeCount-style helpers that return 0 on error exist in lib/metric/time-saved-server.ts:8, app/(app)/dashboard/intelligence/page.tsx:12 and app/(app)/display/page.tsx; replace with one helper returning null|number so X1/X4/M38/M18/M40/W3 surfaces can distinguish 'nothing' from 'read failed'.

**"What I would NOT do" risks in the proposed builds:**
- M26 (book_service outreach) — reusing app/api/concierge-calls/place, which today marks rows 'calling' and reports placed=N without dialing, would persist a claim that a provider was contacted. Violates 'never claim a real-world action succeeded without provider confirmation'; the queue must stay 'queued' until a provider webhook confirms, and the existing route should be fixed first.
- M26 (service layer, XL) + M17 (market recommendations) — 'compareQuotes'/'market suggestions' risk paid bias: 0065_affiliates (commission partners) and marketplace stores exist; ranking must exclude any affiliate/commission signal and the tests should pin that. Also the strongest 'module because competitors have it' candidate — an XL build with no persisted family demand signal.
- W2 (existing) — ai-showcase.tsx renders a 'Live' badge on a hardcoded DEMOS array; already a false real-time claim on the public site. The proposed relabel must ship before any new public proof.
- M36/W4 (existing) — app/(marketing)/security/page.tsx claims SOC 2, HIPAA, MFA, biometrics, HSM, regions, 99.99% SLA that the product lacks; every day this stays is a 'claim without evidence'. W4's claims-contract extension should land before M36's new controls are marketed.
- M27 (partner API) — proposed apply path writes calendar/tasks directly via services under systemScopeForFamily, bypassing preview/approval; a school/team pushing structured events is an 'opaque action' unless it lands in family_inbox_messages/needs for a one-tap accept or is trust-gated like AI tool writes.
- M7 (learned policies) — expanding autonomy from approval history must keep the policy narrow (tag = one tool), show evidence, and require an explicit accept; auto-inserting trust_policies rows from a cron would be an opaque autonomy expansion.
- X11 / W3 / M40 — 'worth ≈$X vs your plan' and 'Family+ saves N hours' rest on hard-coded minute weights (5/4/2) and an assumed $25/h; presenting them without an 'estimate' label optimises perceived time-in-app value rather than work removed, and risks marketing a fabricated number. Labeling tests are required, not optional.
- M39 (reward via Stripe balance) — status 'rewarded' must be written only after the Stripe API confirms the credit; a webhook replay could double-credit.
- M6 (undo) — must verify the delete (records_exist check inverse) before marking a step 'undone'; otherwise the ledger claims a reversal that may not have happened.
- X7 (graph completeness 'add X' card) and M22 traits — nudging families to fill graph slots edges toward 'forced manual graph maintenance'; keep it a projection-driven score with optional prompts, never a gate.
- M33 /dashboard/search, M5 /dashboard/needs-you, M26 /dashboard/service-requests, M35 /dashboard/concierge/runs, M36 settings panels, M38 admin benchmarks — each adds a route; acceptable only because none touches the shared sidebar (standing rule). Watch that follow-ups do not turn them into nav items ('not every capability a nav item').
- W5 / W2 / M19 — MARKETING_NAV currently has 'AI Assistant' and the /ai page; W5's 'See it work' → /ai and M19 'Voice Everywhere' keep AI as the headline. Keep outcome labels primary and AI as the mechanism ('do not market AI as the primary benefit').
- M37 / M18(c) / W6 — a 'certified devices program' and display pairing table are content + optional; fine under 'no proprietary hardware first' as long as M37 stays a compatibility list rather than a hardware SKU.
- M4 (morning delivery via notify) and M21 (priority 'now') — pushes must respect quiet hours and produce actionable rows; a daily push that only says 'your brief is ready' optimises time in app.
- M20/M2 (auto-reply email/SMS exists today) — an outbound reply is a real-world action; the current route auto-replies without approval or evidence in the run ledger (recordOutboundMessage only). Routing inbound into the planner should also bring auto-replies under the trust gate.

## Migration asks (owner approval required before any is added)

- M2 + M20 + M9 (ONE migration): family_inbox_messages add request_id uuid → ai_requests, handled_at timestamptz, paperwork_item_id uuid → paperwork_items, member_id uuid, linked_type text, linked_id uuid, sub_intent text; extend channel CHECK with 'share','scan','paste','form'. RLS unchanged.
- M3: graph_entities/graph_edges add source text default 'projection', confidence smallint, observed_at timestamptz; extend kind CHECK with 'asset','obligation','preference','provider'; add calendar_events/bills/paperwork_items/family_facts to the 0134 mark_model_dirty trigger list. No RLS change.
- M6: ai_plan_steps add undone_at timestamptz, undone_by uuid; ai_run_events event_type add 'undone'; ai_tool_calls created_refs jsonb ONLY for multi-row writes (resource_table/resource_id already exist). Service-role writes only.
- M35: CREATE OR REPLACE claim_ai_runs (0263 dead-letter path) to mark orphaned ai_tool_calls rows 'unknown' when a run is dead-lettered. No schema/RLS change.
- M10: grocery_lists add budget_cents int, grocery_items add estimated_price_cents int (nullable). RLS unchanged. (Calendar-awareness, substitutions, handoff can ship first without it.)
- M21: notifications add priority text not null default 'now' CHECK in ('now','digest') + index (user_id/family scope, priority, is_read). No RLS change.
- M24: new table ai_context_access_log(request_id, family_id, slice, table_name, row_count, sensitive bool, created_at) with manager-read RLS, insert from the context builder (service/invoker). Activity tab can ship first without it.
- M36: new table account_deletion_requests(family_id, requested_by, requested_at, execute_after, status) manager-only RLS; plus Supabase project setting: enable MFA/TOTP (no schema).
- M12: home_assets add replaced_by_asset_id uuid → home_assets, retired_on date; optional inventory_items.asset_id uuid → home_assets. Existing RLS covers.
- M15 (CONFIRMED REQUIRED — 0070 has no status column): vacation_flights and vacation_lodging add disruption_status text (CHECK on_time/delayed/cancelled) and delay_minutes int.
- M16: new tables care_circles, care_circle_members (scopes text[], can_write), care_handoffs + is_circle_member(circle_id, scope) helper + scoped SELECT policies on calendar_events, todos, documents, rides, care_log.
- M32: new table savings_ledger(id, family_id, source CHECK subscription_cancelled|warranty_claim|grocery_swap|duplicate_avoided|bill_negotiated|other, amount_cents, cadence one_time|monthly, evidence_table, evidence_id, note, recorded_by, created_at) + is_family_member RLS + index (family_id, created_at); add to the 0275 money-write sweep.
- M18(c) optional: new table display_devices(family_id, code_hash, label, last_seen_at, revoked_at) + RLS + SECURITY DEFINER redeem RPC. Ask/handled tiles and /display/setup need no migration.
- M26: new tables service_providers, service_requests (status queue→quoting→booked→done, linked maintenance_task/trip/care), service_quotes with is_family_member RLS and a circle-share policy modelled on 0176.
- M27: new tables partner_api_keys(family_id, partner_name, key_hash, key_prefix, scopes text[], created_by, last_used_at, revoked_at) and partner_events(family_id, key_id, idempotency_key, kind, payload jsonb, status, result_ref) with unique(family_id, idempotency_key); manager read/revoke RLS, service-role hash lookup.
- M23: new RLS on documents/notes/journal_entries honouring member_id (owner or can_manage_family) + SQL helper has_active_delegation(family_id, domain) consulted by sensitive-table read policies. No new tables.
- M33: new SECURITY INVOKER function public.search_household(family_id, q, limit) (search_path pinned) UNIONing pg_trgm similarity over documents, inventory_items, trips/vacations, bills, home_warranties/renewals, decisions, calendar_events, notes, family_facts + GIN trigram indexes on their title/name columns. No new tables; underlying RLS applies.
- W2 + W7 + W3 (ONE migration): CREATE OR REPLACE public.public_stats() adding ai_handled_30d bigint and avg_first_brief_minutes numeric (SECURITY DEFINER, anon-executable, excludes the demo family).
- X1 + X2 + X4 + X6 + X9 + X11 (ONE migration): new table family_metric_weeks(family_id, week_start date, time_saved_minutes int, handled_actions int, auto_captured_pct numeric, signal_precision numeric, value_ratio numeric, weekly_active bool, created_at) unique(family_id, week_start), family-scoped SELECT RLS, service-role writes from the weekly-digest cron.
- X8 + X9: ALTER TABLE family_members ADD COLUMN last_active_at timestamptz + index (family_id, last_active_at desc); service-role updates only, throttled from requireUserContext.
- X2 optional follow-up: created_via text on calendar_events/todo_items/bills/family_reminders so manual captures are stamped explicitly.

## All 60 items

### M1 — Chief of Staff Outcome Engine — **exists**
- Reachable: /home Ask bar (components/home/ask-bar.tsx -> AskBubaly -> POST /api/ai/requests or askBubalyAction); /dashboard/concierge (ConciergeModule) and /dashboard/concierge/runs/[id]; AI access gated by assertAIAccess/requireUserContext, no plan gate on intake itself
- Persisted: ai_requests, ai_plans, ai_plan_steps, family_automation_runs (run row with lease/run_after/attempt), ai_run_events, ai_tool_calls, approval_requests, family_ai_recommendations; claim_ai_runs RPC
- Tested: tests/run-executor.test.ts, tests/planner-validation.test.ts, tests/planner-schema.test.ts, tests/planner-templates.test.ts, tests/run-states.test.ts, tests/api-ai-runs.test.ts, tests/intake-answer-cas.test.ts, tests/run-actions-gates.test.ts, tests/run-controls.test.ts
- Mock risk: Planner needs a configured provider (isAIConfigured) — with none, requests fall to the stub provider (lib/ai/provider.ts:541) so plans in dev may be scripted. Executor tests use a fake port, not Supabase. Replan branch always blocks (executor.ts:1050), so a plan that needs mid-run re-planning ends partially_completed.
- Gap: Behaviour is real end to end, but (a) the re-planning port exists in lib/ai/planner/index.ts (replanRun) and is never wired into the executor/cron, so any plan that needs a mid-run decision from the model blocks; (b) cross-domain breadth is bounded by INTENT_TOOL_DOMAINS and the 9 fixed workflow templates (lib/ai/planner/templates) — a truly novel broad request falls to `other` with no template skeleton; (c) 'safe work first' relies on the trust dry-run per step, which is correct, but there is no explicit 'auto-execute low-risk retrieve/act steps before the first approval' ordering guarantee beyond the dependency graph.
- Build (M, migration: no): Wire the existing replanRun into the executor port (cron/ai-runs and intake continuation) so replan steps run instead of blocking; add a 'chief_of_staff' intent that gets the full tool catalogue plus a generic template that always emits parallel read steps first, then act steps grouped by domain with approvals only where the trust dry-run demands; add one end-to-end planner->executor test proving a multi-domain request yields a graph with independent reads and a single approval gate.
- Evidence: `lib/ai/planner/schema.ts:56-66` PlanStepSchema carries depends_on, condition, approval_required, verify — the model emits a dependency graph, not a list; `lib/ai/planner/schema.ts:29-34` `replan` deliberately NOT offered to the model because the executor has no re-planning port wired; `lib/ai/planner/index.ts:1-27` planRequest: loads trust inputs once, builds prompt from intent tool catalogue + template + context, validates, persists plan+run (savePlan/createRun), or returns recommendation/clarification/answer; `lib/ai/planner/prompts.ts:35-58` INTENT_TOOL_DOMAINS spans calendar/education/tasks/chores/scheduling/meal_planning/shopping/travel/home_maintenance/documents/finances/messaging; `other` gets the broad set; `lib/ai/planner/validate.ts:536-545` approvalRequired computed per act step from the trust engine dry-run (require_approval / prepare behaviour) — only necessary decisions are gated; `lib/ai/runs/executor.ts:410-503` runGraphWith: blocks dependents of failed steps, runs ready batches with Promise.allSettled, parks the run in awaiting_approval; `lib/ai/runs/executor.ts:753-764` approval gate opens an approval_requests row and parks the step; `lib/ai/runs/executor.ts:1050-1058` replan step is blocked with 'Re-planning is not available for this run' when port.replan is absent; grep of cron/ai-runs, intake.ts, continue.ts finds no replan wiring

### M2 — Universal Household Inbox — **partial**
- Reachable: /dashboard/inbox (Communications Hub, requireFeature), /dashboard/contact-center (requirePlanLevel(2)), /capture, /dashboard/scan, /dashboard/paperwork, /dashboard/voice — five separate surfaces, no single inbox
- Persisted: family_communications, family_inbox_messages, paperwork_items, calendar_events/todo_items/grocery_items/notes (capture), concierge writes via runAction
- Tested: tests/capture-save.test.ts, tests/paperwork-triage.test.ts, tests/contact-center.test.ts, tests/ai-voice.test.ts, tests/voice-command-router.test.ts, tests/documents-import-actions.test.ts
- Mock risk: Communications Hub is a manual log (user types the message) — it looks like an inbox but nothing flows into it automatically; contact-center inbound requires CONTACT_CENTER_INBOUND_SECRET + provider wiring and a Family+ plan; scan requires an AI provider for /api/ai/flyer
- Gap: No single household inbox: email/SMS/voice land in family_inbox_messages (Contact Center, Family+ only), pasted text in family_communications, camera scans go straight to calendar, paperwork to paperwork_items, and there is no share-target. Inbound email/SMS are classified and auto-replied but never become bills, receipts, reservations, forms, events or tasks — recordInboundMessage is storage-only. No triage queue that unifies sources with 'turn into…' actions, and no bill/receipt/reservation extraction for inbound mail.
- Build (L, migration: yes): Make /dashboard/inbox the one queue: server page reads family_inbox_messages + paperwork_items + family_communications (+ scan/voice captures stamped as inbox rows) into one ranked list; add a 'Handle it' action per row that pipes the body through the existing intake (submitRequest with intent 'capture') or /api/ai/import, gated by trust, and stamps the message with the created request id; add a PWA share_target pointing at /capture so shared email/screenshots enter the same queue; extend paperwork triage kinds with 'receipt' and 'reservation'.
  - Migration: family_inbox_messages needs request_id uuid (FK ai_requests) + handled_at timestamptz, and the channel check constraint must admit 'share','scan','paste' so non-telephony sources can live in the same table; RLS unchanged (family-scoped select already exists in 0214).
- Evidence: `app/(app)/dashboard/inbox/page.tsx:1-10` /dashboard/inbox is the 'Communications Hub' (InboxModule), plan-gated by requireFeature; `components/modules/inbox-module.tsx:74,428,628,730` reads/writes family_communications (manual log of calls/emails/messages); :397 /api/ai/assist; :704 /api/ai/import for pasted text; `app/api/ai/import/route.ts:16-24` Magic Import maps extracted items to create_calendar_event/create_chore/create_reminder/add_grocery_item/create_meal_plan_entry via runAction, trust-gated; `lib/contact-center/server.ts:130-160` recordInboundMessage upserts email/sms/voice into family_inbox_messages with classify/summarize; storage only, no domain records created; `app/api/contact-center/email/route.ts:96-113` inbound email webhook -> runConcierge -> recordInboundMessage -> auto-reply; grep for todo_items/calendar_events/submitRequest in lib/contact-center and app/api/contact-center returns nothing; `app/(app)/capture/page.tsx` /capture text capture; lib/capture/save.ts:73-116 saves notes/calendar_events/todo_items/grocery_items; undoCapture at :119-126; `components/modules/scan-module.tsx:52,83` camera scan posts to /api/ai/flyer; app/api/ai/flyer/route.ts:79 inserts calendar_events; `lib/paperwork/triage.ts:1-13` deterministic triage of forms/permission slips/bills; app/(app)/dashboard/paperwork/actions.ts:28,80 writes paperwork_items and calendar_events

### M3 — Family Intelligence Graph — **partial**
- Reachable: /dashboard/graph (requireUserContext, no plan gate); /dashboard/family-knowledge-graph redirects; graph data also feeds /dashboard/concierge, /dashboard/briefing, /dashboard/decisions, /dashboard/prep-plans via loadFamilyContext
- Persisted: graph_entities, graph_edges, family_model_dirty (trigger), family_facts (with source/confidence), legacy family_knowledge_nodes/edges
- Tested: tests/graph-reason.test.ts, tests/twin-project.test.ts, tests/twin-projection.test.ts, tests/reasoning-context.test.ts, tests/reasoning-context-read-boundary.test.ts, tests/family-facts.test.ts, tests/memory-write-path.test.ts
- Mock risk: graph_entities.ref_table/ref_id is the only provenance — no source/confidence/observed_at on nodes or edges; an empty graph renders as 'no entities' and the page still loads; the manual GraphModule insert path bypasses projection so hand-made nodes have no provenance
- Gap: Canonical graph covers people, pets, vehicles, classes, teams, routines, places, accounts and providers with ref_table/ref_id links, auto-refreshed on dirty. Missing for the strategy: events, items/assets, obligations (bills, paperwork actions, renewals) and preferences are not projected (preferences live in family_facts, obligations in domain tables); graph rows carry no source/confidence/observed_at provenance (only family_facts does, per 0265); the legacy family_knowledge_nodes graph (0022) is still written by lib/family/actions.ts, so there are two graphs.
- Build (L, migration: yes): Extend the pure projector with calendar_events (upcoming 60d), inventory items/vehicles as assets, obligations from bills/paperwork_items/renewals, and preferences from family_facts (category=preference, carrying source+confidence into attributes); add provenance columns to graph rows and stamp them in project-server; redirect lib/family/actions.ts knowledge-node writes to graph_entities so there is one graph; keep the existing dirty-trigger refresh.
  - Migration: graph_entities and graph_edges need source text (default 'projection'), confidence smallint, observed_at timestamptz; extend kind check with 'asset','obligation','preference','provider'; add calendar_events/bills/paperwork_items/family_facts to the 0134 dirty-trigger table list. No RLS change.
- Evidence: `supabase/migrations/0129_family_graph.sql:19-54` graph_entities (kind in person/activity/place/org/event/item/pet/topic/other, ref_table/ref_id, attributes jsonb) and graph_edges (relation, weight, unique per family/source/target/relation), RLS family-member; `lib/twin/project.ts:1-33` pure projector from members, pets, vehicles, classes, teams, routines, places, accounts, providers — no calendar events, items, documents, obligations or preferences; `lib/twin/project-server.ts:17-25,51,70` reads nine domain tables and upserts graph_entities/graph_edges idempotently on (family_id, ref_table, ref_id); `lib/reasoning/auto-refresh.ts:11-24` re-projects in background when family_model_dirty is set by the 0134 trigger; model-refresh cron is the backstop; `supabase/migrations/0134_model_dirty.sql:13-61` family_model_dirty table + mark_model_dirty trigger on source tables; `lib/reasoning/context.ts:41-45,112-114,152-168,185` loadFamilyContext reads graph_entities/graph_edges, computes orphans/stats, schedules auto refresh; `app/(app)/dashboard/graph/page.tsx:12-45` /dashboard/graph renders contextSummary + GraphModule; family-knowledge-graph/page.tsx:1-8 redirects here; `components/modules/graph-module.tsx:54,58,305,346` client reads/inserts graph_entities and graph_edges directly (manual node/edge creation)

### M4 — Family Daily Brief — **partial**
- Reachable: /dashboard/briefing (requireFeature plan gate) and the client's POST /api/ai/briefing; /home shows the decisions/handled halves separately
- Persisted: reads calendar_events, reminders, bills, medications, meal_plan_entries, family_automation_runs, agent_activity; brief snapshot writes to home_briefs are blocked (store.ts quarantine, migration 0262) so nothing is persisted
- Tested: tests/briefing-build.test.ts, tests/briefing-store.test.ts, tests/briefing-cache-isolation.test.ts, tests/api-ai-briefing-schema.test.ts, tests/briefing-read-boundary.test.ts, tests/home-today-rank.test.ts
- Mock risk: Without an AI provider the route still returns the deterministic buildBrief so the page never looks empty; the client cache (cache-isolation) can show a previously generated brief; 'decisions' are absent so the brief can look complete while approvals wait on Home
- Gap: Today/handled/upcoming/exceptions exist and are honest, but the brief has no 'decisions' section (pending approval_requests, runs awaiting answers, recommendations live only in Home's NeedsAttention); the API route is not viewer-role aware (a child member gets bills/money content — the role policy in lib/ai/context/policy.ts is not applied here); snapshot persistence is quarantined so there is no stored/delivered brief and no scheduled delivery (buildBrief is only called from the route).
- Build (M, migration: no): Add a decisions slice to Brief: reuse lib/services/approvals listPending + lib/home/needs-build buildHomeNeeds inside app/api/ai/briefing and lib/briefing/build.ts (decisions: NeedItem[]), render it first in BriefingModule with the existing ApprovalCard; gate money/bills/meds slices on isManager(ctx.active.role) in the route (mirroring lib/ai/context/policy.ts); add a morning delivery step to app/api/cron/notifications that calls buildBrief and notify() with the headline + decision count. Leave the quarantined store alone (its unblocking needs the 0262 revalidation work).
- Evidence: `lib/briefing/build.ts:1-16,131-155` buildBrief composes calendar half (buildFirstBrief), cross-domain digest, and handled = mergeCompletedByBubaly(completedRuns) — handled can never over-report; counts include timeSavedMinutes; `lib/briefing/build.ts:184-186` readinessPct credits handled work; `lib/briefing/store.ts:14-45` saveBrief always returns denied, loadBrief always null, markDelivered false — snapshot persistence quarantined by 0262; `app/api/ai/briefing/route.ts:297-312,380` generates the AI brief when configured (withAiRequest) then buildBrief; grep for isManager/child/viewer/policy finds no viewer-role gating (only members list at :90 and child schedule helper at :436); `app/(app)/dashboard/briefing/page.tsx:16-19` /dashboard/briefing plan-gated by requireFeature; renders BriefingModule + ChangeRecap + RelationshipInsights; `components/modules/briefing-module.tsx:111,772` 'Needs attention today' card is the ConciergeDigest (bills/meds/maintenance), not pending approvals; client reads calendar_events/reminders at :664-673 and fetches /api/ai/briefing via lib/briefing/cache-isolation.ts:135; `app/(app)/home/page.tsx:246-262,299-310,412,422,489` Home renders NeedsAttention (approvals), WorkingOn, CompletedByBubaly from family_automation_runs/agent_activity/approval_requests — the decisions live here, not in the brief; `grep buildBrief( app lib` only app/api/ai/briefing/route.ts calls it — no cron delivers the brief

### M5 — Needs Your Decision — **partial**
- Reachable: /home NeedsAttention (top 5 only) and /dashboard/trust approvals list; /dashboard/decisions is a different feature; no dedicated all-decisions page
- Persisted: approval_requests (decide/edit/expire/remind), parent_approvals (wallet), family_ai_recommendations, family_automation_runs state, calendar conflicts derived live
- Tested: tests/approval-execution.test.ts, tests/approval-card.test.ts, tests/home-needs-approvals.test.ts, tests/approval-expiry.test.ts, tests/approval-pending-once.test.ts, tests/approval-reminders.test.ts
- Mock risk: Home shows only the top 5 needs so a family with more decisions sees an incomplete center; the trust page's approvals list is read from the client component without the ApprovalCard resume path for non-AI rows; the 'Decision Engine' route name suggests an approval center that does not exist
- Gap: The approval spine (approval_requests + decide/edit that resumes runs) is real and tested, and Home ranks AI approvals, money approvals, recommendations, conflicts, renewals and chore sign-offs together. Missing: a single dedicated page (Home caps at 5; /dashboard/decisions is the trade-off scorer); ambiguous facts (family_facts kind=suggestion / playbook suggestions), messages awaiting reply (family_inbox_messages) and forms (paperwork_items actions) are not sources; approval payload kinds cannot represent a message send or a form submission.
- Build (M, migration: no): Add /dashboard/needs-you (route only; sidebar untouched — link from the Home 'Needs you' header and the trust page) that server-renders buildHomeNeeds uncapped with the same NeedsAttention component; extend needs-build with three pure mappers: family_facts suggestions (confirm/dismiss via services/memory confirmFact), unread family_inbox_messages with intent appointment/personal (reply/archive), and paperwork_items open actions (sign/pay/rsvp); no new tables.
- Evidence: `supabase/migrations/0093_trust_engine.sql:86-112` approval_requests: domain, capability, requested_by_kind, payload, amount_cents, reasoning, approval_model, status, priority, expires_at; `lib/services/approvals/index.ts:112,744,880,985,1076` listPending, decide (resumes the parked run), editAndApprove, expireStale, remindPendingApprovals; `lib/approvals/card-data.ts:89-93` payload kinds are only tool / plan_steps / concierge_plan — no message, form or fact approvals; `lib/home/needs-build.ts:14-31,40-72` buildHomeNeeds merges money approvals, AI approval_requests, runs awaiting approval/answer, family_ai_recommendations, calendar conflicts, chore sign-offs, renewals, document expiry; no family_facts suggestions, inbox messages or paperwork actions; `components/concierge/needs-attention.tsx:1-40` ranked list with inline ApprovalCard / HomeApprovalActions / RecommendationActions; kids/guests read-only; `app/(app)/home/page.tsx:246-264,323,412` Home builds needs from family_automation_runs, agent_activity, family_ai_recommendations, approval_requests, money approvals, chores, todos, reminders and renders topNeeds(homeNeeds, 5) — capped, no 'see all'; `app/(app)/dashboard/approvals-actions.ts:24,36` decideApproval / editAndApproveApproval server actions; `app/(app)/dashboard/trust/page.tsx:33,58-66` /dashboard/trust lists the last 50 approval_requests alongside policies and audit logs — a trust console, not a decision inbox

### M6 — Bubaly Handled — **partial**
- Reachable: /home CompletedByBubaly section, /dashboard/concierge/runs/[id] run detail, /dashboard/activity trail, /dashboard/briefing handled list
- Persisted: family_automation_runs (state, progress, completed_at), ai_plan_steps, ai_run_events, ai_tool_calls, agent_activity, audit_logs
- Tested: tests/home-today-rank.test.ts, tests/briefing-build.test.ts, tests/run-detail-read-boundary.test.ts, tests/ai-activity.test.ts, tests/household-trail.test.ts, tests/run-executor.test.ts (partial completion)
- Mock risk: CompletedItem carries title/detail/href/partial only — 'source/reason' is one click away in run detail (reasoning_summary), not on the ledger row; agent_activity rows are written by specialist agents and are not verified against records; no undo means 'recovery' is manual
- Gap: A verified ledger exists (completed runs with verify-step downgrades to partial, ai_tool_calls per write, household audit trail, reasoning_summary on run detail). Missing: undo/recovery — nothing can reverse a run's writes (only quick captures have undoCapture); ledger rows do not surface source (tool/domain) and reason inline; agent_activity items are not verified.
- Build (L, migration: yes): Record the created row ids per step (result_json already holds tool output — normalise to {table, ids}) and add undoRunStepAction that calls the owning service's delete/restore under the same trust gate, marking the step and tool call undone; show 'Undo' plus source (tool domain) and reason (reasoning_summary excerpt) on CompletedByBubaly items and the run detail timeline; keep undo limited to tables with idempotency_key (0256) so reversal is exact.
  - Migration: ai_plan_steps needs undone_at timestamptz + undone_by uuid; ai_tool_calls needs created_refs jsonb (table + ids) so undo can target exact rows; an ai_run_events event_type 'undone' value. Service-role writes only, no RLS widening.
- Evidence: `components/concierge/completed-by-bubaly.tsx:1-45` renders CompletedItem list; partial runs flagged with a warning icon, each linking to the run; `lib/home/today.ts:294-305` mergeCompletedByBubaly: only runs in completed/partially_completed plus agent_activity action rows — never a claim; `app/(app)/home/page.tsx:251-260,299,489` Home reads family_automation_runs (completed states), agent_activity (kind=action,status=done), autopilot_suggestions auto_executed and renders CompletedByBubaly; `lib/ai/runs/detail.ts:60-77,150-151,239` run detail loads steps, events, approvals and exposes ai_plans.reasoning_summary as the user-facing 'why'; `lib/ai/runs/executor.ts:900-904` a step whose verify check fails is marked partially_completed rather than completed; lib/ai/runs/verify.ts implements records_exist checks; `lib/ai/tools/execute.ts:18-27,227` every tool write is reserved in ai_tool_calls under a deterministic idempotency key — the per-write ledger; `lib/activity/trail.ts:1-16` household trail from audit_logs carrying both person and Bubaly actors; /dashboard/activity page; `grep -i undo|rollback|revert lib/ai/runs lib/services/approvals lib/approvals components/concierge app/(app)/dashboard/concierge lib/activity` only a comment in lib/ai/runs/controls.ts:71 and run-controls.tsx:11 — no undo for run writes; the only undo is lib/capture/save.ts:119-126 undoCapture for quick captures

### M7 — Household Autopilot: detect repeated successful workflows and offer narrow explainable automation policies — **partial**
- Reachable: /dashboard/autopilot (requireFeature), routines panel inside /dashboard/calendar, /dashboard/family-automation, Trust > Policies tab at /dashboard/trust (nav lib/constants/navigation.ts:84), concierge dial via lib/autonomy/loop
- Persisted: autopilot_suggestions (read/write), reminders (auto-executed inserts), routine_templates, routine_template_items, calendar_events, trust_policies, family_automation_rules, family_automation_runs, routine_runs, ai_requests
- Tested: tests/autopilot-engine.test.ts, tests/autopilot-persistence-boundaries.test.ts, tests/autopilot-twin.test.ts, tests/routines.test.ts, tests/routines-service.test.ts, tests/cron-family-routines.test.ts, tests/autonomy-loop.test.ts, tests/family-automation-page-honesty.test.ts
- Mock risk: The 'Autopilot' name suggests learned automation but every suggestion is a static rule over upcoming dates; nothing reads approval_requests / ai_tool_calls / family_automation_runs history to learn what the family repeatedly approves (grep for policy-suggestion terms returned 0 hits). Legacy automation rules on the family-automation page never fire.
- Gap: No detection of repeated SUCCESSFUL WORKFLOWS (same tool/domain/capability approved N times with no rejections or failures) and no offer of a narrow, explainable trust policy from that evidence. Routine detection covers calendar events only; the only policy-creating paths are the manual Trust module form and the one concierge dial.
- Build (M, migration: no): Add a pure candidate-policy learner over approval_requests + ai_tool_calls history, surface candidates as autopilot_suggestions of kind 'policy' with evidence text, and accept them by inserting a narrow trust_policies row (domain+capability, subjectKind 'ai', conditions.tags=[toolName]) through the existing trust actions; pass the executing tool name as a tag in ActionContext so the engine's existing tag condition scopes the policy.
- Evidence: `lib/autopilot/engine.ts:1-27` Rule-based prediction engine with confidence tiers auto>=90 / approve 70-89 / ask; every suggestion is a hand-authored rule over upcoming signals (renewals, appointments, chores, birthdays...), not learned from behaviour; `lib/autopilot/scan.ts:41-79,198-262,294-300` Server scan reads 16 real tables, reconciles autopilot_suggestions, auto-executes 'auto' tier by inserting reminders rows, notifies via notify(); `components/modules/autopilot-module.tsx:44-55` Mission Control UI realtime-queries autopilot_suggestions; WhyThis explanation via lib/ai/explanation; `app/(app)/dashboard/autopilot/page.tsx:8` Route gated by requireFeature('/dashboard/autopilot'); API app/api/autopilot/scan/route.ts + cron app/api/cron/autopilot-scan/route.ts; `lib/routines/detect.ts:77-135` detectRoutines: groups calendar_events by normalized title+weekday+30-min bucket, suggests routines seen on >=3 distinct weeks; materializeRoutine expands templates to events; `components/modules/routines-panel.tsx:68-72,93,104-109,132-141,172-175` Reads/writes routine_templates + routine_template_items, applies to calendar via applyRoutineToCalendarAction, delete template; only mounted inside calendar-module.tsx; `lib/autonomy/loop.ts:24-54` Concierge autopilot dial (auto/ask/off) maps to a trust_policies effect for domain 'scheduling' capability 'automate' — a single hand-set dial, not learned; `lib/services/routines/index.ts:1-60` Scheduled routines ('every Sunday plan meals') stored on family_automation_rules with schedule_kind, fire ai_requests through the normal planner/trust gate; cron app/api/cron/family-routines/route.ts

### M8 — Schedule Intelligence — **partial**
- Reachable: /dashboard/moments (prep per event), /dashboard/trip-intel (real departure), /dashboard/rides (driver conflicts), /dashboard/conflicts, /dashboard/prep-plans (requireUserContext), Home needs list (conflicts)
- Persisted: calendar_events, rides, prep_plans, prep_plan_steps, moment_activations, vehicles (not used for scheduling), meal_plan_entries (not linked)
- Tested: tests/moments-prep.test.ts, tests/trip-departure.test.ts, tests/rides-schedule.test.ts, tests/planning-prep.test.ts, tests/home-conflicts.test.ts, tests/family-conflicts.test.ts, tests/planning-refresh.test.ts
- Mock risk: leave-by on ordinary events is a static buffer (moments/prep.ts:77), so it looks like travel intelligence without a route; the real OSRM departure engine is only reachable for trips; the rides driver model is limited to the rides table and ignores calendar events and vehicles
- Gap: The pieces exist as separate pure engines (prep, departure, driver conflicts, double-booking, horizon prep plans) but there is no single schedule model per event that links prep, real travel/departure, driver availability, shared vehicles, meal timing and care (pet/sitter/childcare) dependencies; real drive-time departure is confined to trip-intel; nothing persists a per-event schedule-intelligence result.
- Build (L, migration: no): Add a pure lib/schedule/intelligence.ts that, for each upcoming timed event, composes the existing engines: moments/prep for prep items, trips/departure with the trip-intel OSRM fetcher when the event has a location, rides driverConflicts + calendar overlap for driver/parent availability, vehicles count as the shared-resource constraint, meal_plan_entries for a dinner-timing warning, and pets/family_members (childcare flag) for care gaps; surface the result on the event detail sheet and as Home 'today' reasons; no persistence in the first cut.
- Evidence: `lib/moments/prep.ts:1-12,47-49,77,98-112` per-event prep bundle with leave-by computed from a fixed category travel buffer (not real travel time), packing, shopping, weather, budget items; `lib/trips/departure.ts:1-11` Smart Departure engine with real drive time (OSRM) + traffic/weather penalties — used only by app/(app)/dashboard/trip-intel/actions.ts and components/modules/trip-intel-module.tsx; `lib/rides/schedule.ts:68,110,122` assessDriverSchedule, driverConflicts, needsDriverCount for the rides table; components/modules/rides-module.tsx:63,118 reads/writes rides; `lib/home/conflicts.ts:1-18` double-booking detection for Home; app/(app)/dashboard/conflicts/page.tsx:6,34 uses lib/family/conflicts detectConflicts + quickFixMoveAfter over calendar_events; `lib/planning/prep.ts:1-9` horizon prep plans (trip/birthday/doc_expiry/school_start) with lead-day steps; lib/planning/prep-server.ts:15-60 upserts prep_plans/prep_plan_steps; components/modules/planning-module.tsx:48-81 renders them at /dashboard/prep-plans; `app/(app)/dashboard/moments/page.tsx:31-65` moments page reads members/vacations/holidays/homework and persists moment_activations; `lib/calendar/scheduling.ts:1-4` find-a-time engine over calendar events (no travel/resource model); `grep travelMinutes|leaveBy|departure lib` departure/travel logic is split across moments/prep, trips/departure, moments/reminders, moments/notify, autopilot/engine — no shared model of shared resources (vehicles), meals or care dependencies

### M9 — School + Sports Front Desk: ingest communications and coordinate schedules, forms, fees, gear, transport and changes — **partial**
- Reachable: /dashboard/school, /dashboard/sports, /dashboard/signups, /dashboard/inbox, /dashboard/front-desk, /dashboard/contact-center (all requireFeature); webhooks /api/contact-center/{email,sms,voice} require CONTACT_CENTER_INBOUND_SECRET + an external inbound provider
- Persisted: family_inbox_messages, family_contact_channels, family_communications, family_contacts, school_events, school_classes, homework_assignments, grades, teams, sports_events, opportunities, call_logs, front_desk_settings
- Tested: tests/contact-center.test.ts, tests/service-school-sports.test.ts, tests/school-timetable.test.ts, tests/opportunities-deadlines.test.ts, tests/family-school-read-boundary.test.ts, tests/family-sports-read-boundary.test.ts
- Mock risk: 'AI Front Desk' is phone screening; inbox 'AI Import' is a manual paste that yields generic actions; automatic email ingestion only works when a provider is wired to the webhook, and even then the message is filed with a generic intent and never turned into a school/sports action.
- Gap: No school/sports-aware pipeline: inbound messages are not classified as school/sports nor decomposed into forms-to-sign, fees due, gear lists, transport/carpool needs or schedule changes; no link from a message to a child/team/event; no coordination (change -> calendar update + reminder + ride). Forms and gear have no entity at all (grep permission slip/gear/equipment in school/sports modules: 0 hits).
- Build (L, migration: yes): Add a pure school/sports classifier (intent + sub-kind form/fee/gear/transport/schedule_change + extracted date/amount/child) used by both the inbound webhook and the inbox AI import; when it fires, run the existing runAction/gateAiAction pipeline to propose school_events/sports_events (via services), a reminder for the form deadline, an opportunities row for the fee, grocery items for gear, and a rides entry for transport, all approval-gated; show a 'School & Sports desk' card in the school module listing pending linked messages with one-tap apply.
  - Migration: Add nullable member_id, linked_type, linked_id (and sub_intent text) to family_inbox_messages so a message can be tied to a child/team/event and its applied action; RLS unchanged (family-scoped). Owner approval required.
- Evidence: `app/api/contact-center/email/route.ts:45-120` Inbound email webhook (secret-gated) resolves family by @bubaly.com local-part, files into family_inbox_messages via recordInboundMessage, runs concierge classification, auto-replies; SMS/voice siblings at app/api/contact-center/{sms,voice}; `lib/contact-center/server.ts:126-155` recordInboundMessage upserts family_inbox_messages with ai_intent/ai_summary, dedupes on channel+provider_ref; `lib/contact-center/routing.ts:9-45` Intents are urgent/appointment/delivery/sales/spam/personal only — no school, sports, form, fee, gear or transport intent; `components/modules/inbox-module.tsx:30-41,74,686-704` Communications Hub over family_communications (0090) with school/sports channels and category labels; AI Import modal posts pasted text to /api/ai/import; `app/api/ai/import/route.ts:17-23` Magic import can only propose create_calendar_event, create_chore, create_reminder, add_grocery_item, create_meal_plan_entry — no forms, fees, gear, transport; `components/modules/school-module.tsx:28,118-128` School hub reads school_events (types general/holiday/field_trip/parent_meeting/exam/concert/sport/graduation/assignment/announcement), school_classes, grades; manual forms only; `lib/services/school/index.ts:44-133 and lib/services/sports/index.ts:1-50` Read services for school events, homework, A/B timetable, teams and recurring practices used by planner/brief; `components/modules/signups-module.tsx:30-56` Registrations over opportunities table with cost, deadline, status registered/waitlisted — the only fee/deadline tracking

### M10 — Meal -> Pantry -> Grocery Loop: calendar-aware meals, pantry use, grocery delta, budget, substitutions and purchase handoff — **partial**
- Reachable: /dashboard/meals, /dashboard/pantry, /dashboard/fridge-chef (requireFeature), /dashboard/grocery (no gate), /dashboard/kitchen; addFromMealPlan only via concierge plan-meals/plan-week templates
- Persisted: meals, meal_plans, family_recipes, pantry_items, grocery_lists, grocery_items, recipe votes, medical_profiles.allergies (read)
- Tested: tests/meal-planner.test.ts, tests/ai-meal-plan-persistence.test.ts, tests/tool-meals-memory-messages.test.ts, tests/service-groceries.test.ts, tests/pantry-logic.test.ts, tests/pantry-chef.test.ts, tests/grocery-retailers.test.ts, tests/grocery-write-path.test.ts
- Mock risk: lib/grocery/retailers.ts passes its tests but is dead code; the meal planner only writes meal_plans when write=true; the grocery delta is real but has no UI entry point, so a manual reviewer of the pages would never see the loop close.
- Gap: Missing: calendar-awareness (busy nights), budget (grep budget in grocery libs/pages: 0 hits), substitutions (only a recipe-notes placeholder), purchase handoff in the UI (retailer module unwired; no 'bought -> pantry +qty -> record purchase' flow), and a visible 'add this week's plan to the list' action.
- Build (L, migration: yes): Make the planner read the week's calendar_events and tag busy nights; add a pure substitutions helper (allergy/dislike/pantry-driven swaps with reasons) applied to planGroceryNeeds output; wire lib/grocery/retailers.ts into shopping-module as chips + copy list; add a 'Bought' flow that increments pantry via pantryAdjust and optionally records the purchase through the finances service; add an 'Add from this week's plan' button in meals-module wrapping groceries.addFromMealPlan; show an estimated total against a grocery budget.
  - Migration: Budget needs grocery_lists.budget_cents and grocery_items.estimated_price_cents (nullable) — no price column exists today; RLS unchanged. Calendar-awareness, substitutions, handoff and the plan button need no migration and can ship first.
- Evidence: `app/api/ai/meals/plan/route.ts:55,79-93` AI week planner reads pantry_items and passes expiringSoon items + dietary flags to the model; no calendar_events read (grep calendar/busy: 0 hits); `lib/meals/planner.ts:46-53,62-80` PlannerRequest has candidates/dietary/expiring/avoidRepeats/notes — no busy-night or budget input; `lib/services/groceries/index.ts:358-390,393-452` planGroceryNeeds = ingredients minus stocked pantry minus already-listed; addFromMealPlan reads meal_plans + pantry_items and writes grocery_items; `lib/ai/tools/meals.ts and lib/ai/planner/templates/plan-meals.ts` groceries.addFromMealPlan is exposed only as an AI tool / planner template step; no UI button calls it (grep in app/components: 0 hits); `lib/services/groceries/index.ts:514-718` pantryAdjust (delta/quantity/location/expiry), savePantryItem, removePantryItem; pantry page actions at app/(app)/dashboard/pantry/actions.ts; `lib/pantry/logic.ts:60-100` isLowStock, expiringSoon, lowStockItems, pantrySummary pure helpers; `lib/meals/pantry-chef.ts and app/api/ai/pantry-chef/route.ts` Fridge Chef: photo -> recipes with have/need lists, allergen annotation; `lib/grocery/retailers.ts:1-70` Retailer deep-links + copy-paste list for purchase handoff — imported by nothing outside its own test (grep RETAILERS/itemSearchUrl in app/components/lib: 0 hits), so unreachable

### M11 — Family CFO: Coverage-aware forecast, spending explanation, subscriptions and affordability scenarios tied to household plans — **partial**
- Reachable: /dashboard/family-cfo via navigation.ts:207 (minLevel 2, requireUserContext only on page); /dashboard/subscriptions via navigation.ts:187 + requireFeature('/dashboard/subscriptions') (feature-catalog.ts:123 'basic'); /dashboard/money-timeline reachable ONLY through finances-module.tsx:208 link (no nav entry); spend scenario via /dashboard/family-digital-twin (navigation.ts:206, minLevel 2).
- Persisted: Reads: financial_accounts, bills, savings_goals, transactions, budgets, calendar_events, subscriptions_tracked. Writes: money_timeline_insights (0168) for insight status; twin_simulations for saved activity projections; spending review writes ai_plans/ai_plan_steps via the run ledger.
- Tested: tests/finance-timeline.test.ts (forecast brain), tests/family-cfo-read-boundary.test.ts (fail-closed page), tests/finance-subscriptions.test.ts, tests/subscription-candidates*.test.ts, tests/subscription-price-history*.test.ts, tests/twin-simulate.test.ts (spend verdicts lines 60-88), tests/planner-templates.test.ts + tests/ai-eval/scenarios/spending-review.json, tests/service-finances.test.ts.
- Mock risk: The page named 'Family CFO' is a static summary; the forecast/insights/scenario logic lives on three other surfaces (money-timeline, digital twin, concierge run) that the CFO page never links to. money_timeline_insights persistence has no dedicated test beyond tests/silent-empty-read-ratchet.test.ts. Affordability sim ignores upcoming bills, goals and plan budgets, so a 'clear' verdict can be wrong.
- Gap: No single CFO surface: forecast excludes tracked subscriptions and all plan-linked commitments (vacation_budgets, moves.budget_cents, home_projects), so it is not 'tied to household plans'; 'coverage' (what autopay/bills/insurance already cover) is not modelled; affordability scenario only checks one budget category, never the forward balance; spending explanation exists only as a concierge workflow, not surfaced on the CFO page; money-timeline is not in navigation.
- Build (M, migration: no): Unify on the existing pieces without new tables: (1) extend loadMoneyTimeline to add subscriptions_tracked (monthlyCostCents), remaining vacation_budgets for upcoming vacations, moves budget minus spent, and open home_projects budget as MoneyMoments (kind 'plan'); (2) add an optional `scenario` input to buildCashflowTimeline (an injected one-off or recurring moment) and a 'Can we afford it?' form on the CFO page that renders the resulting lowest balance/insights; (3) make the CFO page link to /dashboard/money-timeline, the digital-twin spend simulator and a 'Explain this month' button that submits the spending_review intent through the existing concierge request path; (4) treat bills with autopay/status paid as covered in the forecast copy.
- Evidence: `app/(app)/dashboard/family-cfo/page.tsx:26-33` CFO page is a read-only summary: reads financial_accounts, bills (30d), savings_goals, transactions (month), budgets; fails closed on read error (line 41-46). No forecast, no scenarios, no explanation on this page; every SectionCard viewAllHref points to /dashboard/billing.; `lib/finance/timeline.ts:172` buildCashflowTimeline(): real 12-week cash-flow forecast (bills expanded by recurrence, goal target dates, calendar overlay, running balance, heavy-week/low-balance/goal-at-risk/recurring-creep insights). Pure and deterministic.; `lib/finance/timeline-load.ts:20-58` loadMoneyTimeline() reads bills, savings_goals, financial_accounts, calendar_events only. subscriptions_tracked, vacation_budgets, moves.budget_cents, home_projects budgets are NOT in the forecast — not tied to household plans.; `app/(app)/dashboard/money-timeline/actions.ts:12-40` Insight acknowledge/dismiss persisted to money_timeline_insights (migration supabase/migrations/0168_money_timeline_insights.sql), upsert on family_id,dedupe_key.; `components/modules/finances-module.tsx:208` Only in-app link to /dashboard/money-timeline; the route is absent from lib/constants/navigation.ts (grep for money-timeline returns no nav entry).; `lib/ai/planner/templates/spending-review.ts:10-40` Deterministic 'Why did we spend too much?' workflow: six finances.* retrieve steps (budgetVsActual, spendingByCategory, comparePeriods, merchantMovement, recurringChanges, unusualTransactions) then notify managers — the spending explanation behaviour.; `lib/ai/tools/finances.ts:80-246` The six finance analysis tools are registered and delegate to lib/services/finances (budgetVsActual line 105, merchantMovement line 183, unusualTransactions line 233).; `lib/finance/subscriptions.ts:31-69` summarizeSubscriptions / isStale / wastedMonthlyCents over subscriptions_tracked (migration 0076); candidates API app/api/subscriptions/candidates/route.ts and price-history API app/api/subscriptions/price-history/route.ts.

### M12 — Home Digital Twin: Assets, warranties, manuals, maintenance, inventory, projects and replacement history — **partial**
- Reachable: /dashboard/home hub + /home/warranties, /home/maintenance, /home/service, /home/pros, /home/diagnose via navigation.ts:146 (minLevel 1, requirePlanLevel(1)); /dashboard/projects via navigation.ts:150 + requireFeature (feature-catalog.ts:72 'basic'); /dashboard/inventory via navigation.ts:147 (feature-catalog.ts:68 'basic').
- Persisted: homes, home_assets, home_warranties, home_contractors, home_service_records, maintenance_tasks, documents(asset_id), home_projects, project_materials, project_quotes, inventory_items/home_locations/inventory_moves (separate).
- Tested: tests/home-maintenance.test.ts (warranty/life/cadence math), tests/home-actions-write-boundary.test.ts (warranty/contractor/service write failures), tests/home-management.test.ts, tests/projects-planner.test.ts, tests/projects-module-write-boundary.test.ts, tests/inventory-finder.test.ts, tests/twin-project.test.ts (graph projection, no home nodes).
- Mock risk: Each piece is a real module, but there is no 'twin' view that joins them: replacement history is only inferable from lifeRemaining + service records; 'manuals' would have to be uploaded as a 'warranty' document; the Digital Twin page (family-digital-twin) contains no home data, so a reviewer could mistake the twin page for a home twin.
- Gap: Missing: manuals (no document category/field), replacement history/lineage (no replaced_by/retired_on, no 'replaced' event), asset↔inventory link, home assets/warranties/projects in the twin graph (lib/twin/project.ts), and a single asset detail view that shows warranty + manual + service history + open maintenance + related project.
- Build (M, migration: yes): (1) Manuals without migration: allow documents.category 'manual' on the asset card in home-module and list warranty+manual docs per asset; (2) replacement lineage: add home_assets.replaced_by_asset_id + retired_on and a 'Replace this asset' action in home/actions.ts that creates the new asset, links the old one, copies location and logs a home_service_records row of kind 'replacement'; (3) add home assets, warranties and projects to the twin projection (lib/twin/project.ts + project-server.ts) with edges asset→home, warranty→asset, project→asset; (4) optional inventory_items.asset_id to link big-ticket inventory to an asset.
  - Migration: ALTER home_assets ADD replaced_by_asset_id uuid REFERENCES home_assets, retired_on date; optionally inventory_items.asset_id uuid. Existing RLS on home_assets covers new columns; owner approval required.
- Evidence: `supabase/migrations/0002_tables.sql:301-316` home_assets (name, category, location, brand, model, purchased_on, warranty_until) and maintenance_tasks (asset_id, recurrence, interval_days, due_at) lines 318-335.; `supabase/migrations/0036_home_maintenance.sql:41-46,78-92` home_assets gains installed_on, expected_life_years, last_serviced_on; home_warranties table (provider, warranty_type, coverage, expires_on, cost, claim_phone); also homes, home_contractors, home_service_records.; `lib/home/maintenance.ts:22-83` warrantyStatus, assetAgeYears, lifeRemaining (TYPICAL_LIFESPAN_YEARS), DEFAULT_CADENCES per asset category — the forecasting backbone.; `app/(app)/dashboard/home/actions.ts:27-171` saveWarrantyAction, saveContractorAction, saveServiceRecordAction (updates home_assets.last_serviced_on line 119), scheduleRecommendedTasksAction inserts maintenance_tasks from cadences (line 135-171).; `lib/home/queries.ts:22-72` getAssets/getWarranties/getContractors/getServiceRecords fail closed; getHomeOverview joins assets, warranties, open maintenance_tasks.; `components/modules/home-module.tsx:355-358` Warranty document upload inserts documents with asset_id and category 'warranty' (documents.asset_id from 0007). No 'manual' category or manual URL anywhere (grep manual in lib/home/components/home/migrations → only device integration enum).; `supabase/migrations/0246_home_projects.sql` home_projects, project_materials, project_quotes; lib/projects/planner.ts (scope templates, compareQuotes, budgetHealth) used by components/modules/projects-module.tsx (reads home_contractors too).; `supabase/migrations/0242_home_inventory.sql:25-53` inventory_items has no asset_id — inventory and home_assets are unlinked silos.

### M13 — Where Is It?: Visual household inventory with confirmed location and natural-language search — **exists**
- Reachable: /dashboard/inventory via navigation.ts:147 and requireFeature('/dashboard/inventory') (feature-catalog.ts:68 'basic'); AI find-it button inside the module.
- Persisted: inventory_items, home_locations, inventory_moves (0242), photos in storage bucket family-media; AI answers logged through the insights route (ai_requests).
- Tested: tests/inventory-finder.test.ts (paths, tree, searchItems with request wording, lent/warranty/value summaries), tests/inventory-module-write-boundary.test.ts (guarded writes, move history insert, photo upload errors).
- Mock risk: Search is client-side over rows already loaded by useRealtimeQuery, so it is bounded by what the page fetched; 'confirmed location' is only as fresh as the last move row — there is no explicit 'still here' confirmation timestamp; photos are stored but never analysed (not visual search).
- Gap: Minor: no explicit 'confirm it is still here' action/last_confirmed signal; NL search only inside the module (no lib/ai/tools inventory tool so the concierge/command bar cannot find items); no camera/recognition capture.
- Build (S, migration: no): Add an inventory tool for the assistant and a confirmation signal without a migration: (1) lib/ai/tools/inventory.ts with inventory.find (reuses searchItems over inventory_items+home_locations via a new lib/services/inventory) and inventory.recordMove; register in lib/ai/tools/registry.ts and add 'where is/where's my' routing in lib/ai/context/intents.ts; (2) 'Confirm it's here' button that inserts inventory_moves with from=to and reason 'confirmed', and show 'last confirmed' from the latest move in the item card.
- Evidence: `supabase/migrations/0242_home_inventory.sql:11-68` home_locations (nested rooms/containers), inventory_items (photo_path, location_id, status in_place/lent/lost/disposed/in_repair, lent_to/lent_on, warranty_until, tags), inventory_moves (from/to location, moved_by, moved_at, reason) with RLS blocks.; `lib/inventory/finder.ts:96-131` searchTokens strips request phrasing ('where's my', 'where do we keep', 'help me find') then searchItems scores name/brand/model/serial/tags/location/category; every token must match — natural-language 'where is X' answered with the room › container path (locationPath line 66).; `components/modules/inventory-module.tsx:72,134-137,327-335,470-480` Live search box over items+locations; photo upload to storage bucket family-media; Move flow updates inventory_items.location_id then inserts inventory_moves with moved_by/reason.; `lib/ai/insights.ts:659-684` AI insight kind 'inventory' ('AI find it') builds the catalog, rooms and recent moves into the prompt and answers 'where is X' with the exact path; invoked from inventory-module.tsx:124 <AiInsight kind="inventory">.; `lib/constants/navigation.ts:147` 'Home Inventory' nav entry (minLevel 0); page gated by requireFeature('/dashboard/inventory') (feature-catalog.ts:68 'basic').; `lib/ai/tools/registry.ts` No inventory tool exists in lib/ai/tools (grep inventory_items in lib → only lib/ai/insights.ts and lib/realtime/published-tables.ts), so the global assistant/concierge cannot answer 'where is the passport?'.

### M14 — AI Moving OS: Move date orchestrates packing, utilities, address changes, school, pets, accounts and setup — **exists**
- Reachable: /dashboard/moving via navigation.ts:149 (minLevel 1) + requireFeature('/dashboard/moving') (feature-catalog.ts:71 'basic'); date change flows through POST /api/moving/recalculate.
- Persisted: moves, move_tasks, move_boxes (0245); recalculation request ids (0271); AI insight requests in ai_requests.
- Tested: tests/moving-planner.test.ts (template filtering, planTasks dating/dedupe, timeline), tests/move-date-recalculation.test.ts, tests/move-date-recalculation-api.test.ts, tests/move-date-recalculation-ui.test.ts, tests/moving-module-write-boundary.test.ts.
- Mock risk: Orchestration is a deterministic checklist — 'address changes' and 'utilities' are tasks for the family, not automated actions; tasks are not derived from the family's real subscriptions/bills/contacts/school records, and the 'AI' part is a read-only insight, not a planner workflow.
- Gap: No concierge integration (no plan_move intent, template or moving.* tools), tasks not personalised from live data (subscriptions_tracked, bills, school_classes, pets, family_contacts), no link between move_boxes and inventory_items, and the life-events 'moving' template does not launch the Move Planner.
- Build (M, migration: no): Add the AI layer on the existing planner: (1) lib/services/moving/index.ts (getMove, planTasks via lib/moving/planner, setMoveDate reusing the recalculation logic, addTask) and lib/ai/tools/moving.ts registered in the registry; (2) planner template plan-move.ts (intent 'plan_move') that reads subscriptions_tracked, bills, school_classes and pets to generate specific address-change/utility/school/vet tasks with assignees, then reminders; (3) make the life-events 'moving' template launch a moves row instead of a parallel checklist.
- Evidence: `supabase/migrations/0245_move_planner.sql` moves, move_tasks, move_boxes tables; 0271_move_date_recalculation.sql adds the date-change preview/apply support.; `lib/moving/planner.ts:49-84` MOVE_TEMPLATE T-8w→T+2w: utilities (utilities-new/old), address (mail, address-bank incl. subscriptions, address-gov), school (school-notify/enrol, kids-bag), pets (vet, pet-sitter, pet-tag), finance/accounts (budget, insurance, deposit), packing, settling (register-local, unpack-all); filtered by has_kids/has_pets/is_renting_out/move_kind (applicableTemplate line 100).; `lib/moving/planner.ts:120-139` planTasks dates every task from move_date and never duplicates (template_key), so the move date drives the whole plan; timeline/phase grouping line 140, budgetHealth line 161, box tracking findInBoxes line 220.; `app/api/moving/recalculate/route.ts:1-40` Move-date recalculation API: validates preview (lib/moving/recalculation.ts isMoveDatePreview), plan-tier gate, shifts relative tasks while preserving completed/fixed ones.; `components/modules/moving-module.tsx` Client module reads/writes moves, move_tasks, move_boxes (grep .from).; `lib/ai/insights.ts:731-740` AI insight kind 'moving' (what to do this week, what is slipping, day-one plan) grounded in the move/tasks/boxes rows.; `lib/ai/planner/templates/index.ts` No move intent/workflow template (templates: daily-brief, find-vendor, organize-weekend, plan-meals, plan-week, prepare-vacation, remind-everyone, spending-review, what-am-i-forgetting) and no moving tools in lib/ai/tools — the concierge cannot plan or update a move.; `lib/life-events/templates.ts:53` Life-events 'moving' template duplicates a smaller checklist without linking to the Move Planner.

### M15 — Family Travel Agent: Budget/calendar-aware travel, reservations, packing, home/pet prep and disruption replanning — **partial**
- Reachable: /dashboard/vacations (+ [id]/budget, packing, travel, lodging, itinerary, documents, ai-assistant) via navigation.ts:158 and requireFeature('/dashboard/vacations'); /dashboard/trips via navigation.ts:157 (feature-catalog.ts:96 'basic'); /dashboard/trip-intel via navigation.ts:86 (feature-catalog.ts:34); concierge intent plan_trip/prepare_vacation (lib/intent/detect.ts, templates/index.ts).
- Persisted: vacations + 26 vacation_* tables (0070), trips/trip_items, trip_plans/departure_plans (0098), calendar_events (sync), family_reminders, todos via workflow; AI conversations in vacation_ai_conversations/messages.
- Tested: tests/vacations.test.ts (dates, budget, weather, packing), tests/trips-planner.test.ts, tests/trip-departure.test.ts, tests/service-trips.test.ts, tests/api-vacations-ai-output.test.ts, tests/vacation-ai-persistence-boundaries.test.ts, tests/vacations-detail-write-boundary.test.ts, tests/trip-weather-packing-boundary.test.ts, tests/planner-templates.test.ts (prepare_vacation).
- Mock risk: 'Budget-aware' is trip-internal (planned vs spent per trip) and never consults household budgets or the cash-flow forecast; pet/home prep is a single generic todo line rather than per-pet tasks; no reservation booking or monitoring integration, so 'reservations' are manually entered rows.
- Gap: Disruption replanning is absent (no flight/lodging delay or cancellation handling, no itinerary re-flow, no notification); trip budget not tied to household budget/forecast; pet prep not driven by the pets table; two overlapping travel modules (trips vs vacations).
- Build (M, migration: yes): (1) Add trips.replanDisruption service + tool: given a vacation_flights/vacation_lodging row marked delayed/cancelled (new status value), shift affected vacation_itinerary_items via lib/vacations/conflicts.ts rules, flag reservations to rebook, and notify() the family; expose an 'Report a disruption' action on [id]/travel; (2) make prepare-vacation read pets (lib/services/family or pets table) and create one sitter/boarding task per pet; (3) feed remaining vacation_budgets into loadMoneyTimeline (shared with M11) and show 'household impact' on the budget page; (4) add a 'Trip disrupted' follow-up in the prepare-vacation template.
  - Migration: Only if vacation_flights/vacation_lodging lack a status/delay column — verify in 0070 before adding `disruption_status text` + `delay_minutes int`; itinerary shifts reuse existing columns.
- Evidence: `supabase/migrations/0070_vacations.sql` 27 vacation_* tables incl. vacation_budgets, vacation_expenses, vacation_reservations, vacation_lodging, vacation_flights, vacation_transportation, vacation_packing_lists/items, vacation_documents, vacation_itinerary_days/items, vacation_weather_snapshots, vacation_members (role adult/child/grandparent/caregiver line 57).; `lib/vacations/budget.ts:26-62` summarizeBudget/overruns per category — trip-local budget only; no read of household budgets/transactions anywhere in lib/vacations or app/(app)/dashboard/vacations (grep from('budgets') empty).; `lib/services/trips/index.ts:775-800` commitmentConflicts checks calendar events, school events, sports practices, homework and unpaid bills that fall inside the trip window — calendar- and bill-aware.; `lib/services/trips/index.ts:312-419` buildPlan writes vacation_activities, itinerary days/items and upserts vacation_budgets; createPackingList (line 511-544) writes packing lists/items using lib/vacations/packing.ts suggestPacking.; `lib/ai/planner/templates/prepare-vacation.ts:52-96` Prepare-vacation workflow: getTrip, commitmentConflicts, documentsRisk, computeReadiness, home.listOpenMaintenance (line 58), buildPlan, packing lists, calendar sync, home-away checklist 'trash out, mail held, pets covered, thermostat set' (line 72), reminders, verify step, T-2 follow-up.; `lib/vacations/readiness.ts:42` computeReadiness: weighted 0-100 score over bookings, packing, documents, emergency contacts, budget, itinerary coverage.; `lib/trips/departure.ts:64-167` Departure planning with traffic factor and weather delay; app/(app)/dashboard/trip-intel/actions.ts:191 refreshDeparturePlanAction recomputes leave-by (departure_plans, migration 0098) — the only disruption-style replanning, and it is for local event departures, not travel.; `components/vacations` grep disrupt|replan|resched across lib/vacations, lib/trips, components/vacations finds only status labels (vacations-list.tsx, vacations-reports.tsx); no flight-delay/cancellation replanning logic exists.

### M16 — Extended Family Care: Permissioned care circles for appointments, tasks, documents, transport and handoffs — **partial**
- Reachable: /dashboard/care via navigation.ts:172 (minLevel 1) + requireFeature('/dashboard/care') (feature-catalog.ts:101 'basic'); /dashboard/grandparent-portal via navigation.ts:115 (free); /dashboard/family-access via navigation.ts:175 (manage only).
- Persisted: care_log (0032); grandparent portal reads family_photos, family_milestones, family_announcements, family_dates; child_logins; rides (driver_id); vacation_members.role.
- Tested: tests/care-log.test.ts, tests/grandparent-digest.test.ts, tests/grandparent-portal-read-boundary.test.ts, tests/pets-care.test.ts (pets, unrelated).
- Mock risk: Role labels promise scoped access ('View only the areas assigned to them') that no table or RLS policy enforces; a 'care circle' reviewer could count caregiver/guest roles as permissioning when they are UI-only labels (roles.ts:2 says RLS is the boundary, and RLS has no caregiver policies).
- Gap: No care-circle entity (no care_circles/care_circle_members tables), no way to invite an outside relative/nanny with scoped read/write on appointments, tasks, documents and transport, no handoff record (who has the child/parent when, notes, meds), no shared calendar/task views for circle members; care_log is a single-family elder log.
- Build (L, migration: yes): Create care circles on top of existing objects: tables care_circles (family_id, subject_member_id, name), care_circle_members (circle_id, member_id or invited email/user_id, scopes text[] among appointments/tasks/documents/transport/care_log, can_write) and care_handoffs (circle_id, from_member, to_member, starts_at, ends_at, notes, checklist jsonb); RLS: circle members may SELECT calendar_events/todos/documents/rides/care_log rows tagged with the subject member and the scope, using an is_circle_member(circle_id, scope) helper; UI: /dashboard/care gains Circles + Handoffs tabs (reuse care-module, rides driver picker, documents picker); invites reuse the existing family invite flow in components/modules/settings-module.tsx with role caregiver; notify() on handoff start/end.
  - Migration: New tables care_circles, care_circle_members, care_handoffs + is_circle_member() helper + scoped SELECT policies on calendar_events, todos, documents, rides, care_log; owner approval required. Mirror the RLS proof pattern in tests/marketplace-circles-rls.test.ts.
- Evidence: `supabase/migrations/0032_care_log.sql:13-35` care_log: member being cared for, log_type (check_in/visit/call/meal/medication/appointment/incident/note), wellbeing 1-5, note, logged_by — an elder-care timeline only.; `lib/care/log.ts:40-60` Pure helpers: lastContact, hoursSinceLastContact, isContactOverdue (24h), averageWellbeing, groupByDay; used by components/modules/care-module.tsx (reads/writes care_log lines 68,114).; `app/(app)/dashboard/grandparent-portal/page.tsx:22-33` Read-only digest for grandparents: family_members, family_photos, family_milestones, family_announcements, family_dates via lib/grandparent/digest.ts:48 buildGrandparentDigest. No write scope, no per-person permissions.; `lib/constants/roles.ts:4-33` member_role enum parent/adult/teen/child/caregiver/guest; caregiver described as 'View only the areas assigned to them' but no assigned-areas storage exists (grep member_permissions|module_access|allowed_modules|assigned_areas across migrations+lib → none).; `supabase/migrations/0001_extensions_enums.sql:10` 'caregiver' and 'guest' exist only as enum values; grep caregiver in supabase/migrations finds no RLS policy differentiating them, so a caregiver sees whatever a family member sees.; `app/(app)/dashboard/family-access/page.tsx:10-27` 'Kid Logins' — child_logins PIN management for managed members; not an external-circle invite.; `supabase/migrations/0070_vacations.sql:57` vacation_members.role allows grandparent/caregiver — trip-scoped only.; `lib/rides/schedule.ts:14-123` Rides have driver_id (family_members) and driverConflicts/unassigned counts — transport exists inside the family only; no cross-household driver or handoff record.

### M17 — Household Purchase Advisor: Check owned items, compatibility, preferences and budget before current-market recommendations — **missing**
- Reachable: No route, button or intent for a purchase advisor. Building blocks reachable separately: /dashboard/wishlists (navigation.ts:128), /dashboard/inventory (navigation.ts:147), /dashboard/family-digital-twin spend sim (navigation.ts:206).
- Persisted: Nothing for this behaviour. Reusable data: inventory_items, home_assets, closet items, wishlist_items, budgets, transactions, family_facts (preferences).
- Tested: None for the advisor. Related: tests/wishlists-gifts.test.ts, tests/inventory-finder.test.ts, tests/twin-simulate.test.ts.
- Mock risk: The 'AI shopping tips' and 'AI gift ideas' buttons could be mistaken for an advisor; they never read owned items or budgets and produce LLM text only.
- Gap: Entire behaviour missing: no owned-item check, no compatibility reasoning (brand/model/serial fields exist on inventory_items and home_assets but are unused for this), no preference lookup from family_facts, no budget gate before recommending, no market recommendation step, no surface.
- Build (M, migration: no): Minimal advisor reusing existing engines: (1) lib/purchases/advisor.ts (pure): input = candidate item text/url/price + inventory_items + home_assets + closet + wishlist_items + budgets/spent + family_facts; output = owned duplicates (via lib/inventory/finder searchItems), compatibility hints from brand/model of related assets, preference facts, and an affordability verdict via lib/twin/simulate simulateDecision('spend'); (2) server action app/(app)/dashboard/wishlists/actions.ts adviseBeforeBuying assembling the rows (family-scoped) and optionally calling the AI insights route with a new 'purchase_advisor' kind for market suggestions grounded in the deterministic result; (3) 'Before you buy' panel in wishlists-module on each item and a 'Should we buy X?' intent in lib/ai/context/intents.ts.
- Evidence: `lib/ai/insights.ts:261-270` 'shopping' insight = generic buying tips over the grocery/shopping list; no lookup of owned items.; `lib/ai/insights.ts:333-341` 'wishlists' insight = gift ideas from wishlist_items; no inventory, compatibility or budget check.; `supabase/migrations/0043_wishlists.sql:13-29` wishlist_items (title, url, price, priority, claimed_by, is_purchased) — a wish/gift register, not an advisor; lib/wishlists/gifts.ts:18-25 is claim-privacy logic (tests/wishlists-gifts.test.ts).; `lib/services/finances/index.ts:741-781` Record a purchase on the household books (finances.createTransaction, alias record_purchase at lib/ai/tools/finances.ts:297) — after-the-fact recording, no pre-purchase advice.; `lib/twin/simulate.ts:182` Spend simulation checks one budget category; not connected to wishlists, inventory or any recommendation.; `lib/marketplace/assistant.ts:1-25` Marketplace assistant answers from the family's own listings/offers/comparables — not external market recommendations for a purchase.; `lib` grep 'purchase advisor|before you buy|already own|duplicate purchase|shouldWeBuy|owned items' across lib/app/components returns no product code (hits are unrelated comments in marketing/auth/stripe).

### M18 — Kitchen / Wall Mode — tablet/browser command center — **exists**
- Reachable: Sidebar 'Kitchen Display' → /display (navigation.ts:166, minLevel 1); requireFeature('/display') → feature catalog 'kitchen' default tier 'basic' (feature-catalog.ts:105, plans.ts:113); super-admin bypass. Also a valid service-launcher href for tiles.
- Persisted: Reads: family_members, calendar_events, chore_assignments, chores, meal_plans, meals, grocery_items, reminders, notes, family_recipes, family_photos. Writes: display_layouts (tiles, settings, updated_by) via client upsert under family RLS (0026 + 0200). Timers: localStorage only.
- Tested: tests/display-render.test.ts, tests/display-tiles.test.ts, tests/display-ambient.test.ts, tests/display-server-safety.test.ts, tests/display-recover.test.ts, tests/display-imagery.test.ts, tests/display-name-firstname.test.ts. No Playwright spec touches /display (grep '/display' tests/e2e → none).
- Mock risk: By design every widget read is best-effort: a failing query is console.error'd and the tile renders empty/'—' (page.tsx:87-90, emptyDisplay fallback at 231-237), so a broken RLS or missing table looks like an empty family rather than an error. Requires a normal signed-in user session on the tablet — there is no kiosk pairing/device login, so a logged-out tablet just redirects to sign-in.
- Gap: The wall itself is real and hardened, but it is a read-mostly board: no Ask Bubaly / voice on the display (grep voice|ask|assistant in components/display → none), no kiosk pairing or device-scoped session (a full parent login lives on the counter), timers are per-device localStorage, no 'handled today' / brief / approvals widgets from the Phase 5-7 runtime, and zero e2e coverage of /display.
- Build (M, migration: yes): Keep the existing shell; add (a) an 'ask' widget key that mounts components/concierge/ask-bubaly with the existing useSpeechRecognition mic and a 'handled today' widget fed from lib/briefing handled(), (b) a /display/setup guide route (install PWA, fullscreen, keep-awake, orientation) linked from the display header, (c) optional kiosk pairing: a display_devices table (family_id, hashed pairing code, label, last_seen_at, revoked_at) + a pairing page that mints a short code from a signed-in phone and a cookie-scoped read-only session for the tablet. (a)+(b) need no migration; (c) does.
  - Migration: Only the optional kiosk pairing (display_devices + RLS + a SECURITY DEFINER RPC to redeem a code) needs a migration; the ask/handled widgets and the setup guide ship without one.
- Evidence: `app/(app)/display/page.tsx:20-256` Server page 'Kitchen Display' (force-dynamic). loadDisplay() reads family_members, calendar_events (today/14d/month), chore_assignments, meal_plans, grocery_items, reminders, notes, family_recipes, family_photos and display_layouts (lines 54-80); requireFeature('/display') gate with reconnect screen on transient failure (214-225); AutoRefresh every 120s and client-only DisplayShellClient (244-256).; `components/display/display-grid.tsx:499-503,541-547,573-575` Fullscreen toggle, kiosk one-viewport fit, and layout save = supabase.from('display_layouts').upsert({family_id, tiles, settings, updated_by}, {onConflict:'family_id'}). 14 built-in widgets + a 'service' launcher tile for any app route.; `lib/display/tiles.ts:20-24,84-108` Pure tile model; resolveTiles() validates untrusted stored jsonb; SERVICE_WIDGET href allow-list.; `lib/display/ambient.ts:1-6` Pure day-part theming, now/next, countdown, settings normalization used by the wall.; `supabase/migrations/0026_display_layouts.sql:8-35` display_layouts table (one row per family, tiles jsonb) with is_family_member RLS on all four verbs.; `supabase/migrations/0200_display_settings.sql:15-16` settings jsonb column on display_layouts (clock24, tempUnit, theme, ambient, screensaver).; `components/display/kitchen-timers.tsx:6,29,37` Kitchen timers persist only in localStorage (per device, not shared across displays).; `app/(app)/display/error.tsx:3-11` Kiosk error boundary with stale-bundle hard-reload escalation (lib/display/recover.ts).

### M19 — Voice Everywhere — hands-free Ask Bubaly across phone, tablet, shared display — **partial**
- Reachable: /dashboard/voice via sidebar (navigation.ts:205, free tier); mic inside /dashboard/assistant (assistant-module.tsx:380-388); journal + capture shell dictation. Not reachable from the home Ask bar, the command bar, the concierge Ask Bubaly panel, /display, or the Expo app.
- Persisted: voice_commands rows (0121) + whatever saveCapture writes (todo_items/notes/calendar_events/grocery_items via lib/capture/save). Assistant voice turns persist as normal assistant conversations; voice mode/voice name only in localStorage.
- Tested: tests/ai-voice.test.ts, tests/voice-command-router.test.ts, tests/voice-transcript.test.ts (pure helpers). No tests for /api/ai/voice/transcribe or /speak routes, none for use-voice/use-speech-recognition, none e2e.
- Mock risk: Web Speech API only works in Chromium/WebKit (module falls back to typing). Transcribe/speak return 503 without an OpenAI key (honest, but the mic then does nothing useful). Command routing is regex-only — 'Ask Bubaly' questions typed into /dashboard/voice become notes/tasks, not answers.
- Gap: Voice exists in two silos (Voice Control capture page; mic in the assistant chat) but not 'everywhere': no mic on the home Ask bar, command bar, or concierge Ask Bubaly; nothing on the kitchen display; the Expo app cannot record; the voice API routes are cookie-only (mobile bearer callers would get 401); no wake-word/hands-free loop; no route tests.
- Build (M, migration: no): Extract the assistant's mic flow into one shared <MicButton> (useVoice for recording→/api/ai/voice/transcribe with useSpeechRecognition as the no-key fallback) and mount it in ask-bar.tsx, command-bar.tsx, ask-bubaly.tsx and a display 'ask' tile; make /api/ai/voice/transcribe and /speak accept the same bearer auth as /api/ai (lib/supabase/bearer.ts); add expo-av recording in mobile/app/(tabs)/assistant.tsx posting to the transcribe route with the bearer token. No migration.
- Evidence: `app/(app)/dashboard/voice/page.tsx:1-10` /dashboard/voice renders VoiceModule behind requireUserContext.; `components/modules/voice-module.tsx:9,55-58,87-105` Web Speech API (useSpeechRecognition) → classifyVoiceCommand → saveCapture() into the capture pipeline, then inserts a voice_commands history row with resolved_kind/action_table/action_count; undo supported.; `lib/voice/command-router.ts:1-6,65-80` Pure wake-word stripping and intent routing (task/note/event/shopping) reusing lib/capture/parse.; `lib/hooks/use-speech-recognition.ts:3-6` SSR-safe wrapper over SpeechRecognition/webkitSpeechRecognition; also used by journal-module.tsx and capture-shell.tsx.; `lib/hooks/use-voice.ts:24-31` MediaRecorder capture → server transcription + TTS playback; mode/voice persisted per device in localStorage.; `components/modules/assistant-module.tsx:29,104,369,380-388` The AI assistant chat has a mic (onMicPress → voice.startRecording → send(cleaned)) and speaks replies when voice mode is on.; `app/api/ai/voice/transcribe/route.ts:16-31` POST audio → OpenAI Whisper; requireUserContext (cookie session only), rate-limited, honest 503 when no OpenAI key.; `app/api/ai/voice/speak/route.ts:15-22` Text-to-speech via OpenAI; same cookie-only auth.

### M20 — Family Phone + Email Front Desk — **partial**
- Reachable: /dashboard/contact-center (requirePlanLevel(2), parent-only writes); webhooks /api/contact-center/{email,sms,voice} gated by CONTACT_CENTER_INBOUND_SECRET / Twilio signature; /dashboard/front-desk (requireFeature) is a separate manual surface
- Persisted: family_contact_channels, family_inbox_messages (upsert on channel+provider_ref), Twilio provisioning; front_desk_settings/call_logs only via manual client inserts
- Tested: tests/contact-center.test.ts, tests/guardian-callback-security.test.ts (Twilio signature/replay pattern), tests/guardian-authz.test.ts
- Mock risk: Without Twilio config provisionFamilyNumber flips to 'pending' and the page shows a request state — a number never arrives; the Front Desk page looks like telephony but its call log is hand-entered; classification is regex-based unless an AI provider is configured
- Gap: The optional household identity (claimable @bubaly.com address, provisioned Twilio number, inbound email/SMS/voice filed into family_inbox_messages with classification, urgent escalation and auto-reply) is real, plan-gated and tested. Missing: 'routes them into Bubaly' — inbound messages never become events/tasks/approvals/runs; no inbound forms channel; the older /dashboard/front-desk (front_desk_settings/call_logs) is a disconnected manual duplicate of the Contact Center.
- Build (L, migration: yes): After recordInboundMessage, hand appointment/delivery/personal messages to the planner via submitRequest (intent 'capture', system scope for the family, approval gating from trust) and stamp the message with the request id so the inbox row links to the run; accept inbound email attachments/links as paperwork by calling lib/paperwork/triage and inserting paperwork_items; point /dashboard/front-desk at family_inbox_messages voice rows and family_contact_channels settings so one surface remains (route kept, sidebar untouched).
  - Migration: family_inbox_messages: add request_id uuid references ai_requests, paperwork_item_id uuid references paperwork_items, and extend channel check to include 'form'; same migration as M2's link columns — ship once.
- Evidence: `supabase/migrations/0214_family_contact_center.sql:16,36,73-77` family_contact_channels (email_local, phone_number, provisioning_status, ai_concierge_enabled, forward_to_phone) and family_inbox_messages with family-scoped RLS; `lib/contact-center/server.ts:19-33,55-66,82-118,130-160` channel get-or-create, routing by number/email local-part, Twilio number provisioning (pending when unconfigured), recordInboundMessage/recordOutboundMessage; `lib/contact-center/routing.ts:9-13,30-45` deterministic intents urgent/appointment/delivery/sales/spam/personal and route escalate/auto_reply/file; `app/api/contact-center/email/route.ts:29-33,74-113` secret-gated inbound email webhook: resolve family, run concierge, file message, urgent SMS to forward_to_phone, auto-reply email; voice/route.ts:1-4 Twilio voice with transcription into inbox; sms/route.ts; `app/(app)/dashboard/contact-center/page.tsx:15-20` /dashboard/contact-center requires plan level 2 (Family+); actions.ts:16-24 parent-only management, :27-61 assign @bubaly.com local and provision number; `grep todo_items|calendar_events|reminders|paperwork_items|submitRequest lib/contact-center app/api/contact-center` no hits — inbound messages are filed and classified but never routed into planner/needs/records; `app/(app)/dashboard/front-desk/page.tsx` /dashboard/front-desk (AI Front Desk) renders FrontDeskModule; components/modules/front-desk-module.tsx:85,100,582,711 reads call_logs/front_desk_settings and inserts call_logs manually — no API route writes call_logs (grep app/api: none); `supabase/migrations/0092_front_desk.sql:1-40` front_desk_settings + call_logs — an earlier, disconnected front-desk surface

### M21 — Actionable Notifications: every notification is actionable; low-priority information is compressed into briefings — **partial**
- Reachable: /dashboard/notifications (requireFeature), bell components/app/notification-bell.tsx, push via EnablePushButton, email digest via lib/server/notification-emails.ts cron
- Persisted: notifications (insert/update/delete), family_ai_settings (quiet hours read), family_members (recipient resolution)
- Tested: tests/service-notifications.test.ts, tests/notification-due-surfaces.test.ts, tests/notification-digest.test.ts, tests/notification-write-boundary.test.ts, tests/cron-notification-failure-status.test.ts, tests/briefing-build.test.ts
- Mock risk: Rows carry related_type/related_id but the UI ignores them, so a notification is informational only; 'digest' in the code is an email grouping, not a low-priority compression into the brief.
- Gap: Notifications are not actionable (no deep link or inline action derived from type/related_*), there is no priority concept, and low-priority notices are not compressed into the briefing — the brief never reads the notifications table.
- Build (M, migration: yes): Add a pure notification->action mapper (type + related_type/related_id -> href + optional inline action using existing chore/approval/reminder actions), render it in the module and bell; add a priority ('now' | 'digest') to notify() and the AI tool, persisted on the row; bell/list show 'now' only, and lib/briefing/build.ts folds unread 'digest' rows into an 'Also today' section via listUnread and marks them read when the brief is viewed.
  - Migration: Add notifications.priority text not null default 'now' check in ('now','digest') plus index (family_id, priority, is_read); no RLS change. The action mapper and UI links can ship without the migration.
- Evidence: `lib/services/notifications/index.ts:41-55,122-190` notify() single entry point: recipients family/managers/member ids, type, title, body, relatedType/relatedId, sendAt, urgent; dedupe on unread + related_id/title; quiet-hours deferral from family_ai_settings; `supabase/migrations/0002_tables.sql:405-418` notifications table: user_id, type, title, body, related_type, related_id, is_read, send_at, sent_at — no priority, no action/href column; `components/modules/notifications-module.tsx:114-138` List row renders title/body/time; click only marks read; trash deletes; related_type/related_id are never turned into a link or action; `app/(app)/dashboard/notifications/page.tsx:8-17` Route gated by requireFeature; EnablePushButton for web push; `lib/ai/tools/notifications.ts:28-45` AI tool notifications.notify exposes title/body/audience/type/related/send_at/urgent — no action target; `lib/notifications/digest.ts:1-45` Email digest groups pending rows by user with icons — grouping for email, not priority compression; `lib/briefing/build.ts:6-16,51-58,111-123` Briefing is built from calendar, concierge digest and completed runs ('handled', timeSavedMinutes); it does not read the notifications table, so nothing low-priority is folded into it; `lib/concierge/digest.ts:1-30` Concierge digest ranks deadline obligations (bills, meds, maintenance, warranty, trips, pantry) — independent of notifications

### M22 — Household Memory Controls: users inspect, correct and delete what Bubaly believes about routines/preferences — **exists**
- Reachable: /dashboard/knowledge (requireUserContext), Settings > Bubaly AI > memory panel (/dashboard/settings), /dashboard/playbook, routines panel in /dashboard/calendar, /dashboard/family-automation
- Persisted: family_facts (read/insert/update/delete), family_playbook_suggestions (read/insert/update/delete), family_ai_settings.memory_enabled (read), routine_templates, routine_template_items, family_automation_rules
- Tested: tests/service-memory.test.ts, tests/memory-write-path.test.ts, tests/memory-opt-out.test.ts, tests/memory-expiry-lifecycle.test.ts, tests/memory-expiry-review.test.ts, tests/family-facts.test.ts, tests/family-knowledge.test.ts, tests/playbook-learn.test.ts, tests/tool-meals-memory-messages.test.ts
- Mock risk: Controls are split across four surfaces (Knowledge, Settings AI memory, Playbook, calendar routines) and the /dashboard/family-memory route is a redirect to the photo/journal Memories page, so a reviewer landing there could conclude memory controls are absent; digital-twin traits are inferred beliefs with no UI.
- Gap: Residual: no single 'what Bubaly believes about us' view, and the autopilot digital-twin traits (family_digital_twin_profiles) cannot be inspected or reset by the family.
- Build (S, migration: no): Extend the Settings AI-memory panel with a 'Routines & traits' section that lists detected routine_templates (delete via existing delete) and each member's twin traits from family_digital_twin_profiles.metadata with a 'reset' action; link the panel from the Knowledge page header so the four surfaces are one click apart.
- Evidence: `lib/services/memory/index.ts:1-45,130,399,452,543,599,673` Two-table policy: confirmed family_facts vs unconfirmed family_playbook_suggestions; rememberFact, recallFacts, confirmFact, forgetFact, clearAiMemory (only AI-sourced rows), updateFact; provenance column source (0265), expires_at, sensitive categories refused; `lib/ai/tools/memory.ts:66-200` memory.remember routes inferences to the review inbox unless asked_for, refuses medical/account; memory.recall respects memoryEnabled; memory.forget deletes fact or dismisses suggestion; `app/(app)/dashboard/knowledge/page.tsx:8 and app/(app)/dashboard/knowledge/actions.ts:47-106` Knowledge Base page (KnowledgeBaseModule) with saveFactAction (updateFact), setFactPinnedAction, forgetFactAction; `components/settings/ai-memory.tsx:86,126,134,161` Settings > Bubaly AI memory panel: clear what Bubaly learned, confirm or dismiss suggestions, forget individual AI facts (mounted from components/settings/ai-settings.tsx); `app/(app)/dashboard/playbook/playbook-actions.ts:44-167` Playbook inbox: refresh (learnPlaybook), accept, dismiss suggestions; `components/modules/routines-panel.tsx:68-72,172-175` Routine templates Bubaly detected are listed and deletable; scheduled routines toggle/delete on /dashboard/family-automation; `app/(app)/dashboard/family-memory/page.tsx:9-11` /dashboard/family-memory redirects to /dashboard/memories (journal) — the fact surface is /dashboard/knowledge; `lib/autopilot/twin.ts and lib/autopilot/scan.ts:57` Digital-twin member traits (family_digital_twin_profiles.metadata) are inferred and consumed by autopilot but have no inspect/correct UI

### M23 — Permission Model as a Feature — **partial**
- Reachable: /dashboard/trust via lib/constants/navigation.ts:84 (Trust & Permissions, free tier per lib/constants/plans.ts ROUTE_PLAN_LEVEL '/dashboard/trust': 0); /dashboard/family-access via navigation.ts:175 (manage-only); /family/permissions page exists; /kids and /kid-login for children.
- Persisted: permission_grants, trust_delegations, trust_policies, emergency_sessions, trust_audit_logs, approval_requests (0093); documents RLS via is_sensitive_document (0266); ai_plans/ai_plan_steps/family_facts role policies (0264); child_logins; static `permissions` table.
- Tested: tests/trust-engine.test.ts (grants, delegations, role defaults), tests/trust-read-boundary.test.ts, tests/document-vault-boundary.test.ts, tests/0264-ai-surface-role-privacy.test.ts, tests/child-login.test.ts, tests/child-login-persistence.test.ts, tests/child-login-action-security.test.ts.
- Mock risk: Grants/delegations gate AI and privileged ACTIONS through evaluateTrust; they do not scope what a caregiver/guest can READ in ordinary pages (RLS is role-based: is_family_member vs can_manage_family). The documents 'Private {member}' visibility is client-only (member_id, no RLS). Finance tables are explicitly role-blind (0264 header). So 'caregiver scopes' and 'object-level sharing' look present in the Trust UI but do not bound data access.
- Gap: Missing: (1) object-level / per-member sharing enforced in the database (documents.member_id, journal, notes have no member-scoped RLS; no share-with-member or share-link with expiry table); (2) caregiver/guest read scoping — delegations and grants only affect the trust engine, not RLS on calendar/chores/medical/finance tables, and finance tables are role-blind; (3) the model is not surfaced as a feature: no marketing features card, no plain-language 'Sharing & access' panel with presets (babysitter tonight, grandparent this week) — delegations are buried in the admin-style Trust page; (4) no role-aware landing for guests/caregivers.
- Build (L, migration: yes): Surface what exists first: add an 'Access & Sharing' section to /dashboard/trust with time-limited presets built on trust_delegations (reuse createDelegationAction) and a per-member 'what they can see' summary derived from ROLE_DEFAULTS + grants; add a Features/Security marketing card that links to it. Then enforce object-level privacy in the DB: RLS on documents/notes/journal honouring member_id (owner or manager) and a caregiver read-scope check that consults active trust_delegations domains for sensitive tables (medical, finances). Ratchet with SQL-string tests like document-vault-boundary.
  - Migration: New RLS on documents/notes/journal_entries honouring member_id (owner or can_manage_family) and a SQL helper has_active_delegation(family_id, domain) used by sensitive-table read policies; no new tables strictly required. Owner approval needed.
- Evidence: `supabase/migrations/0093_trust_engine.sql:47-83` permission_grants (per-member domain×capability allow/deny) and trust_delegations (domains[], starts_at, expires_at NOT NULL, revoked_at) — real tables for scoped + time-limited authority.; `lib/trust/engine.ts:83-119` ROLE_DEFAULTS per role incl. caregiver (view/create/edit; finances/banking/passports/insurance/documents sensitive) and guest (view only); :267-268 deny grant wins, :300-305 allow grant / active delegation, :410 RESTRICTED_READ_ROLES for teen/child/caregiver/guest.; `lib/trust/server.ts:60-93` loadTrustInputs reads trust_policies, permission_grants, trust_delegations (revoked_at null, expires_at > now), emergency_sessions; evaluateTrust writes trust_audit_logs and approval_requests.; `app/(app)/dashboard/trust/actions.ts:135-197` setPermissionGrantAction upserts/deletes permission_grants; createDelegationAction inserts trust_delegations with future expiry; revokeDelegationAction sets revoked_at. Manager-only.; `app/(app)/dashboard/trust/page.tsx:31-32` Trust page reads permission_grants and active trust_delegations for the family.; `lib/constants/navigation.ts:84,175,29` '/dashboard/trust' (Trust & Permissions) and '/dashboard/family-access' (Kid Logins, manage:true) in nav; manage-only items hidden from non-managers.; `lib/ai/context/policy.ts:33-80` SENSITIVE_TABLES deny-list keeps credentials, documents, health, finance, location out of AI context; role-keyed slice visibility.; `supabase/migrations/0266_document_vault_boundary.sql:40-96` is_sensitive_document() + documents RLS: sensitive/secure docs readable/writable only by can_manage_family (parent/adult) — DB-enforced private adult data.

### M24 — AI Trust Center: evidence, confidence, approvals, autonomous policies, actions and sensitive-access history (in-app) — **partial**
- Reachable: /dashboard/trust via sidebar nav (lib/constants/navigation.ts:84); /dashboard/settings AI autonomy dials; /dashboard/concierge/runs/[id] per run
- Persisted: trust_policies, permission_grants, trust_delegations, approval_requests, emergency_sessions, trust_audit_logs (reason, confidence), family_ai_settings, ai_requests/ai_plans/ai_plan_steps/ai_tool_calls (runs), ai_request_context.sensitive_omitted (written, not shown)
- Tested: tests/trust-engine.test.ts, tests/trust-read-boundary.test.ts, tests/approval-card.test.ts, tests/approval-execution.test.ts, tests/approval-model-enforcement.test.ts, tests/approval-pending-once.test.ts, tests/0260-trust-ledger-lockdown.test.ts, tests/ai-runtime-lockdown.test.ts, tests/assistant-trust-wrapper.test.ts
- Mock risk: The Trust page is a permissions console; 'evidence' for a decision is a reason string, not the data it was based on; sensitive_omitted exists in the DB but no page shows it, so a reviewer could count it as sensitive-access history when families cannot see it.
- Gap: No in-app sensitive-access history (which sensitive tables/slices the AI read or was denied, per request); no evidence view linking an approval/decision to the context snapshot it used; actions taken (ai_tool_calls) are only visible per run, not as a household-wide ledger; autonomous policies are split between trust_policies and family_ai_settings without one Trust Center view.
- Build (M, migration: yes): Add an 'Activity' tab to the Trust module that unifies: recent ai_tool_calls (via lib/ai/runs/store) with links to runs, the family's autonomy dials (family_ai_settings) beside trust_policies, and a per-request 'What Bubaly read / withheld' list from ai_request_context (snapshot slice names + sensitive_omitted); add a 'based on' expander to approval cards showing the slice names from the request's context snapshot; record per-slice reads in a new access log so the history is complete beyond omissions.
  - Migration: New table ai_context_access_log(request_id, family_id, slice, table_name, row_count, sensitive bool, created_at) with family-manager read RLS and service/invoker insert from the context builder; the Activity tab and 'based on' expander can ship first using existing ai_request_context.sensitive_omitted and snapshot without a migration.
- Evidence: `components/modules/trust-module.tsx:62,142-147,728-731` Trust & Permissions page tabs: approvals, policies, permissions, delegations, emergency, audit; audit tab renders trust_audit_logs with reason; `app/(app)/dashboard/trust/page.tsx:29-35` Reads trust_policies, permission_grants, trust_delegations, approval_requests, emergency_sessions, trust_audit_logs (decision, reason, confidence); `supabase/migrations/0093_trust_engine.sql:163-166` trust_audit_logs has decision, reason (human-readable), confidence numeric(4,3); `lib/trust/server.ts:1-80 and lib/trust/engine.ts:1-16` Single evaluateAction entry point loads policies/grants/delegations/emergency, records explainable audit, opens approval requests; `lib/approvals/card-data.ts:25-34,72-73,177` Approval card shows title + plain-language consequences; deliberately no payload/reasoning chain; row carries confidence + reasoning; `components/settings/ai-settings.tsx:34-35,119,152` Autonomy dials (recommend/ask/auto) per category persisted to family_ai_settings — autonomous policies live here AND in trust_policies; `app/(app)/dashboard/concierge/runs/[id]/page.tsx:1-12` Run page shows objective, state, steps, event messages, reasoning_summary; per-run only; `lib/ai/context/policy.ts:35-114 and supabase/migrations/0250_ai_runtime_core.sql:89` Sensitive tables excluded from AI context; slices withheld are recorded in ai_request_context.sensitive_omitted — readers are only lib/ai/context/builder.ts and policy.ts, never a UI

### M25 — Outcome Templates — Tournament Day, School Morning, Vacation, Moving, Back-to-School, Holiday, Emergency Prep — **partial**
- Reachable: /dashboard/outcomes and /dashboard/prep-plans (sidebar, free tier); /dashboard/life-events page (life-events-module); executable templates are reached only when the concierge classifier maps a request to one of the nine intents.
- Persisted: Life events → life_event_plans + life_event_plan_items; prep → prep_plans + prep_plan_steps; executable templates → ai_requests/ai_plans/ai_plan_steps via the run executor. Outcomes launcher persists nothing.
- Tested: tests/outcomes-launcher.test.ts, tests/planner-templates.test.ts, tests/life-events-templates.test.ts, tests/planning-prep.test.ts (all pure). No persistence test for launchLifeEventAction or runPrepGeneration.
- Mock risk: 'Outcomes' page looks like launchable templates but only deep-links to modules; 'Vacation' and 'Moving' exist as checklists (life events) and Vacation also as a run, but Tournament Day, School Morning, Holiday and Emergency Prep exist nowhere (grep tournament|school morning|holiday|emergency across lib/outcomes, lib/ai/planner, lib/life-events → none).
- Gap: No unified, launchable template catalogue: three disconnected engines (static outcome links, six life-event checklists, nine planner workflows). Missing templates: Tournament Day, School Morning, Back-to-School (only a school_start checklist; prep never generates it), Holiday, Emergency Prep. The Outcomes page cannot start a run; life-event launches are not ledgered in ai_requests.
- Build (L, migration: no): Add WorkflowTemplates tournament_day, school_morning, back_to_school, holiday, emergency_prep and moving under lib/ai/planner/templates (composed from existing tools: calendar/tasks/reminders/groceries/documents/notify), register the intents in lib/ai/context/intents.ts + INTENT_SLICES, and give components/modules/outcomes-launcher.tsx a 'Have Bubaly do it' button that POSTs to /api/ai/requests with a template hint so the launch lands in ai_requests/ai_plans. Add holiday + emergency_prep life-event checklists for families without AI enabled. No migration.
- Evidence: `lib/outcomes/launcher.ts:8-10,40-49,62-129` Eight static outcomes (run_today, feed_family, plan_trip, prepare_school, manage_money, stay_healthy, celebrate, prepare_unexpected); buildOutcomePlan() returns link steps (href to modules) with live badges — a launcher, not an executable template.; `app/(app)/dashboard/outcomes/page.tsx:32-56` Real count snapshot (calendar_events, todo_items, grocery_items, family_members) feeds the badges; nothing is created on 'launch'.; `components/modules/outcomes-launcher.tsx:91-104` Each step is a next/link <Link href> — no run/plan is started.; `lib/ai/planner/templates/index.ts:106-116,135-152` Executable WorkflowTemplate registry: daily_brief, plan_meals, plan_week, organize_weekend, remind_everyone, prepare_vacation, spending_review, find_vendor, what_am_i_forgetting; instantiateTemplate() yields a Plan for the run executor.; `lib/ai/planner/templates/prepare-vacation.ts:1-11` Vacation is the only strategy template that exists as an executable workflow (keys off vacation_id).; `lib/ai/context/intents.ts:32-36` INTENT_KEYS has no tournament_day, school_morning, back_to_school, holiday, emergency_prep, moving.; `lib/life-events/templates.ts:1-8,33` Life-event playbooks (checklists with day offsets): new_baby, moving, school_start, vacation, new_pet, new_job.; `app/(app)/dashboard/life-event-actions.ts:18-52` launchLifeEventAction inserts life_event_plans + life_event_plan_items (rolls back the plan on item failure).

### M26 — Marketplace / Service Layer — providers, AI-scoped requests, compare, manage — **partial**
- Reachable: /marketplace (+ browse/deals/auctions/selling/orders/…) from the sidebar; /dashboard/projects for quotes; /dashboard/concierge-calls; find_vendor via the concierge classifier.
- Persisted: marketplace_* tables (20+ migrations) with owner-gated RPCs; home_contractors, home_service_records, maintenance_tasks; home_projects + project_quotes (0246); concierge_calls (0173).
- Tested: tests/marketplace-*.test.ts (~50 files incl. authz, order lifecycle, negotiation security), tests/planner-templates.test.ts (find_vendor), tests/concierge-calls-brief.test.ts, tests/concierge-calls-authz.test.ts, tests/home-management.test.ts.
- Mock risk: The 'AI calls a provider' surface is a queue whose placement step never dials (place/route.ts:60-62); find_vendor deliberately never names an external provider; 'quotes' are typed in by the parent. The marketplace is real but is family/circle goods trading, not a service-provider layer.
- Gap: No provider directory or service categories (home/care/travel/shopping), no service_requests object the AI can scope from a request, no multi-provider quote solicitation/comparison, no booking-workflow state machine, no partner-facing supply side. Existing pieces cover only the family's own contractor book, manual quotes on home projects, a non-dialing call queue, and a goods marketplace.
- Build (XL, migration: yes): Add a thin service layer on top of what exists: migration for service_providers (family-scoped + optional circle-shared, category, contact, source), service_requests (scoped brief, category, status queue→quoting→booked→done, linked maintenance_task/trip/care) and service_quotes (reusing the project_quotes shape); a lib/services/providers domain service + tools services.scopeRequest / services.requestQuotes / services.compareQuotes / services.book registered in lib/ai/tools; extend find_vendor into a 'book_service' WorkflowTemplate whose outreach step enqueues concierge_calls or a templated email via notify(); UI at /dashboard/service-requests reusing the projects quote-compare component.
  - Migration: New tables service_providers, service_requests, service_quotes with is_family_member RLS and a circle-share policy modelled on 0176; requires owner approval.
- Evidence: `app/(app)/marketplace/page.tsx:85-104` Marketplace home reads marketplace_listings, marketplace_offers, marketplace_matches, marketplace_saves, marketplace_stores, marketplace_follows, marketplace_reviews.; `lib/marketplace/listings.ts:14-20` Listing kinds are sell/rent/borrow/free/wanted/swap — goods only, no service kind.; `lib/marketplace/community.ts:1-7` Cross-family reach is opt-in circles via invite code (migration 0176) — not a provider directory.; `supabase/migrations/0120_marketplace.sql` Plus 0150-0199 (matches, v2, saved searches, questions, ownership RPCs, circles, auctions, negotiations, handoffs, price history, returns, reports) — a full peer-to-peer goods marketplace.; `tests/marketplace-authz.test.ts:1-12` Owner-gated SECURITY DEFINER RPC invariants pinned to migrations; ~50 marketplace-* tests in tests/ (listings, auctions, negotiation, order-lifecycle, handoff, returns, read boundaries).; `lib/ai/planner/templates/find-vendor.ts:1-7,19-31` Workflow D 'Find someone to fix it' is deterministic and explicit: 'Bubaly has no external provider search' — it checks home_contractors/service history and hands the booking to a parent as a task.; `lib/services/home/index.ts:137,269-272` Contractor book (home_contractors), home_service_records, maintenance_tasks — the family's own vendors, no marketplace of providers.; `supabase/migrations/0246_home_projects.sql:7-10,62-67` project_quotes: contractor quotes to compare (linked to home_contractors).

### M27 — Bubaly API / Partner Platform — authenticated inbound partner API with keys/scopes — **missing**
- Reachable: No route: nothing under app/api matches partner/v1/ingest; no settings UI to mint keys (grep 'api key' across app/(app)/dashboard/settings → none).
- Persisted: No partner key/scope table (grep migrations for api_key → none); adjacent stores: family_inbox_messages (0214), push_devices (0035), sync_* (0018).
- Tested: None for a partner API. Adjacent: tests/contact-center.test.ts (address/routing helpers), tests/public-webhook-signature-boundary.test.ts, tests/sync-feed-contract.test.ts.
- Mock risk: The inbound-email endpoint could be mistaken for a partner API: it is a single shared secret for all families, accepts free text only, and in non-production runs with no secret at all (email/route.ts:29-31).
- Gap: Nothing exists for authenticated, scoped partner pushes: no key issuance/revocation, no per-family scopes, no structured event schema (calendar_event, task, announcement, roster change, device reading), no partner event ledger, no idempotency for partner deliveries, no docs page. Schools/teams reach households only via pasted ICS feeds or the shared-secret email webhook.
- Build (L, migration: yes): Migration: partner_api_keys (id, family_id, partner_name, key_hash, key_prefix, scopes text[], created_by, last_used_at, revoked_at) + partner_events (family_id, key_id, idempotency_key, kind, payload jsonb, status, result_ref). Route app/api/partner/v1/events (POST): read x-api-key, look up by prefix, timingSafeEqual on sha256 hash (pattern from lib/sync/feed-token.ts), enforce scopes, validate a zod union of structured events, then apply through lib/services/* under systemScopeForFamily (calendar.createEvent, tasks.createTodo, notify()) and mirror a summary into family_inbox_messages via recordInboundMessage so the front desk sees it. Settings → Integrations page to mint (show once) and revoke keys. Add '/api/partner' to the middleware public allowlist and a public docs page.
  - Migration: Two new tables with RLS (family managers read/revoke their keys; service role only for hash lookup), unique(family_id, idempotency_key) on partner_events.
- Evidence: `lib/ai/provider.ts` grep -rln 'api_key|x-api-key|partner' over lib, app/api, supabase/migrations hits only OpenAI/GIF keys, relationship 'partner' phrasing, and 0065_affiliates (commission partners) — no partner API key table or route.; `supabase/migrations/0065_affiliates.sql:3-5` 'partners' here are affiliate marketers with a code + commission rate, not API partners.; `app/api/ai/route.ts:7-9,44-55` Only bearer auth in the app is a Supabase user JWT (Expo mobile) — no machine/partner credential.; `lib/supabase/bearer.ts:1-7,16-21` extractBearerToken + RLS-bound client for user JWTs; reusable as the partner-scoped client pattern.; `app/api/contact-center/email/route.ts:1-9,29-34` Inbound-email webhook for a family's @bubaly.com address: ONE global CONTACT_CENTER_INBOUND_SECRET (query ?key= or x-inbound-secret), unstructured mail, resolved by recipient local-part.; `lib/contact-center/server.ts:122-147` recordInboundMessage() files inbound into family_inbox_messages with classification — the closest existing 'push into a household' path (migration 0214_family_contact_center.sql).; `app/api/guardian/inbound/sms/route.ts:1-3,27-31` Twilio-signed SMS/voice/WhatsApp inbound — vendor signature, not partner keys.; `lib/calendar/providers.ts:52-58,79-85` Schoology and TeamSnap are supported only as ICS feed URLs the family pastes (pull), not as partners pushing structured updates.

### M28 — Family Network Effects — **partial**
- Reachable: Invite with role via Settings module (components/modules/settings-module.tsx:455-500) and email link /join?token= (app/join/page.tsx → components/auth/join-invite.tsx:44 accept_invite); family switcher in components/app/app-shell.tsx; /dashboard/grandparent-portal via navigation.ts:115.
- Persisted: invites, family_members (role), user_preferences.active_family_id, families; grandparent portal reads family_photos/family_milestones/family_announcements/family_dates.
- Tested: tests/grandparent-digest.test.ts, tests/grandparent-portal-read-boundary.test.ts, tests/onboarding-family.test.ts (invite path in onboarding), no test of multi-household switching or accept_invite.
- Mock risk: Cross-household participation exists only as 'be invited into each family separately and switch' — there is no link between households, no aggregated view across the grandkids' families, and the grandparent portal shows the active family only and is not gated to or defaulted for guest/caregiver roles. 'Scoped access' for extended family is role-level, not the caregiver-specific scoping M23 needs.
- Gap: Missing: (1) a cross-household view (a grandparent with two invited families sees one family at a time; no 'all my grandchildren' digest iterating ctx.memberships); (2) role-aware landing — guest/caregiver land on /home like everyone, the Grandparent Portal is just another nav item; (3) invite presets for extended family (grandparent = guest + optional photo/celebration-only scope; caregiver = time-boxed delegation) tying invites to trust_delegations; (4) any notion of household links (co-parent households, shared custody) — would need a new table.
- Build (M, migration: no): Reuse the multi-membership context: make /dashboard/grandparent-portal iterate ctx.memberships (query each family the user belongs to via the RLS-bound client) and render a merged digest with buildGrandparentDigest per family; default guest-role users to the portal (defaultDashboard in app-context/plan); add 'Invite a grandparent / caregiver' presets to the settings-module invite form that pick the role and, for caregivers, create a trust_delegations row on accept. No schema change for the minimal version; household linking (co-parents) is a follow-up migration.
- Evidence: `lib/supabase/auth.ts:100-147` getUserContext builds memberships[] across every family_members row for the user and picks active by user_preferences.active_family_id — a user can belong to multiple households with a distinct role in each.; `app/(app)/actions.ts:37-55` setActiveFamilyAction upserts user_preferences.active_family_id (household switching).; `components/app/app-shell.tsx:72-80,130-139` Sidebar family switcher lists `families` from useApp and calls setActiveFamilyAction.; `supabase/migrations/0002_tables.sql:66-78` invites table carries role (member_role), token, status, expires_at (14 days).; `supabase/migrations/0136_accept_invite_idempotent.sql:13-56` accept_invite RPC inserts family_members with the invite's role and sets active_family_id when null; email must match.; `components/modules/settings-module.tsx:455-500` Invite form inserts invites with a role chosen from INVITABLE_ROLES (adult/teen/caregiver/guest) then POSTs /api/email/invite (app/api/email/invite/route.ts:12-48 sends InviteEmail).; `lib/constants/roles.ts:5-30` Roles parent/adult/teen/child/caregiver/guest; caregiver 'View only the areas assigned to them', guest 'View limited shared events only'; isManager = parent/adult.; `app/(app)/dashboard/grandparent-portal/page.tsx:17-36` Grandparent Portal reads families, family_members, family_photos, family_milestones, family_announcements, family_dates for the ACTIVE family; requireUserContext only — not role-gated and not cross-household.

### M29 — Zero-Setup Import — **partial**
- Reachable: /dashboard/migrate ('Switch to Bubaly', navigation.ts:193), /dashboard/sync ('Calendar Sync', navigation.ts:191) with accounts/conflicts/feeds/history subpages, onboarding 'value' step (lib/onboarding/flow.ts:24).
- Persisted: calendar_events, chores, grocery_lists, grocery_items, notes, audit_logs (migrate); sync_accounts, sync_calendar_events, sync_reminders, sync_conflicts, sync_job_runs (sync); onboarding_imports (onboarding).
- Tested: tests/migrate-parse.test.ts, tests/onboarding-ics.test.ts, tests/sync-*.test.ts (18 files incl. sync-conflict, sync-adapter, sync-google-item-persistence), tests/documents-import-actions.test.ts.
- Mock risk: The migrate wizard's 'Nothing left behind' card (page.tsx:34-36) overstates: only events/tasks/grocery/notes from ICS/CSV; contacts, files and household data (members, recipes, medical, home) are not imported. 'Guided entity resolution' is limited to title+start dedupe on events; imported tasks/notes are never mapped to members.
- Gap: Missing: (1) contacts import (vCard/CSV → family_contacts); (2) file/cloud import (documents cloud-provider actions deliberately disconnected); (3) guided entity resolution — a review step assigning imported events/tasks to family members (name-token matching via resolveMemberByName-style heuristics), merging duplicates against existing chores/notes/contacts, and inferring categories; (4) importing broader household data (members from competitor exports, recipes) and recording imports in a structured table rather than audit_logs metadata.
- Build (M, migration: no): Extend lib/migrate/parse.ts with a vCard/contacts-CSV parser and a pure `resolveImportedItems()` that proposes member assignment (name tokens vs family_members display names) and duplicate matches against existing rows; add a 'Review' step to MigrateWizard between upload and commit showing proposed member/category per item with overrides; extend commitImport to write family_contacts and to persist member_id/category on events and chores. Keep audit_logs as the import ledger to avoid a migration.
- Evidence: `app/(app)/dashboard/migrate/actions.ts:23-118` commitImport writes calendar_events (dedupe by title|starts_at :35-45), chores, grocery_lists/grocery_items ('Imported Groceries'), notes, and an audit_logs row (resource 'migration').; `app/(app)/dashboard/migrate/page.tsx:17-24,47-66` Migrate page lists recent imports from audit_logs and renders MigrateWizard.; `components/migrate/migrate-wizard.tsx:24-72,190-192` Wizard: pick competitor → upload .ics/.csv → parse in browser → per-file target select (tasks/grocery/notes) → commit. No member assignment or duplicate review step.; `lib/migrate/competitors.ts:19-90` Sources: Cozi, FamilyWall, Google Calendar, Apple Calendar, OurHome.; `lib/migrate/parse.ts:44-144` parseICS, parseCSV, matchColumn (fuzzy header match), csvToItems.; `tests/migrate-parse.test.ts:25-90` ICS/CSV parsing and column matching tested.; `lib/sync/registry.ts:17-21` Provider adapters google/microsoft/apple; lib/sync/engine/generic.ts:1-12 two-way engine writing sync_calendar_events/sync_reminders, open sync_conflicts, sync_job_runs.; `app/(app)/dashboard/sync/page.tsx:25-45` Sync hub with capability matrix; app/(app)/dashboard/sync/conflicts/page.tsx:20-30 lists open sync_conflicts; 18 tests/sync-*.test.ts files.

### M30 — 30-Minute Wow Onboarding — **partial**
- Reachable: /onboarding (app/onboarding/page.tsx → OnboardingWizard) after signup; /dashboard/setup for completion; /dashboard/onboarding-funnel (super-admin only).
- Persisted: profiles, families (onboarding_claim_family RPC), family_members, family_onboarding, invites, calendar_events, onboarding_imports, onboarding_progress, onboarding_events, activation_events, subscriptions (trial).
- Tested: tests/onboarding-flow.test.ts, tests/onboarding-first-brief.test.ts, tests/onboarding-ics.test.ts, tests/onboarding-funnel.test.ts, tests/onboarding-completeness.test.ts, tests/onboarding-idempotency.test.ts, tests/onboarding-failure-safety.test.ts, tests/onboarding-family.test.ts, tests/onboarding-draft.test.ts.
- Mock risk: The first brief is a computed preview; if the user skips the value step ('Skip — I'll connect my calendar later', onboarding-wizard.tsx:210) nothing valuable is produced and no real workflow runs. The 'wow' is measured to first_outcome_viewed but onboarding never hands the user into an outcome run or captures facts, so the 30-minute goal has no instrumentation and no guided path.
- Gap: Missing: (1) fact discovery — onboarding answers (child ages, goals, region, referral source, dinner habits) and imported calendar patterns are never written to family_facts via lib/services/memory rememberFact; (2) a guided 'complete one real workflow' handoff on the done screen (e.g. launch Plan Week / Remind Everyone from lib/outcomes/launcher.ts with the imported events) that emits first_outcome_viewed; (3) a 30-minute window metric in ttv-audit/activation (only a 90-second TTV and session-1 milestone reach exist); (4) recovery for users who skip the value step (setup page does not offer the calendar import).
- Build (M, migration: no): After finalizeOnboardingAction succeeds, call rememberFact for each structured answer (child ages, household size, goals, region, dinner cadence) with source 'onboarding'; add a 'Do one thing now' card on the done step and on /home for first-session users that launches an outcome via buildOutcomePlan (lib/outcomes/launcher.ts) seeded from the imported events and fires the first_outcome_viewed beacon; add `under30MinRate` to lib/onboarding/ttv-audit.ts and lib/analytics/activation.ts; expose calendar import on /dashboard/setup for skippers.
- Evidence: `lib/onboarding/flow.ts:24-36` ONBOARDING_FLOW = profile → family → value → about → members → pin → done; 'value' step imports the calendar and shows an instant first brief before configuration.; `app/onboarding/actions.ts:72-103` previewCalendarImportAction parses pasted/sample ICS server-side and builds the first brief with dinner ideas from meal_ideas.; `app/onboarding/actions.ts:526-942` finalizeOnboardingAction: profile, family claim RPC onboarding_claim_family, parent membership upsert, trial subscription, calendar_events upsert (:774), onboarding_imports (:782-793), invites (:706), onboarding progress.; `lib/onboarding/first-brief.ts:1-50` Pure first-brief: today's timeline, conflicts, prioritized actions, time-saved opportunities.; `components/onboarding/onboarding-wizard.tsx:179,313-319` ValuePanel calls previewCalendarImportAction and tracks onboarding 'value' step.; `lib/analytics/activation.ts:14-28` Activation milestones first_brief_viewed / first_outcome_viewed / first_capture; FIRST_VALUE_MILESTONE = first_outcome_viewed; beacons on app/(app)/dashboard/briefing/page.tsx and app/(app)/dashboard/outcomes/page.tsx.; `lib/onboarding/ttv-audit.ts:8-40` TTV audit over onboarding_progress; TTV_GOAL_SEC = 90 (90-second goal, not a 30-minute wow).; `app/(app)/dashboard/onboarding-funnel/page.tsx:18-40` Super-admin-only funnel over onboarding_events + activation_events (service role).

### M31 — Time-Saved Ledger — **partial (covered by X1)**
- The lean audit regrouping omitted M31 as its own item. Its behaviour (hours of household admin eliminated from verified completed actions) is exactly what X1 audited: `lib/metric/time-saved-server.ts` loadTimeSaved + `lib/briefing/build.ts` timeSavedMinutes are DEFINED (twice, inconsistently) and SHOWN per family, but not STORED as a weekly series nor aggregated. Build once with X1/X4 via the cross-cutting 'handled / time-saved accounting' primitive and the `family_metric_weeks` migration.

### M32 — Savings Ledger: Track savings from groceries, subscriptions, warranties and avoided duplicate purchases — **missing**
- Reachable: No surface. /dashboard/savings (navigation.ts:301) shows goals; AI savings suggestions are invoked from the finances/savings UI via POST /api/ai/savings but are ephemeral.
- Persisted: Nothing for realised savings. Related existing tables: savings_goals, subscriptions_tracked (status canceled), home_warranties, transactions, ai_requests.
- Tested: None for a ledger. tests/ai-savings-response-integrity.test.ts and tests/ai-savings-observability-outcomes.test.ts cover the suggestion route only.
- Mock risk: The 'Savings' nav label and the AI 'up to $X/mo recoverable' copy read like a savings tracker; no number is ever written or accumulated, and time-saved is a different metric.
- Gap: Entire behaviour missing: no ledger table, no writers (subscription cancellation, warranty claim, grocery swap, avoided purchase), no rollup on Savings/CFO/briefing surfaces, no north-star 'money saved' metric.
- Build (M, migration: yes): (1) New table savings_ledger (id, family_id, source enum subscription_cancelled|warranty_claim|grocery_swap|duplicate_avoided|bill_negotiated|other, amount_cents, cadence one_time|monthly, evidence_table/evidence_id, note, recorded_by, created_at) with family RLS; (2) lib/savings/ledger.ts pure rollups (monthly/annualised, by source, since date) + lib/services/savings/index.ts recordSaving (assertFinanceWriter pattern from lib/services/finances); (3) writers: subscriptions-module status→canceled writes monthly cost (monthlyCostCents), warranties client 'Log a claim' writes claim value, purchase advisor (M17) 'already own it' writes duplicate_avoided, grocery module swap accept writes grocery_swap; (4) show 'Saved so far' tile on /dashboard/savings and /dashboard/family-cfo and add moneySavedCents beside timeSavedMinutes in lib/briefing/build.ts and lib/metric.
  - Migration: CREATE TABLE savings_ledger + RLS (is_family_member) + index (family_id, created_at); owner approval required. Wire into the money-table role boundary sweep (tests/0275-money-permissive-write-sweep.test.ts).
- Evidence: `supabase/migrations` grep for tables matching saving|handled|outcome finds only savings_goals; no savings ledger/events table exists (grep savings_ledger|saved_cents|amount_saved|savings_events in lib/app/components/migrations → only wallet goal funding rows).; `components/finance/savings-view.tsx:29` /dashboard/savings is the Savings Goals module (savings_goals table) — goal progress, not realised savings.; `app/api/ai/savings/route.ts:24-70` POST computes stale-subscription waste (wastedMonthlyCents) and over-budget categories, asks the model for ideas, deterministic fallback; result is returned only — no persistence beyond withAiRequest observability (ai_requests).; `lib/finance/subscriptions.ts:67-69` wastedMonthlyCents is explicitly 'monthly cost needing a usage review, not confirmed waste or achievable savings' — nothing records a cancellation's saved amount.; `lib/metric/time-saved.ts:1-40 and lib/metric/time-saved-server.ts:19-26` The category metric is TIME saved (minutes from autopilot_suggestions, agent_activity, family_reminders counts), not money; lib/briefing/build.ts:51-57 'handled' list is likewise action counts.; `lib/groceries, lib/grocery, lib/home/maintenance.ts, lib/inventory/finder.ts` No grocery price-saving capture, no warranty-claim value capture (home_warranties has cost/coverage but no claim rows), no 'avoided duplicate' event (see M17 missing).

### M33 — Best-in-Class Household Search: search household history, documents, items, trips, bills, warranties and decisions with evidence — **partial**
- Reachable: Cmd-K bar (components/app/command-bar.tsx) app-wide; per-module search boxes on /dashboard/documents, /dashboard/knowledge, /dashboard/memories
- Persisted: Per-module family-scoped selects (documents, family_facts, family_memories) filtered client/server in memory; no search index, no RPC
- Tested: tests/command-bar-route.test.ts, tests/memory-search.test.ts, tests/family-facts.test.ts (filterFacts)
- Mock risk: The command bar reads as a household search box but only matches nav labels and routes text to capture/assistant; typing 'furnace warranty' yields 'Ask the assistant', not a record.
- Gap: No unified household search over documents, inventory items, trips, bills, warranties/renewals, decisions, events and facts; no ranking, no evidence (source, date, link) in results; no search index or RPC; only isolated per-module filters.
- Build (L, migration: yes): Add a SECURITY INVOKER SQL function search_household(family_id, q, limit) that UNIONs pg_trgm similarity + ilike over documents, inventory items, trips, bills, renewals/warranties, decisions, calendar_events, notes and family_facts, returning (kind, id, title, snippet, occurred_at, rank); wrap it in lib/services/search with ServiceScope (RLS-enforced), expose a read-only AI tool search.household, add a 'record' result kind to lib/command-bar/route.ts rendered in the bar, and a /dashboard/search results page with evidence (source table, date, deep link).
  - Migration: New function public.search_household (security invoker, search_path pinned) plus GIN trigram indexes on title/name columns of the searched tables; no new tables, RLS of underlying tables applies. Owner approval required.
- Evidence: `lib/command-bar/route.ts:1-13,53-100` Cmd-K bar ranks results across navigate (fuzzy nav labels), capture (task/note/event/shopping), intent (reasoning engines) and 'Ask the assistant' fallback — it never queries household data; `components/app/command-bar.tsx:7,146` Placeholder 'Search, add a task, or ask'; non-matches POST to /api/ai/requests; no search RPC or table query; `lib/files/overview.ts:33-34 and components/modules/documents-module.tsx:195-311` Documents search is a client-side title/category filter over already-loaded rows; `lib/services/memory/index.ts:399-421 and lib/family/memory-search.ts` Facts and memories are filtered in memory after a family-scoped select (filterFacts / memoryMatches); `supabase/migrations/0001_extensions_enums.sql:5` pg_trgm extension enabled but no trigram/tsvector index or search function over household tables (grep tsvector|to_tsquery|websearch_to_tsquery|embedding|vector( in migrations/lib/app: only marketing platform + i18n strings); `find app/api -path '*search*'` Only app/api/recipes/search (external recipe provider) and app/api/gif/search — no household search endpoint; `tests/command-bar-route.test.ts, tests/memory-search.test.ts, tests/definer-search-path-pinned.test.ts` Tests cover command routing and in-memory memory filtering; no cross-entity search behaviour

### M34 — Life-Event Intelligence: Orchestrate transitions: school year, camp, move, new pet, aging parent, renovation, holidays — **partial**
- Reachable: /dashboard/life-events via navigation.ts:161 (free tier, requireUserContext); templates launched from the module via launchLifeEventAction.
- Persisted: life_event_plans, life_event_plan_items (0145); reads family_facts.
- Tested: tests/life-events-templates.test.ts (catalog integrity, getTemplate, addDays/buildPlanItems). No tests for life-event-actions.ts or the module.
- Mock risk: A launched plan looks orchestrated but is a self-contained checklist: nothing lands on the calendar, tasks, reminders, Move Planner, Projects, Pets or Vacations, and no signal (school_classes, calendar season, pets, family_facts, home_projects) ever proposes a transition — the 'intelligence' half is absent.
- Gap: Missing 4 of 7 named transitions (camp, aging parent, renovation, holidays); no detection/suggestion from household data; no cross-module orchestration (items are not todos/reminders/calendar events; moving/renovation/vacation templates do not launch the Move Planner, Home Projects or Vacations); no concierge intent.
- Build (M, migration: no): (1) Extend the catalog with camp, aging_parent, renovation, holidays templates (reuse SEASONAL_CHECKLIST ideas from lib/home/maintenance.ts and moments/celebrations dates); (2) lib/life-events/detect.ts (pure): from school_classes/timetable dates, pets created recently, home_projects in planning, family_facts (e.g., parent care), calendar holiday windows and lib/celebrations/dates → ranked suggested transitions with a reason; render as 'Coming up' suggestions in the module and as a family_signals/proactive slice entry; (3) on launch, create linked artefacts through existing services: todos via lib/services/tasks for 'buy/plan/notify' items, reminders for dated items, and hand off to Move Planner (moves) / Home Projects (home_projects) / Vacations (trips.findOrCreateVacation) for moving/renovation/vacation keys, storing the link in life_event_plan_items.note (or a new linked_ref column); (4) add intent 'life_event' → template life-event.ts in the planner so 'we're getting a puppy' launches the plan.
- Evidence: `lib/life-events/templates.ts:34-122` Six templates only: new_baby, moving, school_start, vacation, new_pet, new_job. No camp, aging parent, renovation or holidays (tests/life-events-templates.test.ts:13 pins 'six expected life events').; `lib/life-events/templates.ts:142-163` getTemplate + buildPlanItems materialise dated items from an anchor date (pure, tested).; `app/(app)/dashboard/life-event-actions.ts:19-53` launchLifeEventAction inserts life_event_plans + life_event_plan_items (migration 0145) with rollback on failure; setLifeEventStatusAction updates status.; `components/modules/life-events-module.tsx:51-62` Module reads family_facts, life_event_plans, life_event_plan_items; grep suggest|recommend finds no logic — facts are listed, never used to propose a transition.; `lib/ai/planner/templates/index.ts and lib/intent/detect.ts` No life-event intent or workflow (intents: check_availability, check_readiness, make_decision, plan_event, plan_meals, plan_trip, prep_for); plan items are checklist rows, never converted into todos, calendar_events, reminders, moves, home_projects, pets or vacations.; `lib/moments/prep.ts:1-30` Related but different: per-event moment prep bundles (sports/trip/appointment/school/celebration) — orchestration for one calendar event, not a transition.; `lib/constants/navigation.ts:161` 'Life & Milestones' nav entry (minLevel 0); page uses requireUserContext only.

### M35 — Developer-Grade Reliability — **exists**
- Reachable: cron /api/cron/ai-runs (CRON_SECRET), run detail /dashboard/concierge/runs/[id], /dashboard/family-automation run lists, Home Working-on, /dashboard/sync/conflicts
- Persisted: ai_tool_calls (unique idempotency key), idempotency_key columns per 0256, family_automation_runs lease/attempt/run_after, ai_run_events, ai_plan_steps retry_count, sync_conflicts, claim_ai_runs RPC
- Tested: tests/run-executor.test.ts, tests/0256-idempotency.test.ts, tests/0263-dead-letter-reconcile.test.ts, tests/ai-retry.test.ts, tests/sync-conflict.test.ts, tests/run-controls.test.ts, tests/run-states.test.ts, tests/allowance-cron-idempotency.test.ts
- Mock risk: Executor and reconcile tests run against fakes/SQL text, not a live Postgres; the cron only fires if the GitHub Actions dispatcher and CRON_SECRET are configured in production; there is no single 'all runs' history page — history is split across run detail, family-automation and Home
- Gap: All four checks are met: idempotency ledger and per-table keys, provider retry limited to reads, per-step bounded retries, leases with dead-letter reconciliation, partial completion, sync conflict reconciliation, deterministic fallbacks and run history. Remaining polish: no single chronological run-history list for a family (per-run detail plus the family-automation page), and ai_tool_calls rows stuck in 'started' after a worker death are only reconciled at the run level.
- Build (S, migration: yes): Add /dashboard/concierge/runs (index) listing a family's family_automation_runs with state filters and links to detail, reusing loadRunDetail's read boundary; extend the 0263 dead-letter path so ai_tool_calls rows left 'started' by a dead worker are marked 'unknown' with a timeline note; both are small and reuse existing modules.
  - Migration: A follow-up migration (create or replace claim_ai_runs) to also mark orphaned ai_tool_calls rows 'unknown' when a run is dead-lettered; no schema or RLS change.
- Evidence: `lib/services/idempotency.ts:1-20,55-60` makeKey stable sha256 and keyed probes; 0256 partial unique indexes on (family_id, idempotency_key) for AI-written tables; `supabase/migrations/0250_ai_runtime.sql:285-320` ai_tool_calls per-invocation ledger with unique (family_id, idempotency_key) and conflict semantics (succeeded => return prior result); `lib/ai/tools/execute.ts:18-27,184,227-277` executor reserves the ledger row before the write and returns the prior result on a duplicate key; `lib/ai/runs/executor.ts:7-23,835-865,943-950` bounded retries only for retryable errors with persisted retry_count and jittered backoff; approval parks the run; partially_completed reported honestly; `lib/ai/runs/executor.ts:900-904` verify checks downgrade a step to partially_completed; dependents still proceed; `lib/ai/runs/store.ts:558-663` claimRuns via claim_ai_runs RPC, claimRun with lease token, heartbeatRun guarded by lease owner; `supabase/migrations/0263_dead_letter_reconcile.sql:1-25` expired-lease recovery reconciles run, legacy status, steps, ai_requests and appends a timeline event (dead-letter); `app/api/cron/ai-runs/route.ts:1-25` continuation worker boxed at 85s inside a 120s function; unreached claims handed back

### M36 — Security as Product Surface: MFA/passkeys, privacy center, encryption, RLS, signed files, child controls and sensitive-access audit, customer-facing — **partial**
- Reachable: Settings > App lock (/dashboard/settings); child login management under /family; document downloads via signed URLs; /dashboard/security is home alarms; marketing /security page public
- Persisted: user_preferences (app lock config), child_logins + auth users (child accounts), documents/storage bucket (signed URLs), sync_tokens (encrypted), trust_audit_logs; RLS policies across family tables
- Tested: tests/app-lock.test.ts, tests/child-login.test.ts, tests/child-login-action-security.test.ts, tests/child-throttle.test.ts, tests/rls-isolation-sweep.test.ts, tests/tenant-isolation-rls.test.ts, tests/storage-bucket-rls-boundary.test.ts, tests/a11-documents-storage-rls.test.ts, tests/csp-header.test.ts, tests/0264-ai-surface-role-privacy.test.ts, tests/production-security-boundaries.test.ts
- Mock risk: The marketing security page advertises MFA-grade controls, HSM/column encryption, cert pinning, data export and instant deletion that the app does not implement; 'Security' in the sidebar is home alarms; the app lock is a client PIN, not authentication.
- Gap: Missing customer-facing pieces: MFA/passkeys (none), a privacy center (no data export, no account/family deletion, no session list), an in-app sensitive-access audit (see M24), and honest reconciliation between the marketing security page and shipped controls; credentials vault has no at-rest encryption.
- Build (L, migration: yes): Ship TOTP MFA via Supabase Auth (enrol/challenge/verify in a new Settings > Security panel, AAL2 required for managers on money/documents/trust routes via requireUserContext), a Privacy Center under Settings with family-scoped JSON export through existing services to a private bucket + signed URL, a deletion request flow processed by cron, and a 'who accessed what' list reusing trust_audit_logs + the M24 access log; keep the /dashboard/security sidebar route untouched per the standing rule; rewrite app/(marketing)/security copy to match shipped controls (or back each claim with code).
  - Migration: New table account_deletion_requests(family_id, requested_by, requested_at, execute_after, status) with manager-only RLS; MFA uses Supabase Auth's built-in factors (no schema) but requires the project's MFA setting enabled by the owner; passkeys depend on WebAuthn being enabled on the Supabase project — ship TOTP first.
- Evidence: `grep mfa|passkey|webauthn|totp|enrollFactor|challengeAndVerify|listFactors|aal2 in lib app components supabase/migrations` 0 code hits — no MFA enrolment, challenge or AAL enforcement anywhere; the only text match is prose in app/(app)/admin/admins/page.tsx; `lib/security/app-lock.ts:1-6 and components/settings/app-lock-settings.tsx` Opt-in 4-digit PIN app lock, explicitly 'NOT a server security boundary'; tests/app-lock.test.ts; `app/(app)/dashboard/security/page.tsx:8 and components/modules/security-module.tsx:19-33` /dashboard/security is HOME ALARM events (home_security_events), not account security; `lib/storage/documents.ts:43 and lib/services/documents/index.ts:168` Documents served via createSignedUrl with TTL; tests/a11-documents-storage-rls.test.ts, tests/storage-bucket-rls-boundary.test.ts; `tests/rls-isolation-sweep.test.ts:1-15, tests/tenant-isolation-rls.test.ts, tests/admin-tables-service-role-rls.test.ts, tests/sql-security-contract.test.ts` RLS on every family-scoped table is proven by docs/audit/rls-isolation-check.sql in CI; `app/(app)/family/child-login-actions.ts:1-12, lib/auth/child-throttle.ts:9-30, lib/notifications/child-channels.ts:22-37` Child logins (username+PIN, manager-only management), login throttling, child delivery channels; tests/child-login*.test.ts, tests/child-throttle.test.ts, tests/child-channels-delivery.test.ts; `lib/sync/crypto.ts:1-15` AES-256-GCM app-level encryption for provider OAuth tokens (sync_tokens); `supabase/migrations/0119_family_credentials.sql:9` Credentials vault stores secrets masked by default; at-rest/client-side encryption is 'documented' not implemented

### M37 — Hardware Partnerships — certified tablets/displays/stands program + in-app device setup — **missing**
- Reachable: Nothing for a certified-devices program: no marketing route, no in-app 'set up a display/tablet' path (no /display/setup, no onboarding step; grep -rln kiosk|pairing lib app components hits only display internals). /dashboard/devices reaches smart-home inventory only.
- Persisted: None for displays. smart_devices (home inventory) and push_devices (push tokens) exist but model different things.
- Tested: None. tests/home-management.test.ts covers smart-home helpers only; no e2e for /display on tablets.
- Mock risk: 'Smart Home' in the sidebar and 'Kitchen Display Mode: iPad/Android tablet/Smart display' on pricing could be read as a hardware program; both are unrelated to certifying or setting up displays.
- Gap: No certified-devices list, no compatibility matrix (tablet models, browsers, stands, keep-awake behaviour), no public program page, no in-app 'Set up this display' guide, no display device registration/pairing, no device test script for the wall in the QA plan.
- Build (M, migration: no): Ship the program as content + a guided setup path, no schema: (1) app/(marketing)/display/page.tsx — 'Works with any tablet' certified-devices page (tiers: recommended/compatible, models, stand links, setup steps) driven by a typed catalog in lib/marketing/certified-devices.ts; (2) app/(app)/display/setup/page.tsx — in-app device setup checklist (install PWA, fullscreen, auto-lock off, orientation, refresh cadence) with a 'Test this display' self-check using existing lib/display/recover + fullscreen APIs; (3) add a wall-display script (D3/D5) to docs/PHYSICAL_DEVICE_TEST_PLAN.md. Pairing/device registration is deferred to M18(c). Marketing site and per-page links are free under the standing sidebar rule.
- Evidence: `app/(app)/dashboard/devices/page.tsx:1-10` 'Smart Home' page → DevicesModule behind requireFeature('/dashboard/devices').; `components/modules/devices-module.tsx:18,29-31,44-45` A manual inventory of smart_devices (insert/update/delete) — lights, locks, thermostats; nothing about displays or tablets.; `lib/home/devices.ts:4-11` DEVICE_TYPES light/lock/thermostat/camera/sensor/plug/hub/speaker/doorbell/vacuum/other; integrations homekit/google/alexa/smartthings/matter/manual — no 'display'/'tablet' type, no certification concept.; `supabase/migrations/0081_home_management.sql` smart_devices table origin (home management).; `tests/home-management.test.ts:115-128` Only summarize/groupByRoom/integrationLabel are tested.; `supabase/migrations/0035_push_devices.sql:1-8` push_devices = per-user push registrations (web/ios/android tokens), not display devices.; `docs/PHYSICAL_DEVICE_TEST_PLAN.md:1-30` Manual QA plan for mobile web (iPhone/iPad/Android); grep -i 'display|kiosk' finds no wall-display or tablet-kiosk script.; `app/manifest.ts:14-17` Only hardware-facing accommodation: orientation 'any' because the /display kiosk targets landscape tablets.

### M38 — Privacy-Safe Household Benchmarks — **partial**
- Reachable: /dashboard/intelligence via navigation.ts:79; cron /api/cron/network-aggregate (vercel.json:68).
- Persisted: network_consent (read/write by family), network_contributions and network_aggregates (written by cron with service role; RLS-gated reads), sources family_members/meal_plans/teams/school_classes.
- Tested: tests/network-aggregate.test.ts, tests/network-contribution.test.ts, tests/network-insights.test.ts (pure cores); cron route not tested.
- Mock risk: With the 100-family launch gate (AGG_DEFAULTS.globalMinFamilies) and only two banded metrics, network_aggregates is empty in practice and the Intelligence page shows no insights — the pipeline is real but currently yields nothing visible; and nothing turns aggregates into research or content.
- Gap: Missing: (1) the research/content half — no admin benchmark report, no public /resources or blog page rendering published aggregates, no export for content marketing; (2) metric breadth — only dinner_habit and activities are contributed; no chores-per-kid, screen-time, bedtime, spending bands; (3) the cron/server path has no test; (4) no in-product 'how you compare' card outside the intelligence page (e.g. in the weekly briefing).
- Build (M, migration: no): Add 3–4 banded metrics to contributionFeatures/featuresToContribution (chores per child, bedtime band, weekly spend band, reminders per week) with tests; build an admin 'Household Benchmarks' report page (service role) over network_aggregates and a public marketing page /resources/benchmarks that renders only rows already k-anonymized (reusing aggregatesToInsights, no cohort filter), with an admin toggle for publication in marketing_settings; surface a single 'families like yours' line in the weekly briefing when the family has consent enabled.
- Evidence: `lib/network/insights.ts:16-40` Consent scopes timing/benchmarks/recommendations; K_ANONYMITY_FLOOR = 20; visibleInsights suppresses below floor and when not opted in.; `lib/network/aggregate.ts:1-45` cohortKey = kids age-bands × household-size band (no geography); AGG_DEFAULTS minCohort 20, globalMinFamilies 100, Laplace noiseScale 1.5.; `lib/network/aggregate-server.ts:1-36` Cron core: CONTRIBUTE (per consenting family coarse features → network_contributions; opted-out deleted) then AGGREGATE (k-anon + DP + gate → network_aggregates). Only two metrics: dinner_habit and activities (:31).; `lib/network/contribution.ts:1-45` Informed-consent preview banding ages/counts.; `supabase/migrations/0132_network_consent.sql:12-40` network_consent (enabled default false, scopes jsonb) with family-scoped RLS.; `supabase/migrations/0135_network_aggregates.sql:17-65` network_contributions (family-owned, select/delete only) and network_aggregates (service-written; select policy requires a consent row with the matching scope true).; `app/(app)/dashboard/intelligence/page.tsx:26-58` Reads family_members/meal_plans/teams/school_classes for the contribution preview and network_aggregates for the family's cohort; renders IntelligenceModule.; `components/modules/intelligence-module.tsx:35-53` Reads and upserts network_consent (master toggle + scopes).

### M39 — Family Referral Flywheel — **partial**
- Reachable: /referrals via navigation.ts:194 (share link + manual code entry); admin referral config via marketing settings.
- Persisted: referral_codes, referrals (0039), marketing_settings (config); conversion status via Stripe webhook.
- Tested: tests/referrals.test.ts (core), tests/marketing-core-referral-boundaries.test.ts, tests/admin-referrals-read-boundary.test.ts; no test of signup capture or reward application (neither exists).
- Mock risk: The loop is broken at both ends: the ?ref= param is dropped at signup so a referred family only counts if they later type the code on /referrals; and 'converted' never becomes a credit, so the promised '$10 credit' is displayed but never granted. Referral counts on the page are real rows but reflect manual entry only.
- Gap: Missing: (1) capture of /signup?ref= (cookie or auth metadata) and auto-apply in finalizeOnboardingAction; (2) reward fulfilment — apply referrer/referred credit (Stripe customer balance or coupon) on conversion and set status 'rewarded'; (3) product-value ties: inviting a spouse/grandparent/caregiver (invites) and inviting another household should both surface the referral link and count; (4) in-product prompts (post-invite success, home card, briefing) linking to /referrals; (5) email invite from the referral panel.
- Build (M, migration: no): Read `ref` in the signup form, persist it in auth signUp options.data (referral_code) and a short-lived cookie, then in finalizeOnboardingAction call applyReferralCode with source 'signup_link'. In the Stripe webhook, after markReferralConverted, credit both families via Stripe customer balance (lib/stripe) and update status to 'rewarded' with a new `rewardReferral()` in lib/referrals/server.ts. Add a referral CTA to the invite-success state in settings-module and a home card when the family has ≥1 member invited. Reuse InviteEmail pattern for a referral email in the panel.
- Evidence: `lib/referrals/core.ts:13-60` DEFAULT_REFERRAL_CONFIG ($10 credit both sides), generateReferralCode, normalizeCode, referralLink → /signup?ref=CODE, summarizeReferrals.; `lib/referrals/server.ts:41-67` getOrCreateReferralCode upserts referral_codes with the service role.; `lib/referrals/server.ts:94-131` applyReferralCode: validates code, one referral per referred family, inserts referrals with status 'signed_up' and reward cents.; `lib/referrals/server.ts:134-147` markReferralConverted flips signed_up → converted; nothing writes 'rewarded' or applies any credit.; `app/api/webhooks/stripe/route.ts:62-66` Stripe webhook calls markReferralConverted on active/trialing; grep for coupon/promotion_code/customer_balance in lib/stripe, lib/billing, app/api/stripe finds nothing — reward never reaches billing.; `app/(app)/referrals/page.tsx:14-60` Refer a Family page: code, config, list, summary tiles (Invited/Signed up/Upgraded); app/(app)/referrals/actions.ts:7 applyReferralCodeAction for manual code entry.; `components/referrals/referral-panel.tsx:34-79` Copy link and navigator.share only; no email invites.; `lib/constants/navigation.ts:194` '/referrals' (Refer a Family) in nav; grep for '/referrals' links in components/dashboard/home finds none elsewhere.

### M40 — Outcome-Based Premium Packaging — **partial**
- Reachable: /pricing (app/(marketing)/pricing/page.tsx → pricing-content.tsx), UpgradeModal on locked nav items (components/app/app-shell.tsx resolveItems with featureTiers/planLevel), /dashboard/billing (ROUTE_PLAN_LEVEL 0).
- Persisted: subscriptions (plan/status via app/api/webhooks/stripe/route.ts:40-60), billing_customers, admin feature-tier overrides; time-saved reads autopilot_suggestions/agent_activity/family_reminders.
- Tested: tests/billing-plans.test.ts, tests/feature-tiers.test.ts, tests/plans.test.ts, tests/billing-tiers.test.ts, tests/billing-conversion.test.ts, tests/billing-rbac.test.ts, tests/billing-entitlement-consistency.test.ts, tests/mobile-pricing-toggle-touch-target.test.ts.
- Mock risk: Plan gating and checkout are real and tested, but the packaging strategy itself (sell Family+ on work eliminated/automation) is absent: pricing, upgrade prompts and billing all present longer checklists. The only outcome metric (loadTimeSaved) is not connected to any purchase surface, so any 'outcome-based' claim would be copy-only unless wired.
- Gap: Missing: (1) outcome framing on the pricing page (what Family+ handles for you, hours/week, automations run) instead of feature lists; (2) UpgradeModal and BillingModule showing the family's own numbers (Bubaly handled N items / saved X hours this month; what Family+ would automate) using loadTimeSaved and briefing handled counts; (3) outcome-oriented PLANS.features copy as a single source of truth reused by pricing and upgrade modal; (4) a test that pins the outcome copy source.
- Build (S, migration: no): Add `outcomes: string[]` to each PLANS entry in lib/constants/plans.ts and render them as the primary column on pricing-content.tsx and in UpgradeModal (features become secondary). Pass the family's loadTimeSaved result and briefing handled count into UpgradeModal/BillingModule via app-context (server-loaded in AppFrame) to render 'This month Bubaly handled N things and saved ~X h; Family+ automates …'. Keep checkout/gating untouched.
- Evidence: `lib/constants/plans.ts:1-120` Three tiers (free/basic/plus), prices, planLevel, ROUTE_PLAN_LEVEL route gating (e.g. autopilot/command-center/weekly-briefing/conflicts at level 2).; `lib/constants/plans.ts:170-182` Family+ = tagline 'The Family Chief of Staff.' with a feature checklist (briefings, command center, missions, school/sports assistant, emergency hub) — no outcome or time-saved framing.; `app/(marketing)/pricing/pricing-content.tsx:36-60` FREE/BASIC/PLUS feature lists are checklists; :300-304 positioning line 'Category creator: your family's AI Chief of Staff' is the only outcome-flavoured copy; grep for hours saved/time saved/work eliminated/handled returns nothing.; `components/app/upgrade-modal.tsx:36-80` UpgradeModal promotes the required tier with price and plan features via Stripe checkout; no outcome vocabulary (grep empty).; `app/(app)/dashboard/billing/page.tsx:1-8` Billing page composes BillingModule/FinancesModule; no outcome/time-saved copy (grep empty).; `lib/metric/time-saved-server.ts:19-26` loadTimeSaved counts autopilot_suggestions, agent_activity, family_reminders — a real per-family 'work eliminated' metric, used only on app/(app)/dashboard/experience/page.tsx:15-21 (TimeSavedBanner).; `lib/billing/plans.ts:1-60` Pure plan/interval mapping and change classification; lib/features/tiers.ts:1-50 tier resolution over FEATURE_CATALOG with admin overrides.; `lib/billing/conversion.ts:1-30` isNewPaidConversion/isChurn for admin alerts.

### W1 — Breadth: market 5-7 hero outcomes, keep breadth as supporting proof — **partial**
- Reachable: Public: / (hero + FEATURE_RAIL), /features, /pricing all in MARKETING_NAV (lib/constants/navigation.ts:37-49) and tests/e2e/public-routes.ts. In-app outcomes: /dashboard/outcomes via APP_NAV_GROUPS Suggested (navigation.ts:74) / All Services — not in the default PRIMARY_NAV.
- Persisted: Marketing outcome/feature copy is static constants. Pricing matrix reads feature_tier overrides via getResolvedFeatureTiers(createServiceClient()) (lib/server/feature-tiers). In-app outcomes page reads calendar_events/todo_items/grocery_items/family_members counts.
- Tested: tests/e2e/marketing-public.spec.ts (rail links), tests/outcomes-launcher.test.ts (in-app registry), tests/feature-tiers.test.ts (tier resolution). No test asserts hero outcomes on the public site.
- Mock risk: FamilyWorkflowPanel cards look like outcomes but are unlinked static copy; the 'Every feature by plan' table is live admin data and will keep growing, so any hero band must actively demote it rather than sit beside it.
- Gap: No hero-outcomes band on /, /features or /pricing; the public site leads with 6 feature tiles, 7 topics, 8 cards, 8 highlight cards and a 103-row matrix. The 8-outcome vocabulary exists only in-app (lib/outcomes/launcher.ts). Breadth is the headline, not supporting proof.
- Build (M, migration: no): Add a pure lib/marketing/hero-outcomes.ts (5-7 outcomes mapped from OUTCOMES ids to marketing copy + hrefs into existing /features anchors, /ai, /how-it-works); render a HeroOutcomesBand on the homepage above FEATURE_RAIL, at the top of /features, and above WhySwitch on /pricing; wrap FeatureMatrixTable and the plan-card bullet lists in a collapsed 'See all N features' <details> so breadth becomes supporting proof. Marketing site only — no app nav changes.
- Evidence: `app/(marketing)/page.tsx:45-49` Homepage hero is the generic 'AI Operating System for Family Life' headline; no outcome list.; `components/marketing/visual-mocks.tsx:857-864` FEATURE_RAIL: 6 feature tiles (calendar, chores, meals, school, health, documents) linking to /features anchors — feature-first, not outcome-first.; `components/marketing/reference-showcases.tsx:158-166` Features page leads with 7 FEATURE_TOPICS then 8 FeatureCards (lines 200-268) — feature accumulation.; `app/(marketing)/pricing/page.tsx:27-33` Pricing builds a full matrix from FEATURE_CATALOG filtered by admin tier overrides (getResolvedFeatureTiers).; `lib/constants/feature-catalog.ts:23-135` FEATURE_CATALOG has 103 F(...) entries; all non-'off' rows render in the 'Every feature by plan' table.; `app/(marketing)/pricing/pricing-content.tsx:37-62` FREE/BASIC/PLUS_FEATURES plan-card bullet lists (dozens of items) plus SWITCH_HIGHLIGHTS (73-90) — 8 more cards.; `app/(marketing)/pricing/pricing-content.tsx:326-351` FeatureMatrixTable renders every catalog row; no collapse/summary.; `lib/outcomes/launcher.ts:38-47` In-app OUTCOMES registry: 8 north-star outcomes (Run Today, Feed the Family, ...) with buildOutcomePlan — the outcome vocabulary already exists but is not used by the marketing site.

### W2 — AI proof: real demos, activity history and Bubaly Handled evidence on the public site — **partial**
- Reachable: /ai via MARKETING_NAV (navigation.ts:44) and footer; real demo only via the /pricing Demo Account card (pricing-content.tsx:175-213) → /home behind a 5-minute email gate (demo/actions.ts:36-103).
- Persisted: Real demo: demo_sessions, demo_email_uses, seeded family tables incl. agent_activity/autopilot_suggestions/approval_requests (lib/demo/seed.ts). In-app evidence: ai_requests, agent_activity, home_briefs.handled. Public /ai showcase persists nothing.
- Tested: tests/briefing-build.test.ts (handled ⊆ completed), tests/time-saved.test.ts, tests/demo-config.test.ts (config only). No test for ai-showcase or for a public handled metric.
- Mock risk: The 'Live' badge on a hardcoded DEMOS array; demo-family 'handled' rows are seeded, not produced by Bubaly; public_stats has no AI columns so any public 'handled N things' number would currently have to be invented.
- Gap: The public AI proof is a scripted showcase labelled 'Live'; the only real demo is buried on /pricing; there is no public activity history or aggregate 'Bubaly handled' evidence, and the RPC that feeds public stats has no AI counts.
- Build (M, migration: yes): (a) No-migration step: on /ai add a 'Try it live' form using the existing startDemoAction, relabel the scripted showcase 'Example' (drop the Live badge), and add a 'What the demo family sees' link to /home's CompletedByBubaly. (b) Extend public_stats() with ai_handled_30d (count of ai_requests in completed/partially_completed states + agent_activity status='done' in the last 30 days) and read it in lib/marketing/stats.ts; add handledNote() to lib/marketing/format.ts and show it in TrustStrip/SocialProof and on /ai. Keep demo seed evidence out of the aggregate by excluding the demo family id (lib/demo/config DEMO_ACCOUNT_NAME lookup) in the RPC.
  - Migration: CREATE OR REPLACE FUNCTION public.public_stats() adding an ai_handled_30d bigint column (SECURITY DEFINER, anon-executable, excludes the demo family). Step (a) — real demo CTA on /ai and honest labelling — needs no migration and can ship first.
- Evidence: `components/marketing/ai-showcase.tsx:24-75` /ai centrepiece AiActionDemo is a hardcoded DEMOS array (6 scripted prompt/reply/action sets); auto-rotates on a timer (88-92).; `components/marketing/ai-showcase.tsx:148-150` Renders a green 'Live' badge next to the scripted demo — nothing is live.; `components/marketing/homepage-interactions.tsx:2-26` Homepage AssistantConversation is two static message pairs.; `app/(marketing)/pricing/pricing-content.tsx:175-213` Real 'Demo Account' card: form action startDemoAction, only on /pricing.; `app/(marketing)/demo/actions.ts:16-29` startDemoAction resets + seeds the shared demo family, signs the visitor in, redirects to /home.; `lib/demo/session.ts:133-139` resetDemoData wipes and re-seeds ~200 rows via seedDemoFamily.; `lib/demo/seed.ts:287-297` Seed inserts autopilot_suggestions, approval_requests and agent_activity rows — the demo's 'handled' evidence is fabricated seed data.; `app/(app)/home/page.tsx:258-300,489` Home reads agent_activity + completed runs and renders CompletedByBubaly — honest in-app handled evidence.

### W3 — Family+ pricing justified with quantified time saved, savings found, work handled; wow in first session — **partial**
- Reachable: /pricing (MARKETING_NAV). In-app: /home (TimeSavedBanner), /dashboard/experience, onboarding value step (app/onboarding).
- Persisted: onboarding_imports.time_saved_minutes, onboarding_progress.time_saved_minutes, home_briefs.time_saved_minutes (lib/database.types.ts:586-598); live reads from autopilot_suggestions/agent_activity/family_reminders. Nothing on the public pricing page is data-backed beyond the families count.
- Tested: tests/time-saved.test.ts, tests/onboarding-first-brief.test.ts, tests/briefing-build.test.ts. No test asserts any value/ROI content on /pricing.
- Mock risk: Time-saved is a fixed per-action heuristic (5/4/2 minutes), not measured; a public 'hours saved' figure must be labelled as an estimate. The demo's TimeSavedBanner is fed by seeded rows (lib/demo/seed.ts:287-297).
- Gap: The quantified metric and the first-session brief exist in-app and are tested, but /pricing shows $249.99/yr next to a feature list with zero value quantification; there is no 'savings found' (money) metric at all; no per-plan 'what Family+ is worth' panel.
- Build (M, migration: no): Add a pure lib/marketing/plan-value.ts that derives a labelled, conservative 'Family+ is worth ~N hours/month' estimate from computeTimeSaved's MINUTES constants and typical weekly action counts, plus the first-brief opportunity minutes; render a PlanValuePanel beside the Family+ card on /pricing (hours saved/week, things handled, 'try it: 5-minute demo' CTA reusing startDemoAction) and a first-week 'Your first brief saved you N minutes' callout on /home using home_briefs.timeSavedMinutes (lib/home/home-brief.ts:36,110). 'Savings found' has no data source today — record it as a follow-on requiring a bills/subscriptions savings signal rather than inventing a number.
- Evidence: `lib/constants/plans.ts:16-17` PLUS_MONTHLY_CENTS 2499 / PLUS_ANNUAL_CENTS 24999 ($249.99/yr) — shown as-is on /pricing.; `app/(marketing)/pricing/pricing-content.tsx:55-62,480-489` Family+ card is a feature bullet list ('The Family Chief of Staff'); no hours saved, no dollars found, no things-handled figure anywhere on the page.; `app/(marketing)/pricing/pricing-content.tsx:508` TrustStrip only shows the families count (familiesNote).; `lib/metric/time-saved.ts:12,41-53` computeTimeSaved: per-action minute weights (autopilot 5, assistant 4, reminder 2) → hours + headline; pure and tested.; `lib/metric/time-saved-server.ts:19-34` loadTimeSaved counts autopilot_suggestions auto_executed, agent_activity done, family_reminders completed (7 days).; `app/(app)/home/page.tsx:343,492` TimeSavedBanner rendered on /home; also /dashboard/experience/page.tsx:15-21.; `components/metric/time-saved-banner.tsx:7-9` Banner hides when nothing handled (data.show false).; `lib/onboarding/first-brief.ts:189-243` First-session wow: buildFirstBrief computes timeline, conflicts, opportunities and timeSavedMinutes from the imported calendar.

### W4 — Trust: customer-facing AI Trust/Privacy Center and independent security evidence — **mock**
- Reachable: /security via MARKETING_NAV (navigation.ts:47) and footer. In-app trust at /dashboard/trust (APP_NAV_GROUPS). No public /trust or /privacy-center route; no security.txt.
- Persisted: Public security page: none (static). In-app: trust_policies, permission_grants, trust_delegations, approval_requests, emergency_sessions, trust_audit_logs, ai_requests.
- Tested: In-app trust: tests/0260-trust-ledger-lockdown.test.ts, tests/guardian-trust.test.ts, tests/assistant-trust-wrapper.test.ts; security posture: tests/csp-header.test.ts, tests/e2e/csp.spec.ts, tests/production-security-boundaries.test.ts, tests/sql-security-contract.test.ts. No test guards the security page's claims (claims-contract excludes it).
- Mock risk: The public page is a fully static claims wall (SOC 2, HIPAA, HSM, MFA, biometrics, regions, SLA) with no evidence and several features that do not exist in the product; the AI-autonomy story (approvals, ledger) is real but only visible after login.
- Gap: No customer-facing AI Trust/Privacy Center; the /security page asserts certifications and controls the product does not have and links to no evidence; the claims-contract test has a hole that lets 'Bank-Level Security' ship; real evidence (RLS boundary suite, CSP, trust ledger) is never shown publicly.
- Build (M, migration: no): (1) Rewrite /security to verifiable statements and extend tests/marketing-claims-contract.test.ts to include the security page, banning SOC 2/HIPAA/HSM/biometric/99.99%/region claims until an artefact exists. (2) Add app/(marketing)/trust/page.tsx — AI Trust & Privacy Center: what Bubaly does without asking vs. what needs approval (table generated from lib/approvals/threshold.ts + lib/trust/ai-gate.ts constants), what is logged (ai_requests, trust_audit_logs) and where families see it (/dashboard/trust), model providers from lib/ai/models.ts, retention/deletion, subprocessors, and an 'Evidence' section listing CSP enforcement, the RLS boundary suite and migration replay in CI. (3) Add public/.well-known/security.txt. (4) Add Trust to MARKETING_NAV and the footer (marketing nav is free to change under memory.md).
- Evidence: `app/(marketing)/security/page.tsx:64-81` COMPLIANCE_BADGES claim 'SOC 2 Type II — Audited annually', HIPAA, GDPR, CCPA as static copy; no report, link or date.; `app/(marketing)/security/page.tsx:26` 'Bank-Level Security' — the exact phrase tests/marketing-claims-contract.test.ts:22 bans, but that test only scans visual-mocks/reference-showcases/format.ts (lines 5-9), not the security page.; `app/(marketing)/security/page.tsx:43-46,129` Claims MFA (TOTP/SMS) and biometric login; grep for mfa/totp/biometric across lib, app, components hits only this marketing page — not implemented.; `app/(marketing)/security/page.tsx:53-56,117-121,132,297` HSM key management, three selectable AWS regions, 99.99% SLA/status page — no region setting exists in the app (grep data region/frankfurt/sydney outside this page: none); no status route.; `app/(marketing)/security/page.tsx:266-268` 'Trust Center' eyebrow is just the badge block; no AI-specific trust content (what AI may do alone vs. with approval, what is logged, which models).; `public/` No .well-known/security.txt (ls public shows brand, icons, images, launch, sw.js only).; `app/(app)/dashboard/trust/page.tsx:20-36` Real in-app Trust & Permissions page: trust_policies, permission_grants, trust_delegations, approval_requests, emergency_sessions, trust_audit_logs — fail-closed on read errors (43-52).; `lib/trust/ledger.ts:1-9` trust_audit_logs writes are service-role only (0260 removed member INSERT).

### W5 — Navigation: outcome-first navigation and contextual discovery — **partial**
- Reachable: Marketing nav on every public page (SiteHeader in app/(marketing)/layout.tsx). App: /home, /dashboard/outcomes (via All Services / pin), Settings → Navigation Choices.
- Persisted: Per-user nav: user_preferences.notification_prefs.sidebarNav + sidebarNavChildren; service tooltip overrides: service_descriptions (0203). Marketing nav: static constant.
- Tested: tests/navigation.test.ts, tests/navigation-customize.test.ts, tests/outcomes-launcher.test.ts, tests/e2e/marketing-public.spec.ts (header/mobile menu behaviour). No test asserts outcome-first ordering anywhere.
- Mock risk: Outcome-first exists as a page (/dashboard/outcomes) that most users never see because it is not a default sidebar key; tooltips are the only 'contextual discovery' and depend on hover (absent on touch).
- Gap: Marketing nav and footer are page-type-first; the app's default sidebar is feature-first with the outcomes launcher hidden behind All Services; contextual discovery is hover tooltips only. Allowed without owner: MARKETING_NAV/header/footer, /home and /dashboard/command-center layout, an opt-in per-user 'Outcome-first' preset, related-outcome hints on module pages. Needs owner: adding /dashboard/outcomes to PRIMARY_NAV/DEFAULT_SIDEBAR_NAV_KEYS, reordering APP_NAV_GROUPS, or changing FreeTierSidebar/SidebarBody/nav-shared behaviour.
- Build (M, migration: no): (a) Marketing: reshape MARKETING_NAV into outcome-led labels (e.g. 'What Bubaly handles' → /features with outcome anchors from W1, 'See it work' → /ai, Pricing, Trust, Blog) and add an 'Outcomes' footer group. (b) App, allowed surfaces only: add an OutcomesStrip to /home and /dashboard/command-center reusing OUTCOMES + buildOutcomePlan with counts /home already loads; add a pure outcomesForRoute(href) to lib/outcomes/launcher.ts and a RelatedOutcomes chip row on module pages (touch-friendly discovery); add an opt-in 'Outcome-first' preset button in Navigation Choices that writes the user's own sidebarNav via saveSidebarNavAction (per-user, no global default change). (c) Ask the owner separately whether /dashboard/outcomes should join DEFAULT_SIDEBAR_NAV_KEYS.
- Evidence: `lib/constants/navigation.ts:37-49` MARKETING_NAV = Features, How It Works, AI Assistant, Pricing, FAQ, Security, Blog — page-type-first. Not in memory.md's protected list, so free to change.; `components/marketing/site-header.tsx:75-90,128-141` Header renders MARKETING_NAV verbatim (desktop + mobile).; `components/marketing/site-footer.tsx:28-64` Static footer GROUPS (Product/Company/Get started/Legal); no outcomes grouping.; `components/app/app-shell.tsx:323-324` SidebarBody → FreeTierSidebar (protected by memory.md).; `lib/constants/navigation.ts:277-329` PRIMARY_NAV default sidebar is feature-first (Home, Calendar, Tasks, Meals, Chores, Finances, ...); /dashboard/outcomes is not a default key (DEFAULT_SIDEBAR_NAV_KEYS :400).; `lib/constants/navigation.ts:74` '/dashboard/outcomes' lives in APP_NAV_GROUPS Suggested (minLevel 0) — reachable via All Services or per-user pin only.; `components/app/free-tier-sidebar.tsx:184-212,405-412` All Services modal: full plan-gated catalog with pin toggles — discovery surface.; `components/services/service-tooltip.tsx` Hover descriptions from lib/services/descriptions.ts (admin-overridable via service_descriptions) — the only contextual discovery today.

### W6 — Hardware: shared display mode exceptional and easy on existing tablets, marketed as such — **partial**
- Reachable: In-app: /display via APP_NAV_GROUPS (navigation.ts:166), family dashboard quick link, All Services (feature-catalog 'kitchen'); Family Basic+ only. Public: no dedicated page; one pricing bullet + highlight card.
- Persisted: display_layouts (tiles jsonb, settings jsonb) + 12 family tables read per render.
- Tested: tests/display-render.test.ts, display-tiles, display-ambient, display-recover, display-imagery, display-server-safety, display-name-firstname. No e2e for /display; no test for any marketing mention.
- Mock risk: None on the product side (real data, persisted layout). Marketing risk: 'Smart display' in the pricing bullet implies a device category that has no setup path; DeviceShowcase's unused 'Smart Display' type could be mistaken for a shipped surface.
- Gap: The display is real, persisted and unit-tested, but 'easy on existing tablets' is unaddressed (no wake lock, no first-run setup checklist, no pairing) and it is essentially unmarketed: no /display marketing page, no comparison with Skylight/Hearth beyond a one-line 'replacement' mention (pricing-content.tsx:329-330), no mention on /features, /how-it-works or /mobile.
- Build (M, migration: no): (a) Product ease, minimal: add a Screen Wake Lock helper (navigator.wakeLock with visibilitychange re-acquire) used by display-grid.tsx, and a dismissible 'Set up this tablet' first-run card on /display (Add to Home Screen, iPad Guided Access / Android screen pinning, fullscreen, wake lock status) stored in display_layouts.settings via the existing normalizeSettings path. (b) Marketing: app/(marketing)/display/page.tsx 'Turn the tablet you already own into the family display' with setup steps, a Skylight/Hearth comparison (hardware price vs $0 + Family Basic) sourced from a pure lib/marketing/display-compare.ts, render the unused 'Smart Display' card in DeviceShowcase linking to it, and add it to the footer Product group + /mobile page. Device-code pairing is deferred (needs a new table).
- Evidence: `app/(app)/display/page.tsx:21-22,37-38,214` Kitchen Display route, force-dynamic, gated by requireFeature; 'Family Basic feature… layout persisted per-family in display_layouts'.; `app/(app)/display/page.tsx:58-81` Reads family_members, calendar_events, chore_assignments, meal_plans, grocery_items, reminders, notes, family_recipes, display_layouts, family_photos — best-effort per widget.; `lib/constants/plans.ts:113` '/display': 1 — Family Basic plan gate.; `lib/constants/navigation.ts:166` '/display' Kitchen Display in APP_NAV_GROUPS (minLevel 1); also components/dashboard/family-dashboard.tsx:185 quick link and feature-catalog.ts:105.; `components/display/display-grid.tsx:494-502,592` Fullscreen toggle via requestFullscreen; idle photo frame (photo-frame.tsx:5,33).; `components/display/auto-refresh.tsx:10-16` Kiosk auto-refresh + hard reload on stale bundle (lib/display/recover.ts).; `lib/display/ambient.ts:57-94,188-222` Ambient themes, display settings, kitchen timer presets, hints — persisted via display_layouts.settings (0200).; `supabase/migrations/0026_display_layouts.sql` display_layouts table; 0200_display_settings.sql adds settings.

### W7 — Social proof: verified outcomes, case studies, referral loops, time-saved metrics on the public site — **partial**
- Reachable: Families count: /pricing, /features, /how-it-works. Reviews: /reviews (unlinked). Referrals: /referrals in-app only. Testimonials/case studies: admin only. Public time-saved: nowhere.
- Persisted: public_stats RPC (families/family_members/chore_assignments); reviews; testimonials; case_studies; referral_codes; referrals; marketing_settings (referral config).
- Tested: tests/referrals.test.ts, tests/marketing-reviews.test.ts, tests/marketing-reputation.test.ts (pure helpers), tests/marketing-claims-contract.test.ts. No test covers a public testimonial/case-study render, /reviews linking, or ?ref= attribution.
- Mock risk: '1,000+ registered families' is count(*) of families including demo/test families — registered, not verified customers; testimonials/case_studies tables may be empty, so a public band must hide when unpublished rather than seed placeholders (claims-contract forbids invented names).
- Gap: Only the raw families count reaches the public site. Testimonials and case studies have tables and admin but no public surface; the reviews wall is orphaned; the referral link is not attributed at signup so the loop is manual; no public time-saved or verified-outcome metric.
- Build (M, migration: yes): (a) lib/marketing/social-proof.ts (server): read published testimonials + case_studies with createServiceClient and publishedOnly; SocialProofBand on / and /pricing (hidden when empty) and app/(marketing)/customers/[slug] for case studies (result_metric as the headline outcome). (b) Link /reviews from the footer Company group and TrustStrip. (c) Close the referral loop: read ?ref= in app/(auth)/signup/page.tsx, stash in a short-lived cookie, and call applyReferralCode in the family-creation path of app/onboarding/actions.ts (createFamilyAction) — reusing the existing server function. (d) Public time-saved metric: extend public_stats() with avg_first_brief_minutes (avg onboarding_imports.time_saved_minutes) and handled_30d (shared with W2) — migration.
  - Migration: Steps (a)-(c) need no migration (tables exist; service-role reads match 0059's intent). Step (d) shares the public_stats() CREATE OR REPLACE proposed in W2 (add handled_30d + avg_first_brief_minutes; exclude the demo family). Owner approval required for (d) only.
- Evidence: `supabase/migrations/0011_public_stats.sql:20-31` public_stats() = count(families), active members, chore_assignments approved/done — anon-executable, SECURITY DEFINER.; `lib/marketing/stats.ts:21-38` getPublicStats calls the RPC (1h cache), degrades to zeros on error.; `lib/marketing/format.ts:5-17` formatFamilies rounds down to '1,000+'; familiesNote falls back to 'Built for modern family life' when 0 — honest formatter.; `components/marketing/reference-showcases.tsx:389-403,477-486` TrustStrip/SocialProof render familiesNote on /features and /how-it-works; pricing-content.tsx:508 on /pricing.; `supabase/migrations/0059_reputation.sql:9-45` testimonials + case_studies tables (is_published, result_metric e.g. 'Saved 6 hrs/week'); RLS enabled, no policies — service-role reads intended for the public site.; `app/(app)/admin/marketing/reputation/page.tsx` Admin CRUD exists; grep for testimonials/case_studies in app/(marketing) and components/marketing finds no public reader.; `lib/marketing/reputation.ts:17-22` publishedOnly helper exists (tested in tests/marketing-reputation.test.ts) but is unused by any public page.; `app/reviews/page.tsx:12-21` Public reviews wall reads reviews with PUBLIC_STATUSES via service client; /reviews/new accepts submissions.

### W8 — Switching cost: zero-setup imports, ambient ingestion, entity resolution — as marketed and as built — **partial**
- Reachable: Onboarding value step (/onboarding); /dashboard/sync (Connections) for provider OAuth; /dashboard/scan (Family Basic); /dashboard/front-desk + family @bubaly.com address (contact-center); marketing: /how-it-works MagicPanel, /pricing Smart Imports — none link to the real surfaces.
- Persisted: onboarding_imports, calendar_events, sync_* tables, sync_conflicts, family_inbox_messages, family_contact_channels, ai_requests (concierge runs). Entity resolution: family_members only.
- Tested: tests/onboarding-ics.test.ts, tests/onboarding-first-brief.test.ts, tests/onboarding-flow.test.ts, tests/sync-*.test.ts, tests/contact-center.test.ts, tests/paperwork-triage.test.ts. No test for OAuth-at-onboarding (does not exist) or for resolving external entities.
- Mock risk: The MagicPanel and 'Add or Forward Anything' are illustrations; the forwarding address is real but requires provider webhook + secret configuration (route rejects in production without CONTACT_CENTER_INBOUND_SECRET), and provider 'Connect' buttons only appear when OAuth keys are configured — an unconfigured deployment silently markets paths that are not live.
- Gap: Built: paste-.ics onboarding import, post-onboarding Google/Microsoft/Apple sync, inbound email/SMS/voice ingestion, flyer scan — all persisted and tested. Missing: zero-setup (OAuth) import during onboarding, entity resolution beyond member names (no canonical school/team/contact/sender matching or alias memory), and marketing that names the real import/forwarding paths instead of a static flyer mock.
- Build (L, migration: no): (a) Onboarding: add a 'Connect Google/Microsoft/Apple' option to the value step when configuredAdapters() is non-empty, reusing app/api/sync/[provider] OAuth and feeding the first synced events into buildFirstBrief (same onboarding_imports row, source 'url'→ add 'oauth' only if the CHECK allows; otherwise record as 'ics'). (b) Marketing: replace MagicPanel bullets with the four real paths (connect calendar, paste .ics, forward to your @bubaly address, snap a flyer) each linking to its surface, and a /features#imports card; keep copy honest via claims-contract. (c) Entity resolution, minimal: lib/graph/resolve.ts pure resolver that matches inbound mentions (school, team, provider, sender) against graph_entities names + family_facts, returning entity keys; call it from lib/contact-center/concierge.ts and lib/paperwork triage so imported items attach to canonical entities; unresolved mentions become family_facts with source (tranche H) for later confirmation.
- Evidence: `components/marketing/reference-showcases.tsx:537-581` How-it-works MagicPanel: static flyer→details→calendar mock with hardcoded 'Saves you hours every week' bullets.; `components/marketing/reference-showcases.tsx:404-420` FLOW_CARDS 'School Email Arrives → AI Extracts' and WORK_STEPS 'Add or Forward Anything' — static; no link to the real forwarding address or import surfaces.; `app/(marketing)/pricing/pricing-content.tsx:51,79-80,497-505` 'Smart Imports' marketed as upload flyers/PDFs/screenshots → events/tasks.; `app/onboarding/actions.ts:72-94` previewCalendarImportAction accepts only source 'paste' (ICS text) or 'demo' (sample week) — no OAuth/zero-setup import during onboarding.; `components/onboarding/onboarding-wizard.tsx:301-324` Value step UI: paste .ics or sample week.; `app/onboarding/actions.ts:784-790,908` Imported events persisted into calendar_events + onboarding_imports (0138) at finalize.; `lib/onboarding/ics.ts:1-8` Pure ICS parser for onboarding (tested: tests/onboarding-ics.test.ts).; `lib/sync/registry.ts:17-36` Provider adapters google/microsoft/apple; configuredAdapters() only when OAuth keys are set; /dashboard/sync/{accounts,conflicts,feeds,history} exist; app/api/sync/[provider], google, run, feeds routes exist.

### X1 — Household admin minutes eliminated / week — **partial**
- Reachable: /home (app/(app)/home/page.tsx:343,492) and /dashboard/experience (page.tsx:15-21) for any signed-in member; no admin view.
- Persisted: READ: autopilot_suggestions, agent_activity, family_reminders (7-day window). WRITE: none for the weekly metric — home_briefs.time_saved_minutes exists (0140) but lib/briefing/store.ts:19 always refuses; onboarding_progress.time_saved_minutes (0159:43) holds only the onboarding estimate.
- Tested: tests/time-saved.test.ts (pure compute), tests/briefing-build.test.ts (row shape). No test of loadTimeSaved or any snapshot.
- Mock risk: safeCount swallows read errors to 0 and the banner hides at 0, so a broken RLS/table looks like 'nothing to show'. Two different numbers are both called 'time saved' (handled-action weights vs first-brief opportunity estimate). Weights (5/4/2 min) are hard-coded assumptions, not measured.
- Gap: DEFINED (twice, inconsistently) and SHOWN per family, but NOT STORED as a weekly series and not aggregated for admin; loader untested; error path renders as empty.
- Build (M, migration: yes): Make loadTimeSaved the single definition (relabel first-brief's number as 'estimated planning time'); add a family_metric_weeks snapshot table (family_id, week_start, time_saved_minutes, handled_actions, …) written by the existing weekly-digest cron (app/api/cron/weekly-digest/route.ts already iterates families) via loadTimeSaved; surface a 'Time saved (all families, this week)' tile in admin/reports and a per-family 8-week sparkline on /home; make safeCount log the error and return null so the banner can distinguish 'nothing handled' from 'read failed'.
  - Migration: New table public.family_metric_weeks (family_id, week_start date, time_saved_minutes int, handled_actions int, auto_captured_pct numeric, signal_precision numeric, value_ratio numeric, created_at) unique(family_id, week_start), family-scoped RLS select; writes via service role from the cron. Shared by X1/X4/X6/X11.
- Evidence: `lib/metric/time-saved.ts:12` DEFINED: MINUTES weights autopilot=5, assistant=4, reminder=2 per handled action; computeTimeSaved at :41 returns minutes/hours/actions/rows/headline/show (pure).; `lib/metric/time-saved-server.ts:19` loadTimeSaved counts last-7-day autopilot_suggestions(status=auto_executed), agent_activity(status=done), family_reminders(status=completed). safeCount at :8-11 returns 0 on ANY read error, so a broken table silently renders as 'getting started'.; `lib/onboarding/first-brief.ts:196` SECOND, INCONSISTENT definition: timeSavedMinutes (:216) = sum of top-3 'opportunity' ESTIMATES (recurring*5, conflicts*15, week-events*2) — potential planning time, not actual handled work.; `lib/briefing/build.ts:142` Brief counts.timeSavedMinutes carries the first-brief estimate; briefRow (:160-176) maps it to home_briefs.time_saved_minutes.; `lib/briefing/store.ts:19` NOT STORED: saveBrief always returns fail (quarantine, migration 0262); loadBrief always returns null (:27-33). home_briefs.time_saved_minutes is never written.; `lib/server/onboarding-progress.ts:53` onboarding_progress.time_saved_minutes (migration 0159:43) stores the ONE-TIME onboarding estimate (app/onboarding/actions.ts:860), not a weekly series.; `app/(app)/home/page.tsx:343` SHOWN (family): loadTimeSaved → TimeSavedBanner at :492; also app/(app)/dashboard/experience/page.tsx:15-21.; `components/metric/time-saved-banner.tsx:8` Banner hides when data.show is false (no handled actions) — so an all-zero/erroring read produces no surface at all.

### X2 — % of household obligations auto-captured — **missing**
- Reachable: No route, page, or admin tile shows an auto-captured share. grep -rni 'auto-captur|auto_captur' over lib/app/components/tests returns nothing.
- Persisted: Provenance inputs exist (calendar_events.feed_id, ai_tool_calls.resource_table/resource_id, autopilot_suggestions.source_kind/status) but no metric is computed or stored.
- Tested: None for the metric. tests/capture-save.test.ts covers manual capture; tests/autopilot-engine.test.ts covers suggestion drafting.
- Mock risk: None yet (nothing rendered). Future risk: counting feed_id imports as 'auto-captured' inflates the number for families who simply subscribed to a Google calendar.
- Gap: Nothing defines, stores or shows the ratio; provenance is only partially recorded (imports and AI tool writes), and manual captures carry no origin marker.
- Build (M, migration: yes): Pure lib/metric/auto-captured.ts: autoCapturedShare({imported, aiCreated, autoExecuted, totalCreated}) → 0..1 with breakdown. Server loader counts, over the last 7 days: calendar_events with feed_id not null; ai_tool_calls(state='succeeded', resource_table in ['calendar_events','todo_items','bills','family_reminders','grocery_items']) distinct resource_id; autopilot_suggestions status='auto_executed'; denominator = rows created in those tables in the window. Render as a row in TimeSavedBanner and persist into family_metric_weeks.auto_captured_pct (X1 table).
  - Migration: Uses the family_metric_weeks table proposed in X1 (auto_captured_pct column). Optional follow-up (owner decision): add created_via text on calendar_events/todo_items/bills/family_reminders so lib/capture/save.ts can stamp 'manual' explicitly instead of inferring it.
- Evidence: `lib/capture/save.ts:71` Manual captures insert into notes/calendar_events/todo_items/grocery_items (:77-116) with created_by only — no provenance flag distinguishing manual vs automatic.; `supabase/migrations/0045_calendar_feeds.sql:38` Partial provenance exists for imports: calendar_events.feed_id + external_uid stamp synced events.; `supabase/migrations/0250_ai_runtime_core.sql:289` ai_tool_calls carries resource_table/resource_id (:309-310) for AI-created rows — usable as 'captured by Bubaly' provenance, but nothing aggregates it.; `supabase/migrations/0085_autopilot.sql:31` autopilot_suggestions.source_kind/source_id and status auto_executed (enum :16) record automatic actions; lib/autopilot/scan.ts:205-262 sets auto_executed when it creates reminders/grocery items.; `lib/contact-center/server.ts:122` Inbound messages are persisted to family_inbox_messages (recordInboundMessage), but app/(app)/dashboard/contact-center/actions.ts:83 only marks read/archived — no extraction of obligations from the inbox.; `lib/onboarding/completeness.ts:79` Only 'completeness' computation in the repo is account-setup completeness — unrelated to obligation capture.

### X3 — % of AI plans completed without rework — **partial**
- Reachable: /admin/ai-activity (super-admin via app/(app)/admin/layout.tsx) shows failed/in-flight/total; no rework figure on any family or admin surface.
- Persisted: ai_plans(version, status='superseded'), ai_run_events(event_type='step_retried'), family_automation_runs.state — all written by lib/ai/runs/store.ts, executor.ts, controls.ts.
- Tested: tests/run-executor.test.ts, tests/ai-activity.test.ts, tests/api-ai-runs.test.ts exercise the ledger; nothing asserts a 'without rework' ratio.
- Mock risk: Runs from surfaces that have not adopted withAiRequest leave no rows (admin page footer, tests/ai-observability-coverage.test.ts), so a ratio computed from the ledger covers only instrumented paths.
- Gap: Raw ledger fully persisted; no function defines 'completed without rework', nothing stores the ratio, nothing displays it.
- Build (S, migration: no): Add pure summarizeRework(runs, plansByRequest, events) to lib/ai/activity.ts: numerator = runs in state 'completed' whose request has exactly one ai_plans version and zero step_retried/replanned events; denominator = runs in TERMINAL_RUN_STATES in the window. Extend summarizeAiActivity to load those three reads and add a 'Completed without rework · last 7d' tile to admin/ai-activity plus a family-level line on /dashboard/concierge (runs list).
- Evidence: `supabase/migrations/0250_ai_runtime_core.sql:98` PERSISTED raw inputs: ai_plans.version and status incl. 'superseded'; ai_run_events.event_type incl. 'step_retried' (:254-263); family_automation_runs.state terminal states.; `lib/ai/runs/store.ts:302` On a re-plan (version > 1) prior draft/approved/executing plans are marked 'superseded' — this IS the rework signal, recorded but never counted.; `lib/ai/runs/executor.ts:299` Step disposition kind 'replanned' (:478-487 swaps run.plan_id) and 'step_retried' events (:555, :861; also lib/ai/runs/controls.ts:315).; `lib/ai/runs/states.ts:41` TERMINAL_RUN_STATES = completed | partially_completed | failed | cancelled — the denominator for a completion ratio.; `lib/ai/activity.ts:197` Only summary that exists: summarizeAiActivity → {failed, inFlight, total} over ai_requests in a window. No rework/completion ratio.; `app/(app)/admin/ai-activity/page.tsx:97` SHOWN (admin) only: Failed / In flight / Total last 24h tiles + paged request table.; `tests/run-executor.test.ts:162` TESTED: run completion semantics (walks graph, partial when a write is unconfirmed). tests/ai-activity.test.ts:73 covers listAiActivity/normalizeStatusFilter, not rework.

### X4 — Actions handled automatically / household / week — **partial**
- Reachable: /home and /dashboard/experience (TimeSavedBanner), /dashboard/concierge AutopilotPanel (components/modules/concierge-module.tsx), /dashboard/autopilot module. Admin: none.
- Persisted: READ: autopilot_suggestions, agent_activity, family_reminders, family_automation_runs. WRITE: no weekly snapshot (home_briefs quarantined).
- Tested: tests/time-saved.test.ts, tests/autonomy-loop.test.ts:63-71, tests/briefing-build.test.ts:34-60.
- Mock risk: Three surfaces compute three different 'handled' numbers; loadTimeSaved's safeCount hides read errors as 0; autopilotStats ignores concierge/routine/trigger runs (only plan_accepted).
- Gap: Defined, shown and tested per family, but with three inconsistent definitions, no stored weekly series, and no cross-household admin view.
- Build (M, migration: yes): Unify on one countHandledThisWeek(supabase, familyId, now) in lib/metric/time-saved-server.ts that adds family_automation_runs in state completed/partially_completed (any trigger) to the existing three counts; make AutopilotPanel and the brief's counts.handled use it; persist to family_metric_weeks.handled_actions from the weekly cron; add 'Handled actions / household / week' (median and total) to admin/reports.
  - Migration: Shares the family_metric_weeks table from X1 (handled_actions column).
- Evidence: `lib/metric/time-saved-server.ts:19` DEFINED (a): weekly counts of autopilot auto_executed + agent_activity done + reminders completed; time-saved.ts:41 exposes `actions`.; `lib/autonomy/loop.ts:94` DEFINED (b): autopilotStats.executedThisWeek counts family_automation_runs with trigger_type='plan_accepted' and status='executed' in 7 days — a different, narrower population.; `lib/home/today.ts:294` DEFINED (c): mergeCompletedByBubaly lists completed/partially_completed runs + agent activity but is capped at limit=6, so lib/briefing/build.ts:141 counts.handled is a list length, not a weekly total.; `components/metric/time-saved-banner.tsx:19` SHOWN (family): '· N things handled for you' on /home and /dashboard/experience.; `components/concierge/autopilot-panel.tsx:130` SHOWN (family): 'N plans executed for you this week' from autopilotStats (reads family_automation_runs at :55).; `components/modules/autopilot-module.tsx:84` SHOWN (family): `handled` = suggestions with status auto_executed/executed/approved (list, not weekly count).; `lib/briefing/store.ts:19` NOT STORED: home_briefs.handled column exists but saveBrief always fails; no other weekly handled snapshot (reasoning_snapshots stores attention_count only, lib/reasoning/engine-server.ts:83-88).; `tests/autonomy-loop.test.ts:63` TESTED: autopilotStats. tests/briefing-build.test.ts:34-60 pins handled ⊆ completed runs; tests/time-saved.test.ts pins action totals.

### X5 — Decision compression ratio — **missing**
- Reachable: No surface. /dashboard/reasoning shows 'N of 6 areas need attention' (page.tsx:37,60), not a signals-to-decisions ratio.
- Persisted: Inputs persisted: family_signals, autopilot_suggestions, family_ai_recommendations, approval_requests, reasoning_snapshots.attention_count. No ratio stored.
- Tested: tests/reasoning-engine.test.ts, tests/decisions-engine.test.ts, tests/hard-signals.test.ts cover the engines, not a compression metric.
- Mock risk: None rendered yet. Future: counting reasoning 'items' (capped at 4–5 per answer) as the denominator would understate raw signal volume.
- Gap: No definition, no stored value, no surface; the raw-signal and human-decision ledgers both exist.
- Build (S, migration: no): Pure decisionCompression({rawSignals, humanDecisions}) in lib/reasoning/engine.ts (ratio + label, null when no signals). Server: in loadAndSnapshotReasoning count, for the last 7 days, family_signals rows first_seen, autopilot_suggestions created, family_ai_recommendations created, ai_run_events(event_type='approval_requested') as raw; approval_requests decided (approved/rejected/modified) + autopilot approved/dismissed as human decisions. Put the value in reasoning_snapshots.report (already persisted daily) and show it on /dashboard/reasoning header.
- Evidence: `lib/reasoning/engine.ts:120` answerFamilyQuestions folds signals/insights/orchestrator into six answers; 'decide_next' (:142-144) lists open decisions; reasoningSummary (:172-177) emits per-answer item counts — the closest thing to 'few human decisions', but no ratio to raw signals.; `lib/operating-index/orchestrator.ts:163` decide_next answer built from pendingApprovals + openVotes, capped at 5 items.; `lib/ai/context/slices/proactive.ts:74` Raw-signal inputs: family_signals(active), autopilot_suggestions(open), family_ai_recommendations(pending); count summed at :150 for context sizing only.; `supabase/migrations/0093_trust_engine.sql:107` approval_requests.status pending/approved/rejected/modified/expired/cancelled — the 'human decisions' ledger; lib/services/approvals/index.ts decide path (:177,:247).; `lib/reasoning/engine-server.ts:83` reasoning_snapshots stores all_clear + attention_count + report jsonb daily — a place a ratio could live, but none is computed.; `lib/decisions/engine.ts:74` evaluateDecision is an option-scoring engine (cost/time/travel/load/benefit); unrelated to signal→decision compression. grep for 'compression' across lib/app/tests returns nothing.

### X6 — Proactive signal precision — **partial**
- Reachable: /dashboard/family-signals and /dashboard/autopilot show individual signals with acknowledge/dismiss controls; no precision number anywhere (family or admin).
- Persisted: family_signals.status, autopilot_suggestions.status/resolved_at, approval_requests.status — written by family actions; no aggregate stored.
- Tested: tests/hard-signals.test.ts, tests/autopilot-engine.test.ts, tests/family-signals-read-boundary.test.ts.
- Mock risk: scan.ts:188-190 deletes stale open suggestions and hard-signals upserts in place (unique family,kind,subject_key), so historical outcomes are overwritten — a precision computed from current rows is biased toward recent decisions.
- Gap: Feedback is captured and persisted, but precision is not defined, stored or displayed; suggestion history is overwritten/deleted, limiting accuracy.
- Build (M, migration: yes): Pure signalPrecision(rows: {kind, status}[]) in lib/intelligence/hard-signals.ts: (acknowledged+approved+executed+auto_executed) ÷ (those + dismissed), per source and overall, null when no decided rows. Server counts family_signals(status≠active) and autopilot_suggestions(resolved_at in window) and renders a 'Signal precision' tile on /dashboard/family-signals and an all-families tile on admin/ai-activity; persist weekly to family_metric_weeks.signal_precision. Stop deleting stale suggestions in scan.ts — mark them status='snoozed' with expires_at instead so the denominator survives.
  - Migration: Shares family_metric_weeks (signal_precision column) from X1. No schema change to family_signals/autopilot_suggestions required.
- Evidence: `supabase/migrations/0142_family_signals.sql:24` PERSISTED feedback: family_signals.status active | acknowledged | dismissed.; `app/(app)/dashboard/family-signals/actions.ts:26` setSignalStatusAction writes acknowledged/dismissed/active; lib/intelligence/hard-signals-server.ts:99-107 preserves dismissed keys across re-detection.; `supabase/migrations/0085_autopilot.sql:16` autopilot_status enum open/approved/executed/auto_executed/dismissed/snoozed; components/modules/autopilot-module.tsx:98-130 records approved/executed/dismissed.; `app/(app)/dashboard/family-signals/page.tsx:46` SHOWN only as two lists (active vs hidden) — no accepted/dismissed ratio.; `lib/autopilot/scan.ts:188` Stale OPEN suggestions are DELETED on rescan, so un-acted suggestions vanish from any future precision denominator.; `tests/hard-signals.test.ts:12` TESTED: detection engines; tests/family-signals-read-boundary.test.ts, tests/autopilot-engine.test.ts. No precision test.

### X7 — Household graph completeness — **partial**
- Reachable: /dashboard/graph (hubs, projection counts), /dashboard/setup (setup completeness ring), /dashboard/family-knowledge-graph. No graph-completeness score on any of them; no admin view.
- Persisted: graph_entities/graph_edges (0129) written by lib/twin/project-server.ts; onboarding_progress.completeness (0159) written by lib/server/onboarding-progress.ts:65. No graph-completeness value stored.
- Tested: tests/graph-reason.test.ts:33, tests/onboarding-completeness.test.ts:18, tests/autopilot-twin.test.ts.
- Mock risk: Reporting onboarding_progress.completeness as 'graph completeness' would be a false positive — it measures wizard steps, not context coverage per member.
- Gap: Graph is persisted and projected, but there is no definition of expected coverage, no score stored, and no surface; only a setup-completeness proxy exists.
- Build (M, migration: no): Pure graphCompleteness(snapshot: TwinSnapshot) in lib/twin/project.ts: expected slots per member (school class or team, ≥1 routine, ≥1 provider) and per household (≥1 place, ≥1 account, vehicle/pet optional) → 0–100 score + missing[] with hrefs (mirrors lib/onboarding/completeness.ts shape). Compute in runTwinProjection and in loadReasoningReport; show a 'Household graph: N% complete — add X' card on /dashboard/graph and fold into reasoning_snapshots.report; family_facts count as a bonus dimension.
- Evidence: `lib/twin/project-server.ts:15` runTwinProjection projects members/pets/vehicles/classes/teams/routines/places/accounts/providers into graph_entities (:51) and graph_edges (:70); projectionSummary (lib/twin/project.ts:117) returns entities/edges/byKind counts.; `lib/graph/reason.ts:192` orphans(), hubs() (:180), soleDependencies() (:211) — structural graph diagnostics, no completeness score.; `components/modules/graph-module.tsx:16` SHOWN: top hubs only; app/(app)/dashboard/graph/twin-actions.ts:14 returns entity/edge counts after projection. No completeness %.; `lib/onboarding/completeness.ts:79` Proxy that DOES exist end-to-end: account-setup completeness 0–100 (name, questionnaire, goals, calendar, members, PIN) — not graph coverage.; `lib/server/onboarding-progress.ts:65` STORED proxy: onboarding_progress.completeness; SHOWN at app/(app)/dashboard/setup/page.tsx:59 (ScoreRing); TESTED tests/onboarding-completeness.test.ts:18.; `lib/readiness/assess.ts:32` ReadinessCoverage complete|partial|unknown per evidence type — a coverage notion for readiness, not the graph.; `supabase/migrations/0129_family_graph.sql:19` graph_entities.kind person|activity|place|org|event|item|pet|topic|other; family_facts (0123) hold memory facts — no 'expected slots' model exists.

### X8 — Multi-member weekly active households — **missing**
- Reachable: No surface computes multi-member weekly activity; /admin shows roster counts only.
- Persisted: No per-member activity timestamp; audit_logs and journey_events are partial proxies.
- Tested: tests/admin-reports-read-boundary.test.ts covers the reports page reads, nothing about activity.
- Mock risk: Admin's 'Active members' label makes it easy to mistake roster size for engagement. Deriving from audit_logs alone under-counts read-only members.
- Gap: No activity timestamp per member, no definition, no stored value, no surface.
- Build (M, migration: yes): Add family_members.last_active_at (touched at most once per hour from requireUserContext in lib/supabase/auth.ts via a service-role helper). Pure multiMemberWeeklyActive(members: {family_id, last_active_at}[], now) → {households, multiMemberHouseholds, share}. Tile on admin/reports plus a 12-week bar; also drives X9.
  - Migration: ALTER TABLE public.family_members ADD COLUMN IF NOT EXISTS last_active_at timestamptz; index (family_id, last_active_at desc). Update via service role only (no new member UPDATE policy needed).
- Evidence: `app/(app)/admin/page.tsx:63` 'Active members' = family_members.is_active flag count, and 'Top families by active members' (:160-166) = roster size — neither is weekly activity.; `app/(app)/admin/reports/page.tsx:50` Activity volume = audit_logs rows per day (:91-98), not distinct actors per family.; `supabase/migrations/0002_tables.sql:454` audit_logs(family_id, actor_id, created_at) — the only cross-feature activity ledger with a per-user actor; written only on audited mutations.; `lib/analytics/use-journey.ts:41` journey_events insert carries member_id — another partial activity signal (instrumented journeys only).; `supabase/migrations/0035_push_devices.sql` push_devices.last_seen_at per user/device — device heartbeat, not app activity. No last_active/last_seen column on profiles or family_members (grep of migrations).

### X9 — 90-day household retention — **missing**
- Reachable: No surface. Nearest is /admin/subscriptions churn 30d.
- Persisted: families.created_at exists; no household activity timestamp (see X8) so retention cannot be derived.
- Tested: None.
- Mock risk: Presenting subscription churn as 'household retention' would be a false positive — free-tier families dominate the install base (admin/page.tsx:170-186 defaults to Free).
- Gap: No definition, no data (needs a per-household activity timestamp), no surface.
- Build (S, migration: yes): Depends on X8's last_active_at. Pure retention90(families: {created_at, lastActiveAt}[], now) in lib/analytics/active-households.ts: for each monthly signup cohort aged ≥90 days, share with any member activity in days 90–120 after signup (or, cheaper, last_active_at ≥ created_at+90d). Admin/reports tile (latest mature cohort) + 6-cohort bars.
  - Migration: Requires family_members.last_active_at from X8 (same migration). To measure 'active in the window' rather than 'ever active after day 90', also keep the X1 family_metric_weeks rows (weekly active flag per family).
- Evidence: `app/(app)/admin/subscriptions/page.tsx:107` Only churn figure in the product: 30-day SUBSCRIPTION churn (canceled ÷ active+canceled), shown at :170 — billing state, not household activity.; `lib/billing/conversion.ts:34` isChurn detects a paying family lapsing (fires an admin notification from app/api/webhooks/stripe/route.ts:84-97); no retention cohort.; `app/(app)/admin/reports/page.tsx:75` New families / new users per month (monthBuckets) — acquisition, not retention.; `lib/network/aggregate.ts:34` 'cohort' in the codebase refers to k-anonymity cohorts for network insights, not retention cohorts (grep 'retention|retained|90-day' across lib/app/components: no hits).

### X10 — Family+ conversion after demonstrated value — **partial**
- Reachable: /dashboard/onboarding-funnel (super-admin) for activation; /admin/billing, /admin/subscriptions, /admin/reports for subscriptions. No joined metric anywhere.
- Persisted: subscriptions(plan,status,created_at), activation_events(family_id, milestone, ms_since_signup), referrals.converted_at. No stored conversion-after-value value.
- Tested: tests/billing-conversion.test.ts, tests/activation.test.ts, tests/stripe-growth-alerts-contract.test.ts, tests/billing-tiers.test.ts.
- Mock risk: activation_events.family_id is nullable and client-recorded rows may be anonymous (0146), so the join can undercount activated families; the funnel page reads via service role only.
- Gap: Both halves are defined, persisted and tested; the joined metric (Family+ conversion among families that reached first value / N handled actions) is not defined, stored, or shown, and no upgrade prompt is value-triggered.
- Build (S, migration: no): Pure conversionAfterValue(activations: {family_id, milestone, created_at}[], subs: {family_id, plan, status, created_at}[]) in lib/billing/conversion.ts → {activatedFamilies, convertedToPlus, rate, medianDaysToConvert}; require subscription.created_at > first_outcome_viewed time. Load in /dashboard/onboarding-funnel's activation panel and as a tile on /admin/billing. Second step (optional): emit an activation milestone when family_metric_weeks.handled_actions first reaches ≥5 and let UpgradeModal cite it.
- Evidence: `lib/billing/conversion.ts:17` DEFINED (conversion half): isNewPaidConversion / isPaidActive; TESTED tests/billing-conversion.test.ts:15.; `app/api/webhooks/stripe/route.ts:71` PERSISTED: on conversion the webhook records an admin notification (:73-83), marks referral converted (:64); subscriptions table is the durable state.; `lib/constants/plans.ts:11` PlanId plus|plus_annual = Family+ ($24.99/mo, :16-17); planLevel/planMonthlyCents (:25, :189).; `lib/analytics/activation.ts:78` DEFINED (value half): summarizeActivation → activation rate, TTFV median/p90 from activation_events (0146); first value = first_outcome_viewed (:28).; `app/(app)/dashboard/onboarding-funnel/page.tsx:22` SHOWN (super-admin only via isSuperAdmin): activation_events read at :37, summarizeActivation at :41 — activation is shown, but never joined to subscriptions.; `app/(app)/admin/billing/page.tsx:44` Admin billing/subscriptions/reports show MRR, plan mix, churn — no conversion-after-value rate.; `components/app/upgrade-modal.tsx:61` Upgrade prompt is feature-lock driven (requiredLevel), not triggered by demonstrated value (no reference to handled/time saved).; `lib/marketing/automation-triggers.ts:7` EVENT_TRIGGERS include payment_completed / onboarding_completed but no 'value demonstrated' event.

### X11 — Verified time/savings value vs subscription price — **missing**
- Reachable: No surface (family or admin) compares delivered value to price.
- Persisted: None. Inputs: plan price constants, weekly time saved (not stored, see X1), autopilot expense suggestions (autopilot_suggestions rows).
- Tested: None for this metric (tests/time-saved.test.ts and tests/plans.test.ts cover the inputs).
- Mock risk: Any 'value' number rests on X1's hard-coded minute weights and an assumed hourly rate; must be labelled as an estimate.
- Gap: No definition, no stored value, no surface; both inputs (time saved, price) exist separately.
- Build (S, migration: yes): Pure valueVsPrice({minutesSavedPerWeek, hourlyValueCents = 2500, realizedSavingsCents = 0, planMonthlyCents}) in lib/metric/value.ts → {valueCents, ratio, label like '≈4× your plan'}; realizedSavingsCents from autopilot_suggestions(kind='finance', status in executed/auto_executed) payload monthlyCents. Show under TimeSavedBanner ('worth ≈$X this month vs $Y plan') on /home for paid families and in billing-module; persist weekly to family_metric_weeks.value_ratio; admin/reports shows median ratio by plan.
  - Migration: Shares family_metric_weeks (value_ratio column) from X1; no other schema change.
- Evidence: `lib/constants/plans.ts:14` Price inputs exist: BASIC 999c, PLUS 2499c monthly; planMonthlyCents(:189).; `lib/metric/time-saved.ts:41` Time input exists: computeTimeSaved minutes/hours per week — no monetary value or comparison to price.; `lib/autopilot/engine.ts:277` expenseSuggestions flags unused tracked subscriptions with monthlyCents (:260) — potential savings suggestions, never recorded as realised savings.; `lib/finance/subscription-price-history.ts` Tracks the FAMILY's own subscriptions' price changes (their Netflix etc.), not Bubaly's value vs price. grep for ROI/'pays for itself'/'worth $' across lib/app/components: no hits (only lib/marketing/identity.ts:15 comment).; `components/modules/billing-module.tsx` Billing module shows plan/checkout state; no value framing. app/(marketing)/pricing/pricing-content.tsx has feature lists only.

### X12 — Referral/invite coefficient — **partial**
- Reachable: /referrals (any signed-in member, app/(app)/referrals/page.tsx) and /admin/marketing/referrals (super-admin). No coefficient on either.
- Persisted: referral_codes, referrals (0039) written by lib/referrals/server.ts and the Stripe webhook; invites (0002); onboarding_progress.members_invited (0159).
- Tested: tests/referrals.test.ts, tests/admin-referrals-read-boundary.test.ts, tests/marketing-core-referral-boundaries.test.ts.
- Mock risk: Admin conversion rate is computed over only the 100 most recent rows; 'Invited' on the family page counts referral rows (created at referred signup), so pre-signup shares are invisible and invites-per-household is understated.
- Gap: Referral pipeline exists end-to-end (reachable, persisted, tested); the coefficient (referrals sent per household × referred-signup rate, or k = invites × conversion) is not defined, stored, or shown.
- Build (S, migration: no): Pure referralCoefficient({households, referralRows, inviteRows}) in lib/referrals/core.ts → {referralsPerHousehold, signupRate, k = referralsPerHousehold × signupRate, inviteK for member invites}. Admin page: replace the 100-row sample with head:true counts by status and add 'Referral coefficient (90d)' tile; family page: show 'You've brought N families' unchanged. Optionally record share clicks (ReferralPanel copy/share) as referrals(status='pending') so pre-signup invites count.
- Evidence: `supabase/migrations/0039_referral_program.sql` PERSISTED: referral_codes (one per family) and referrals(status pending|signed_up|converted|rewarded|void, converted_at, reward cents), family-readable RLS, service-role writes.; `lib/referrals/core.ts:79` DEFINED (partial): summarizeReferrals → total/pending/signedUp/converted/earnedCents per referrer; no coefficient (invites per household × conversion).; `lib/referrals/server.ts:94` applyReferralCode inserts the referral at signup; markReferralConverted (:134) called from app/api/webhooks/stripe/route.ts:64 on paid conversion.; `app/(app)/referrals/page.tsx:20` SHOWN (family): code, link, Invited/Signed up/Upgraded/Earned tiles (:56-67) via getOrCreateReferralCode + listReferralsForFamily.; `app/(app)/admin/marketing/referrals/page.tsx:46` SHOWN (admin): total referrals, converted, conversion rate (converted ÷ total, :48), credits owed, top referrers (:50-53); limited to the latest 100 rows (:36) so the rate is a sample, not the population.; `app/onboarding/actions.ts:833` Member invites counted into onboarding_progress.members_invited (:866,:904); invites table (0002) written by app/api/email/invite/route.ts — never combined with referrals.; `tests/referrals.test.ts:49` TESTED: summarizeReferrals; also tests/admin-referrals-read-boundary.test.ts, tests/marketing-core-referral-boundaries.test.ts.

