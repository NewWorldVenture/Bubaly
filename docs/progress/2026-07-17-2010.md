# Bubaly Audit Progress — 2026-07-17 20:10 UTC

- Agent: `agent-fable-opus` (Opus 4.8, this relay) · Unit: Family CFO page (fail-closed money reads)
- Overall completion: **~11%** (weighted math owned by `docs/AUDIT_PROGRESS.md`; five page-level units hardened this relay)
- Decision: **NO-GO** (owner-gated P0/P1 blockers remain — LB-001/003/010/011 + live E2E; LB-015 CI-infra red)
- Build: `tsc --noEmit` 0 (only the known optional `@axe-core/playwright` e2e noise) · eslint 0 on changed files · **vitest 572 files / 3562 tests green**

## Completed this increment — PLA-0776: Family CFO fails closed on money reads (P1)

`/dashboard/family-cfo` read five financial tables in one `Promise.all` and dropped every `error`. The page header promises "**every figure is live**", yet a silent failure rendered **Net position $0, "Due in 30 days $0", $0 spent, no savings goals / no bills** — a confidently-wrong financial picture a family could act on (miss a bill, assume savings vanished). The highest-stakes reassuring-but-wrong case found so far.

- **Fix** (`app/(app)/dashboard/family-cfo/page.tsx`): capture all five results; collect `[accountsRes.error, billsRes.error, goalsRes.error, spendRes.error, budgetsRes.error].find((e) => e && !isMissingTableError(e))`; on a real error `console.error('[dashboard/family-cfo] finance read failed', …)` + `return <ErrorState … />` before computing any figure. Missing table still tolerated as empty.
- **Guard** (`tests/family-cfo-read-boundary.test.ts`, new, 3): five-error collection with missing-table filter, log+ErrorState, derive-after-guard ordering.

## Fail-closed sweep — running tally (this relay)

| Page | Route | Severity | Outcome |
| --- | --- | --- | --- |
| readiness | `/dashboard/readiness` | P1 | PLA-0770 — hardened ✅ |
| kitchen | `/dashboard/kitchen` | P1 | PLA-0773 — hardened ✅ |
| family-digital-twin | `/dashboard/family-digital-twin` | P2 | PLA-0774 — hardened ✅ |
| memories | `/dashboard/memories` | P2 | PLA-0775 — hardened ✅ |
| family-cfo | `/dashboard/family-cfo` | P1 | PLA-0776 — hardened ✅ |
| calm | `/dashboard/calm` | — | left intact — deliberate `safe()` aggregator |
| dining | `/dashboard/dining` | — | left intact — deliberate `safe()` on low-stakes feature |

## Next candidates (unhardened multi-read pages, highest-stakes first)

`family-health` (5), `trust` (7), `family-emergency` (4), `family-coo` (7), `grandparent-portal` (6), `economy` (5), `family-cfo`✅. Money/health/safety surfaces prioritized. One validated increment at a time.

## Still owner-gated (unchanged — cannot be closed from the agent sandbox)

- **LB-001 (P0)** Auth Admin HTTP 500; **LB-003 (P0)** credential rotation; **LB-010 (P0)** apply 0217 (wallet mint); **LB-011 (P1)** apply 0219 (admin PII).
- LB-002 remote migration ledger; LB-005/006 live E2E + provider smoke; LB-008 backup drill; LB-013 storage RLS-on assert; LB-014 SEED_ALL coverage; LB-015 CI infra red.
