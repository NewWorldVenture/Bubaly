# Launch Audit — Progress Update · 2026-07-17 12:31 UTC

**Agent:** agent-01 (Opus 4.8) · **Commit:** `2955868e` · Taxonomy: Codex A-01…A-20

## 🔒 MILESTONE — money-ledger lockdown trilogy COMPLETE
A single systemic flaw — the `"Members manage" … FOR ALL … is_family_member`
policy loop — was applied to every family ledger, so a child (real Supabase
session) could INSERT ledger rows directly via PostgREST and mint value,
bypassing the manager-gated app. Found + fixed all three, each proven live:

| Ledger | Bug | Severity | Migration | PLA |
|--------|-----|----------|-----------|-----|
| **Wallet** (real money) | child mints completed credit | **CRITICAL** | 0217 | 0580 |
| **Economy** (tokens) | child mints currency | HIGH | 0218 | 0590 |
| **Investing** (holdings) | child mints shares | HIGH | 0220 | 0600 |

Each keeps SELECT open to members but restricts writes to `can_manage_family()`,
with a member-INSERT exception for the child's legitimate *request* rows
(`economy_redemptions`, `invest_orders`). Trusted writes (cron/webhooks/RPCs)
bypass RLS via the service role; the chore auto-approve reward was rerouted
through service-role (0217).

**Cross-agent validation:** agent-04 independently found the same FOR-ALL pattern
in **admin tables** and shipped `0219_admin_tables_service_role_rls_lockdown`.

## Also this window
- **PLA-0591 (product decision):** `family_credentials` vault is readable by all
  members incl. children (no visibility column) — documented a recommended
  `visibility`/`min_role` model for the A-11 owner (not unilaterally changed).
- Resolved a 0219 migration-number collision (renumbered invest → 0220).

## Status board
| Check | State |
|-------|-------|
| Build / lint / typecheck | ✅ / ✅ / ✅ (0) |
| Unit tests | ✅ **3435 passing** |
| Migration audit | ✅ clean (next 0221) |
| Supabase harness | ✅ 234 migrations + SEED_ALL + 2-tenant RLS |
| Latest verified commit | `2955868e` |

## Security scorecard (this session, agent-01)
**1 CRITICAL + 5 HIGH** defects found via one systematic pattern, all fixed +
proven live + guarded + documented: wallet mint (CRIT), economy mint, invest
mint, chore self-approve, guardian self-disable, geofence tamper. Plus CRM seed
(data), calendar SSRF verify, marketplace/billing/A-03 isolation proofs.

## ⚠️ Human punch-list (agents can't apply prod migrations)
Apply pending prod migrations **0217/0218/0220** (money-ledger locks — **prod is
exploitable until applied**, LB-010 P0) + 0215/0216/0219/0164; plus P0 infra
(Auth Admin 500, migration-ledger reconcile, credential rotation).

## Next
- Sweep the remaining `FOR ALL` loop tables (0070/71/72/73/81/85/101) to formally
  confirm each is collaborative-by-design (calendar-like) vs. sensitive; keep gate green.
