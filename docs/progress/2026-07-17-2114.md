# FamilyOS Audit Progress — 2026-07-17 21:14 UTC

- Agent: `agent-fable-opus` (Opus 4.8, this relay) · Unit: Family School + Family Sports hubs (fail-closed reads)
- Overall completion: **~13%** (weighted math owned by `docs/AUDIT_PROGRESS.md`)
- Decision: **NO-GO** (owner-gated P0/P1 blockers remain — LB-001/003/010/011 + live E2E; LB-015 CI-infra red)
- Build: `tsc --noEmit` 0 (only the known optional `@axe-core/playwright` e2e noise) · eslint 0 on changed files · vitest full-suite green (gate re-run on final HEAD before push)

## Completed this increment — PLA-0803 + PLA-0804: School & Sports hubs fail closed (P2)

Both `/dashboard/family-school` and `/dashboard/family-sports` read four tables and dropped every `error`, rendering false-empty content: school → "No classes/grades/school events" + `—` avg; sports → "No teams/results/schedule" + a 0-0-0 record — for families that actually have them (a parent misses tomorrow's game or believes a kid has no grades).

- **Fix**: capture all four results per page; collect `[…].find((e) => e && !isMissingTableError(e))`; on a real error log + return `<ErrorState … />` before rendering. Missing table tolerated as empty.
- **Guards**: `tests/family-school-read-boundary.test.ts` (3), `tests/family-sports-read-boundary.test.ts` (3).

## Fail-closed sweep — running tally (this relay)

readiness (0770) · kitchen (0773) · family-digital-twin (0774) · memories (0775) · family-cfo (0776) · family-health (0777) · family-emergency (0778) · trust (0779) · grandparent-portal (0781) · economy (0782) · **family-school (0783)** · **family-sports (0784)** — all ✅. Plus app-wide null-`display_name` crash fix (0780). calm/dining left intact (deliberate `safe()`).

## Owner asks — status (unchanged)

- ✅ OpenAI `/admin/settings` → **PR #322** (awaiting merge).
- ⏳ Seed 500 rows/table → LB-014, actively owned by seed agent; deferred to avoid collision; flagged in handoff.
- ⏳ Feature-tier review → referenced list not attached to chat; flagged in handoff.
- ⏳ Tier 8 AI Concierge (ChatGPT + Twilio) → parked by owner.

## Next candidates

`intelligence` (4), `family-coo` fail-closed reads, then continue the per-`page.tsx` sweep.

## Still owner-gated (unchanged)

- **LB-001 (P0)** Auth Admin 500; **LB-003 (P0)** credential rotation; **LB-010 (P0)** apply 0217; **LB-011 (P1)** apply 0219.
- LB-002 remote migration ledger; LB-005/006 live E2E + provider smoke; LB-008 backup drill; LB-013 storage RLS-on assert; LB-014 SEED_ALL coverage; LB-015 CI infra red.
