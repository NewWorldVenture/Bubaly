# Launch Audit — Progress Snapshot 2026-07-16 22:58 UTC

**Agent:** `agent-04` (Fable 5) · **Unit:** A-13 (Vacations / travel / concierge)

## Overall completion
- Weighted verified: **10 / 100 = 10.0%** (unchanged — this increment fixes a P1 inside an in-progress unit; A-13 is not yet launch-complete, so per the completion rules the % does not rise).
- Decision: **NO-GO** (audit ongoing).

## What I did (last 30 min)
- Pushed the three A-05 audit-doc updates to `main` (`cf1fe3af`): agent-04 registration, PLA-0490, §3b crash-class broadcast, 22:28 snapshot.
- **Claimed A-13** on the coordination board (`a1e9cd0c`) and audited it.
- **FOUND + FIXED a §3a P1 authorization bug (PLA-0500)** — `c5e72c6f`:
  - The **AI Concierge Calls** feature places REAL outbound phone calls that book / reschedule / cancel / confirm appointments with businesses on the family's behalf (and can incur telephony cost). `requestCallAction`, `cancelCallAction`, `requeueCallAction` only called `requireUserContext()`; `concierge_calls` RLS is family-scoped `is_family_member()` FOR ALL (migration 0173), and children have real logins — so a **child could invoke the server action directly** to dial out, cancel a family member's booking, or re-trigger a call. Same class as PLA-0450 (chore self-approve) and PLA-0470 (guardian self-disable).
  - Fix: `isManager(ctx.active.role)` gate (parent/adult) on all three mutations; `cancelCallAction`/`requeueCallAction` also now capture `ctx`. New `tests/concierge-calls-authz.test.ts` (5 static guards). tsc/eslint clean.
- Verified the rest of A-13:
  - RLS **family-scoped** on vacations tables (0070 loop policy `FOR ALL USING is_family_member`) + concierge_calls (0173 explicit per-op) — cross-family isolation covered by the global proof PLA-0415.
  - **No mocks / placeholders / dead code** in A-13 code (only a legit input `placeholder` prop + enum names).
  - Vacations create path is **genuinely wired** — `VacationsList` client insert columns match the real `vacations` schema exactly (family_id/title/kind/destination/dates/budget_cents/description/is_international/created_by); writes are client-side Supabase, RLS-enforced, collaborative-by-design (like calendar).

## Status checks
- Tests: `tests/concierge-calls-authz.test.ts` 5/5. `tsc --noEmit` exit 0. eslint clean on touched files.
- Supabase: no schema/migration change (server-action authorization only). No prod migration applied (human-owned).
- Security: closed a P1 child-safety + cost + real-world-effect gap.
- Latest verified commit: `c5e72c6f`.

## Blockers / dependencies
- **A-13 seed < 500 rows** (DoD gap, dependency on A-02): the ~30 vacation_* tables + concierge get ~65 rows in `SEED_ALL.sql`; a standalone `supabase/seed_vacations_trips.sql` (~60 rows) is **not appended to SEED_ALL** and the PG16 harness loads only SEED_ALL, so those rows are never verified. A-02 (seed baseline) should fold it in / expand vacation seeds to ≥500.
- The corrupted binary blob mid-`docs/PRODUCT_LAUNCH_AUDIT.md` (~PLA-0389 region) still needs its write-region owner to repair it.

## Next 30 min
- Continue A-13 DoD: live CRUD walkthrough of a vacation (create → members → itinerary → packing → budget); per-action judgment on `trip-intel` `deleteTripPlanAction`/`deleteDeparturePlanAction` (currently open to all members — collaborative planning, likely OK, confirm).
- Run the PG16 harness cross-family RLS probe scoped to `vacations`/`concierge_calls` to record A-13-specific evidence.

## Estimated remaining effort
- A-13 to launch-complete: moderate (vacation sub-surfaces × routes/roles/empty/mobile/error + seed expansion). This increment closes one P1 within it.
