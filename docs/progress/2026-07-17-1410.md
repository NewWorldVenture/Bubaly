# Bubaly Audit Progress — 2026-07-17 14:10 UTC

- Agent: `agent-03` (Opus 4.8) · Units: A-11 + A-16 (IN_REVIEW) · +A-20 observability increments
- Overall completion: **~10%** (weighted math owned by `docs/AUDIT_PROGRESS.md`)
- Decision: **NO-GO** (owner-gated P0/P1 blockers remain)

## Completed this window — observability trilogy + a live-smoke fix (PLA-0611)

Closed 2 of A-20's 3 logged observability gaps (Sentry remains owner-gated), all agent-doable and shipped to `main`:

1. **`GET /api/health`** — public readiness endpoint. 200 `{status:"ok"}` only when required Supabase env is present AND PostgREST is reachable; else 503 `{status:"error"}`. Pure decision logic (`lib/health/status.ts`) + bounded, RLS-independent probe (`lib/health/probe.ts`, 3s abort, fails closed). Body reports only booleans/latency/missing-var **names** — never a secret. 13 unit tests. Commit `3c3b07d6`.
2. **Boot env guard** — wired `checkRequiredEnv` into Next's `instrumentation.ts` `register()`; a missing/blank Supabase core var now emits ONE greppable `[boot] MISSING REQUIRED ENV: …` line at server startup (Node runtime only), log-only so it can never take down a deploy. 3 tests. Commit `33e7f80b`.
3. **Live-smoke fix (`middleware.ts`)** — running `next start` + curling `/api/health` revealed the middleware `PUBLIC` allowlist did NOT include it, so it **307-redirected to `/login`** — unreachable for its purpose (monitors can't authenticate). Added `/api/health` to `PUBLIC` + a regression test. Commit `f04fb89d`. *This is exactly the class of bug a live probe catches that unit tests cannot.*

### Validation
- Unit: health 13/13 · boot-guard 3/3 · middleware-boundary 4/4 — all green.
- `tsc --noEmit` clean (only known `@axe-core/playwright` optional-e2e noise).
- `npm run build` exit 0 across all three commits (routes + middleware compile).
- Live smoke: confirmed the pre-fix 307→/login redirect, then re-smoking post-fix build to confirm the route now serves the health JSON directly (503 in this env = no Supabase creds, which itself proves the env guard path).

## Method note

This window demonstrates the value of live-probing shipped code, not just unit tests: the middleware auth-boundary bug was invisible to unit tests (they can't exercise the Next middleware pipeline) and only surfaced by actually starting the server and hitting the route.

## Still owner-gated (unchanged)

LB-010 (P0, apply 0217), LB-011 (P1, apply 0219), 0216/0218 + 0118–0135 bundle, LB-003 credential rotation, LB-005/006 live E2E + provider smoke, LB-008 backup drill, Sentry; product decisions LB-009 / PLA-0610 / PLA-0581.

## Next

Hold A-11/A-16 IN_REVIEW; heartbeat. Continue picking up non-conflicting, non-owner-gated surface; re-green-gate on new HEADs. Do not raise the launch decision — release rule bars GO while any P0 is open.
