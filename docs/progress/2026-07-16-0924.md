# Production Audit Progress - 2026-07-16 09:24 -04:00

## Current service / feature

Sync provider capability honesty, focused on Apple CalDAV account setup and Reminders.

## Issues reviewed and resolution

- Apple `lib/sync/providers/apple.ts` explicitly drops VTODO collections and returns no task list.
- The shared capability matrix incorrectly advertised Apple Reminders as two-way CalDAV sync.
- Apple setup guidance incorrectly said the app-specific password enabled Calendar and Reminders.
- Marked Apple Reminders unsupported with a precise VTODO limitation; kept Apple Calendar over CalDAV and removed the Reminders promise from setup guidance.

## Tests and static evidence

- Focused suites: `tests/sync-capabilities.test.ts` and `tests/sync-apple.test.ts`, 34 assertions passed.
- Full Vitest: 481 test files, 3,240 tests passed.
- `npm.cmd run typecheck`: passed.
- `npm.cmd run lint`: passed with no ESLint warnings or errors; known `next lint` deprecation notice only.
- `npm.cmd run build`: passed; 250 routes generated; known webpack cache serialization and Supabase Edge-runtime warnings remain.
- `git diff --check`: passed.

## Launch decision

NO-GO. This increment removes a false capability claim, but it does not complete Apple VTODO sync or the broader launch gates.

## Remaining blockers and dependencies

- Live Supabase Auth Admin users query still returns HTTP 500: `Database error finding users`.
- Remote migration ledger, secret rotation, live OAuth callbacks, provider sync, RLS/role, browser/device, backup/restore, and deployment smoke evidence remain unverified.
- Apple VTODO read/write sync must be implemented and tested before Reminders can be exposed.

## Next audit dependency

Continue the route and provider audit at the next unsupported or misleading user-facing integration surface, while preserving the live Auth blocker as P0.
