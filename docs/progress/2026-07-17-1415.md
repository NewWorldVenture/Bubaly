# Bubaly Audit Progress — 2026-07-17 14:15 UTC

- Agent: `agent-01` (Opus 4.8) · Unit: A-07 (IN_REVIEW) — the deferred DB-layer restriction now closed
- Overall completion: **~10%** (weighted math owned by `docs/AUDIT_PROGRESS.md`)
- Decision: **NO-GO** (owner-gated P0/P1 blockers remain — LB-010 wallet-mint, LB-011 admin-PII, credential rotation, live E2E)

## Context on resuming

Rebased/pulled cleanly onto latest `origin/main` (health-endpoint work from `agent-03`: PLA-0611 + migration 0221 marketplace-bid revoke from `agent-04`). Gate green on the merged HEAD before starting: tsc 0, migration audit next=0222, 3451 vitest.

## Completed this increment — PLA-0612: A-07 chore submission (two findings, one root cause)

The A-07 board note flagged a DEFERRED DB-level restriction with a documented safe path ("route the auto-approve reward write through service-role FIRST, then add the trigger"). Executing it surfaced a **second, more urgent** finding, and both share one root cause: server-authoritative writes in `submitProofAction` ran under the child's RLS session.

- **Finding 1 — P1 FUNCTIONAL BREAK (new):** `chore_ai_validations` has RLS enabled with a **SELECT-only** policy (0043 comment: "written by the service-role engine"), but `submitProofAction` INSERTed into it using the **child** session. **PG16-proven:** that INSERT fails `new row violates row-level security policy` — so kid chore-proof submission was **broken for every user** (no authenticated role has INSERT on that table). Fixed by routing the insert (and all decision writes) through `createServiceClient()`.
- **Finding 2 — MEDIUM integrity (the deferred item):** `chore_submissions` shipped `is_family_member` FOR ALL, so a child could `update ... set status='approved'` directly via PostgREST and forge an approval (the app action was already gated in PLA-0450; the direct-DB path was open). **PG16-proven** succeeding pre-fix. The money path is already decoupled (0217), so this forges only the workflow/audit status — no minting.

### Fix
- **App** (`app/(app)/missions/actions.ts`): `submitProofAction` derives `const service = createServiceClient()` and routes the AI-validation insert + every decision-status transition (`approved`/`parent_review`/`rejected`/`needs_improvement` + pending rollback) through it. Submission INSERT (`pending`) + dispute (`disputed`) stay on the child session (the only member-authoritative writes).
- **DB** (`supabase/migrations/0222_chore_submission_decision_guard.sql`, new): a `before insert or update` trigger blocks any transition **into** a decision status unless the caller is service-role, an unauthenticated server/migration/seed context (`auth.uid() is null`), or a family manager (`can_manage_family`). Additive/idempotent (`to_regclass`-guarded).

### Validation (PG16 harness, production-accurate grants)
Mirrored Supabase (`grant all … to authenticated`/`service_role`) so RLS/triggers are the real gate. Matrix proven: child self-approve UPDATE → **BLOCKED**; child self-approve INSERT → **BLOCKED**; child submit (`pending`) → OK; child dispute (`disputed`) → OK; manager approve → OK; service-role auto-approve → OK. Pre-fix: child `chore_ai_validations` INSERT → RLS-denied; child self-approve UPDATE → succeeded. Fresh harness `up` with 0222 present: migration_fail=0, trigger present, seed unaffected (`SEED_ALL` doesn't touch `chore_submissions`).

### Gate
- New guard `tests/chore-submission-decision-guard.test.ts` (5). Updated `chore-state-transition-persistence` + `wallet-ledger-write-rls` for the service-role routing.
- Full suite **3456 passed**; tsc 0; eslint 0 (changed file); migration audit next=**0223**.

## Still owner-gated (unchanged, cannot be done by an agent)

- **LB-010 (P0):** apply 0217 (wallet mint) to prod.
- **LB-011 (P1):** apply 0219 (admin PII) to prod.
- 0215/0216/0218/0220/0221 + **new 0222** + the 0118–0135 feature bundle → prod (all human-owned).
- LB-003 credential rotation; LB-005/006 live E2E + provider smoke; LB-008 backup drill.
- Product decisions: LB-009 (public→signed URLs), PLA-0610 (family-PII visibility model), PLA-0581 (vault gating).

## Next

Hold A-07 IN_REVIEW. Optional low-priority A-07 follow-up: extend the same decision-status guard to `chore_assignments` (a child can still forge `chore_assignments.status='approved'`, which likewise mints no money). Keep heartbeating; re-run the green-gate on new HEADs and pick up any newly-unblocked, non-conflicting, non-owner-gated surface. Do not raise the launch decision — the release rule bars GO while any P0 is open.
