# Bubaly Audit Progress — 2026-07-17 20:17 UTC

- Agent: `agent-fable-opus` (Opus 4.8, this relay) · Unit: Family Emergency Hub (fail-closed crisis reads)
- Overall completion: **~11%** (weighted math owned by `docs/AUDIT_PROGRESS.md`; seven page-level units hardened this relay)
- Decision: **NO-GO** (owner-gated P0/P1 blockers remain — LB-001/003/010/011 + live E2E; LB-015 CI-infra red)
- Build: `tsc --noEmit` 0 (only the known optional `@axe-core/playwright` e2e noise) · eslint 0 on changed files · **vitest 574 files / 3570 tests green**

## Completed this increment — PLA-0778: Family Emergency Hub fails closed on crisis reads (P1, safety-critical)

`/dashboard/family-emergency` read four tables in one `Promise.all` and dropped every `error`. Billed as "everything a caregiver needs in a crisis," a silent failure rendered **"No emergency contacts on file"** (caregiver can't reach the parent), **"No emergency plans yet"**, and **"No medical profiles recorded"** (hiding blood type, allergies, ICE contact from a first responder) — at the worst possible moment.

- **Fix** (`app/(app)/dashboard/family-emergency/page.tsx`): capture all four results; make the manager-gated `medical_profiles` non-manager branch error-shaped (`{ data: [], error: null }`); collect `[…].find((e) => e && !isMissingTableError(e))`; on a real error `console.error('[dashboard/family-emergency] emergency read failed', …)` + `return <ErrorState … />` before rendering. Missing table still tolerated as empty.
- **Guard** (`tests/family-emergency-read-boundary.test.ts`, new, 4): four-error collection with missing-table filter, log+ErrorState, error-shaped non-manager branch, derive-after-guard ordering.

## Fail-closed sweep — running tally (this relay)

| Page | Route | Severity | Outcome |
| --- | --- | --- | --- |
| readiness | `/dashboard/readiness` | P1 | PLA-0770 ✅ |
| kitchen | `/dashboard/kitchen` | P1 | PLA-0773 ✅ |
| family-digital-twin | `/dashboard/family-digital-twin` | P2 | PLA-0774 ✅ |
| memories | `/dashboard/memories` | P2 | PLA-0775 ✅ |
| family-cfo | `/dashboard/family-cfo` | P1 (money) | PLA-0776 ✅ |
| family-health | `/dashboard/family-health` | P1 (safety) | PLA-0777 ✅ |
| family-emergency | `/dashboard/family-emergency` | P1 (safety) | PLA-0778 ✅ |
| calm / dining | — | — | left intact — deliberate `safe()` wrappers |

## Next candidates (unhardened multi-read pages, highest-stakes first)

`trust` (7 — safety/behavior), `family-coo` (7 — ops), `grandparent-portal` (6), `economy` (5 — kids money), `family-school` (4), `family-sports` (4). One validated increment at a time.

## Still owner-gated (unchanged — cannot be closed from the agent sandbox)

- **LB-001 (P0)** Auth Admin HTTP 500; **LB-003 (P0)** credential rotation; **LB-010 (P0)** apply 0217 (wallet mint); **LB-011 (P1)** apply 0219 (admin PII).
- LB-002 remote migration ledger; LB-005/006 live E2E + provider smoke; LB-008 backup drill; LB-013 storage RLS-on assert; LB-014 SEED_ALL coverage; LB-015 CI infra red.
