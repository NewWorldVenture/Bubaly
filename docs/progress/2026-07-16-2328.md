# Launch Audit — Progress Snapshot 2026-07-16 23:28 UTC

**Agent:** `agent-04` (Fable 5) · **Units this window:** A-15 (AI) + A-18 (integrations); next A-17 (admin)

## Overall completion
- Weighted verified: **10 / 100 = 10.0%** (unchanged — these are verification passes + a P1 fix inside in-progress units; no whole unit reached DONE, so per the completion rules the % does not rise). Decision: **NO-GO** (audit ongoing).

## What I did (last 30 min)
- **A-15 (AI assistants/chat/voice) — VERIFIED clean, lock added** (`b642c2d2`, PLA-0510):
  - All ~30 `app/api/ai/*` routes auth'd (`POST /api/ai/gift` is intentionally public for the gift-link giver — IP rate-limited, token-scoped, read-only). Family-scoped + RLS; tier gate + per-user/per-day rate limits + bounded request/response bodies (cost/DoS). Provider genuinely wired to OpenAI (`lib/ai/provider.ts` — real API, streaming, tools, vision, honest error classification; no mock). Money routes are read-only coaching.
  - Key §3a surface CLOSED: the tool-executing chat assistant routes every WRITE tool through the family Trust Engine with the caller's role (`lib/assistant/trust-wrapper.ts` — deny/require_approval/allow), so a child cannot drive a privileged write through the model. Added `tests/assistant-trust-wrapper.test.ts` (5) to lock it.
- **A-18 (third-party sync) — VERIFIED clean, lock added** (`de427760`, PLA-0520):
  - FAIL CLOSED end-to-end: `sync/run` → 503 when a provider isn't configured; registry hides unconfigured adapters; Apple/Microsoft key-gated dark; **Apple iCloud VTODO** writes throw `SyncApiError(501)` and reads advertise "no task sync" — never a silent success (the board's standing concern).
  - OAuth CSRF-safe (32-byte CSPRNG state, `timingSafeEqual`, callback validates state before code-exchange, single-use cookie), refuses to store tokens without `SYNC_TOKEN_KEY`, AES-256-GCM token-at-rest with tamper detection. Real provider calls (Google/Apple CalDAV/Microsoft Graph) — no mocks.
  - Existing tests already lock OAuth-CSRF + crypto-tamper; added `tests/sync-apple-vtodo-failclosed.test.ts` (5) to close the VTODO gap.

## Status checks
- Tests: new specs 10/10 (assistant-trust-wrapper 5, sync-apple-vtodo 5); sync security suite 33/33; full suite 3383/3384 earlier with the 1 failure a filesystem-walk **timeout flake** (passes in 435ms isolated). No regressions from my changes.
- Supabase: no schema/migration changes this window (verification + tests). No prod migration applied (human-owned).
- Security: A-13 concierge-calls P1 fixed earlier this session (PLA-0500); A-15 tool-authz and A-18 OAuth/crypto verified + locked.
- Latest verified commit: `de427760`.

## Coordination
- Another agent landed a locator `savePlace`/`deletePlace` authz fix + `tests/locator-places-authz.test.ts` (arrived on rebase) — the §3a broadcast is being worked in parallel. No collisions.

## Blockers / dependencies
- A-13 seed < 500 rows for vacation tables (dependency on A-02 — flagged in PLA-0500).
- Corrupted binary blob mid-`docs/PRODUCT_LAUNCH_AUDIT.md` (~PLA-0389) still awaits its write-region owner.
- A-15/A-18 remaining: live E2E with real provider/OpenAI keys in staging.

## Next 30 min
- Claim **A-17 (admin / marketing / social)** and verify admin-route authorization (super-admin gating), then continue toward whichever OPEN unit remains (A-19 mobile/a11y, A-20 e2e/perf/observability).
