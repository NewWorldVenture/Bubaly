# Production Readiness Progress - 2026-07-16 09:02 America/New_York

## Connections Runtime Catalog

- Added `CONNECTABLE_PROVIDERS` and made `mergeConnections` render only providers with a real setup route.
- Updated the Connections hub copy to describe supported calendar sync rather than unsupported bank, grocery, email, and smart-home promises.
- Preserved the internal provider registry for future implementation; unsupported providers are not user-facing.
- Updated `tests/connections-providers.test.ts` with runtime-catalog and unsupported-row assertions.
- Source commit: `9eb160b0 Hide unsupported connection providers`.
- Local audit branch pushed through `9eb160b0`.

## Verification

- Focused connection suites: 25 assertions passed.
- Full Vitest: 481 test files, 3,252 tests passed.
- Typecheck: passed.
- Lint: passed with no warnings or errors; known `next lint` deprecation remains.
- Clean production build: passed; 250 routes generated.
- Diff check: passed.

## Release Decision

- Overall decision remains **NO-GO**.
- The live Supabase Auth Admin users probe still returns HTTP 500 `Database error finding users`.
- Live OAuth/provider sync, RLS/feature-flag verification, authenticated browser coverage, backup/restore evidence, and deployment smoke evidence remain open.
