# Bubaly Audit Progress — 2026-07-17 20:30 UTC

- Agent: `agent-fable-opus` (Opus 4.8, this relay) · Unit: app-wide null-`display_name` crash class
- Overall completion: **~12%** (weighted math owned by `docs/AUDIT_PROGRESS.md`)
- Decision: **NO-GO** (owner-gated P0/P1 blockers remain — LB-001/003/010/011 + live E2E; LB-015 CI-infra red)
- Build: `tsc --noEmit` 0 (only the known optional `@axe-core/playwright` e2e noise) · eslint 0 errors on changed files · **vitest 576 files / 3577 tests green**

## Completed this increment — PLA-0780: null display_name white-screen crash class fixed app-wide (P1)

`family_members.display_name` is nullable in the DB (`0002_tables.sql:14`) but typed `string`, and `0212_atomic_family_provisioning.sql` provisions members with `p_display_name default null`. 23 raw `.display_name.split(' ')[0]` sites assumed non-null — in **server components** (`/home`, `/kids`, `/dashboard/family-coo`, `family-dashboard`) a single null-name member throws during SSR render → **white screen** (the same crash class that caused the `/display` "Reconnecting…" loop, which `e92fd897` only fixed in `display-grid.tsx`).

- **Fix**: added null-safe `firstName()` to `lib/utils/format.ts` and routed all 21 UI `.tsx` sites through it (kids, home, family-coo, family-dashboard + the family/school/sports/messages/documents/locator/passwords modules). The 2 API `.ts` routes already guard with `if (m?.display_name)`.
- **Guard** (`tests/display-name-firstname.test.ts`, new, 5): `firstName` null/blank/normal behavior + a source grep asserting **zero** raw `display_name.split` in any `app/`+`components/` `.tsx`.

## Note for the owner / next agent (from the mid-turn credentials paste)

The owner pasted a full set of **live production secrets** into chat (Stripe live keys, Supabase service-role, OpenAI, Google, Resend, Brevo, VAPID). **None were written to the repo** — they belong in the Vercel/Supabase env stores and code reads them via `process.env` only. Recommend rotating the live Stripe secret and the Supabase service-role key since they were shared in cleartext. Env wiring (OpenAI etc.) is tracked in `AGENT_HANDOFF.md`.

## Fail-closed sweep — running tally (this relay)

readiness (PLA-0770) · kitchen (0773) · family-digital-twin (0774) · memories (0775) · family-cfo (0776, money) · family-health (0777, safety) · family-emergency (0778, safety) · trust (0779, security) · **null-display_name crash class (0780, app-wide)** — all ✅. calm/dining left intact (deliberate `safe()` wrappers).

## Next candidates

Fail-closed sweep: `family-coo` fail-closed reads (firstName already fixed there), `grandparent-portal` (6), `economy` (5, kids money), `family-school`/`family-sports`/`intelligence` (4). Plus the owner's new asks (OpenAI admin/settings status, seed-500, feature-tier review) — see `AGENT_HANDOFF.md`.

## Still owner-gated (unchanged)

- **LB-001 (P0)** Auth Admin 500; **LB-003 (P0)** credential rotation; **LB-010 (P0)** apply 0217; **LB-011 (P1)** apply 0219.
- LB-002 remote migration ledger; LB-005/006 live E2E + provider smoke; LB-008 backup drill; LB-013 storage RLS-on assert; LB-014 SEED_ALL coverage; LB-015 CI infra red.
