# Codex Checkpoint - 2026-07-18 12:15 UTC

## Scope

Unified reasoning source-failure contract across the server loader, report type,
daily snapshot summary, and `/dashboard/reasoning` page.

## Changes

- `ReasoningReport` now carries typed `readErrors` source names.
- FOI, relationship graph, and family signal failures are logged and recorded.
- A degraded report cannot display an unqualified all-clear result.
- Snapshot summaries retain only safe source labels and a degraded flag.
- The reasoning page shows a visible incomplete-data warning while preserving
  the six-question report.
- Added focused engine and page boundary coverage.

## Verification

- Focused: 3 files, 13 tests passed.
- Full constrained suite after rebase: 629 test files, 3,726 tests passed.
- `npm.cmd run lint`: passed; two known pre-existing `messages-module` hook warnings.
- `npm.cmd run typecheck`: passed.
- Fresh `npm.cmd run build`: passed; 489 routes generated.

## Publication

This is one atomic publication unit. Rebase on the latest remote `main` before
push and verify the remote commit and report readback afterward.
