# Bubaly Audit Progress — 2026-07-17 00:10 UTC

- Agent: `agent-03` (Opus 4.8) · Units: **A-11** (IN_REVIEW) + **A-16** (IN_REVIEW)
- Overall completion: **10.0%** (weighted math owned by `docs/AUDIT_PROGRESS.md`)
- Decision: **NO-GO** (live launch blockers remain)

## Completed Increment — shipped the safe half of LB-009 (migration `0216`, commit `46556214`)

Brought the `family-media` Storage bucket into version control. It was created out-of-band in the dashboard, so a fresh Supabase project / the PG16 harness had **no bucket** and Photos, Create-Memory, Message attachments, and Reminder attachments all failed to upload, with its policies living outside git.

- **Migration 0216** — idempotent bucket create (`on conflict do nothing`, 25 MB limit, **public=true** to preserve the `getPublicUrl` contract the app + already-stored URLs depend on) + **family-folder write RLS** on `storage.objects` (insert/update/delete + authenticated select) scoped to `is_family_member(((storage.foldername(name))[1])::uuid)`, mirroring the private `documents` bucket (0007).
- **PG16-validated**: applies + idempotent (×2); **cross-family write PROVEN blocked** — an authenticated member of family A can write into A's folder but is blocked from B's; authenticated SELECT is family-scoped.
- Guard `tests/a11-family-media-bucket.test.ts`; migration-numbering guard bumped to `0217` (renumbered from 0215→0216 after an agent-01 collision on 0215).
- Flagged for prod in `PENDING_PROD_MIGRATIONS.md`; `on conflict do nothing` means applying it never mutates the existing prod bucket — it only adds/refreshes the write policies.

## LB-009 status: P1 → P2 (downgraded, partially mitigated)

- **Fixed:** reproducibility (fresh envs/harness now have the bucket) + cross-family write isolation.
- **Still open (owner):** reads stay public (`getPublicUrl`), so object URLs are unauthenticated. The private+signed-URL switch is deferred because existing `family_photos`/`family_messages` rows store public URLs — flipping to private without a URL data-migration would break every stored link. Plus a human must apply `0216` to prod.

## Cross-agent note

`tests/display-render.test.ts` (agent-04, A-05) is **RED on origin/main** (`ReferenceError: React is not defined` — a missing `import React` / JSX-runtime setup in that test file, 9 cases). Confirmed pre-existing (fails with my changes stashed); not in my A-11/A-16 scope and not caused by migration 0216. Flagged for agent-04. My increment is orthogonal and green.

## Verification

- `tests/a11-family-media-bucket.test.ts` (2) + `tests/migration-version-safety.test.ts` (3) + `tests/launch-audit-docs.test.ts` — passing.
- PG16 live validation as above.
- Pushed to `main` (`46556214`); rebased cleanly over concurrent agent pushes + a 0215 migration-number collision.

## agent-03 session totals (all on `main`)

17 increments: PLA-0405..0417 (silent-failure sweep), 0432/0434/0441 (A-16), 0442 (A-11 docs/storage RLS), 0461/LB-009 finding + the `0216` bucket fix.

## Next

Await owner decision on LB-009 read-privacy (public→signed URLs). Continue A-11 messages live-path + A-16 delivery matrix as infra allows. Heartbeat ≤30 min. Do not raise the launch decision.
