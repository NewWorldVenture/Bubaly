# Launch Audit — §3b SSR-crash class sweep 2026-07-18 10:40 UTC

**Agent:** `agent-02` · **Scope:** cross-cutting, non-owning, read-only

## What

Closed out a **flagged-but-unswept** defect class from `COORDINATION.md §3b`:
the null-string SSR-throw class (a client component's server render calling
`.split()`/`.charAt()`/`.toLowerCase()` on a nullable Supabase column → an
uncatchable SSR throw that crashes the whole route; found live once on
`/display`, PLA-0490). §3b listed "known suspects" but no sweep had confirmed
containment.

## Method

Grepped all 74 `components/modules/*.tsx` for string-method calls (296 sites),
filtered to render-path (non-search-callback) calls on values that originate
from DB columns, and traced each to its source column constraint or derivation.

## Result — CONTAINED, no live bug

Every render-path string-method call lands on one of:

- **schema-`NOT NULL` columns** — `family_contacts.name` (mig 0014, drives
  `initials()`/`avatarColor()` in contacts-module), `family_members.display_name`
  (briefing avatar). Verified the migration DDL, not just the TS type.
- **locally-derived non-null values** — briefing "Coming Up" time is
  `new Date(e.starts_at).toLocaleTimeString('en-US',{hour:'numeric',minute:'2-digit'})`
  (always `H:MM AM`); `documents-module` `folderLabel`/`folderColor` take derived
  group keys; `messages-module` sender initial is `(msg.sender_name ?? '?')[0]`
  (already guarded).
- **`?.`-guarded nullable columns** in search filters — `c.email?`, `n.title?`,
  `n.body?`, `r.cuisine?`, `r.description?`, `p.caption?`.

No fix required in the shared modules. The originally-flagged
`.display_name.split(' ')[0]` is confirmed `NOT NULL` → low-risk as §3b already
noted. `app/(app)/{home,kids}/` + `family-dashboard.tsx` (A-05, agent-05 active)
left to their owner to confirm their own server-component paths — not touched.

## Integration gates (re-confirmed at latest main)

- `tsc --noEmit` clean · `vitest run` 3,647 tests green · `next build` exit 0
  (full route tree). See `2026-07-18-0945.md` (type/test) and `-1030.md` (build).

## Honest state

Unchanged: **10.0% weighted · NO-GO.** No new defect surfaced; a swept-clean
class is negative evidence, not progress. Blockers remain owner/live-prod
(prod migrations 0217/0219/0221/0224, credential rotation, Auth Admin 500, live
third-party + E2E smokes). No inflation, no history rewritten, no other agent's
source files touched — only the shared board's §3b section (append) and my own
progress lane.
