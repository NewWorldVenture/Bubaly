# Bubaly Audit Progress — 2026-07-17 16:10 UTC

- Agent: `agent-01` (Opus 4.8) · Units: A-07 (chores) + A-12 (guardian), both IN_REVIEW
- Overall completion: **~10%** (weighted math owned by `docs/AUDIT_PROGRESS.md`)
- Decision: **NO-GO** (owner-gated P0/P1 blockers remain — LB-010 wallet-mint, LB-011 admin-PII, credential rotation, live E2E)

## Completed this session (4 verified increments, all pushed to `main`)

Closed the A-07 chore-integrity surface, then discovered and swept a **new bug class** to completion.

- **PLA-0612** (`a7a3f806`) — A-07 chore submission, two findings one root cause: (1) **P1 functional break** — `chore_ai_validations` is SELECT-only for members but `submitProofAction` INSERTed via the child session → kid proof submission was RLS-broken for *every* user; (2) integrity — a child could `update chore_submissions set status='approved'` directly. Fix: route AI-verdict/auto-approve/decision-status writes through service-role + migration **0222** trigger. PG16-proven.
- **PLA-0613** (`b25369ed`) — A-07 sibling: a child could forge chore COMPLETION via a direct `chore_assignments.status='approved'` write. Migration **0223** trigger restricts `approved`/`rejected` to managers/service-role. PG16-proven.
- **PLA-0616** (`ecc543b9`) — A-07: the chore approval **audit trail** (`chore_approval_events`, SELECT-only) was written via the caller's user session → every non-auto event silently RLS-denied. `logChoreEvent` now writes via service-role.
- **PLA-0617** (`277f77d4`) — A-12: the Guardian child-safety **audit trail** (`guardian_audit_log`, SELECT-only) was written via the parent's user session → every guardian audit event silently RLS-denied; the safety audit trail never recorded. All 4 audit writes now route through service-role.

## The bug class (now swept to completion)

`chore_ai_validations` (PLA-0612) revealed a class: **RLS-enabled tables with a SELECT policy but no authenticated write policy** (service-role-write by design) that **app code nonetheless writes via a user-session client** — the write is best-effort, so it fails invisibly and the feature silently doesn't persist. Enumerated all ~42 such tables on the PG16 harness and cross-referenced every app write against its client:

- **3 real defects** — `chore_ai_validations`, `chore_approval_events`, `guardian_audit_log` (all fixed above).
- **Everything else verified correct** — reference/catalog tables (badges, roles, permissions, meal_ideas, blog_posts, feature_flags, service_descriptions, family_apps) are read-only; webhook/ledger tables (stripe_*, resend, network_*, loyalty_*) are written by service-role webhooks/crons (`app/api/webhooks/stripe/route.ts`, `app/api/cron/network-aggregate/route.ts` both `createServiceClient()`); admin-console writes (loyalty, content, services) use `requireMarketingAdmin`/admin service clients; marketplace bid/negotiation writes go through SECURITY DEFINER RPCs.

## Gate

All four increments: tsc 0, eslint 0, migration audit clean (next=**0224**), full suite **3487 passing**. Each DB behavior proven on the PG16 harness with production-accurate grants (`grant all … to authenticated`/`service_role`, mirroring Supabase so RLS/triggers are the real gate).

## Still owner-gated (unchanged, cannot be done by an agent)

- **LB-010 (P0):** apply 0217 (wallet mint) to prod. **LB-011 (P1):** apply 0219 (admin PII) to prod.
- 0215/0216/0218/0220/0221 + **new 0222/0223** + the 0118–0135 feature bundle → prod (human-owned). (0616/0617 need no migration — app-layer, take effect on deploy.)
- LB-003 credential rotation; LB-005/006 live E2E + provider smoke; LB-008 backup drill.
- Product decisions: LB-009 (public→signed URLs), PLA-0610 (family-PII visibility model), PLA-0581 (vault gating).

## Next

Hold A-07 + A-12 IN_REVIEW. The chore/guardian surfaces and the SELECT-only write-client class are closed at app+DB layers. Keep heartbeating; pick up any newly-unblocked, non-conflicting, non-owner-gated surface. Do not raise the launch decision — the release rule bars GO while any P0 is open.
