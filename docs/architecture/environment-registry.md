# Environment registry

G0 documentation only. Status: **partial static inventory**, not deployment readiness or capability completion. Feature code and production remain held; the matrix/foundation dependency remains parent-owned and pending.

Source SHA256: `9d279dd1d614ffd6376492b6f8d3b3d44fa4e5dfe6516ea51e2100c91e2441c3` (user-supplied blueprint SHA, not recalculated and not a hash of these source files).

## Coverage and safety

- 100 distinct names: 73 declared in `.env.example`, plus 27 observed only in scoped source/workflow references.
- The source scan covered `app`, `lib`, `scripts` and `.github` once, with bounded, value-suppressed context. It did not read complete files for a new code review.
- Reported lexical file searches: `lib`: 498; `app`: 644; `scripts`: 30; `.github`: 5. Prior-read exclusions and dynamic lookup sites are recorded in the JSON.
- Previously inspected files were not reread. Retained excerpts were reused where available; this is an explicit coverage limit.
- No captured consumer: `CAP_SERVER_URL`, `E2E_ALLOW_REMOTE_SUPABASE`, `SEED_CREATED_BY_USER_ID`. Their purpose/provider and requiredness remain unverified.
- Dynamic names, helper chains, ignored files, root configuration and test consumers are not exhaustively covered. Git tracking was not verified.
- No actual environment files, runtime values, credential/token stores, GitHub secret values or provider accounts were read. No network, application scripts, tests or post-write checks were run.
- Only names, repo-relative evidence paths and descriptive behavior are recorded. No configured values, example values, private machine/attachment paths or blueprint text are reproduced.

## Classification

`secret` means credential/signing/encryption material. `public-config` means non-credential configuration or a public key/identifier; it does **not** mean internal contacts, allowlists, tenant/family identifiers, endpoints or repository selectors may be published. Requiredness applies only to the cited consumer; unverified means the bounded source evidence does not establish the behavior.

## Variables

The JSON contains additional consumer locations, template definition lines, sensitivity notes and coverage exclusions. Evidence references below are repo-relative.

| Variable | Provider / purpose | Classification | Source-supported behavior | Evidence |
| --- | --- | --- | --- | --- |
| `AI_CITATION_API_KEY` | AI citation integration: Credential for AI-citation synchronization. | `secret` | **conditional-unverified**: The synchronization path checks endpoint and key together and writes state on missing configuration. Provider identity and final status are unverified. | `lib/marketing/provider-sync.ts:124` |
| `AI_CITATION_API_URL` | AI citation integration: Configured endpoint for AI-citation synchronization. | `public-config` | **conditional-unverified**: The synchronization path checks endpoint and key together and writes state on missing configuration. Provider identity and final status are unverified. | `lib/marketing/provider-sync.ts:123` |
| `AI_MODEL` | OpenAI routing: AI model-selection override. | `public-config` | **optional-override**: Settings can prefer a stored model; getProvider accepts a recognized model or falls back to DEFAULT_OPENAI_MODEL. Dynamic task-model environment aliases remain unresolved. | `lib/ai/provider.ts:502` |
| `ANTHROPIC_API_KEY` | Anthropic setting: Credential retained in AI configuration and readiness displays. | `secret` | **unverified**: Configuration/status reads are evidenced. The inspected provider factory creates an OpenAIProvider; active Anthropic request usage is not established. | `lib/ai/settings.ts:32` |
| `APPLE_CALDAV_BASE_URL` | Apple CalDAV: CalDAV endpoint override. | `public-config` | **optional-override**: The provider defines a literal endpoint fallback. | `lib/sync/providers/apple.ts:37` |
| `APPLE_SYNC_ENABLED` | Apple CalDAV: Apple sync configuration gate. | `public-config` | **conditional**: isAppleSyncConfigured compares this variable to a literal. The enabled literal and actual configured state are not reproduced. | `lib/sync/providers/apple.ts:47` |
| `APPLY_RELEASE` | Release workflow: Workflow condition selecting an apply branch. | `public-config` | **unverified**: A workflow condition compares this binding to a literal. Input defaults and downstream authorization are unverified; release execution remains held. | `.github/workflows/supabase-forward-release.yml:39` |
| `BING_WEBMASTER_API_KEY` | Bing Webmaster: Credential for webmaster synchronization. | `secret` | **conditional-unverified**: The captured missing-key branch writes synchronization state. Its precise final status and return behavior are not established by the bounded context. | `lib/marketing/provider-sync.ts:98` |
| `CAP_SERVER_URL` | unverified: unverified: name appears in the example, but a purpose-defining consumer was not captured. | `public-config` | **unverified**: No purpose-defining source consumer was captured within the bounded allowed scan. | `.env.example:169` |
| `CHILD_LOGIN_SECRET` | Bubaly authentication: Secret used by child-login configuration and sign-in. | `secret` | **required-for-operation**: childSignInAction returns an unsuccessful result when the secret is missing. | `app/(auth)/actions.ts:62` |
| `CI` | CI / local script guard: CI-mode input and workflow binding. | `public-config` | **conditional**: The migration-version helper exits when CI is absent and dry-run is not selected. Other CI uses do not establish a global application requirement. | `scripts/ci-dedupe-migration-versions.mjs:83` |
| `CONTACT_CENTER_INBOUND_SECRET` | Bubaly contact center: Shared secret checked by inbound-email authorization. | `secret` | **conditional-unverified**: When absent, authorization depends on NODE_ENV rather than failing uniformly. The mode literal and complete deployment behavior are not documented from the value-suppressed extract. | `app/api/contact-center/email/route.ts:29` |
| `CONTACT_INBOX` | Bubaly contact form: Destination mailbox configuration for contact submissions. | `public-config` (sensitive config) | **optional-override**: The contact route provides a literal fallback destination. No destination value or delivery was inspected. | `app/api/contact/route.ts:42` |
| `CRON_BASE_URL` | Bubaly scheduled jobs: Base URL for the cron-dispatch script. | `public-config` | **optional-override**: The script supplies a fallback base URL. This inventory does not authorize dispatching to any endpoint. | `scripts/cron-dispatch.mjs:134` |
| `CRON_SECRET` | Bubaly scheduled jobs: Scheduled-request authorization and Guardian fallback credential. | `secret` | **conditional**: hasCronAuthorization requires a nonempty secret; cron-dispatch returns before dispatch when missing. Guardian escalation can use it as an alternative secret. | `lib/server/cron-auth.ts:5` |
| `E2E_ALLOW_REMOTE_SUPABASE` | unverified: unverified: name appears in the example, but a purpose-defining consumer was not captured. | `public-config` | **unverified**: No purpose-defining source consumer was captured within the bounded allowed scan. | `.env.example:148` |
| `E2E_AUTHENTICATED` | CI E2E configuration: Authenticated-test environment binding in the CI workflow. | `public-config` | **unverified**: The workflow declares the binding. Test consumers are outside the requested source scope; authentication behavior and requiredness are unverified. | `.github/workflows/ci.yml:186` |
| `E2E_AUTH_EMAIL` | CI E2E configuration: Authenticated-test environment binding in the CI workflow. | `public-config` (sensitive config) | **unverified**: The workflow declares the binding. Test consumers are outside the requested source scope; authentication behavior and requiredness are unverified. | `.github/workflows/ci.yml:187` |
| `E2E_AUTH_PASSWORD` | CI E2E configuration: Authenticated-test environment binding in the CI workflow. | `secret` | **unverified**: The workflow declares the binding. Test consumers are outside the requested source scope; authentication behavior and requiredness are unverified. | `.github/workflows/ci.yml:188` |
| `EMAIL_FROM` | Resend / Bubaly email: Sender-address configuration. | `public-config` (sensitive config) | **optional-override**: FROM_EMAIL provides a literal fallback; no sender value or verified-domain state was inspected. | `lib/email.ts:21` |
| `EXPO_NO_TELEMETRY` | CI / Expo configuration: Telemetry-related CI environment binding. | `public-config` | **unverified**: The workflow declares this name. Downstream Expo consumption and actual telemetry behavior were not inspected. | `.github/workflows/ci.yml:93` |
| `FCM_SERVER_KEY` | Firebase Cloud Messaging: Credential for the FCM sending path. | `secret` | **conditional**: fcmConfigured checks presence and sendFcm returns false when no key is available. | `lib/server/push.ts:46` |
| `GIPHY_API_KEY` | Giphy: Server credential for GIF search. | `secret` | **required-for-operation**: The GIF search route returns 503 when the key is missing. | `app/api/gif/search/route.ts:26` |
| `GITHUB_ENV` | GitHub Actions workflow: Environment-export handoff variable referenced by CI shell commands. | `public-config` | **unverified**: Workflow commands reference the variable while exporting environment bindings. Its path value and runner state were never read. | `.github/workflows/ci.yml:147` |
| `GITHUB_FEEDBACK_REPO` | GitHub feedback integration: Target-repository configuration or alias for feedback integration. | `public-config` (sensitive config) | **optional-alias**: githubRepo prefers GITHUB_FEEDBACK_REPO, then GITHUB_REPO, then a literal fallback. No configured repository value was inspected. | `lib/integrations/github.ts:20` |
| `GITHUB_FEEDBACK_TOKEN` | GitHub feedback integration: Authentication credential or alias for the feedback integration. | `secret` | **optional-alias**: githubToken prefers GITHUB_TOKEN, then GITHUB_FEEDBACK_TOKEN, then a literal fallback. No GitHub secret store or account was accessed. | `lib/integrations/github.ts:17` |
| `GITHUB_REPO` | GitHub feedback integration: Target-repository configuration or alias for feedback integration. | `public-config` (sensitive config) | **optional-alias**: githubRepo prefers GITHUB_FEEDBACK_REPO, then GITHUB_REPO, then a literal fallback. No configured repository value was inspected. | `lib/integrations/github.ts:20` |
| `GITHUB_TOKEN` | GitHub feedback integration: Authentication credential or alias for the feedback integration. | `secret` | **optional-alias**: githubToken prefers GITHUB_TOKEN, then GITHUB_FEEDBACK_TOKEN, then a literal fallback. No GitHub secret store or account was accessed. | `lib/integrations/github.ts:17` |
| `GOOGLE_CLIENT_ID` | Google OAuth: Legacy OAuth client identifier and Google Sync fallback. | `public-config` | **conditional-unverified**: Legacy OAuth construction consumes it directly; Google Sync can use it when GOOGLE_SYNC_CLIENT_ID is absent. End-to-end configuration validation is unverified. | `lib/google.ts:12` |
| `GOOGLE_CLIENT_SECRET` | Google OAuth: Legacy OAuth token-exchange credential and Google Sync fallback. | `secret` | **conditional-unverified**: Legacy token requests consume it; Google Sync falls back to it from GOOGLE_SYNC_CLIENT_SECRET. End-to-end missing-credential handling is unverified. | `lib/google.ts:30` |
| `GOOGLE_SEARCH_CONSOLE_ACCESS_TOKEN` | Google Search Console: Preferred direct credential for search-console synchronization. | `secret` | **optional-alias**: tokenForGoogle prefers this input and otherwise considers the legacy key. Overall provider-sync requiredness is unverified. | `lib/marketing/provider-sync.ts:38` |
| `GOOGLE_SEARCH_CONSOLE_KEY` | Google Search Console: Legacy search-console credential input and settings readiness indicator. | `secret` | **optional-alias**: The provider considers it after the direct access token and accepts only a format-conditioned value. Accepted values and account state were not inspected. | `lib/marketing/provider-sync.ts:40` |
| `GOOGLE_SYNC_CALENDAR_READONLY_SCOPE` | Google Sync: Inputs to the Google synchronization scope builder. | `public-config` | **unverified**: The scope builder reads and combines configured inputs. Effective scopes, provider consent and missing-input behavior beyond the captured context are unverified. | `lib/sync/providers/google.ts:44` |
| `GOOGLE_SYNC_CALENDAR_SCOPES` | Google Sync: Inputs to the Google synchronization scope builder. | `public-config` | **unverified**: The scope builder reads and combines configured inputs. Effective scopes, provider consent and missing-input behavior beyond the captured context are unverified. | `lib/sync/providers/google.ts:43` |
| `GOOGLE_SYNC_CLIENT_ID` | Google Sync: Dedicated OAuth client identifier for calendar/task synchronization. | `public-config` | **optional-alias**: The helper falls back to GOOGLE_CLIENT_ID and then a literal fallback. A valid effective configuration was not tested. | `lib/sync/providers/google.ts:28` |
| `GOOGLE_SYNC_CLIENT_SECRET` | Google Sync: Dedicated OAuth credential for calendar/task synchronization. | `secret` | **optional-alias**: The helper falls back to GOOGLE_CLIENT_SECRET and then a literal fallback. A valid effective configuration was not tested. | `lib/sync/providers/google.ts:31` |
| `GOOGLE_SYNC_REDIRECT_URI` | Google Sync: OAuth redirect override. | `public-config` | **optional-override**: The redirect helper supplies a fallback; no redirect value or provider registration was inspected. | `lib/sync/providers/google.ts:38` |
| `GOOGLE_SYNC_TASKS_SCOPES` | Google Sync: Inputs to the Google synchronization scope builder. | `public-config` | **unverified**: The scope builder reads and combines configured inputs. Effective scopes, provider consent and missing-input behavior beyond the captured context are unverified. | `lib/sync/providers/google.ts:45` |
| `GUARDIAN_INTERNAL_SECRET` | Bubaly Guardian: Authorization secret for internal Guardian escalation. | `secret` | **optional-alias**: Guardian escalation falls back to CRON_SECRET and returns 401 if no effective secret or matching authorization is present. | `app/api/guardian/escalate/route.ts:23` |
| `INTERNAL_SECRET` | Bubaly internal services: Internal-request authorization secret and unsubscribe-signing fallback. | `secret` | **conditional**: hasInternalSecret returns false without a secret. Marketing unsubscribe signing can use this as a fallback to MARKETING_UNSUB_SECRET. | `lib/server/cron-auth.ts:9` |
| `MARKETING_UNSUB_SECRET` | Bubaly marketing: Unsubscribe-signing secret. | `secret` | **optional-alias**: Signing falls back through INTERNAL_SECRET and SUPABASE_SERVICE_ROLE_KEY. A NODE_ENV-conditioned error branch exists when all are absent; no fallback secret values are reproduced. | `lib/marketing/unsubscribe.ts:8` |
| `MICROSOFT_SYNC_CLIENT_ID` | Microsoft Sync: OAuth application identifier. | `public-config` | **unverified**: The client-ID helper has a literal fallback. Complete configuration validation and provider behavior were not captured. | `lib/sync/providers/microsoft.ts:30` |
| `MICROSOFT_SYNC_CLIENT_SECRET` | Microsoft Sync: OAuth client credential. | `secret` | **unverified**: The client-secret helper has a literal fallback. Complete configuration validation and provider behavior were not captured. | `lib/sync/providers/microsoft.ts:33` |
| `MICROSOFT_SYNC_REDIRECT_URI` | Microsoft Sync: OAuth redirect override. | `public-config` | **optional-override**: The redirect helper supplies a fallback; no provider registration was inspected. | `lib/sync/providers/microsoft.ts:39` |
| `MICROSOFT_SYNC_SCOPES` | Microsoft Sync: OAuth synchronization scope override. | `public-config` | **optional-override**: The scope helper falls back to DEFAULT_SCOPES. Exact scopes and consent were not inspected as configured values. | `lib/sync/providers/microsoft.ts:43` |
| `MICROSOFT_SYNC_TENANT` | Microsoft Sync: OAuth authority tenant selector. | `public-config` | **optional-override**: Authority construction uses an environment value or literal fallback. | `lib/sync/providers/microsoft.ts:24` |
| `NEXT_PUBLIC_APP_URL` | Bubaly application: Application origin used in links, callbacks and local test-runner configuration. | `public-config` | **optional-in-observed-fallbacks**: Multiple inspected consumers provide a fallback. Email URL selection falls back through NEXT_PUBLIC_SITE_URL. This does not prove every URL-dependent journey works without configuration. | `lib/email.ts:25` |
| `NEXT_PUBLIC_BUILD_ID` | Build diagnostics: Public build identifier used on the display error screen. | `public-config` | **optional-display-override**: The error screen prefers the commit identifier, then NEXT_PUBLIC_BUILD_ID, then a literal fallback. No current identifier value was inspected. | `app/(app)/display/error.tsx:21` |
| `NEXT_PUBLIC_SITE_URL` | Bubaly marketing: Site origin used in marketing URLs and provider synchronization. | `public-config` | **conditional**: Several URL consumers have fallbacks, but provider-sync siteUrl throws when this variable is missing. | `lib/marketing/provider-sync.ts:32` |
| `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` | Stripe: Client publishable-key configuration. | `public-config` | **optional-environment-fallback**: effectivePublishableKey prefers a stored setting, then this variable, then null. Downstream missing-key behavior is unverified. | `lib/stripe/settings.ts:34` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase: Public anonymous client key for browser/server clients and selected tooling. | `public-config` | **unverified**: Client factories use a TypeScript non-null assertion, not runtime validation. Tooling can select alternative keys; global requiredness is not established. | `lib/supabase/client.ts:8` |
| `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | Supabase: Publishable-key probe input and CI environment binding. | `public-config` | **unverified**: The probe reads a fallback value and CI exports the name. Primary application adoption and missing-key validation were not established. | `scripts/probe-supabase-rest.mjs:17` |
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase: Database API URL for browser/server clients and tooling. | `public-config` | **conditional**: Client factories pass it without a local runtime guard. Seed tooling requires this URL or SUPABASE_URL; schema-audit tooling also guards the URL. | `lib/supabase/client.ts:7` |
| `NEXT_PUBLIC_VAPID_PUBLIC_KEY` | Web Push / VAPID: Public key for push subscription and server VAPID configuration. | `public-config` | **conditional**: Server VAPID initialization checks both public and private keys; the browser helper also exposes this public key input. Delivery was not exercised. | `lib/server/push.ts:24` |
| `NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA` | Build diagnostics: Public build identifier used on the display error screen. | `public-config` | **optional-display-override**: The error screen prefers the commit identifier, then NEXT_PUBLIC_BUILD_ID, then a literal fallback. No current identifier value was inspected. | `app/(app)/display/error.tsx:20` |
| `NODE_ENV` | Bubaly / runtime: Runtime-mode input to authorization, fallback and diagnostic branches. | `public-config` | **conditional-unverified**: Inspected consumers use different mode-dependent branches. Global absence behavior and configured mode are unverified. | `app/api/contact-center/email/route.ts:30` |
| `NODE_OPTIONS` | CI / Node configuration: Node runtime-options binding in CI. | `public-config` | **unverified**: The workflow declares this binding; option values and runner behavior were not inspected. | `.github/workflows/ci.yml:41` |
| `OPENAI_API_KEY` | OpenAI: AI credential used by provider resolution, readiness checks and embeddings. | `secret` | **conditional**: Settings/provider paths can prefer stored credentials. The embeddings helper throws without an effective key. No credential store or provider account was accessed. | `lib/marketing/platform.ts:331` |
| `OPENAI_EMBEDDING_MODEL` | OpenAI: Embedding-model override. | `public-config` | **optional-override**: The embeddings helper provides a literal model fallback. | `lib/marketing/platform.ts:333` |
| `OPENAI_TRANSCRIBE_MODEL` | OpenAI: Voice-transcription model override. | `public-config` | **optional-override**: The transcription route provides a literal model fallback. | `app/api/ai/voice/transcribe/route.ts:51` |
| `OPENAI_TTS_MODEL` | OpenAI: Speech-synthesis model override. | `public-config` | **optional-override**: The speech route provides a literal model fallback. | `app/api/ai/voice/speak/route.ts:40` |
| `OPENAI_TTS_VOICE` | OpenAI: Speech voice input passed to voice normalization. | `public-config` | **unverified**: A request voice takes precedence over the environment input. The normalizer's missing-input contract was not captured. | `app/api/ai/voice/speak/route.ts:47` |
| `PLAYWRIGHT_EXTERNAL_SERVER` | Local E2E runner: Child-process environment binding in the E2E launcher. | `public-config` | **unverified**: The runner sets the binding, but the downstream consumer is outside the captured source evidence. | `scripts/run-e2e.mjs:9` |
| `PLAYWRIGHT_PORT` | Local E2E runner: Port input forwarded to the E2E child environment. | `public-config` | **optional-override**: run-e2e provides a literal fallback port. | `scripts/run-e2e.mjs:5` |
| `PLAYWRIGHT_SKIP_BUILD` | Local E2E runner: Build-step control for the E2E launcher. | `public-config` | **optional-switch**: The runner executes its build branch unless the variable equals a literal. The switch value is omitted and no build ran. | `scripts/run-e2e.mjs:77` |
| `RESEND_API_KEY` | Resend: Email API credential and email-readiness input. | `secret` | **conditional**: emailEnabled returns its presence and getResend passes it to the client. Complete missing-key behavior across all sending callers is unverified. | `lib/email.ts:7` |
| `RESEND_WEBHOOK_SECRET` | Resend: Webhook signature-verification secret. | `secret` | **required-for-verification**: The webhook verifier returns false when this secret is missing. | `app/api/webhooks/resend/route.ts:19` |
| `ROUTE` | Cron workflow: Optional route selector forwarded to the cron-dispatch script. | `public-config` | **optional-filter**: The workflow forwards a route argument only when the binding is nonempty. | `.github/workflows/cron-dispatch.yml:60` |
| `SEATGEEK_CLIENT_ID` | SeatGeek: Server-side credential/identifier enabling SeatGeek discovery. | `secret` | **optional-provider-branch**: The discovery route enters this provider branch only when the identifier is present. Treat as a private integration credential conservatively. | `app/api/weekend/discover/route.ts:68` |
| `SEED_CONFIRM_FAMILY_ID` | Bubaly seed tooling: Confirmation of the intended seed-family target. | `public-config` (sensitive config) | **required-for-operation**: requireEnv rejects absence; requireSeedScope rejects a confirmation that differs from the target family ID. | `scripts/seed-client.mjs:29` |
| `SEED_CREATED_BY_USER_ID` | unverified: unverified: name appears in the example, but a purpose-defining consumer was not captured. | `public-config` (sensitive config) | **unverified**: No purpose-defining source consumer was captured within the bounded allowed scan. | `.env.example:19` |
| `SEED_ENVIRONMENT` | Bubaly seed tooling: Explicit seed-environment safety selector. | `public-config` | **required-for-operation**: requireEnv rejects absence and requireSeedScope checks an allowlist. No allowed values are reproduced and no seeding was performed. | `scripts/seed-client.mjs:23` |
| `SEED_FAMILY_ID` | Bubaly seed tooling: Explicit family target for seed operations. | `public-config` (sensitive config) | **required-for-operation**: requireUuid delegates to requireEnv and UUID validation; the captured seed scope uses this name. | `scripts/seed-client.mjs:28` |
| `STRIPE_MONEY_WEBHOOK_SECRET` | Stripe: Dedicated signature-verification secret for the money webhook. | `secret` | **optional-alias**: The money webhook falls back to STRIPE_WEBHOOK_SECRET and returns 503 when neither is available. | `app/api/webhooks/money/route.ts:26` |
| `STRIPE_PRICE_BASIC_ANNUAL` | Stripe: Subscription-plan price identifier and legacy/current plan alias. | `public-config` | **optional-alias**: Basic and family identifiers fall back to one another in the plan map. The webhook also compares identifiers; valid configured prices and checkout readiness are unverified. | `lib/stripe.ts:31` |
| `STRIPE_PRICE_BASIC_MONTHLY` | Stripe: Subscription-plan price identifier and legacy/current plan alias. | `public-config` | **optional-alias**: Basic and family identifiers fall back to one another in the plan map. The webhook also compares identifiers; valid configured prices and checkout readiness are unverified. | `lib/stripe.ts:30` |
| `STRIPE_PRICE_FAMILY_ANNUAL` | Stripe: Subscription-plan price identifier and legacy/current plan alias. | `public-config` | **optional-alias**: Basic and family identifiers fall back to one another in the plan map. The webhook also compares identifiers; valid configured prices and checkout readiness are unverified. | `lib/stripe.ts:31` |
| `STRIPE_PRICE_FAMILY_MONTHLY` | Stripe: Subscription-plan price identifier and legacy/current plan alias. | `public-config` | **optional-alias**: Basic and family identifiers fall back to one another in the plan map. The webhook also compares identifiers; valid configured prices and checkout readiness are unverified. | `lib/stripe.ts:30` |
| `STRIPE_PRICE_PLUS_ANNUAL` | Stripe: Plus subscription-plan price identifier. | `public-config` | **unverified**: The plan map reads the identifier with a literal fallback and the webhook compares it. A usable price and missing-configuration checkout behavior were not established. | `lib/stripe.ts:33` |
| `STRIPE_PRICE_PLUS_MONTHLY` | Stripe: Plus subscription-plan price identifier. | `public-config` | **unverified**: The plan map reads the identifier with a literal fallback and the webhook compares it. A usable price and missing-configuration checkout behavior were not established. | `lib/stripe.ts:32` |
| `STRIPE_SECRET_KEY` | Stripe: Server-side payment API credential. | `secret` | **conditional**: getStripe throws if the environment key is absent; effectiveSecretKey in the settings helper can instead prefer a stored setting. Requiredness differs by caller. | `lib/stripe.ts:6` |
| `STRIPE_WEBHOOK_SECRET` | Stripe: Signature-verification secret for the subscription webhook and money-webhook fallback. | `secret` | **required-for-subscription-webhook**: The subscription webhook returns 503 when this environment secret is absent. A separate settings helper supports stored values; that does not establish use by this route. | `app/api/webhooks/stripe/route.ts:105` |
| `SUPABASE_ACCESS_TOKEN` | Supabase: Management credential passed to migration-state and forward-release tooling. | `secret` | **unverified**: Source passes the token into administrative operations; the complete validator was not captured. No administrative script was executed. | `scripts/audit-production-migration-state.mjs:101` |
| `SUPABASE_ANON_KEY` | Supabase: Alternative anonymous API key in schema/public-access tooling. | `public-config` | **optional-alias**: Schema audit selects service-role, public-anon, then this alias; the public-access script prefers this alias over NEXT_PUBLIC_SUPABASE_ANON_KEY. | `scripts/audit-supabase-schema.mjs:107` |
| `SUPABASE_PROJECT_REF` | Supabase: Project selector passed to migration-state and release tooling. | `public-config` (sensitive config) | **unverified**: Administrative scripts and workflow declarations reference this selector. Requiredness and target safeguards beyond captured references are unverified. | `scripts/audit-production-migration-state.mjs:100` |
| `SUPABASE_SECRET_KEY` | Supabase: Secret-key probe input and CI environment binding. | `secret` | **unverified**: The probe and CI reference the name. Requiredness and missing-key behavior outside the captured reads are unverified. | `scripts/probe-supabase-rest.mjs:18` |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase: Privileged server and administrative-script credential. | `secret` | **required-for-seed-helper**: createSeedClient passes this name through requireEnv, which throws when missing. Server client construction also consumes it; other paths have different fallback behavior. | `scripts/seed-client.mjs:50` |
| `SUPABASE_URL` | Supabase: Alternative database URL in the guarded seed client. | `public-config` | **optional-alias**: Seed tooling prefers NEXT_PUBLIC_SUPABASE_URL, falls back to SUPABASE_URL, and throws if neither is available. | `scripts/seed-client.mjs:47` |
| `SUPER_ADMIN_EMAILS` | Bubaly authorization: Email-list input to the super-administrator allowlist helper. | `public-config` (sensitive config) | **unverified**: The helper normalizes a configured or fallback string. Complete resulting authorization behavior was not captured; do not publish this access-control configuration. | `lib/constants/super-admins.ts:13` |
| `SYNC_TOKEN_KEY` | Bubaly sync encryption: Encryption key for stored sync credentials and feed-token signing input. | `secret` | **required-for-encryption**: loadKey throws on absence. signFeedToken separately uses a literal fallback; uniform fail-closed behavior is not established by these differing consumers. | `lib/sync/crypto.ts:19` |
| `THEMEALDB_API_KEY` | TheMealDB: Recipe-provider key override. | `secret` | **optional-override**: The provider supplies a literal key fallback. Its current entitlement and provider availability were not checked. | `lib/recipes/providers/themealdb.ts:8` |
| `TICKETMASTER_API_KEY` | Ticketmaster: Credential enabling the Ticketmaster discovery branch. | `secret` | **optional-provider-branch**: The discovery route enters this provider branch only when the key is present. | `app/api/weekend/discover/route.ts:50` |
| `TWILIO_ACCOUNT_SID` | Twilio: Account identifier consumed by Guardian and concierge-call readiness. | `public-config` (sensitive config) | **conditional**: Concierge providerReady requires this identifier, TWILIO_AUTH_TOKEN and TWILIO_CALLER_NUMBER together. Subsequent missing-provider behavior was not fully captured. | `app/api/concierge-calls/place/route.ts:36` |
| `TWILIO_AUTH_TOKEN` | Twilio: Authentication credential used by Guardian and concierge-call readiness. | `secret` | **conditional**: Concierge readiness checks this token with account and caller configuration. Guardian constructs authentication from the account and token; no requests were sent. | `lib/guardian/twilio.ts:8` |
| `TWILIO_CALLER_NUMBER` | Twilio: Outbound concierge caller-number configuration. | `public-config` (sensitive config) | **conditional**: Concierge providerReady requires this number with the account identifier and token. | `app/api/concierge-calls/place/route.ts:36` |
| `TWILIO_PHONE_NUMBER` | Twilio: Guardian phone-number configuration. | `public-config` (sensitive config) | **unverified**: Guardian reads a literal fallback and the settings UI checks presence. The captured context does not establish the operational requirement. | `lib/guardian/twilio.ts:9` |
| `VAPID_PRIVATE_KEY` | Web Push / VAPID: Private signing key for server push configuration. | `secret` | **conditional**: Server VAPID initialization checks both public and private keys before configuring the sender. | `lib/server/push.ts:25` |
| `VAPID_SUBJECT` | Web Push / VAPID: VAPID subject/contact configuration. | `public-config` (sensitive config) | **optional-override**: The push helper supplies a literal subject fallback. No subject value was inspected. | `lib/server/push.ts:26` |
| `VERCEL_ENV` | Vercel / administration: Environment label shown in the administration system view. | `public-config` | **optional-display-override**: The view falls back through NODE_ENV and then a literal label. | `app/(app)/admin/system/page.tsx:83` |
| `VERCEL_REGION` | Vercel / administration: Region label shown in the administration system view. | `public-config` | **optional-display-override**: The view supplies a literal fallback label. | `app/(app)/admin/system/page.tsx:84` |

## Source-only names

These are absent from the observed example declarations, not automatically defects or provisioning requests: `APPLE_CALDAV_BASE_URL`, `APPLE_SYNC_ENABLED`, `APPLY_RELEASE`, `CI`, `CONTACT_CENTER_INBOUND_SECRET`, `CRON_BASE_URL`, `EXPO_NO_TELEMETRY`, `GITHUB_ENV`, `GITHUB_FEEDBACK_REPO`, `GITHUB_FEEDBACK_TOKEN`, `GITHUB_REPO`, `GITHUB_TOKEN`, `NEXT_PUBLIC_BUILD_ID`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA`, `NODE_ENV`, `NODE_OPTIONS`, `PLAYWRIGHT_EXTERNAL_SERVER`, `PLAYWRIGHT_SKIP_BUILD`, `ROUTE`, `SUPABASE_ACCESS_TOKEN`, `SUPABASE_ANON_KEY`, `SUPABASE_PROJECT_REF`, `SUPABASE_SECRET_KEY`, `SUPABASE_URL`, `VERCEL_ENV`, `VERCEL_REGION`.

## Highest-impact documentation follow-ups

1. Parent should reconcile caller-specific key-source and missing-configuration rules in the pending foundation matrix. `SYNC_TOKEN_KEY` has differing encryption/signing handling (`lib/sync/crypto.ts:19`, `lib/sync/feed-token.ts:19`).
2. Review the documentation coverage for `CONTACT_CENTER_INBOUND_SECRET`: a mode-dependent authorization consumer is present (`app/api/contact-center/email/route.ts:29`) but the name is absent from the observed example.
3. Keep stored-setting alternatives distinct from environment requirements. Examples include Stripe (`lib/stripe.ts:6`, `lib/stripe/settings.ts:24`) and AI settings (`lib/ai/settings.ts:31`). No stores were opened.

These are documentation findings, not authorizations to configure providers, execute scripts, change feature code or access production.

## Authorized file changes

- Removed only `repository`, `sourceAttachment` and `sourceReadDirectory` from `docs/traceability/capability-audit-01-10.json`.
- Created `docs/architecture/environment-registry.json` and `docs/architecture/environment-registry.md`.
- No environment template, source code, tests, navigation or deployment configuration was changed.
