# Strategy remaining work

Verified 2026-09-10: the original runtime findings below were reproduced on `7789a3ea` (merged through #431). Main has since advanced through #445, #448, #456 and the owner's #466, #471, #477 and #478 to `d2db14d0`. The Knowledge Center locale fix merged as `16d09340`, followed by the number and German punctuation corrections at `e081baaf` and `d2db14d0`. The latest unpublished rehearsal now incorporates those exact owner files at `c7016237`; earlier checkpoints below deliberately excluded them. No database application was performed. Its earlier marketing/render correction is `85639a18`. Open changes and their current heads are tracked separately below. This is a completion ledger for concrete findings from the current audit, not a new audit of all 60 strategy items. A queue section marked implemented does not prove that every acceptance requirement shipped. Code, tests, and deployed evidence take precedence over older status prose in [STRATEGY_WORK_QUEUE.md](STRATEGY_WORK_QUEUE.md).

The standing constraints remain: no agent-authored migrations, no shared sidebar changes, translated interface copy in all seven locales, existing service/trust boundaries, and visible errors when required reads fail. The owner controls schema changes and production application.

**Checklist progress:** 13 of the 14 findings in the main audit table have implementing PRs (93%, rounded). This is an unweighted count of those findings, not a percentage of the full roadmap or launch readiness. Whole-application translation, integration, owner-controlled configuration/schema work and deployed/device acceptance remain open; follow-up corrections are recorded separately below.

## Review and integrate work already built

These pull requests were **open** when this ledger was written. Review, required CI, and integration remain; they are not represented here as merged fixes. Local validation reported in a PR is distinct from CI or production verification.

PRs #432–#439, #441–#444, #446–#447, #449–#455, #457–#465, #467–#470 and #472–#476 passed Linux CI, E2E and preview deployment at the recorded heads and are ready for review. #446 is ready again at its test-only pricing compatibility head `c865100c`; #470 is ready at `fa0ebcd3`, and #472 at `a7eef396`. #473 is ready at `14905de3`. The durable-browser follow-up #474 at `016f5336`, stacked on #470, now has all checks green and 432 actual disposable-Supabase browser tests passing, including its four new GoTrue journeys. Billing read-boundary #475 at `aec86461` and Health localization #476 at `8adb12bb` are also ready after all required core, mobile, database, E2E and Vercel checks passed. The owner's #466, #471, #477 and #478 are merged; their production migrations remain blocked at remote-ledger verification, as documented below. The owner's #448 passed checks and has since merged to main `57dd3031`. The earlier shared `/ai` overflow is corrected in its `85639a18` head; #453 now adds the complementary rendered regression only. #437 is stacked on #433; #444 includes #442; #451 is stacked on #444; #458 is stacked on #451; #463 is stacked on #460; #467 is stacked on #450; #468 is stacked on #465; #475 is stacked on #472. Recheck live status and merge-base changes before integration. Green checks on individual branches do not establish that their combined result passes. This documentation PR #440 remains draft for owner review. Its previous head `7c7aa0a2` passed all five required checks in [CI run 34468992574](https://github.com/NewWorldVenture/Bubaly/actions/runs/34468992574), with 428 E2E passes and five inherited durable-session skips; this new documentation update requires its own final-head checks.

| Work | Review | Acceptance to retain during integration |
| --- | --- | --- |
| S-08 grandparent digest reads | [#432 — Keep grandparent digest read failures visible per household](https://github.com/NewWorldVenture/Bubaly/pull/432), head `9992461a` | A failed roster, photos, milestones, announcements, or dates read produces that household's error card. Other households remain visible, and retry restores the recovered card. Query errors and transport failures are covered. |
| S-15 recorded completed plans | [#433 — Count recorded completed plans consistently across family metrics](https://github.com/NewWorldVenture/Bubaly/pull/433), head `7e8a7173` | Autopilot, briefing and the banner count the same dated fully completed plans in one family/seven-day window. Explicit partial/failed states override legacy executed status. Undated history is disclosed, failed exact counts remain unavailable, and family changes suppress stale results. The shared 12-minute assumption is modeled planning time. |
| X6 signal precision and retained decisions | [#434 — Measure signal precision and retain withdrawn suggestion evidence](https://github.com/NewWorldVenture/Bubaly/pull/434), head `6e1b23c6` | Count current accepted/executed versus dismissed decisions exactly, distinguish zero/no decisions/read failure, preserve decision evidence during rescans, and allow a withdrawn signal to recur. This is recorded acceptance/execution, not a claim about satisfaction. Weekly historical snapshots still require owner schema work. |
| S-10 launch rollback | [#435 — Fix rollback after failed life-event launches](https://github.com/NewWorldVenture/Bubaly/pull/435), head `326ae50f` | A move created before `planTasks` fails is removed. Cleanup on an existing move removes only this launch's new tasks. All rollback attempts continue after one failure, and incomplete cleanup is reported honestly. |
| S-06 school proposal approval and source completion | [#436 — Keep school desk proposals behind approval](https://github.com/NewWorldVenture/Bubaly/pull/436), head `fffd99bd` | Propose requires review even under an allow policy. Only a successful same-household execution receipt marks its inbox source handled. If execution succeeds but bookkeeping fails, retry repairs the source without creating duplicate work. |
| M40 own-family planning value | [#437 — Show recorded completed plans and modeled planning time in billing](https://github.com/NewWorldVenture/Bubaly/pull/437), head `7368d106`, base #433 | Billing and opened upgrade prompts use the corrected shared dated-plan count and explicit 12-minute model. Preserve zero, unavailable/retry, undated-history disclosure, session changes and household boundaries. Do not reintroduce the rejected four-source total. |
| S-12 purchase advice in Ask | [#438 — Connect Ask to private purchase advice and complete household evidence](https://github.com/NewWorldVenture/Bubaly/pull/438), head `fcc5a3f2` | Actual intake performs the guarded deterministic read without a provider. All evidence is paged completely; budget, memory and gift restrictions remain. Approved answers use the original requester's current permissions and private conversation storage; family-readable receipts contain no sensitive report. |
| W7 public customer stories | [#439 — Make published customer stories readable and editable](https://github.com/NewWorldVenture/Bubaly/pull/439), head `a9c300fa` | Published stories have signed-out detail pages, grounded metadata and escaped content. Admins can edit or withdraw stored stories. Withdrawal blocks the detail page and invalidates public cards. No invented customer claims or automatic publication. |
| W5 outcome discovery | [#441 — Discover and launch relevant household outcomes](https://github.com/NewWorldVenture/Bubaly/pull/441), head `a041cca3` | Home and Command Center use complete counts to rank launchable outcomes. Related routes open the actual launcher with the selected outcome. The optional personal navigation preset preserves existing choices, respects limits, and uses an owner-scoped compare-and-swap write. Shared sidebar files stay unchanged. |
| W8 canonical entity resolution | [#442 — Resolve confirmed household entities on inbound paperwork](https://github.com/NewWorldVenture/Bubaly/pull/442), head `0514d913` | Exact confirmed household entities and aliases resolve deterministically; ambiguous names stay unresolved. Sender evidence survives filing. Failed enrichment retries from captured paperwork without losing the planner opportunity or duplicating the urgent message. OAuth onboarding is a separate change. |
| M30 thirty-minute timing | [#443 — Report thirty-minute setup and first-outcome timing with valid denominators](https://github.com/NewWorldVenture/Bubaly/pull/443), head `8afdd424` | Setup completion and first-outcome viewing have separate denominators. Missing/invalid durations stay explicit; the inclusive 1,800-second threshold does not admit 1,800.001 seconds. Complete keyset reads use a fixed cutoff and fail closed on late-page errors. Viewing is not completed work. |
| S-01 received attachments | [#444 — Capture inbound email attachments with independent retries](https://github.com/NewWorldVenture/Bubaly/pull/444), head `66a90ae5` | Includes the #442 ancestor. Supported bounded attachments preserve their provider identity; extraction and filing retry independently. Saved siblings survive another attachment's failure. Partial extraction is visible, excludes derived quick actions and automatic drafting, and retains an escaped excerpt for review. This does not complete capture-wizard uploads or public-link retrieval. |
| X11 modeled value versus price | [#446 — Compare modeled completed-plan value with published plan price](https://github.com/NewWorldVenture/Bubaly/pull/446), head `c865100c`, ready | Home and Billing compare the shared dated-plan model with the same seven-day allocation of the active paid plan's published USD list price. Editable hourly USD value, excluded/undated evidence, actual zero and unavailable are explicit. This is neither measured time saved nor realized cash savings. |
| X2 automatic capture share | [#447 — Show verified automatic capture share in family intelligence](https://github.com/NewWorldVenture/Bubaly/pull/447), head `f88e1082` | Supported current-window canonical records form the denominator. Verified successful creation receipts and same-family feed provenance form a deduplicated lower-bound numerator. Unknown origins stay unknown. Complete keyset reads, family scope, empty data and failed reads remain distinct. No weekly history is implied. |
| Localized metadata/admin guidance | [#449 — Localize page titles and shared admin credential guidance](https://github.com/NewWorldVenture/Bubaly/pull/449), head `37769954`, base main after #445 | All 31 repaired titles resolve the request locale and preserve robots settings. Shared diagnostic copy is localized, contains no secret material, and does not infer legacy-key revocation from a publishable key. Admin authentication precedes diagnostic reads. The onboarding title matches #443. |
| W8 connected onboarding | [#450 — Connect calendars during onboarding with private preview and safe imports](https://github.com/NewWorldVenture/Bubaly/pull/450), head `35befc11` | Owned encrypted continuation and private primary-calendar preview; dormant connection until Finish; calendar-only imports with no provider writes. Stable event/member/invite identities survive retries and changed previews, and local edits/deletions survive later imports. Preserve original-zone all-day dates and family-local first briefs. No live provider account has been exercised. |
| S-01 upload/photo capture | [#451 — Capture paperwork from uploads and photos with safe retries](https://github.com/NewWorldVenture/Bubaly/pull/451), head `13d23e32`, base #444 | Shared capture intake supports authenticated bounded files, stable creator/family/capture receipts and saved-text resume. Both upload phases require the current user/family and document AAL2 policy. Family changes invalidate the selected request/result; composer success refreshes the review list. Explicit provider failures retain the selected file for retry. No binary retention or schema changes. |
| Admin referral summary | [#452 — Use complete referral totals and recorded reward confirmation](https://github.com/NewWorldVenture/Bubaly/pull/452), head `10b09199` | Complete keyset reads drive totals, conversion rate and referrer rankings; only the recent table is limited to 25 rows. Unconfirmed referrer credits exclude rewarded rows and recorded settlements. A missing receipt is not proof the provider still owes payment. Zero, absent denominator, late-page failures and configuration failures remain distinct. |
| Public assistant demo rendering | [#453 — Test assistant demo rendering in all seven locales](https://github.com/NewWorldVenture/Bubaly/pull/453), head `8f809311` | The two prompt translation fixes are already in integration `85639a18`. This PR adds only real component rendering tests: all six prompts and the selected user message must resolve the seven base catalogues without visible keys. It complements the integration source guard. |
| X3/X5/X10/X12 metric evidence | [#454 — Use complete recorded evidence for strategy metrics](https://github.com/NewWorldVenture/Bubaly/pull/454), head `102eb24a` | Complete stable reads and exact counts fail only their dependent metric. X10 uses recorded paid-active notifications and the current cohort, distinguishing earlier-only, missing and conflicting-current-status evidence. Reactivation and notification recording time are explicit; no first-ever payment or causation claim. X5 shows recorded counts without a handling claim or a fabricated zero-decision ratio. |
| S-11 mobile assistant lifecycle | [#455 — Bind mobile assistant turns to current account and household](https://github.com/NewWorldVenture/Bubaly/pull/455), head `eaac2139` | Exclusive turns stay bound to the current authenticated user and household; late completions are rejected and recording resources cleaned up. Owner-tagged screen state hides previous-household content before passive effects run. Optional API household assertions fail before assistant/provider work. Unavailable authentication and missing voice configuration remain distinct. The assistant subset is translated in seven locales. Clean exports are packaging evidence, not physical-device permission, recording or background validation. |
| Portable repository audits | [#457 — Make repository audits work on Windows and POSIX](https://github.com/NewWorldVenture/Bubaly/pull/457), head `1370b909` | Native filesystem paths become stable repository/API identities at the audit boundaries. Server roots are identified by filename; structural guards retain their source filters and assertions. Metadata enumeration reuses #449's identical native implementation and nonempty-scan assertion. No application behavior or test timeout changes. |
| S-01 selected document links | [#458 — Import selected document links into Paperwork safely](https://github.com/NewWorldVenture/Bubaly/pull/458), head `db85f67f`, base #451 | Explicitly selected PDF, supported image or plain-text links use bounded HTTPS transport pinned to validated public DNS addresses. Source reads retain caller permissions. Document MFA guards the page and API; user/family context is rechecked before filing and saved-text enrichment. Saved receipts prevent repeated fetch/extraction and preserve the first saved snapshot. Owner changes, Reset, stale callbacks and repeated clicks cannot import for another context. General HTML and automatic inbound URL visits remain deferred. |
| Family Contacts translations | [#459 — Translate Family Contacts forms and birthday dates](https://github.com/NewWorldVenture/Bubaly/pull/459), head `ec3df8f0` | List, detail and add/edit copy, metadata and named icon actions resolve all seven locales. Locale month names and UTC birthday dates preserve stored category IDs and numeric month/day values. Call/email labels retain the contact name. The separate contact timeline is outside this batch. |
| Contact timeline translations | [#460 — Translate the contact timeline and reconnect controls](https://github.com/NewWorldVenture/Bubaly/pull/460), head `d5fa47ae` | Timeline labels, reconnect controls and generated descriptions resolve all seven locales. Date-only values keep UTC calendar dates; USD amounts use the selected locale. Stored kind, tone, cadence and user-authored text remain unchanged. This does not repair failed history reads; that is #463. |
| Photos translations | [#461 — Localize Photos controls, forms and upload feedback](https://github.com/NewWorldVenture/Bubaly/pull/461), head `2530f46d` | Named photo actions, album kinds, forms, timestamps, file counts and upload results resolve seven locales. Stored album IDs, storage paths and write order remain unchanged. Attempted progress is distinguished from successfully saved files; actual favorite/unfavorite rendering and interaction replace the obsolete English-only source assertion. |
| Public aggregate evidence | [#462 — Match public counters to recorded complete and partial runs](https://github.com/NewWorldVenture/Bubaly/pull/462), head `004cd657` | The existing RPC includes both complete and partial run statuses, undated totals and completion dates at or after a 30-day cutoff. Public wording and methodology match that evidence. Strict row/count validation and independent RPC failure groups hide malformed figures while preserving valid unrelated counts. No SQL, public row reads or fully completed-plan metric changes. |
| Contact history read failures | [#463 — Keep failed contact history reads out of timelines and drafts](https://github.com/NewWorldVenture/Bubaly/pull/463), head `60f7cfe6`, base #460 | Required contact/history/communications failures stay unavailable rather than becoming empty or partial evidence. Auth redirects, missing contacts and successful empty history remain distinct. No request ledger or drafting provider starts on incomplete history. Route refresh and draft retry are wired; stage-only logs contain no private history. |
| Autopilot action resolution and retries | [#464 — Resolve Autopilot suggestions only after saved work](https://github.com/NewWorldVenture/Bubaly/pull/464), head `d0cbb3b9` | Review links open fixed existing routes without recording completion. Legacy approval remains separate from execution. Reminder acceptance reloads the same-family source, proves current feature/account/family access and uses durable source-derived idempotency. Matching proposal fingerprints are required on both initial and competing receipts before a guarded status update. Changed proposals require review; ordinary reminder edits preserve hidden bookkeeping tags. |
| Files hub translations | [#465 — Localize Files hub controls and upload feedback](https://github.com/NewWorldVenture/Bubaly/pull/465), head `cc470430` | Hub controls, overview labels, upload feedback and Cloud/Vault/Shared metadata resolve all seven locales. Stored folder/category values and user-authored text remain unchanged. Upload refusal still precedes side effects, and all three metadata routes retain feature and AAL2 gates. The existing General category filter defect is addressed separately by #468. |
| Calendar Sync feature access | [#467 — fix(onboarding): enforce Calendar Sync configuration and entitlement](https://github.com/NewWorldVenture/Bubaly/pull/467), head `5c0610d2`, base #450, ready | Strict relevant feature configuration precedes family creation. The existing family's complete active/trialing subscriptions, actual trial, closure and expiry govern private OAuth, preview, Finish, activation and refresh. Rechecks after provider work and before canonical writes preserve policy changes. Missing settings use the catalog default; failed or malformed reads stay unavailable. |
| Files category filter | [#468 — Filter Files folders by category identity](https://github.com/NewWorldVenture/Bubaly/pull/468), head `d0169e7d`, base #465, ready | Uncategorized rows use a null identity, separate from a stored General label. Exact category selection follows existing trim/case behavior and combines with independent plain-text search. Punctuation remains literal; All Files clears the filter. Family/view changes cannot apply the previous context's selection. Stored rows and upload behavior remain unchanged. |
| Android device label | [#469 — Restore the Android marketing device label](https://github.com/NewWorldVenture/Bubaly/pull/469), head `50bf6a01`, ready | The seven existing catalogues define the missing visualMocks.android key as Android. No prior catalogue value or device identity comparison changes. This repairs the visible key in public device badges and the existing Android rendering branch. |
| Billing subscription read boundary | [#475 — Keep failed subscription reads out of Billing and checkout](https://github.com/NewWorldVenture/Bubaly/pull/475), head `aec86461`, base #472, ready | Loading, unavailable and successful reads stay distinct for the current authenticated user and household. Checkout, plan changes, cancellation and portal controls require a current successful read; stale realtime/retry callbacks cannot restore another owner's state. A successful empty result retains the existing Free flow. Preserve both independent family-value cards above the subscription conditional. |
| Health dashboard translations | [#476 — Localize Health dashboard and forms in seven languages](https://github.com/NewWorldVenture/Bubaly/pull/476), head `8adb12bb`, ready | Tabs, statistics, insights, dates, counts, units, forms, metadata and failure feedback resolve seven locales. Stored units/goal labels, all six family-scoped queries, all seven CRUD functions and the coaching request remain unchanged. German, French and Portuguese retain formal address. |

The latest user priorities are separate from the 14-finding denominator above:

- [#470 — Keep saved sessions through refresh and storage failures](https://github.com/NewWorldVenture/Bubaly/pull/470), head `fa0ebcd3`, ready and stacked on #455: temporary refresh and storage failures preserve credentials, server reads report unavailability, and explicit native sign-out remains visible and guarded against concurrent saves. The pinned native SDK bootstrap rejection is corrected with verified installation hashes and actual CJS/ESM tests. Validation: 280 affected tests, 13 SDK checks, both typechecks, changed lint, eight i18n gates, generated mobile catalogue and Android/iOS exports pass. Hosted Supabase session limits and physical-device cold starts remain unverified; disposable browser/GoTrue verification is tracked separately in #474.
- [#474 — Verify persistent browser sessions against disposable Supabase](https://github.com/NewWorldVenture/Bubaly/pull/474), head `016f5336`, ready and stacked on #470: four isolated Chromium journeys cover restored persistent cookies, real token refresh twice after accelerated stored expiry, parallel requests sharing an expired session, and explicit sign-out that leaves a second device signed in. Each journey owns a local-only account/household; credentials remain in memory and sensitive artifacts are excluded. Twenty-seven fixture/privacy tests, root TypeScript and scoped lint pass. [CI run 34422722335, E2E job 102701430588](https://github.com/NewWorldVenture/Bubaly/actions/runs/34422722335/job/102701430588) confirms `E2E_DURABLE_SESSION=1`, all 432 browser tests passed with no test skips, and successful disposable-Supabase teardown. The four new journeys therefore executed against actual local GoTrue. The signed JWT is unchanged while stored expiry is accelerated; hosted session policy and physical-device restart remain unverified. This follow-up is not in the `e5d08bd3` or `31313959` rehearsal checkpoints.
- [#472 — Set annual family pricing to $9.99 and $24.99 per month equivalent](https://github.com/NewWorldVenture/Bubaly/pull/472), head `a7eef396`, ready: Basic is $119.88/year and Plus $299.88/year, with monthly subscriptions $12.04/$30.11 from reversing the rounded 17% annual saving. The shared catalogue drives all monetary consumers, actual provider prices are validated before paid mutations, historical webhooks retain entitlement, and the upgrade modal discloses the annual total in seven locales. All four new live Stripe prices and product defaults were created and read back. The previous prices remain active pending deployment; initial complete checks found no subscriptions, active payment links or portal references to them. The available Vercel credential cannot access Bubaly, so deployment, production environment read-back and old-price retirement remain pending. No live payment was created for verification.

**Selected pricing plan and interval continuity — built and published:** source review on unpublished `b3256b42` found that `/pricing` supplied `?plan=basic|plus&billing=monthly|yearly`, but signup, auth-screen switches, wizard completion and calendar OAuth dropped the complete choice. This gap is separate from #472's recurring-price correction. The three linked changes below carry one strict four-value `reviewPlan` navigation hint to an explicit billing review. A single safe invite or deep-link return destination takes precedence; navigation never starts checkout or changes a plan. All required core, mobile, database, E2E and Vercel checks passed at their exact heads; #479/#480/#481 are ready for review.

- [#479 — Preserve selected plan review across authentication](https://github.com/NewWorldVenture/Bubaly/pull/479), head `e54ea9b7`, base #474: strict parsing and email, Google, SMS, screen switches and callback retries retain the valid choice. Role/admin/guest defaults, referral metadata, safe redirects and persistent-session cookies/private headers remain intact. The initial implementation passes 299 tests across 13 suites. Actual Chromium found eight narrow-screen OTP clipping cases; a one-class correction passes 41 affected tests and all 28 focused phone/keyboard/paste/autofill-event/retry browser cases. The preceding 84 locale/plan/width signup-switch/password cases passed. These are controlled auth responses, not live-provider or operating-system autofill acceptance.
- [#480 — Preserve selected plans through onboarding and calendar consent](https://github.com/NewWorldVenture/Bubaly/pull/480), head `5f25c318`, base #467: the validated hint follows owned encrypted calendar continuation and successful Finish while preserving current user/family, provider, CSRF, entitlement, pending activation and expiry checks. It stays outside business payloads, draft/idempotency keys and trial writes. Fresh expected-owner checks precede onboarding writes; retired callbacks remain inactive after an owner or selection changes away and back. All 319 tests across 13 suites pass. Actual controlled Wizard/Chromium passes 84 locale/plan/width cases and four failed/stale/unmounted Finish journeys. Live Google/Microsoft consent remains separate.
- [#481 — Review selected family plans before confirming payment](https://github.com/NewWorldVenture/Bubaly/pull/481), head `db1ce323`, base #475: seven-language review shows canonical tier/interval prices and current family/subscription before explicit parent confirmation. Fresh non-provisioning membership checks precede provider work. Provider-confirmed changes retain an account-owned payment lock until successful readback; query/role changes, stale rows and retry cannot repeat payment. Already-applied provider prices are recognized, and canceled/expired/providerless subscriptions can be explicitly repurchased. The existing plan-change fee policy and proration timing are disclosed. All 360 focused tests across 12 suites pass. Actual controlled Chromium covers an 84-case display matrix, nine earlier journeys and 12 final StrictMode cases; no live payment or production acceptance is claimed.

Independent source review confirms the shared parser is identical across all three branches, original catalogue values are preserved, and both Billing value cards remain above the subscription conditional in the combined rehearsal. Root TypeScript, scoped lint and all eight translation gates pass on the individual changes. Combined full-suite/build evidence is recorded at `bcbe500b` below; individual GitHub checks are separate.

**Remaining auth/onboarding interface translations:** [#482 — Localize adult authentication prompts and signup divider](https://github.com/NewWorldVenture/Bubaly/pull/482), head `22c81020`, base #479, is ready after all required checks passed in [CI run 34466656367](https://github.com/NewWorldVenture/Bubaly/actions/runs/34466656367). It replaces six rendered references with six additive messages per locale. All prior values/order and authentication behavior are preserved; 138 existing tests and 56 actual controlled browser journeys pass. Independent review is clear, with 203 tests across eight overlapping suites passing and all 22 tested source/style/catalogue fingerprints verified. [#484 — Localize onboarding setup and completion copy across seven languages](https://github.com/NewWorldVenture/Bubaly/pull/484), head `184045ac`, base #480, is ready after all required checks passed in [CI run 34467318988](https://github.com/NewWorldVenture/Bubaly/actions/runs/34467318988). Its 85 additive messages per locale preserve all 12,667 prior values/order, stored IDs, business defaults, payloads and action/provider behavior. It fixes reproduced narrow-screen household-counter overlap and a restored-preview informal calendar fallback. All 155 focused tests, 170 overlapping independent tests, TypeScript, lint, eight gates and 105 actual browser stage checks/28 journeys pass.

The separate Billing read-boundary correction #475 at `aec86461` is ready with [CI run 34423816163](https://github.com/NewWorldVenture/Bubaly/actions/runs/34423816163) green. Local evidence includes 130 focused tests across six suites, 34 final subscription/ratchet checks, root TypeScript and lint. An independent React 18/Chromium harness passed 12 lifecycle cases against the actual committed hook with external traffic blocked. Health localization #476 at `8adb12bb` is ready with [CI run 34423609165](https://github.com/NewWorldVenture/Bubaly/actions/runs/34423609165) green; 296 tests across seven suites include 191 Health cases, and TypeScript, scoped lint and all eight translation gates pass. It adds 64 keys per locale while preserving all 12,645 prior values and ordering on its standalone base. These checks do not establish live-provider, physical-device or browser-layout acceptance.

The S-10 defect exists on the verified base in `lib/life-events/launch.ts:307`: `createMove` precedes `planTasks`, and the outer rollback does not yet own that new move when task creation fails. Track this through #435 rather than opening duplicate implementation work.

**Production operator guide:** [#483 — Document the Vercel pricing and persistent-session rollout](https://github.com/NewWorldVenture/Bubaly/pull/483), base #472, now has head `7f13b301`. Its original `38b7e70f` passed all five required checks in [run 34466843878](https://github.com/NewWorldVenture/Bubaly/actions/runs/34466843878). The new documentation-only clarification lists the four events handled by the billing webhook and is draft pending [run 34469479844](https://github.com/NewWorldVenture/Bubaly/actions/runs/34469479844). The guide includes all four price IDs, both HTTPS URL settings, legacy aliases, matching admin/environment Stripe accounts, the Bubaly endpoint signing secret requirement and concrete deployment/read-back/retirement checks. Supabase session settings and native acceptance remain separate; original Stripe history is preserved and no secrets/account mutations are included.

**Selected-plan CI acceptance:** #479 [run 34466367655](https://github.com/NewWorldVenture/Bubaly/actions/runs/34466367655) and #482 [run 34466656367](https://github.com/NewWorldVenture/Bubaly/actions/runs/34466656367) each pass 432 E2E tests with zero skips and `E2E_DURABLE_SESSION=1`, executing the four current disposable-GoTrue journeys. #480 [run 34466394815](https://github.com/NewWorldVenture/Bubaly/actions/runs/34466394815) and #481 [run 34466402060](https://github.com/NewWorldVenture/Bubaly/actions/runs/34466402060) each pass 428 E2E tests with five skips. Those skips are their unchanged older durable-session suite, gated by the absent flag; authenticated and concierge tests are enabled. Their E2E source/configuration is identical to their respective actual bases #467 and #475. These two runs therefore do not establish durable-session acceptance. Supabase Preview is an explicitly skipped optional check; all required checks pass at each recorded head.

**Profile-photo picker:** [#485 — Localize profile photo picker labels and upload feedback](https://github.com/NewWorldVenture/Bubaly/pull/485), head `4e889df0`, base #480, is ready after all required checks passed in [CI run 34467735002](https://github.com/NewWorldVenture/Bubaly/actions/runs/34467735002). Seventeen appended keys per locale translate twelve colors, preset/image labels, upload/busy controls and the component's signed-out error. Original preset/data-URI bytes, form values, callbacks and upload behavior are preserved. One hundred existing tests, lint, all eight gates and sixteen actual React/Chromium scenarios pass; fourteen scenarios span all seven locales at 320/1280 and exercise all twelve presets plus removal/upload/error/retry. There were no browser errors or external requests, and all seventeen source/style/catalogue fingerprints match. Root independently reviewed the source and German/Portuguese narrow-screen screenshots. Shared size/type/database/provider error strings remain a separate follow-up; no live storage/RLS acceptance is claimed.

**Complete Setup:** [#486 — Localize the Complete Setup page and form in seven languages](https://github.com/NewWorldVenture/Bubaly/pull/486), head `efedf82b`, base #484, is ready after all required core/mobile/database/E2E/Vercel checks passed in [run 34468728491](https://github.com/NewWorldVenture/Bubaly/actions/runs/34468728491). E2E records 428 passes and five unchanged older durable-session skips, not new session acceptance. Seventeen additive messages per locale preserve all 12,752 prior values/order, completion/headline precedence, authenticated reads and stored payloads. The reproduced mobile save-button height is corrected. Independent review, 135 relevant tests, TypeScript/lint/eight gates and 56 actual browser states/28 journeys pass. Two Windows path failures were independently reproduced on the unchanged standalone parent; the combined checkpoint includes the portable correction and passes them.

## Built work and pending acceptance

**Review integration:** S-12 and M40 are now built in #438 and #437. S-06 and S-12 both extend approval execution; preserve both the school source-completion hook and the private purchase-result hook when integrating.

**X1 / M40 evidence correction built:** `agent_activity` can duplicate work already counted in a run; completed reminders can be human tick-offs; current notification timestamps include skipped or failed attempts. The old four-source indicator therefore lacked proof of disjoint delivered work. #433 and #437 now use only recorded fully completed plans with durable completion dates. #446 shares the same `completed-plans.ts` and `completed-plans-model.ts` modules. Retain the correction during integration and do not describe this subset as all household work.

**S-01 capture wizard built:** #451 corrects the three review findings (family-switch request races, composer list refresh and document AAL2 policy). Final focused tests: 165, plus an independent 33-test rerun. Full native Windows suite: 9,642 passed and the five known baseline failures. Typecheck, lint and i18n gate passed before publication; CI is separate.

**S-11 mobile assistant built:** corrected #455 head `eaac2139` includes exclusive authenticated user/household-bound turns, cancellation and resource cleanup, request-context assertions and seven-locale assistant copy. Cross-review's transient previous-household rendering issue is corrected with owner-tagged state and eight independently reviewed pre-effect render regressions. The repeated full native Windows run had 9,649 passing tests across 1,004 passing files plus the five baseline failures; final targeted validation had 185 passing tests across 13 suites. Initial CI exposed a transport-test import reaching a native dependency absent from the web job; that test boundary is corrected, and web typecheck plus 14 render/transport tests pass with `mobile/node_modules` absent. Mobile typecheck, changed-file lint and clean iOS/Android exports also passed again. Required GitHub checks passed at this final head, and the PR is ready. Physical-device permission, recording and background testing remain outstanding. No spoken replies or wake-word support is claimed.

**Platform metric evidence built:** #454 replaces capped inputs with complete stable reads and exact counts, with independent metric failure groups. Recorded paid-active notifications establish the supported X10 ordering; current paid status, earlier-only notifications, missing evidence and conflicting subscription statuses are distinct. Notifications can record reactivation and are best-effort records, not proof of first-ever payment or causation. X5 reports recorded signals and decisions without treating them as handled work or inventing a ratio when no decisions exist. At `102eb24a`, 165 targeted tests across nine suites and an independent 78-test review passed; the full native Windows run had 9,587 passes plus the five baseline failures. Typecheck, changed-file lint and i18n checks passed. Required GitHub checks subsequently passed at this head, and the PR is ready; combined integration remains separate.

**Admin referral summary built:** #452 separates complete totals/rankings from the recent 25-row table and uses recorded reward confirmation for unconfirmed credits. Missing fulfillment evidence is not a statement that the payment provider still owes money. The actual-page regressions exercise totals larger than recent rows, absent versus zero conversion rates, configuration/read rejection, retry and an alternate locale; nine new page tests plus the existing read-boundary test passed.

**Marketing translations and render regression:** #448 at `85639a18` expands the scanner, localizes marketing examples/shared public helpers and three Knowledge Center readers, and includes the assistant-prompt and pricing-CTA render corrections plus a source guard. #453 now adds only the complementary seven-locale component regression; its original runtime fix is already integrated. Validation of that fix included 16 existing `/ai` device/overflow cases and all 42 prompt/locale combinations at 320px with zero horizontal overflow. After including `85639a18`, the seven component cases plus two source-guard cases passed. These bounded checks do not complete the application-wide literal-copy backlog.

**S-01 explicit linked-document import built:** #458 at `db85f67f` is stacked on #451 at `13d23e32`. User-selected PDF, supported images and UTF-8 plain text use public-address-pinned HTTPS transport with bounded redirects, bytes, headers and total time. Same-family inbox source reads use caller permissions. Document MFA guards the page and API; user/family context is rechecked before filing and saved-text enrichment. Saved-text receipts prevent repeated retrieval/OCR, preserve existing edits and adopt the first concurrent save. Independent review corrected screen restart, owner-change and stale Reset callback behavior. Validation: 285 focused tests across 13 suites, including 18 independently rerun UI tests; typecheck, lint and i18n passed. The branch's full native run had 9,798 passes and five historical Windows failures; #457 fixes those separately. Automatic inbound URL visits and general HTML/login-page extraction remain deferred. Final-head CI and deployed transport/provider verification are separate.

**Local integration rehearsal:** unpublished local commit `4b845ba9` includes main `a8196c80`, integration `85639a18`, the earlier combined implementation work, #451, and the initial #452/#453 revisions. Full unit run: **10,097 tests passed plus the same five Windows baseline failures**. The production build and generated Next route typecheck passed. The browser suite completed with **425 passed and eight authenticated cases skipped**. This evidence applies to that concrete combined commit, not later refreshed PR heads, #454 or #455. The earlier `2830448c` rehearsal's manual conflict resolution preserved both private-purchase and school-source approval hooks, both Billing cards and the corrected shared metric wording, with an independent 116-test check. Its widened scanner reported 2,796 findings in 600 files versus 2,801 in 603 on unchanged `5b4249d1`; the original 2,345 figure used the older scanner. No shared branch was pushed or merged by these rehearsals.

The completed browser run used the already-built `4b845ba9` app with two workers and an **unavailable-backend fixture**: a temporary process-local fetch response for only `example.supabase.co` returned JSON 503 with `Retry-After: 0`. Repository assertions, timeouts and application code were unchanged. Earlier interrupted network-retry and eight-worker timeout attempts are retained as logs. The eight skipped cases were authenticated desktop/iPhone first-value journeys, concierge, and five durable-session checks. This run does not verify a real Supabase connection, authenticated workflows or production RLS.

**Later rehearsal checkpoints:** local `c4544caf` additionally includes main `57dd3031`, the integration owner's [#456](https://github.com/NewWorldVenture/Bubaly/pull/456) head `4ab7c924` with six admin-banner regressions, and #454. A 115-test run across seven suites passed for credential guidance/locales, strategy metric evidence and referral summaries. The newer local `ad429337` also includes corrected #455, with only independent catalogue additions resolved during that merge. Its full suite had **10,265 passing tests and the five known Windows failures**; production build and generated-route typecheck passed. Lint passed with four warnings, and all eight declared i18n gates plus the mobile catalogue parity check passed. Its fresh browser run had 423 passes and two initial navigation timeouts; both timed-out cases passed on an unchanged rerun. Eight authenticated cases remained skipped. The same explicit unavailable-backend fixture and standard two workers were used. These are the checkpoint's own checks, not inherited results from `4b845ba9`.

**Windows audit correction built:** #457 fixes the five separator-related failures and reuses #449's metadata enumeration fix for a main-based checkout. Its complete local suite passed **9,544 tests across 1,002 suites with zero failures**; 30 affected tests, typecheck, changed-file lint and independent review passed. The query-audit CLI resolved 484 tables, 77 functions and 133 routes on that branch. Local rehearsal `e461f226` adds #457, main `593867d5` and the ledger update to `ad429337`; only documentation and audit/test files differ, so the application build is unchanged. Its query audit resolves 484 tables, 77 functions and 134 routes. Its combined full suite passed **all 10,270 tests across 1,057 suites, with zero failures**.

**Contacts localization built:** #459 at `ec3df8f0` covers the Family Contacts page/module with seven-locale copy, locale month labels and UTC birthday rendering. Independent review confirmed visible/accessible labels and preservation of stored category/month/day values. Validation: 30 new rendered/form tests, 105 focused tests across seven suites, typecheck, lint, eight i18n gates and zero findings in the scoped Contacts scan. The full main-based native run had 9,573 passes and six historical Windows failures, including the Unix metadata enumeration; #457 fixes all six. The obsolete English source assertion was replaced by actual call/email label checks containing the contact name in every locale; all Photos tests remain unchanged. The separate contact timeline remains outside this batch.

**Linked-document and Contacts rehearsal:** local unpublished `eafd1a1d` adds #458 and #459 to `e461f226`. Its full suite passed **all 10,455 tests across 1,063 suites with zero failures**. All eight declared i18n gates pass; lint passes with the same four warnings. The query audit resolves 484 tables, 77 functions and 135 API routes. The full translation scan reports 2,769 findings in 597 files; this remains an application-wide backlog. Independent review found no new capture-ownership, receipt-identity, localization or stored-contact compatibility conflict. Production build and generated-route typecheck passed. Its fresh public browser run passed 425 cases, with eight authenticated cases skipped and no failures. It used the same explicit unavailable-backend fixture and two workers described above; it does not verify real sessions, database policies or provider delivery.

## Verified findings and implementation acceptance

**Public aggregate correction built:** #462 passed 307 focused tests across seven suites, native typecheck, changed-file lint and all eight i18n gates. Its main-based full suite had 9,637 passes and the exact six historical Windows audit failures fixed separately by #457. Production build and generated-route typecheck passed. An independent review cleared the counting semantics, seven-locale copy and failure boundaries. A populated browser review passed 84 route/viewport cases across seven locales and widths of 320, 390, 768 and 1,280 pixels, with no overflow. This used process-local fixture responses for the two anonymous aggregate RPCs and an unavailable dummy backend otherwise. The initial run encountered a cached zero result from the build; that single local cache entry was backed up for the fixture run and restored afterward. These fixture counts are not live database totals.

**Timeline and Photos batches built:** #460 passed 44 targeted tests, native typecheck, lint and eight i18n gates; its full native run had 9,580 passes plus six historical Windows failures. #461 passed 88 relevant tests across seven suites, with an earlier 115-test catalogue-inclusive run, native typecheck, lint and all gates; its full run had 9,597 passes plus the same six historical failures. Independent review cleared both batches. #463 then passed 164 focused tests, native typecheck, lint and all gates; its full run had 9,624 passes plus those six failures. Its actual read/retry boundary received an independent 89-test rerun. No new browser/device result is claimed for these isolated branches.

**Next rehearsal checkpoint:** local unpublished `be516b41` combines #460–#463 and the latest published ledger with `eafd1a1d`. All merges were clean, including #449's localized timeline metadata and the two independent Photos/Contacts source-test changes. The 408-test combined localization/public-counter check passed before #463; the 89-test contact read/retry suite then passed with #463. Full-suite, build and browser results from `eafd1a1d` are not assigned to this newer runtime tree. The subsequent Autopilot correction and its newer combined checkpoint are recorded below.

**Autopilot correction built:** #464 passed 148 targeted tests across eight suites, including 78 new runtime cases; independent reviews reran 28 UI cases and 50 authorization/service/editor cases. Native typecheck, lint, all eight i18n gates and catalogue checks passed. Its final main-based full run had 9,620 passes and the exact six historical Windows audit failures fixed separately by #457. One concurrent TypeScript process exited with a Windows access violation without source diagnostics; the subsequent isolated compiler run passed. The review corrected feature-setting fallback authorization, active-family changes, stale proposal retries and a competing-receipt revision race. The reserved source fingerprint is hidden from reminder display/filter/editor tags and survives ordinary edits. No scan-engine or fully completed-plan metric behavior changes are included.

**Autopilot combined checkpoint:** unpublished `ac5dcce5` adds #464 to `be516b41`. The seven catalogue conflicts were reconciled by strict per-key three-way comparison; no competing different key values or non-catalogue conflicts remained. Its full suite passes **all 10,757 tests across 1,070 suites with zero failures**. All eight i18n gates and the query audit (484 tables, 77 functions, 135 API routes) pass. Lint passes with the same four warnings; the full translation scan reports 2,748 findings in 594 files. Production build and generated-route typecheck passed. Independent integration review cleared metric definitions, source receipts, public-counter methodology and reminder tag protection. Its fresh browser run passed 424 cases, skipped eight authenticated cases and had one Pixel navigation assertion exceed its unchanged five-second timeout. The same case then passed all three unchanged diagnostic repetitions. Both logs and the original failure context are retained; this is not represented as a clean initial browser run. The same explicit unavailable-backend fixture and two workers were used, with no live-session, database-policy or provider-delivery claim.

**Files localization built:** #465 at `cc470430` covers the Files hub, overview labels, upload feedback and Cloud/Vault/Shared metadata in seven locales. Stored category/folder identities, upload refusal order and all three page access gates remain intact. Validation: 156 focused tests across five suites, an independent 87-test rerun, native typecheck, changed-file lint, eight i18n gates, zero scoped scan findings and a clean diff. Its main-based full run had 9,607 passes and the same six historical Windows audit failures fixed by #457. The pre-existing filter defect that merged uncategorized rows into a literal General group is corrected separately in #468.

**Calendar Sync access correction built:** #467 adds strict relevant configuration before family creation, then actual family entitlement at the private provider/import boundaries. The verified server scope avoids subscription omissions from caller RLS. Validation: 186 tests across nine affected suites, typecheck, lint, eight i18n gates and diff check pass. Independent production/runtime review passed 113 helper/runtime/service tests. Its #450-based full run had 9,715 passes and the exact five historical Windows path failures fixed by #457. The normal Basic trial, signed ownership receipts, dormant preview accounts and import-only materialization remain intact. No live provider connection is claimed.

**Integration owner's AEO data seed:** [#466 — Seed the AEO Knowledge Center translations (0279)](https://github.com/NewWorldVenture/Bubaly/pull/466) merged at `55720ef6` from reviewed head `22643ce7`. All earlier findings are resolved: 360 conflict updates preserve human/reviewed rows, Portuguese register is formal, the French timing claim is corrected and all nine Dutch fragments are declarative. The full-replay description now matches the 60 published parents in 0229. Thirty related tests across six suites passed in an isolated review checkout. The later [Portuguese plural-agreement finding](https://github.com/NewWorldVenture/Bubaly/pull/466#issuecomment-5610479860) at migration 0279 line 741 is corrected across six locales in merged #477; #478 adds the related German punctuation correction described below. Production application remains owner-controlled and blocked by the verified migration ledger. No SQL was edited or applied by this work; these migrations were outside the rehearsal at that review checkpoint and are later included as exact owner files in `c7016237`. Deployed row counts remain unverified.

**Files category and Android follow-ups built:** #468 passed 167 focused tests across five suites, native typecheck, lint, eight gates, scoped scan and diff check; independent review reran 83 UI/overview tests. No new full suite or production build is claimed for that small follow-up. #469 passed 140 existing catalogue/render-contract/marketing tests across four suites and all gates, with independent confirmation that exactly one key was added per locale and no prior values/order changed. No runtime device code was edited.

**Owner Knowledge Center follow-up:** [#471 — The Knowledge Center follows the same locale chain as the page around it](https://github.com/NewWorldVenture/Bubaly/pull/471), reviewed head `34d032f7`, merged as `16d09340` after passing core, mobile, database, E2E and Vercel checks. It resolves the empty-translation finding: rendering and coverage ignore blank question/answer pairs before nearest-locale selection. An independent rerun passes 48 related tests across five suites. Executing the actual verifier against an intercepted offline fixture fails six blank locale rows and passes the same rows with usable content. [Review follow-up](https://github.com/NewWorldVenture/Bubaly/pull/471#issuecomment-5610866469) records the result.

**Owner grammar corrections merged and reviewed:** [#477 — Knowledge Center answers that agree in number (0280)](https://github.com/NewWorldVenture/Bubaly/pull/477), head `b36fb5d3`, merged as `e081baaf`; [#478 — Close the German clauses the frames run into (0281)](https://github.com/NewWorldVenture/Bubaly/pull/478), head `11aa925a`, merged as `d2db14d0`. Independent review at detached main `d2db14d0` confirms both PR trees match their merges. Pure text comparison against 0279 followed by 0280 identifies exactly six Reminders agreement changes and one closing comma in each of ten German answers, preserving every other question/answer value across 360 distinct pairs. All 16 update statements require `source='machine'` and null `reviewed_at`, so human and approved rows remain excluded; 0281 preserves 0280's plural correction on the overlapping German row. Fifteen static tests across two suites and the migration filename audit pass, with next version 0282. PR and main CI include successful scratch database replay. The author's separate live replay/approval fixtures and handwritten generator changes were not independently executed or inspected. Corrected content converges in order; repeated updates still change `updated_at`, so the evidence does not imply byte-identical timestamps.

**Production migration status verified from release jobs:** [#477 release run 34424994019](https://github.com/NewWorldVenture/Bubaly/actions/runs/34424994019) and [#478 release run 34426223682](https://github.com/NewWorldVenture/Bubaly/actions/runs/34426223682) both fail at **Verify remote migration ledger**. **Apply ordered migrations** and all subsequent production schema/coverage checks are skipped. The latest metadata-only job output records versions `0001`, `0002`, `0003`, 440 tables, 958 policies and `requiresBaselineReview: true`; its diagnostic identifies the unrecorded 0004 baseline and the owner procedure in LB-016 §4. This independently corroborates the release failure, not current live translated content or successful production application. No database commands or credentials were used by this review. The subsequent `c7016237` rehearsal includes the exact owner source files for local validation; it does not apply the migrations.

**Files and Calendar combined checkpoint:** unpublished `f34f42c3` adds #465, #467 and #468 plus ledger `ed80f02c` to `ac5dcce5`. All merges were clean. Its full suite passed **all 10,910 tests across 1,072 suites with zero failures**. Independent integration review confirmed the final Files and Calendar code matches the reviewed branches; upload, selected-link capture, original metadata gates and SQL are unchanged.

**Label checkpoint:** unpublished `1f5ff523` adds only the seven #469 catalogue entries to `f34f42c3`. An 89-test independent check across four existing catalogue/render-contract/public-route suites passes. All eight i18n gates, query audit (484 tables, 77 functions, 135 API routes) and lint pass; the same four warnings remain. The full translation scan reports 2,738 findings in 590 files. Production build and generated-route typecheck passed. A fresh targeted Chromium check passed 21 homepage viewport cases across seven locales and widths 320, 390 and 1,280, plus 14 responsive navigation journeys. Android is visible, its raw catalogue key is absent, and no horizontal overflow occurs. It uses the same unavailable-backend fixture and does not establish authenticated, provider or physical-device acceptance. The full-suite result above is from the immediately preceding runtime tree; no second full run is claimed for this small catalogue addition, and the earlier complete browser matrix is not assigned to this checkpoint.

**Pricing and login validation checkpoint:** pricing head `a7eef396` contains the same production code as build-verified `d8bc5c12`, plus two test-only corrections for an obsolete MRR expectation, the integrated family-value server boundary and typed locale fixtures. Generated-route TypeScript and 24 affected tests pass at the final pricing head. The built pricing page passed 42 browser cases across seven locales, three widths (320/390/1280) and both billing periods, including exact annual totals, monthly equivalents, discount badges and no overflow. External fetches were blocked and no live payment was exercised. The unpublished rehearsal at `e5d08bd3` now combines #470, #472 and #446's price-aware test follow-up; the first combined run exposed the corrected test issues, and its corrected full suite now passes all **11,181 tests across 1,081 suites with zero failures**. Production build (243 generated pages), generated-route TypeScript, eight i18n gates, generated mobile catalogue and query audit pass. The earlier `1f5ff523` results do not establish those newer checks. Production deployment is pending owner integration and Vercel access; no shared branch or main was pushed by the rehearsal.

**Capture localization checkpoint:** [#473 — Localize Capture shortcuts and save feedback in seven languages](https://github.com/NewWorldVenture/Bubaly/pull/473), head `14905de3`, is ready after core, mobile, database, E2E and Vercel checks passed. It adds translated labels/hints for all 40 shortcuts, picker/count/empty/accessibility/save feedback and request-localized page metadata. Forty-seven messages are added per primary locale; IDs, icons, destinations, limits, stored preferences, caller headings and authorization are preserved. Rejected save requests now show the same translated failure as resolved errors. Its 58 focused Capture tests, 55 catalogue tests, root TypeScript, scoped lint, eight gates and zero scoped scan findings pass.

The unpublished Capture merge at `31313959` adds only #473 to `e5d08bd3`. It passes 355 tests across 19 relevant suites, generated-root TypeScript, eight gates, query audit, scoped lint and diff checks. All 13,046 prior keys/values per primary locale are preserved, with 47 added. Document MFA, upload/link services, CaptureShell and shared-text/preferences helpers retain their exact prior blobs; page authorization and first-paint share intake are preserved. A fresh whole-app scan reports **2,659 findings in 588 files**, down 79 findings and two files from the previous scan. The earlier full-suite/build result belongs to `e5d08bd3`; it was not repeated for this bounded copy follow-up. No rehearsal branch was pushed.

**Durable-browser rehearsal checkpoint:** unpublished `b3256b42` adds only the #474 test/CI/runbook follow-up at `016f5336` to `31313959` through a clean merge. Its 27 fixture and artifact tests across two suites pass. No new production files were introduced and no rehearsal was pushed. The subsequent final-head #474 CI run passes all 432 browser tests, including the four actual GoTrue journeys, as recorded above; that run belongs to the PR, not this combined rehearsal. The full 11,181-test/build/generated-route evidence stays attached to `e5d08bd3`, and the 355-test Capture validation and 2,659-findings scan stay attached to `31313959`; neither was rerun or reassigned to this test-only checkpoint.

**Billing and Health rehearsal checkpoint:** unpublished `0e83e938` incorporates #475's subscription-read boundary, the integration test boundary for the two family-value cards, and #476's Health translations. Its full run passes **11,466 tests and fails one obsolete Billing audit-baseline assertion**. The later `938596c9` removes that stale baseline entry and passes all three affected ratchet tests; the full suite was not rerun at this correction. Production build at `938596c9` generates all 243 pages, and generated-route TypeScript, lint, all eight translation gates and the query audit pass. The full translation scan reports **2,623 findings in 587 files**, down 36 findings and one file from the Capture checkpoint. This historical build preceded the selected-plan continuity checkpoint below; its full-scan result remains attached to this exact source.

**Auth continuity rehearsal checkpoint:** unpublished `f0d82b0b` cherry-picks the reviewed local auth change `8fee9120` onto `938596c9`. The merge preserves all 13,157 prior catalogue values and adds two keys per locale. This historical checkpoint preceded onboarding/calendar, Billing review and the OTP sizing correction; its evidence is not reassigned to those later changes.

**Combined selected-plan continuity checkpoint:** unpublished `bcbe500bc61ebf8b69e634d0f42b506acec86403` incorporates the final #479/#480/#481 sources, including the narrow-screen OTP correction and stale-owner/readback fixes. All 13,157 original catalogue values/order remain, with 28 additive messages per locale (13,185 total). Independent review confirms that the two existing Billing value cards and session guards are retained. The full suite passes **11,794 tests across 1,093 suites** with four workers. The initial uncapped run had 14 failures beginning with five-second imports in seven AI suites and followed by shared-state failures. Those unchanged suites pass all 79 tests on a targeted run, then the complete four-worker run passes without changing source, assertions or timeouts. This supports resource contention as the cause; it is not a CPU-profile proof. The production build generates all 243 pages; generated-route TypeScript, changed-file lint, all eight translation gates, query audit (484 tables, 77 functions, 135 API routes) and diff checks pass. No SQL, shared navigation, provider credentials or live payments were used in this validation, and the rehearsal remains unpublished. The fresh full translation scan at this exact head reports **2,621 findings in 585 files**, down two findings and two files from `938596c9`. Owner Knowledge Center merges and the later copy follow-ups are outside this checkpoint.

The evidence below records the historical findings on `7789a3ea`, or the later integration review where specified. Most now have implementing PRs above. Preserve the linked acceptance checks during review instead of opening duplicate work; the whole-application translation backlog and external/owner-controlled verification remain open.

**Auth copy and rollout-guide checkpoints:** unpublished `a69d6732` adds only #482 to `bcbe500b`, preserving all 13,185 prior catalogue values/order and adding six messages per locale (13,191 total). Its 88 affected auth/catalogue tests across four suites, scoped lint, all eight translation gates and diff checks pass. The subsequent `c519f074` adds only the #483 runbook appendix. The preceding full suite/build and 2,621-finding scan are not repeated or reassigned to these copy/documentation changes. No rehearsal branch was pushed.

**Wizard and picker copy checkpoints:** unpublished `73bbcaf7` adds #484 to `c519f074`. Independent review verifies exact approved render/test/docs blobs and all 13,185 pre-copy values/order plus the six auth and 85 wizard messages per locale (13,276 total), without regional-overlay or placeholder collisions. The 193 relevant tests across ten suites, generated-route TypeScript, scoped lint, all eight translation gates and diff checks pass; its fresh full scan is **2,619 findings in 584 files**. Later unpublished `a2388a7b` adds only #485, preserves those 13,276 values/order and appends seventeen messages (13,293 total). Its 100 relevant tests across six suites, lint, eight gates and diff checks pass; the fresh scan is **2,608 findings in 583 files**. Full-suite/build evidence remains at `bcbe500b`, and generated-route TypeScript remains at `73bbcaf7`; no repeated full suite/build or provider/device acceptance is assigned to these bounded copy additions.

**Current-main combined checkpoint:** unpublished `c701623766025f53bbbd5db59672eef978c3f3ac` cleanly merges owner main `d2db14d0` into `a2388a7b`. Independent review confirms all ten changed files exactly match owner blobs and all other 4,015 tracked first-parent files retain their blobs/modes, including login, billing, provider guards and catalogues. All **11,827 tests across 1,096 suites pass** with four workers. The production build generates all 243 pages, and generated-route TypeScript, changed-file lint, eight translation gates, query audit (484 tables, 77 functions, 135 API routes) and migration filename audit (297 files; next version 0282) pass. Build configuration used only process-local dummy backend values and no live provider credentials. The three owner migration files are byte-identical to main; none was authored, edited or applied. Non-SQL diff checks pass; the full merge diff retains the owner's existing `0280:82` blank-line-at-EOF warning without altering SQL. This proves local source/build behavior, not successful production migration application or hosted data.

**CompleteSetup combined checkpoint:** unpublished `465d62bac5e662fe2c955ac77f91814149659adb` adds only #486 to `c7016237`. Independent review verifies the exact approved form/page/test blobs, all 13,293 prior catalogue values/order plus seventeen additions per locale (13,310 total), and all other 4,016 tracked files unchanged. All 175 relevant tests across eight suites pass, including the server-boundary tests that fail on the older standalone base. Generated-route TypeScript, scoped lint, eight translation gates and diff checks pass. The fresh full scan reports **2,607 findings in 582 files**. The full suite and build remain attached to the immediately preceding current-main checkpoint; no repeat full suite/build or hosted acceptance is claimed for this copy change. All rehearsals remain unpublished. The subsequent generated-brief and timezone changes are recorded separately below; they are not included in these historical checkpoints.

| Priority / item | Evidence when found | Implementation and acceptance to retain |
| --- | --- | --- |
| P1 / S-06 explicit proposal approval | `app/(app)/dashboard/school/actions.ts:120` gates a proposal; the `allow` branch at line 141 immediately calls `runAction`. S-06 build requirement 3 explicitly says proposals are approval-gated and never auto-executed. | Built in #436. Retain the reviewable approval even when a routine domain policy allows immediate execution. Under an allow policy, no domain write occurs until a parent approves; denial remains a denial. |
| P1 / S-06 handled state after approval | The same action's comment at line 24 documents that later approval executes elsewhere and never stamps the source inbox message `ai_handled`. The direct allow branch stamps it, so the two paths disagree. | Built in #436. Retain the validated same-household source reference and successful-execution requirement. Failed source bookkeeping must be repairable without duplicate domain work. Preserve the separate #438 private-result hook during integration. |
| P2 / S-01 inbound attachment triage | `app/api/contact-center/email/route.ts:61` maps multipart `File` values to `''`. Only the text fields are forwarded, so an email whose form/bill/receipt/reservation is in an attachment loses that content before triage. S-01 build requirement 3 explicitly includes inbound attachments. | Built in #444. Retain bounded supported content, independent attachment retries, original saved rows and explicit partial/failure states. Unsupported files, extraction failure and failed enrichment must not be acknowledged as fully processed. Deployed provider delivery remains separate. |
| P2 / whole-app translations | `scripts/i18n-scan.mjs:46` explicitly excludes the `everything` surface because broader scanning exposed untranslated copy. The audit reproduced 2,345 findings in 552 files on unchanged `7789a3ea`; #431 completed catalogue entries, not the whole application's literal-copy backlog. | Save/triage the full scan output by surface, translate in bounded batches across all seven locales, and rerun the full scan. Do not use a clean catalogue or declared surface gate as proof that the whole app is translated. Re-enable the whole-app gate when its findings are resolved. |
| P2 / W5 outcome discovery | `lib/outcomes/launcher.ts` has `OUTCOMES` and `buildOutcomePlan`, but no `outcomesForRoute`; no `OutcomesStrip` or `RelatedOutcomes` implementation was found. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:504`. | Built in #441. Retain complete-input relevance ranking, actual outcome launch wiring and route chips. The personal navigation preset remains an explicit user opt-in with scoped conflict-safe persistence; shared sidebar files remain unchanged. |
| P2 / W8 connected onboarding and canonical entities | `app/onboarding/actions.ts:107` accepts `paste` or `demo`; the configured OAuth providers are not part of this onboarding step. `lib/graph/resolve.ts` does not exist. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:532`. | Resolver built in #442; connected onboarding built in #450. Preserve exact confirmed matching, ambiguity, complete private reads and retryable enrichment. Calendar continuation remains owned, dormant until Finish and import-only afterward. Test actual configured provider consent/refresh in a deployed environment before claiming that connection verified. |
| P2 / W7 published case-study detail | Published testimonials are wired, but `app/(marketing)/customers/[slug]/page.tsx` is absent. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:522`. | Built in #439. Retain published-only detail access, signed-out metadata, escaping and withdrawal invalidation. Real claims and permission to publish remain content-owner evidence; do not fabricate examples to populate the route. |
| P2 / M40 own-family delivered value | `components/app/upgrade-modal.tsx` and `components/modules/billing-module.tsx` show plan/checkout and static outcome copy, but receive no `loadTimeSaved` or handled-value input. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:458`. | Built in #437 on #433's corrected shared recorded-plan contract. Retain scoped loading, modal-open loading, session/family race guards and zero/unavailable/undated states. The explicit per-plan assumption models planning time; it does not measure delivered work or saved time. |
| P2 / X2 automatic capture share | No `lib/metric/auto-captured.ts` or capture-ratio surface exists. Existing provenance includes feed IDs, AI tool resource references, and autopilot source/status. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:551`. | Built in #447. Retain the current seven-day supported-record denominator, deduplicated receipt/feed numerator and explicit unknown provenance. Imported capture is separate from assistant-created work. Weekly snapshots and optional explicit `created_via` fields remain owner asks. |
| P2 / X11 estimated value versus price | No `lib/metric/value.ts` or paid-family value/price surface exists; time estimates and plan prices are separate inputs. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:637`. | Built in #446 using the same recorded-plan query/model as #433. Retain editable hourly value, explicit modeled minutes, matching seven-day list-price allocation and paid-plan/zero/unavailable handling. Neither a suggestion's `monthlyCents` nor the modeled comparison is realized cash savings. |
| P2 / M30 onboarding under 30 minutes | `lib/onboarding/ttv-audit.ts` and `lib/analytics/activation.ts` do not define `under30MinRate`. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:370`. | Built in #443. Retain separate setup-completion and first-outcome-view denominators, the inclusive 1,800-second threshold, complete cohorts and missing/invalid duration coverage. A viewed first outcome is not completed work. |
| P2 / platform metric completeness (later integration review) | `lib/metric/strategy-server.ts` caps eight row sources at 2,000, while the families denominator uses an exact count. `Promise.all` also propagates a transport rejection across unrelated metric tiles. | Built in #454. Retain stable complete reads/fixed cutoffs and exact counts, including below-requested server page limits. A query construction failure, rejected request or late-page error makes only dependent metrics unavailable; no successful truncated aggregate is accepted. |
| P2 / conversion-after-value evidence (later integration review) | `lib/metric/strategy-server.ts` falls back to `subscriptions.updated_at`; `lib/billing/conversion.ts` counts that as a conversion and only marks the median approximate. A paid-before-value family can renew afterward and then be incorrectly included. | Built in #454. Require supported recorded paid-active notifications for ordering and expose prior-only, missing and conflicting-current-status evidence. Reactivation and notification recording time are explicit. Do not infer first-ever payment, causal conversion or verified payment time. |
| P2 / admin referral summary (later integration review) | `/admin/marketing/referrals` derives totals, conversion rate, credits owed and top referrers from the latest 100 rows. `rewarded` rows remain included in credits owed. | Built in #452. Keep bounded recent activity separate from complete totals/rankings and use canonical recorded reward settlement. Unconfirmed credits exclude rewarded/settled rows but do not establish outstanding provider liability. Retain unavailable, absent denominator and actual zero as distinct states. |

The first audit of S-01 through S-10 found implemented baseline behavior for S-02, S-03, S-04, S-05, S-07, and S-09. That is a bounded source observation, not full browser/device acceptance or a claim that every related roadmap item is complete. The public display page is `/family-display`; the authenticated kiosk remains `/display`.

Broader X1/M40 work measurement remains an evidence task: prove non-overlapping durable completion records and appropriate timestamps before adding standalone tool executions, autopilot activity or reminders to the recorded-plan subset. Excluding those sources does not establish that they did zero work. A status label or activity row alone does not prove successful execution, disjoint work or measured time saved. Audit existing evidence before deciding whether any new owner-controlled storage is necessary.

**Generated onboarding brief display:** [#487 — Localize generated onboarding briefs while preserving canonical data](https://github.com/NewWorldVenture/Bubaly/pull/487), head `8ac56c22`, base #484, is ready after all five required checks passed in [run 34469891652](https://github.com/NewWorldVenture/Bubaly/actions/runs/34469891652). Actual E2E job 102847076775 logs show 428 passes and five inherited durable-session skips; authenticated and concierge coverage remain enabled. Optional semantic descriptors and a strict pure view formatter translate Wizard Value/Done in all seven locales. Canonical English, IDs, counts, ordering, minutes, raw imported data, dinners and briefSummary bytes remain unchanged. Invalid or legacy metadata falls back per affected item; the view is never persisted. Twenty-six keys per locale preserve all 12,752 existing values/order. Independent review caught a missing locale test mock and a backward-looking DST overlap range; both are corrected, with offsets shown only across clock changes. The original 613-check scope and final 65-test run overlap; typecheck, lint/eight gates and 19 actual controlled browser scenarios pass. All 24 source fingerprints and 45 baseline engine/summary comparisons match, with no browser errors or external requests. The German 320px DST range and action were visually checked.

**Family timezone callers:** [#488 — Use the family timezone consistently in briefing and onboarding previews](https://github.com/NewWorldVenture/Bubaly/pull/488), head `2aef85a5`, base #484, is ready after all five required checks passed in [run 34469973341](https://github.com/NewWorldVenture/Bubaly/actions/runs/34469973341). Actual E2E job 102847339083 logs show 428 passes and the same five inherited durable-session skips. Briefing and Home now forward their existing family timezone. Paste/demo preview accepts the wizard timezone, retains UTC only when omitted and rejects malformed supplied values before auth/calendar reads. The new error uses structured validation paths and one additive message in each locale. Seven wrapper/Home and sixteen preview/control failures were reproduced before; subsequent runs pass 137/294/345 overlapping tests. The final error translation passes 147 tests, including seven actual-action locale responses and error precedence; final standalone typecheck/lint/eight gates pass. Engine, imported start instants, stored identities, provider boundaries and snapshot quarantine remain unchanged. Family-local day selection and resulting counts are intentionally corrected.

**Earlier combined briefing checkpoint:** unpublished `6dedb1684e44d5af5a336967a1806ac0482c5691` adds #487, the first two #488 commits and the #483 webhook-event clarification to `465d62ba`. All **11,918 tests across 1,100 suites pass** with four workers. The final `13c8358e27a12b6e8404c9df151650305e9a2322` adds only the localized timezone error and passes all **221 targeted tests across six suites**. At that final source the production build generates **243 pages**, generated-route TypeScript, scoped lint, eight translation gates and diff checks pass. All 13,310 prior catalogue values/order remain plus 27 additions per locale (13,337 total). The app/components scan remains **2,607 findings in 582 files**; generated library text localization is not measured by that scanner. Original owner SQL, quarantined store and shared navigation remain unchanged; no migrations, provider payments or production deployment occurred. The full-suite result remains attached to 6dedb168, not reassigned to the later error correction.

**Active work after user steering:** the user asked to move past Stripe/pricing and keep building the next features. Calendar correctness and Home briefing presentation are the active work; deployment/configuration status remains recorded above without further pricing work. [#489 — Keep onboarding sample calendars on the family local day](https://github.com/NewWorldVenture/Bubaly/pull/489), head `c8b18f36`, base #488, is ready after all five required checks passed in [run 34470421974](https://github.com/NewWorldVenture/Bubaly/actions/runs/34470421974). Actual E2E job 102848771891 has 428 passes and five unchanged durable-session skips. It fixes UTC-anchored sample dates/hours using the existing zoned-time resolver while preserving sample content/order/durations and the omitted-timezone UTC default. Ten failures were reproduced before; 160 focused tests, standalone TypeScript/lint/eight gates pass, and independent review passes 106 overlapping tests. Pasted ICS parsing is unchanged. The unpublished rehearsal `43abd6f4333480c8cf2d22baa6967e79d7dd7175` adds exactly its four files and passes 220 tests across nine suites, generated TypeScript, scoped lint and diff checks. Full-suite/build evidence remains at the preceding exact checkpoints.

**Point-event overlap correction:** [#490 — Keep point calendar entries out of briefing overlap conflicts](https://github.com/NewWorldVenture/Bubaly/pull/490), head `df0c0aac`, base #489, is ready after all five required checks passed in [run 34471283855](https://github.com/NewWorldVenture/Bubaly/actions/runs/34471283855). E2E job 102851513573 has 428 passes and the same five gated durable-session skips. Explicit equal start/end timestamps remain visible without being expanded into a fabricated one-hour conflict; overlap requires two positive intervals. Missing/invalid ends retain the previous engine fallback. Four failures and two passes were reproduced before, then 182 tests across nine suites, TypeScript, lint and independent review pass. The unpublished `7bedd002` rehearsal passes 217 overlapping tests across ten suites. Imported ICS time parsing remains separate.

**Home outcome translations:** [#491 — Localize Home briefing outcomes across seven languages](https://github.com/NewWorldVenture/Bubaly/pull/491), head `574ed322`, base #488, is published in draft; [run 34471812790](https://github.com/NewWorldVenture/Bubaly/actions/runs/34471812790) is pending. The extracted card translates validated generated facts, locale numbers/progress and setup steps while preserving canonical text, summary bytes, IDs/hrefs/order/completion/readiness, raw meal content, queries and the existing visibility gate. Clear-state copy uses a neutral calendar summary. Twenty-five keys are additive per locale; legacy/inconsistent metadata falls back per item. All 178 author tests across twelve suites, standalone types/lint/eight gates and 84 actual controlled Chromium states (seven locales, 320/1280px, six states) pass. Independent review checks all thirteen browser source fingerprints, unchanged canonical outputs and all 66 dashboard business statements; narrow DE/FR/PT text is readable. Two standalone Windows boundary assertions reproduce on the exact parent and are fixed by #457 in the combined rehearsal, where all affected suites pass. The controlled browser harness is not a live Next/backend/provider journey.

**All-day briefing readers:** [#492 — Keep all-day briefing events on the right date and expose failed reads](https://github.com/NewWorldVenture/Bubaly/pull/492), head `4b3c3751`, base #488, is published in draft; [run 34471857580](https://github.com/NewWorldVenture/Bubaly/actions/runs/34471857580) is pending. API and morning delivery select/map all_day, apply separate timed-local/all-day-UTC date predicates before existing limits, use calendar offsets across DST midnight gaps/folds/skipped dates, preserve original event timestamps and translate all-day labels. API conflicts exclude all-day/point entries. Returned/rejected required calendar reads yield translated 503 before optional model work; successful empty rows remain valid. The 17-failure/one-pass initial reader reproduction, four false-success read cases and four midnight-gap boundary failures now pass. All 529 author tests across seventeen suites and 324 overlapping independent tests across eight suites pass, plus types/lint/eight gates. Family/member boundaries, limits, delivery recipients/deduplication and snapshot quarantine are retained. Start-date-only selection remains; no recurrence or multi-day expansion is added. A single interval does not exactly represent historical noncontiguous local dates such as St. John's 1988 rollback; this helper serves current/future readers.

**Latest Home/calendar combined checkpoint:** unpublished `474379bcaf3948a220167c313d68b7080dd105f4` includes #490, #491 and #492 after the sample-calendar correction. All **12,068 tests across 1,105 suites pass** with four workers; the targeted 385-test run overlaps this total. The production build generates **243 pages**, generated-route TypeScript, scoped lint, all eight translation gates and diff checks pass. All 13,337 previous catalogue values/order are preserved with 25 additions per locale (13,362 total). Independent integration verification confirms twelve exact author file blobs, the API's prior completed-plan/undated-history correction preserved, and only two explicit test-helper resolutions: preserve existing nested OR support alongside strict AND/negation and reject malformed groups. Positive OR compatibility tests pass. The app/components scan remains 2,607 findings in 582 files; generated library copy is outside that scanner. Earlier full-suite/build evidence stays attached to its own recorded head. No rehearsal was pushed and no production/provider/SQL changes were made.

**Briefing work still open:** strict timed ICS import is implemented provisionally and under independent review, including IANA/floating times, explicit temporal errors, normal declarations, durations, zero-length events, listed-occurrence disclosure and owner-bound transient notices. Conflicting declaration and lifecycle regressions are still being resolved; it is not yet published or included in the combined checkpoint. A separate Home calendar-reader correction has nine failing-before cases out of twelve actual reader renders and an approved implementation contract: typed local/all-day windows before existing limits; Today/all-day membership; per-section retryable errors with healthy tasks/reminders retained; dependent calm/outcome/conflict-insight suppression on failed reads; and the Home-specific point-event correction. Home outcome translation #491 does not include this functional fix. Additional API/notification generated copy and durable recipient locale remain open. Legacy home_briefs snapshots remain quarantined. Stripe/pricing work stays parked under the user's steering.

## Owner decisions and schema work

The exact consolidated proposals remain in [STRATEGY_WORK_QUEUE.md, section 6](STRATEGY_WORK_QUEUE.md#6-migration-asks--owner-approval-required-none-has-been-added). Review them against current schema before creating or applying any DDL; this ledger adds none.

| Area | What remains owner-controlled |
| --- | --- |
| Inbox and school source links (M2/M20/M9) | Durable request/handled/paperwork/member/link/sub-intent columns and additional capture channels. Retain the existing-payload runtime fixes above; they do not supply the proposed dedicated columns or every additional channel. |
| Sharing and care (M23/M16) | Member-scoped sensitive-data RLS, delegation-aware read policies, and care-circle tables. Existing sharing presets govern actions; they do not prove restricted data visibility. |
| Invite delegation on acceptance (S-08) | `components/family/invite-form.tsx:31` explicitly leaves deferred delegation intent storage unimplemented. Role presets exist. Choose an owner-approved place to preserve the delegation until acceptance, then test acceptance, expiration, revocation, and scope. Do not call the existing role preset a completed automatic delegation. |
| Durable metrics (X1/X2/X4/X6/X9/X11, X8/X9) | Weekly metric snapshots and activity timestamps. Live totals/precision in #433/#434 do not supply historical weekly storage. |
| Execution and context audit (M6/M35/M24) | Undo fields/event vocabulary, multi-row created references, dead-letter reconciliation in the claim RPC, and context-access log storage. |
| Domain extensions (M3/M10/M12/M15/M18/M26/M27/M32/M33) | Graph provenance/triggers, grocery budget fields, asset replacement links, travel disruption columns, optional display pairing, service-provider workflow, partner keys/events, savings ledger, and search RPC/indexes. Existing no-migration surfaces can ship independently where their acceptance requirements are met. |
| Account and public metrics (M36/W2/W7/W3) | Account-deletion request storage and any required public-stat semantics/schema corrections. `0278_public_handled_stats` already defines the public stats RPC; audit its partial/legacy completion semantics before proposing an addition. MFA/TOTP enablement is a project configuration action, not a code or schema test. |

## Verification outside source implementation

- **Integrated CI:** run required checks at each final PR head and again after integration. Historical native Windows reports recorded five path-separator failures and, on branches without #449, the Unix metadata enumeration failure. #457 fixes these without weakening the guards; the combined `e461f226` run passed all 10,270 tests. These failures are no longer an accepted baseline on a tree containing #457. Record Linux CI separately and investigate any new failure.
- **Inbound delivery:** verify configured inbound email/SMS/voice provider delivery, authentication, retries, and persisted outcomes in a deployed environment. The webhook implementation and unit tests cannot establish that an external provider is connected.
- **Physical display/mobile:** execute and record the scripts in [PHYSICAL_DEVICE_TEST_PLAN.md](PHYSICAL_DEVICE_TEST_PLAN.md), including the D3/D5 wall-display soak, wake lock, install, visibility recovery, and deployment refresh. Emulated Chromium tests do not establish iOS/Android device behavior.
- **Production permissions:** exercise cross-household and role boundaries using actual sessions after deployment, especially private purchase answers and any later owner-applied sharing/RLS changes. In-memory test clients validate application filters but do not execute PostgreSQL RLS.
- **Configuration and public claims:** confirm project settings and externally sourced device/pricing claims before release where the queue requires them. No external service connection, physical-device pass, or production schema application is inferred from a merged pull request.

## Update rule

For each row, record the implementing PR/head, required check results, merge commit, and any separate deployed verification. Remove a runtime item only after its acceptance check is demonstrated. Move schema/configuration work to completed only after the owner-controlled action has evidence. Refresh this ledger when the open PR states change; avoid treating the older work queue's status labels as a completion count.
