# Strategy remaining work

Verified 2026-09-09: the runtime findings below were reproduced on `7789a3ea` (merged through #431). Main has since advanced to `a8196c80` through #445, and integration to `5b4249d1` with the marketing translation work in #448. Open changes are tracked separately below. This is a completion ledger for concrete findings from the current audit, not a new audit of all 60 strategy items. A queue section marked implemented does not prove that every acceptance requirement shipped. Code, tests, and deployed evidence take precedence over older status prose in [STRATEGY_WORK_QUEUE.md](STRATEGY_WORK_QUEUE.md).

The standing constraints remain: no agent-authored migrations, no shared sidebar changes, translated interface copy in all seven locales, existing service/trust boundaries, and visible errors when required reads fail. The owner controls schema changes and production application.

## Review and integrate work already built

These pull requests were **open** when this ledger was written. Review, required CI, and integration remain; they are not represented here as merged fixes. Local validation reported in a PR is distinct from CI or production verification.

PRs #432, #434–#436, #438–#444 and #447 passed required Linux checks, E2E and preview deployment at the heads below and are ready for review. The corrected #433/#437, updated #446 and new #449/#450 remain drafts while their latest checks finish. #437 is stacked on #433; #444 includes #442. Recheck live status and merge-base changes before integration; green checks on individual branches do not establish that their combined result passes.

| Work | Review | Acceptance to retain during integration |
| --- | --- | --- |
| S-08 grandparent digest reads | [#432 — Keep grandparent digest read failures visible per household](https://github.com/NewWorldVenture/Bubaly/pull/432), head `9992461a` | A failed roster, photos, milestones, announcements, or dates read produces that household's error card. Other households remain visible, and retry restores the recovered card. Query errors and transport failures are covered. |
| S-15 recorded completed plans | [#433 — Count recorded completed plans consistently across family metrics](https://github.com/NewWorldVenture/Bubaly/pull/433), head `2422e64a` | Autopilot, briefing and the banner count the same dated fully completed plans in one family/seven-day window. Explicit partial/failed states override legacy executed status. Undated history is disclosed, failed exact counts remain unavailable, and family changes suppress stale results. The shared 12-minute assumption is modeled planning time. |
| X6 signal precision and retained decisions | [#434 — Measure signal precision and retain withdrawn suggestion evidence](https://github.com/NewWorldVenture/Bubaly/pull/434), head `6e1b23c6` | Count current accepted/executed versus dismissed decisions exactly, distinguish zero/no decisions/read failure, preserve decision evidence during rescans, and allow a withdrawn signal to recur. This is recorded acceptance/execution, not a claim about satisfaction. Weekly historical snapshots still require owner schema work. |
| S-10 launch rollback | [#435 — Fix rollback after failed life-event launches](https://github.com/NewWorldVenture/Bubaly/pull/435), head `326ae50f` | A move created before `planTasks` fails is removed. Cleanup on an existing move removes only this launch's new tasks. All rollback attempts continue after one failure, and incomplete cleanup is reported honestly. |
| S-06 school proposal approval and source completion | [#436 — Keep school desk proposals behind approval](https://github.com/NewWorldVenture/Bubaly/pull/436), head `fffd99bd` | Propose requires review even under an allow policy. Only a successful same-household execution receipt marks its inbox source handled. If execution succeeds but bookkeeping fails, retry repairs the source without creating duplicate work. |
| M40 own-family planning value | [#437 — Show recorded completed plans and modeled planning time in billing](https://github.com/NewWorldVenture/Bubaly/pull/437), head `246e80f7`, base #433 | Billing and opened upgrade prompts use the corrected shared dated-plan count and explicit 12-minute model. Preserve zero, unavailable/retry, undated-history disclosure, session changes and household boundaries. Do not reintroduce the rejected four-source total. |
| S-12 purchase advice in Ask | [#438 — Connect Ask to private purchase advice and complete household evidence](https://github.com/NewWorldVenture/Bubaly/pull/438), head `fcc5a3f2` | Actual intake performs the guarded deterministic read without a provider. All evidence is paged completely; budget, memory and gift restrictions remain. Approved answers use the original requester's current permissions and private conversation storage; family-readable receipts contain no sensitive report. |
| W7 public customer stories | [#439 — Make published customer stories readable and editable](https://github.com/NewWorldVenture/Bubaly/pull/439), head `a9c300fa` | Published stories have signed-out detail pages, grounded metadata and escaped content. Admins can edit or withdraw stored stories. Withdrawal blocks the detail page and invalidates public cards. No invented customer claims or automatic publication. |
| W5 outcome discovery | [#441 — Discover and launch relevant household outcomes](https://github.com/NewWorldVenture/Bubaly/pull/441), head `a041cca3` | Home and Command Center use complete counts to rank launchable outcomes. Related routes open the actual launcher with the selected outcome. The optional personal navigation preset preserves existing choices, respects limits, and uses an owner-scoped compare-and-swap write. Shared sidebar files stay unchanged. |
| W8 canonical entity resolution | [#442 — Resolve confirmed household entities on inbound paperwork](https://github.com/NewWorldVenture/Bubaly/pull/442), head `0514d913` | Exact confirmed household entities and aliases resolve deterministically; ambiguous names stay unresolved. Sender evidence survives filing. Failed enrichment retries from captured paperwork without losing the planner opportunity or duplicating the urgent message. OAuth onboarding is a separate change. |
| M30 thirty-minute timing | [#443 — Report thirty-minute setup and first-outcome timing with valid denominators](https://github.com/NewWorldVenture/Bubaly/pull/443), head `8afdd424` | Setup completion and first-outcome viewing have separate denominators. Missing/invalid durations stay explicit; the inclusive 1,800-second threshold does not admit 1,800.001 seconds. Complete keyset reads use a fixed cutoff and fail closed on late-page errors. Viewing is not completed work. |
| S-01 received attachments | [#444 — Capture inbound email attachments with independent retries](https://github.com/NewWorldVenture/Bubaly/pull/444), head `66a90ae5` | Includes the #442 ancestor. Supported bounded attachments preserve their provider identity; extraction and filing retry independently. Saved siblings survive another attachment's failure. Partial extraction is visible, excludes derived quick actions and automatic drafting, and retains an escaped excerpt for review. This does not complete capture-wizard uploads or public-link retrieval. |
| X11 modeled value versus price | [#446 — Compare modeled completed-plan value with published plan price](https://github.com/NewWorldVenture/Bubaly/pull/446), head `740c5069` | Home and Billing compare the shared dated-plan model with the same seven-day allocation of the active paid plan's published USD list price. Editable hourly USD value, excluded/undated evidence, actual zero and unavailable are explicit. This is neither measured time saved nor realized cash savings. |
| X2 automatic capture share | [#447 — Show verified automatic capture share in family intelligence](https://github.com/NewWorldVenture/Bubaly/pull/447), head `f88e1082` | Supported current-window canonical records form the denominator. Verified successful creation receipts and same-family feed provenance form a deduplicated lower-bound numerator. Unknown origins stay unknown. Complete keyset reads, family scope, empty data and failed reads remain distinct. No weekly history is implied. |
| Localized metadata/admin guidance | [#449 — Localize page titles and shared admin credential guidance](https://github.com/NewWorldVenture/Bubaly/pull/449), head `37769954`, base main after #445 | All 31 repaired titles resolve the request locale and preserve robots settings. Shared diagnostic copy is localized, contains no secret material, and does not infer legacy-key revocation from a publishable key. Admin authentication precedes diagnostic reads. The onboarding title matches #443. |
| W8 connected onboarding | [#450 — Connect calendars during onboarding with private preview and safe imports](https://github.com/NewWorldVenture/Bubaly/pull/450), head `ef92782a` | Owned encrypted continuation and private primary-calendar preview; dormant connection until Finish; calendar-only imports with no provider writes. Stable event/member/invite identities survive retries and changed previews, and local edits/deletions survive later imports. Preserve original-zone all-day dates and family-local first briefs. No live provider account has been exercised. |

The S-10 defect exists on the verified base in `lib/life-events/launch.ts:307`: `createMove` precedes `planTasks`, and the outer rollback does not yet own that new move when task creation fails. Track this through #435 rather than opening duplicate implementation work.

## Active implementation

**Review integration:** S-12 and M40 are now built in #438 and #437. S-06 and S-12 both extend approval execution; preserve both the school source-completion hook and the private purchase-result hook when integrating.

**X1 / M40 evidence correction built:** `agent_activity` can duplicate work already counted in a run; completed reminders can be human tick-offs; current notification timestamps include skipped or failed attempts. The old four-source indicator therefore lacked proof of disjoint delivered work. #433 and #437 now use only recorded fully completed plans with durable completion dates. #446 shares the same `completed-plans.ts` and `completed-plans-model.ts` modules. Retain the correction during integration and do not describe this subset as all household work.

**S-01 capture wizard:** active on `codex/strategy-paperwork-capture-wizard-20260909`, stacked on #444. Shared upload/photo intake is built and under final review. Review found family-switch request races, missing list refresh after a composer capture, and a missing existing document AAL2 check at the new API; these are being corrected before publication. Saved-text resume avoids repeated extraction after persistence; provider failure before persistence retains the selected file for explicit retry.

**S-11 mobile assistant:** a bounded audit is checking localization and session/family/recording lifecycle against the current integration branch. Expo audio recording already exists; do not list it as missing without a specific failing path.

**Platform metric evidence:** a fresh read found `ROW_CAP = 2000` in `lib/metric/strategy-server.ts`. X3/X10/X12 can mix truncated row inputs with exact household counts, and one rejected promise can discard otherwise independent metrics. X10 also accepts a paid subscription's latest `updated_at` as evidence of conversion after first value; a renewal can move that timestamp past the milestone without establishing when the family first paid. A follow-up is being scoped around complete reads and explicit missing transition evidence.

**Marketing translations:** #448, head `5b4249d1`, belongs to the integration branch's owner. It expands the scanner, localizes marketing examples and shared public helpers, and routes three more Knowledge Center readers through locale resolution. Do not overlap those files or mistake a clean declared gate for completion of the application-wide literal backlog.

## Verified runtime findings and next changes

The evidence below describes the verified base. S-06, M40 and W7 now have implementations in #436, #437 and #439 above; retain their acceptance checks during review instead of starting duplicate work. Active branch claims are listed above.

| Priority / item | Evidence on the verified base | Next change and completion check |
| --- | --- | --- |
| P1 / S-06 explicit proposal approval | `app/(app)/dashboard/school/actions.ts:120` gates a proposal; the `allow` branch at line 141 immediately calls `runAction`. S-06 build requirement 3 explicitly says proposals are approval-gated and never auto-executed. | Make the desk's **Propose** action persist a reviewable approval even when a routine domain policy would allow immediate execution. Test an allow policy: no domain write occurs until a parent approves; denial remains a denial. |
| P1 / S-06 handled state after approval | The same action's comment at line 24 documents that later approval executes elsewhere and never stamps the source inbox message `ai_handled`. The direct allow branch stamps it, so the two paths disagree. | Carry a validated source-message reference through the existing approval payload/metadata and mark it handled only after the approved domain action succeeds. Verify current writable JSON fields before choosing the representation. Missing inbox link columns are not, by themselves, proof that this no-migration path is impossible. Test approval, failure, retries, and household isolation. |
| P2 / S-01 inbound attachment triage | `app/api/contact-center/email/route.ts:61` maps multipart `File` values to `''`. Only the text fields are forwarded, so an email whose form/bill/receipt/reservation is in an attachment loses that content before triage. S-01 build requirement 3 explicitly includes inbound attachments. | Preserve bounded supported attachment content through the inbound pipeline and existing paperwork triage. Add a webhook-to-paperwork test with an attachment-only message, and tests for unsupported files, size bounds, parser failure, and provider retry. Do not report the attachment handled when extraction fails. |
| P2 / whole-app translations | `scripts/i18n-scan.mjs:46` explicitly excludes the `everything` surface because broader scanning exposed untranslated copy. The audit reproduced 2,345 findings in 552 files on unchanged `7789a3ea`; #431 completed catalogue entries, not the whole application's literal-copy backlog. | Save/triage the full scan output by surface, translate in bounded batches across all seven locales, and rerun the full scan. Do not use a clean catalogue or six-surface gate as proof that the whole app is translated. Re-enable the whole-app gate when its findings are resolved. |
| P2 / W5 outcome discovery | `lib/outcomes/launcher.ts` has `OUTCOMES` and `buildOutcomePlan`, but no `outcomesForRoute`; no `OutcomesStrip` or `RelatedOutcomes` implementation was found. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:504`. | Add outcome launch strips to home/command center and route-relevant outcome chips using the existing launcher. Test route relevance and actual launch wiring. Any sidebar preset remains an explicit user opt-in; do not rewrite the shared sidebar. |
| P2 / W8 connected onboarding and canonical entities | `app/onboarding/actions.ts:107` accepts `paste` or `demo`; the configured OAuth providers are not part of this onboarding step. `lib/graph/resolve.ts` does not exist. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:532`. | Reuse configured calendar OAuth during onboarding, preserving supported persisted source values. Add a pure canonical school/team/contact/sender resolver and call it from inbound adapters; unresolved mentions must remain uncertain. Test duplicate names, household boundaries, disconnected providers, and failure recovery. |
| P2 / W7 published case-study detail | Published testimonials are wired, but `app/(marketing)/customers/[slug]/page.tsx` is absent. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:522`. | Add published case-study cards/details backed by real approved content and attributable results. Unpublished cases stay unavailable. Verify claims and permission to publish; do not fabricate customer evidence to populate a route. |
| P2 / M40 own-family delivered value | `components/app/upgrade-modal.tsx` and `components/modules/billing-module.tsx` show plan/checkout and static outcome copy, but receive no `loadTimeSaved` or handled-value input. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:458`. | Implement the active M40 work described above with one scoped loader/component and honest zero/unavailable/positive states. Load modal data only when opened. Test authentication, household isolation, failures, and estimate wording. |
| P2 / X2 automatic capture share | No `lib/metric/auto-captured.ts` or capture-ratio surface exists. Existing provenance includes feed IDs, AI tool resource references, and autopilot source/status. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:551`. | Define and show the current seven-day numerator and denominator from supported provenance. Deduplicate AI-created resources and keep imported content separate from work performed by Bubaly. Unknown provenance must not silently count as automatic. Weekly snapshots and optional explicit `created_via` fields are separate owner asks. |
| P2 / X11 estimated value versus price | No `lib/metric/value.ts` or paid-family value/price surface exists; time estimates and plan prices are separate inputs. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:637`. | A current estimated comparison can ship without weekly snapshot storage. State the minute weights and assumed hourly value, handle zero/free plans, and label the result as estimated. Never treat a suggestion's `monthlyCents` as realized savings; that requires evidence the saving occurred. This is separate from M40's handled/time display. |
| P2 / M30 onboarding under 30 minutes | `lib/onboarding/ttv-audit.ts` and `lib/analytics/activation.ts` do not define `under30MinRate`. Audit acceptance: `MARKET_DOMINATION_AUDIT.md:370`. | Define completed-under-30-minutes divided by completed onboardings, specify how missing/invalid duration is reported, and wire it into onboarding reporting. Test the threshold, missing data, household cohort, and denominator rather than inferring this rate from a median. |
| P2 / platform metric completeness (fresh integration review) | `lib/metric/strategy-server.ts` caps eight row sources at 2,000, while the families denominator uses an exact count. `Promise.all` also propagates a transport rejection across unrelated metric tiles. | Read every supported source with stable pagination/fixed cutoffs or exact aggregate counts; a late-page error makes only dependent metrics unavailable. Test beyond the cap, smaller provider page limits, mutation during pagination and transport rejection. |
| P2 / conversion-after-value evidence (fresh integration review) | `lib/metric/strategy-server.ts` falls back to `subscriptions.updated_at`; `lib/billing/conversion.ts` counts that as a conversion and only marks the median approximate. A paid-before-value family can renew afterward and then be incorrectly included. | Require a recorded paid transition for temporal ordering and expose missing evidence separately. Existing subscription admin notifications record paid-active transitions, including reactivation; do not call those proof of first-ever payment or causation. Test renewal after value without a transition receipt. |
| P2 / admin referral summary (fresh integration review) | `/admin/marketing/referrals` still derives totals, conversion rate, credits owed and top referrers from the latest 100 rows. `rewarded` rows remain included in credits owed. | Separate bounded recent activity from complete totals and rankings. Derive outstanding rewards from persisted fulfillment state rather than treating rewarded rows as unpaid. Keep unavailable distinct from zero and test more than 100 records and partial rewards. |

The first audit of S-01 through S-10 found implemented baseline behavior for S-02, S-03, S-04, S-05, S-07, and S-09. That is a bounded source observation, not full browser/device acceptance or a claim that every related roadmap item is complete. The public display page is `/family-display`; the authenticated kiosk remains `/display`.

## Owner decisions and schema work

The exact consolidated proposals remain in [STRATEGY_WORK_QUEUE.md, section 6](STRATEGY_WORK_QUEUE.md#6-migration-asks--owner-approval-required-none-has-been-added). Review them against current schema before creating or applying any DDL; this ledger adds none.

| Area | What remains owner-controlled |
| --- | --- |
| Inbox and school source links (M2/M20/M9) | Durable request/handled/paperwork/member/link/sub-intent columns and additional capture channels. The runtime gaps above should still be completed where existing payloads suffice. |
| Sharing and care (M23/M16) | Member-scoped sensitive-data RLS, delegation-aware read policies, and care-circle tables. Existing sharing presets govern actions; they do not prove restricted data visibility. |
| Invite delegation on acceptance (S-08) | `components/family/invite-form.tsx:31` explicitly leaves deferred delegation intent storage unimplemented. Role presets exist. Choose an owner-approved place to preserve the delegation until acceptance, then test acceptance, expiration, revocation, and scope. Do not call the existing role preset a completed automatic delegation. |
| Durable metrics (X1/X2/X4/X6/X9/X11, X8/X9) | Weekly metric snapshots and activity timestamps. Live totals/precision in #433/#434 do not supply historical weekly storage. |
| Execution and context audit (M6/M35/M24) | Undo fields/event vocabulary, multi-row created references, dead-letter reconciliation in the claim RPC, and context-access log storage. |
| Domain extensions (M3/M10/M12/M15/M18/M26/M27/M32/M33) | Graph provenance/triggers, grocery budget fields, asset replacement links, travel disruption columns, optional display pairing, service-provider workflow, partner keys/events, savings ledger, and search RPC/indexes. Existing no-migration surfaces can ship independently where their acceptance requirements are met. |
| Account and public metrics (M36/W2/W7/W3) | Account-deletion request storage and any required public-stat semantics/schema corrections. `0278_public_handled_stats` already defines the public stats RPC; audit its partial/legacy completion semantics before proposing an addition. MFA/TOTP enablement is a project configuration action, not a code or schema test. |

## Verification outside source implementation

- **Integrated CI:** run required checks at each final PR head and again after integration. Native Windows reproduced five baseline failures across `household-trail`, `i18n-server-boundary`, `untrusted-fence-ratchet`, and `supabase-query-audit` in the open PRs' reports; the two server-boundary failures were independently reproduced on unchanged main. Record the Linux CI result separately. Do not silently mark failed local checks passed or relax tests to hide a platform issue.
- **Inbound delivery:** verify configured inbound email/SMS/voice provider delivery, authentication, retries, and persisted outcomes in a deployed environment. The webhook implementation and unit tests cannot establish that an external provider is connected.
- **Physical display/mobile:** execute and record the scripts in [PHYSICAL_DEVICE_TEST_PLAN.md](PHYSICAL_DEVICE_TEST_PLAN.md), including the D3/D5 wall-display soak, wake lock, install, visibility recovery, and deployment refresh. Emulated Chromium tests do not establish iOS/Android device behavior.
- **Production permissions:** exercise cross-household and role boundaries using actual sessions after deployment, especially private purchase answers and any later owner-applied sharing/RLS changes. In-memory test clients validate application filters but do not execute PostgreSQL RLS.
- **Configuration and public claims:** confirm project settings and externally sourced device/pricing claims before release where the queue requires them. No external service connection, physical-device pass, or production schema application is inferred from a merged pull request.

## Update rule

For each row, record the implementing PR/head, required check results, merge commit, and any separate deployed verification. Remove a runtime item only after its acceptance check is demonstrated. Move schema/configuration work to completed only after the owner-controlled action has evidence. Refresh this ledger when the open PR states change; avoid treating the older work queue's status labels as a completion count.
