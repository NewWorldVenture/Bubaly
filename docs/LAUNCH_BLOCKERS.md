# Launch Blockers

The repository is currently NO-GO. These blockers are external, incomplete, or insufficiently verified;
they are not counted as completed audit weight.

| ID | Severity | Blocker | Evidence | Owner/dependency | Exit criteria | Status |
| --- | --- | --- | --- | --- | --- | --- |
| LB-001 | P0 | Supabase Auth Admin users health check returns HTTP 500 | `npm.cmd run db:audit:auth` at 2026-07-18 05:40 America/New_York: public Auth health passed; Admin users returned HTTP 500 `Database error finding users` (request `019f7499-50fd-7ac2-8ec3-0840f2615d41`) | Supabase project/operator access | Admin users endpoint returns healthy response and auth smoke passes | Open |
| LB-002 | P0 | Remote migration ledger is not verified against checkout | Local `npm.cmd run db:audit:migrations` passes with 230 numbered migrations through 0214; remote history is not confirmed | Supabase project access | Reconcile all 230 migrations with remote ledger and safely apply pending launch-critical migrations (including 0208 and 0210-0214 as applicable) | Open |
| LB-003 | P0 | Historical Supabase service credential requires rotation | Credential exposure and rotation dependency are documented in existing readiness report | Supabase owner/secret manager | Rotate/revoke, update deployment secrets, verify old key fails | Open |
| LB-004 | P1 | Local Postgres/RLS lint is unavailable without a running Supabase container | `supabase db lint --local` previously failed with `LegacyDbConnectError` | Docker/Supabase local runtime | Run migration/RLS lint in isolated local or CI database | Open |
| LB-005 | P1 | Authenticated browser E2E coverage includes an intentional skip | Public E2E baseline records 51 passed and 1 authenticated skip out of 52 | Test credentials/isolated environment | Execute authenticated role/device suite with no unexplained skips | Open |
| LB-006 | P1 | Third-party production callbacks and secret rotation are not fully smoke-tested | Service matrix marks Stripe, Google, email and push as partial | Provider sandbox credentials | Verify callback, retry, idempotency, outage and rotation behavior | Open |
| LB-007 | P1 | Page-by-page and interaction-by-interaction audit is incomplete | `docs/AUDIT_PROGRESS.md` has 18 in-progress units | Ongoing audit work | All weighted units meet their completion gate | Open |
| LB-008 | P1 | Backup/restore and rollback rehearsal is not evidenced | No current restore drill artifact in this checkout | Supabase/Vercel operator access | Complete isolated backup restore and deployment rollback drill | Open |
| LB-009 | P1→P2 | `family-media` media bucket is served via public URLs (PLA-0461) | Bucket + family-folder write RLS now defined in migration `0216` (commit `46556214`, PG16-validated: cross-family write blocked). Residual: bucket is `public` and all four consumers use `getPublicUrl`, so object URLs are unauthenticated | Supabase owner: apply `0216` to prod (human-owned) + decide public→signed-URL + migrate stored URLs | Partially mitigated: bucket defined + write-isolated + reproducible. Remaining: flip reads to private/signed URLs (needs a data-migration of stored public URLs in family_photos/family_messages) and apply `0216` to prod | Open (downgraded) |
| LB-010 | P0 | **wallet ledger money-minting** — until migration 0217 is applied to prod, any family member (incl. a child) can INSERT a completed credit into `wallet_transactions` and mint spendable money (PLA-0580). Related: `wallet_audit_logs` was also child-tamperable until **0224** (PLA-0622, append-only) | Proven live on PG16: child INSERT of a $9,999.99 completed credit succeeded pre-0217; blocked post-0217 (sqlstate 42501). Reusable exit-criteria probe: `docs/audit/wallet-write-rls-check.sql` (mint-lock + audit append-only + manager positive control, ALL INVARIANTS PASSED) | Supabase owner (apply 0217 + 0224 — human-owned) | Apply 0217 (+0224); verify a non-manager INSERT to any wallet_* table is rejected in prod — run `docs/audit/wallet-write-rls-check.sql` on the PG16 harness (or adapt to a prod read-replica) to confirm | Open |

| LB-011 | P1 | **admin_users + support_tickets are world-readable** — until migration 0219 is applied to prod, any signed-in user can read every family's support tickets (requester name/email + issue text = cross-tenant PII) and the entire admin roster (`admin_users`: emails, roles, permissions), and tamper with `admin_users` rows (PLA-0600) | Proven live on PG16: as a family-A member, `select * from support_tickets` returned 11 rows (all tenants') + `admin_users` 12 rows pre-0218; both →0 and INSERT blocked post-0219; service-role (admin console) unaffected | Supabase owner (apply 0219 — human-owned) | Apply 0219; verify a non-service-role SELECT of `support_tickets`/`admin_users` returns 0 in prod | Open |

| LB-012 | P2 | **marketplace bid-as-any-family IDOR** — until migration 0221 is applied to prod, any signed-in user can call `marketplace_place_bid_unchecked` directly (SECURITY DEFINER, executable by `authenticated` via a rename that carried the grant) and place an auction bid attributed to another family, bypassing the checked wrapper's `auth.uid()` validation (PLA-0610) | Proven live on PG16: as a family-A member the direct `_unchecked` call was permitted pre-0221, "permission denied" post-0221; checked wrapper unaffected | Supabase owner (apply 0221 — human-owned) | Apply 0221; verify `authenticated` cannot EXECUTE `marketplace_place_bid_unchecked` in prod | Open |

| LB-013 | P1 | **confirm RLS is enabled on `storage.objects` in prod** — the app's storage isolation (private `documents` = driver licenses/insurance/medical, `chore-proof`, `family-media`) relies ENTIRELY on Supabase's platform default of RLS-on for `storage.objects` + the family-scoped policies; RLS-on is NOT set by any migration, so if it were ever disabled every private object would be world-readable (PLA-0630) | Policies verified correct + family-isolating on PG16 (as family A: read family B's document = 0, cross-folder upload rejected); but the enable-RLS step is a Supabase platform default, absent from migrations | Supabase owner (verify + assert in prod: `storage.objects` RLS enabled) | Confirm `storage.objects` has RLS enabled in prod and a non-member cannot read another family's `documents` object | Open |

| LB-014 | P2 | **systemic SEED_ALL coverage gap** — core day-to-day features render empty in a fresh env: `school_events`, `sports_events`, `medical_profiles`, `medication_schedules`, `routine_templates`, `grades`, `home_assets` have NO seed anywhere; the wallet subsystem, `savings_goals`/`goals`, `family_reminders`, `maintenance_tasks`, `rewards`, `nutrition_logs` have orphan seeds never wired into `SEED_ALL` (PLA-0720/0730) | PG16 census: 71,154 anchor rows total, but these tables = 0 for every family; SEED_ALL has 0 inserts for them. **ROOT CAUSE FOUND + FIXED (agent-01):** the anchor/demo family is created by the family trigger with ONLY its parent — **no children** — so every kid-role-filtered seed block (chores/allowances/grades/kid-wallets/investing) produces 0 rows for the anchor and the marketplace hand-off/returns seeds **ERROR** (`requires two existing members for family …f1`). `supabase/seed_anchor_household.sql` gives the anchor a co-parent + 3 kids (deterministic family-scoped ids, `on conflict do nothing` — non-destructive, no cascade); guard `tests/seed-anchor-household-contract.test.ts`. **PG16-proven:** anchor `parent=1` → parent/adult/teen/child×2, and a subsequent SEED_ALL re-run then logs `Seeded 160 pickup hand-offs` instead of erroring (marketplace_handoffs 0→160). **Must be wired EARLY** (right after the family exists, before the kid-dependent blocks). **Partial (agent-01):** `supabase/seed_school_sports_events.sql` authored for the two unseeded calendar tables — 260 `school_events` + 260 `sports_events`, feeds `/dashboard/family-school`+`/dashboard/family-sports`+AI briefing; PG16-idempotent (260/260 ×2); guard `tests/seed-school-sports-contract.test.ts`. Also `supabase/seed_routine_templates.sql` (60 templates × 9 items = 540 rows, feeds the calendar Routines panel; PG16-idempotent 60/540 ×2; guard `tests/seed-routine-templates-contract.test.ts`). Also `supabase/seed_grades.sql` (~528 rows: kids × 8 subjects × ~22 assignments, feeds the Grades section of `/dashboard/family-school`; requires the household seed's kids; PG16-idempotent 528/528 ×2; guard `tests/seed-grades-contract.test.ts`). All standalone (not wired into SEED_ALL to avoid colliding with the LB-014 owner mid-edit). **Wallet slice DE-RISKED (PLA-0740): seed repointed to a reproducible family + PG16-verified 500 conserving rows — ready to wire.** **NEW unseeded table found + fixed (agent-05, PLA-0803): `behavior_logs` (mig 0073, A-05 Behavior Tracking) had ZERO seed coverage anywhere → `/dashboard/behavior` renders empty. `supabase/seed_behavior_logs.sql` authored (standalone): ~30 per-child observations across all 3 kinds + 10 categories over ~90 days for every kid-bearing family. PG16-verified on an isolated DB (migration 0073 verbatim; 2 kids → 60 rows, childless family → 0 rows no-error, 3 kinds/10 cats, signed points, idempotent ×2=60); real DB > ≥500 across families. Guard `tests/seed-behavior-logs-contract.test.ts`. Ready to wire.** **2nd gap OBSERVED (agent-05, not yet fixed — flagged for A-02/Codex to avoid duplicate seed work): `family_facts` (mig 0123, A-05 Knowledge Base / family-memory via `knowledge-base-module`) has NO `SEED_ALL` coverage — it is inserted ONLY by `supabase/seed_demo_account.sql`, which is a standalone file not sourced by SEED_ALL or any pipeline (SEED_ALL has no `\i` includes and no `family_facts` insert; the "facts"/"knowledge" hits in SEED_ALL are `family_playbook_suggestions` + the distinct `graph_entities` Knowledge GRAPH). Net: Knowledge Base renders empty in a fresh SEED_ALL demo. Fix = author `seed_family_facts.sql` (per-family × ~N labeled facts) or wire seed_demo_account's family_facts block into SEED_ALL.** | A-02 (seed baseline) | Wire orphan seeds **+ the new seed_school_sports_events.sql** into SEED_ALL + author seeds for the remaining unseeded features (`medical_profiles`,`medication_schedules`,`home_assets`); each affected unit reaches its ≥500-row DoD; a fresh demo family shows populated School/Sports/Health/Routines/Wallet/Reminders/Home | Open (School/Sports + Wallet seed prepped) |
| LB-015 | P1 | **CI is non-functional (infrastructure, not code)** — every `main` push run for hours completes in ~4–6 s with `runner_id: 0` / no runner assigned and `conclusion: failure`, i.e. both jobs ("Typecheck·Lint·Test·Build" and "E2E") fail at *provisioning* before any step executes. A real quality run (npm ci + build + 3509 tests) takes minutes. Net effect: **there is no automated merge gate** — every commit lands "red" regardless of its contents, masking any genuine regression. | Diagnosed by `agent-03` (PLA-0628): all 7 code-level quality gates pass locally on HEAD — `npm ci` lockfile in-sync, `npm audit --omit=dev` 0 vulns, `db:audit:migrations` OK (next 0225), `tsc --noEmit` clean (with the `@axe-core/playwright` devDep installed as CI does), `next lint` warnings-only, `vitest` 3509 green, `next build` exit 0. The failure is 100% consistent across all recent commits and independent of code. **Workflow config ruled out (agent-01): `.github/workflows/ci.yml` uses `runs-on: ubuntu-latest` (the standard GitHub-hosted label that always has runners) for both jobs, valid `actions/*@v4` steps, and self-provisions Supabase for E2E — so `runner_id: 0` at provisioning is NOT a bad-runner-label / self-hosted-label config bug; it is definitively a GitHub Actions billing / spending-limit / Actions-disabled / org-policy condition. No workflow change will fix it.** | **GitHub Actions owner/org** (not an agent — no runner/billing access) | Check GitHub Actions runner availability + minutes/spending limit + org policy for `NewWorldVenture/Bubaly`; a re-run must actually execute (minutes-long) and the quality job must go green. Once runners are back, no code change is expected to be needed. | Open |

| LB-016 | P1 | **500-article blog ships images of unverified license (LoremFlickr) + structural visual duplicates** — the public blog engine (`0226_blog_500_articles.sql`, 525 posts) sets `hero_image_url = https://loremflickr.com/1600/900/<terms>?lock=N`. `?lock` pins *which* image but NOT its license: LoremFlickr proxies Flickr photos across MIXED licenses (attribution-required / All-Rights-Reserved possible), so "free to use" is not verified — a real licensing exposure on a public marketing surface. It also draws all 525 covers from only **9 keyword pools**, so the covers are visually duplicated (unique URLs ≠ unique images). Violates the "all images free + unique, no duplicates" launch bar. | `grep -oE 'loremflickr\.com/1600/900/[a-z,]+' supabase/migrations/0226_blog_500_articles.sql \| sed -E 's|.*/900/\|\|' \| sort \| uniq -c` → 9 pools / 525 posts. Generator `scripts/generate-blog-posts.mjs` L428 emits loremflickr while its own comments claim "free-license Unsplash" (self-inconsistent). Full analysis: `docs/MARKETING_PLATFORM_COORDINATION.md §6b`. | marketing/blog content owner (codex / agent-05) — code-fixable, no external dep | Replace all 525 covers with bespoke generated on-brand SVG art (per-title seed → free, unique, duplicate-free, theme-aware; approach as `components/blog/blog-hero-art.tsx`) OR pin one distinct licensed asset per post in the `marketing_assets` bucket; drop `loremflickr.com` from `next.config.mjs remotePatterns`; verify no rendered cover hotlinks an unverified-license host | **RESOLVED (agent-05)** — `freeLicensedImage()` in `lib/blog/posts.ts` strips loremflickr at the data layer (all 5 consumers, incl. og:image/JSON-LD); image-less posts render bespoke generated `components/blog/blog-cover.tsx` (per-title seed → free/unique/no-dup, self-contained); `loremflickr.com` removed from `next.config.mjs`. Guard `tests/blog-cover-free-images.test.ts` (4); tsc+eslint+build green. See `docs/MARKETING_PLATFORM_COORDINATION.md §6c`. |

## Release Rule

No production launch recommendation may be changed to Go while any P0 blocker is open or while a P1
blocker lacks an owner, exit criteria, and current evidence.
