# Contact Center automatic SMS reply reservation

Baseline: `fae35e90c0c7705a870f11d6666ee16424975411`. Audit item SMS-001 remains IN PROGRESS, High.

The existing signed callback characterization still reproduces two automatic replies and two outbound rows for one inbound provider SID. Guardian recovery and urgent fallback receipts solve different workflows. The automatic reply design and provider status document have not yet been implemented.

This cycle will implement the reviewed reservation design in `contact-center-sms-reply-plan.md`: persist the first reply candidate in a service-owned receipt before intake, preserve that candidate through planner retries, and permit only one verified reservation to emit TwiML. A missing or ambiguous receipt cannot prove an older message was never answered. Current number ownership and reply settings must be rechecked before emission. Independent urgent processing and planner recovery remain required on replay.

The inbox will distinguish prepared, unconfirmed, suppressed, legacy unknown and unavailable replies through a bounded family-authorized display result. A local response never proves provider acceptance or handset delivery. This cycle does not add an automatic resend after ambiguous emission. Signed provider delivery-status callbacks remain a separate required extension under SMS-001.

Verification will cover replay and concurrency, lost database responses, legacy messages, frozen candidates, planner failures, setting changes, exact household/message ownership, status privacy and translated rendering. New code uses existing database tables; no SQL, dependencies, shared navigation or real outbound provider messages are part of this cycle. The hosted run for the preceding publication continues independently.

Application source is now frozen at `d5df624b0a16d3f057e40c9b5ca9ac91001c1dc6`. The first committed reply candidate is retained in an immutable private receipt. Only an exact successful final reservation update permits a TwiML reply; a lost or ambiguous update response never permits emission through readback. A completed reservation cannot be automatically reopened. Legacy intake without a receipt is held. Number ownership and the current concierge setting are checked again before reservation; urgent fallback and planner recovery run independently before attachment and reservation.

The status reader resolves the caller's family and plan itself, validates the displayed envelope and linked inbound/projection rows, and returns only bounded status strings. No private receipt payload, revision or emission token reaches the page. All seven catalogues retain every prior value and key order, with ten added SMS status entries each. No automatic repeat button or provider-delivery claim is added.

Focused results: 70 installed-SDK helper cases pass, including concurrency, lost responses, malformed receipts, cancellation and timestamp normalization; 42 reader/page/render cases pass; 48 actual signed callback cases pass; eighteen fixture query cases and 91 related onboarding cases pass. The combined 21-file group passed 405 tests before two additional prototype-field signature cases, which pass in the final 48-case route group. Review caught a plain-object parameter parser dropping prototype-named fields; a null-prototype map now preserves signature coverage and rejects duplicates. The shared test fixture explicitly models JSON equality/text extraction and mutation counts; it does not establish PostgreSQL behavior.

The new hosted case creates only its own disposable account/channel, invokes the real signed route, verifies sequential and concurrent single emission, preserves an archived projection on replay, and verifies child receipt-insert denial. It makes no provider send and produces no browser artifacts. Hosted execution is pending. A rolling deployment still serving an older unfenced handler can repeat replies; controlled cutover remains an operational requirement. A reserved response can be lost before reaching the provider and remains unconfirmed. No atomic database/HTTP transaction or durable resend is claimed.

All 14,761 unit tests in 1,197 files pass on `d5df624b` in 42.91 seconds. Its production build compiles in 61 seconds and generates 252 pages. Strict post-build types, lint with four existing warnings, localization and query audits pass. The query audit resolves 491 tables, 77 functions and 143 API routes. The preceding publication's Guardian recovery, login and meal workflows pass all 1,062 hosted browser tests; see `guardian-hosted-acceptance.md`. Source discovery records twenty changed files, six new files, five production files, eight test files, seven locale files and nine newly exported functions in `discovery/contact-center-sms-reservation-inventory.json`.

Private gate logs: `C:/Users/Daniel/AppData/Local/Temp/bubaly-sms-reservation-final-private-<units|build|types|lint|i18n|queries>-20260912.log`. The new hosted reservation test is discovered, not yet executed. Full production/provider workflow sign-off remains pending under SMS-001.
