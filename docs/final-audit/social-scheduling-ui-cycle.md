# Scheduling time and action integration

SOCIAL-003 was recorded before these repairs. The previous Studio submitted an offset-free wall time, accepted past times and advertised schedules without a worker. Root now owns the explicit time/action/presentation contract; the operations lane owns private scheduling authority and the worker. No SQL or shared navigation changes are included.

The action validates future canonical UTC ISO plus an explicit IANA timezone before writes. When a local form time is supplied, it must resolve to that same instant. Scheduling is bounded to one-off text/link posts for up to ten distinct connected X identities. The action checks the schedule ID, calendar receipts and protected prepare/arm calls; once arming starts, failures retain the existing post for review. Scheduled manual publishing delegates to the same private gate. Workspace timezone reads fail visibly; a successfully absent setting uses the visible browser timezone.

The pure time tests pass 29 initial cases, including New York summer/winter, Nepal's fractional offset, Pacific date boundaries, leap dates, DST gaps and folds including Lord Howe's half-hour change, malformed input, explicit future timestamps and old-row UTC display. Later additions also verify localized date display, local-day grouping and whitelisted status copy; combined retest is pending. These execute local Intl behavior and do not verify the deployed scheduler.

The browser lane initially passed 16 actual Studio/locale/time contract cases, then reproduced three remaining lifetime failures before root's lifecycle edits:

- Capture Schedule at 09:00, change the visible draft to 17:00, invoke the old callback: the action still receives 09:00 (13:00Z in New York).
- Capture Schedule, actually unmount the Studio, invoke the old callback: an action still dispatches.
- Hold Save draft, unmount, release the successful response: the retired component calls router.push and navigates the replacement screen.

Root's committed draft/lifetime identity, disabled pending fieldset and owner-keyed Studio instance made all 19 cases pass. Further independent review added two failing cases before the next edit: a retained textarea change callback still changed a pending draft despite its disabled DOM field; a changed account prop also changed draft identity during dispatch. In both cases the stale completion was ignored and the mounted editor remained pending without a review link. Input callbacks need the same synchronous guard as submit, while a changed-but-mounted request must settle into review rather than remain busy. The server still resolves/authenticates current ownership independently.

The first related five-file run had 118 passing cases, two X fixture failures and one suite import failure. The X mock omitted the production context's active member ID, and the direct pipeline fixture imported request-bound React cache code through the new approval helper. Corrections retain actual approval/pipeline logic and restore representative identity fixtures; results will be recorded after rerun. The existing Studio/public persistence browser suite controls only the new private prepare/arm seam; real private receipt/worker execution is tested separately.

Final combined validation and complete live provider/RLS/cron workflow remain pending. Public projection metadata is used for presentation only and never authorizes publishing. Legacy rows are labeled unverified, approval-required work is held, and provider uncertainty remains visible. AUTHZ-003 and SEC-001 remain release failures.

Further independent action review found a real draft false-success/duplicate-retry boundary: committed initial inserts with null, idless, invalid-id or thrown responses lacked a review-required result; draft creation could even return success with an invalid identity. Failed or thrown cleanup also discarded known identity or escaped. The expanded 63-case action review had 49 passes and 14 failures before this repair. Root is adding explicit `reviewRequired` results, valid initial identities and checked cleanup receipts while retaining editable definitive SQL rejections. Separate 26-case actual page rendering passed before two added failures showed a scheduled post with projected dispatching/unknown state still offered Publish now. The private worker already holds these attempts; the detail page must also show consistent review controls.

## Completed local repairs and integration retest

The pending-editor and retired-callback repairs now pass all 23 scheduling browser cases. Typed ambiguous responses with and without a known post ID lock the draft for review; definitive rejection keeps an editable draft. The post detail page consistently holds dispatching/unknown states even when a different allowed error key is projected. All 37 actual page-render cases pass, including nine safe-link regressions and localized timezone/status rendering. The independent presentation report documents its test-only UTF-8 fixture correction separately.

The action now requires a valid initial post identity, classifies committed or unconfirmed creation as review-required, and accepts cleanup only when the exact known post has a confirmed deletion receipt. Unknown platforms and unsafe URLs are rejected before creation. Four additional red cases showed that the scheduling action accepted content the real X adapter would reject: 281 characters, combined body/newline/link exceeding the weighted limit, a title-only empty payload, and a link post without a link. The action now checks the same assembled payload and 280-character weighted limit before writes. All 75 independent action cases and the seven-file 313-case related run pass. These remain bounded fixtures with the private prepare/arm seam controlled; the separate scheduler suite executes that private path and installed SDK.

Root's combined Chromium run passes **83 cases in 16.3 seconds**: 39 QuickCapture/CaptureShell, nine voice capture, 23 scheduling Studio, and 12 existing social publish consumer cases. Root's ten-file focused run passes **320 tests**, including exact catalogue integrity, scheduling time, action, presentation and capture checks. Whole-project TypeScript passes. Broader pinned-source build and regression evidence will be recorded in the capture/scheduling checkpoint. Live provider delivery, deployed cron and PostgreSQL policy/concurrency remain unverified; this report does not pass the enclosing product workflows.
