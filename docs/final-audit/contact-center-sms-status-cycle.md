# Contact Center SMS reply status callback — 2026-09-12

## Provider contract reviewed before implementation

Twilio documents that inbound SMS/MMS and some historical accounts differ in whether a reply uses the TwiML `<Message>` `action` or `statusCallback` attribute. Set both to the same absolute URL with `method="POST"`. The callback reports a Message SID and status; the documented SMS lifecycle includes queued, sending, sent, delivered, undelivered and failed. A local TwiML response alone is not provider acceptance or handset delivery. [Twilio Message verb](https://www.twilio.com/docs/messaging/twiml/message).

The endpoint is `POST /api/contact-center/sms/status?receipt=<UUID>&token=<emission UUID>`. Request authentication covers the configured application origin, exact callback path and unmodified query string, plus every received form parameter in sorted order. Host/forwarding headers do not select the trusted origin. Extra provider form fields remain part of signature verification; ambiguous duplicate fields are rejected. [Twilio request security](https://www.twilio.com/docs/usage/security).

Twilio status callbacks use URL-encoded form POSTs and may omit some standard properties. `MessageSid`/`MessageStatus` and legacy `SmsSid`/`SmsStatus` aliases must agree when both appear; optional From, To and AccountSid must match stored emission identity. Callbacks can arrive out of order, and a successful handler should acknowledge them with HTTP 200. The private receipt helper must preserve monotonic status, treat replay as a no-op, and never infer delivery from an HTTP response generated locally. [Twilio status tracking](https://www.twilio.com/docs/messaging/guides/track-outbound-message-status).

## Implemented contract

Baseline publication: `5648447cff4352d6f3dde6a5460a0eb0983dbc5c`. Application source: `8417fba8959ecc3f37b0dc44bfefcfe726af4d5d`. Final source with two test-fixture corrections: `302c908bba8899f97c8b0ce51e0a285422fb6e8c`. No production file differs between the latter two commits.

The same private held reply receipt now accepts optional delivery observations. It never reopens an emission reservation. Old version-one rows without these fields remain valid; reading or replaying them does not rewrite their outputs. Only a new reservation captures a canonical account SID from signed inbound data or trusted current configuration. A supplied callback account must match that saved snapshot; old account identity is never inferred retrospectively. Optional sender/destination observations must match the original outbound envelope.

The first verified outbound provider SID is immutable and cannot equal the inbound SID. Every observation checks the complete private receipt and both exact inbox envelopes. Queued, sending and sent advance in order. The first terminal delivered, undelivered or failed observation remains terminal, including contradictory later events. Repeated or older callbacks do not churn the revision. Every mutation uses the prior complete output value and immutable identity in a conditional update. Required queries disable SDK retries, carry real abort signals and enforce bounded deadlines. Up to three checked status attempts can reconcile an uncertain committed update without granting emission authority.

The route always verifies the signature, including outside production. The configured origin is authoritative; forwarding headers cannot choose it. The original query encoding/order and every unique form field enter the signature. Duplicate, nonstring or ambiguous parameters and conflicting aliases are rejected. Malformed callbacks receive 400, invalid signatures or bound identities receive 403, and unavailable required storage receives 503. Successful observations return empty TwiML. The middleware allows only the exact new callback path. Both inbound TwiML callback attributes use the same escaped absolute URL and POST method; the origin is validated before consuming emission authority.

The family-authorized reader returns status strings only. The inbox distinguishes provider queueing, sending, carrier acceptance, provider-reported delivery and failure; it never equates carrier acceptance with delivery or delivery with reading. Receipt IDs, tokens, raw outputs and provider identifiers are not page props. All seven base catalogues retain 13,608 existing entries with identical values/order and add twelve entries each. No resend control, SQL, dependency or shared navigation change is introduced.

## Executed private checks

- 85 new installed-SDK delivery cases pass: identity and envelope tampering, competing provider SIDs, concurrent progression, terminal replay, transport/count/CAS failures, committed-but-lost responses, cancellation and preserved archive markers. The 70 original reservation cases remain passing.
- 53 actual signed route cases pass, including the installed provider adapter and required persistence failures. The safe reader passes 41 cases and actual React/page rendering passes 14. Updated signed ingress passes 55 cases, and the related urgent-durability group passes 22.
- Initial wider checking exposed a test-only inferred optional-field union and four failures caused by a preexisting noncanonical synthetic account identifier. The alias table now has an explicit string-map type and the urgent fixture uses a canonical synthetic account. No test assertion was removed or production validation weakened. The combined correction retest passes all 130 cases.
- Final source `302c908b` passes all 14,919 unit tests in 1,199 files in 43.56 seconds. Application `8417fba8` builds 252 pages; compilation takes 61 seconds. Strict types after the fixture corrections, lint with four baseline warnings, localization and query audits pass. Query coverage is 491 tables, 77 functions and 144 API routes.
- Independent review found no blocker in the ingress, private helper, status route, safe reader or hosted fixture. Source inventory verifies 21 changed files, four new files, six production files, eight test files and two new exported functions. Seven permanent audit IDs are added without removing prior IDs.

Private logs: `C:/Users/Daniel/AppData/Local/Temp/bubaly-sms-status-final-private-<units|build|types|lint|i18n|queries>-20260912.log`. The initial failed unit run is retained separately as `bubaly-sms-status-initial-private-units-20260912.log`. Fixture correction checks are in `bubaly-sms-status-fixture-retest-units-20260912.log`.

## Hosted acceptance and remaining work

The hosted case uses only owned disposable local records, synthetic signatures and a local Family+ entitlement. It logs into the real phone-width inbox and checks unknown → queued → carrier accepted → reported delivery, terminal replay, forged signatures/tokens/SIDs/accounts, a concurrent sent/failed result, private-data absence, browser errors and horizontal overflow. It creates no real provider messages and writes no SQL. Publication `0d68bdbb` passes all 1,064 hosted cases in [CI 34718960420](https://github.com/NewWorldVenture/Bubaly/actions/runs/34718960420), including this discovered case with both required flags enabled. The tested tree equals the publication; all web, database, mobile, finance and Vercel checks pass. Exact provenance and limits are recorded in [hosted acceptance](guardian-hosted-acceptance.md).

SMS-001 remains IN PROGRESS. Real provider delivery, production callback and scheduler configuration, controlled draining of old unfenced handlers and the absence of a database/HTTP transaction remain outside this acceptance. A reserved response lost before the provider receives it is still held without automatic resend. The subsequent text-bound repair is privately verified and awaits its own hosted acceptance, as recorded below. Full application regression and production sign-off remain pending.

## Next intake-boundary characterization

Five controlled actual-source and installed-SDK cases pass on `302c908b`, establishing failures rather than desired behavior. A saved family name of 1,550 ASCII characters produces a 1,632-character deterministic reply which is persisted and reserved. A valid emoji crossing the 320-unit model-reply slice becomes an invalid scalar and is rejected before intake. An ordinary 145-unit incoming text can also split an emoji at the 140-unit summary boundary; the actual SDK then submits a lone surrogate in JSON. A 4,097-unit ingress body demonstrates the same split at the route cap, without claiming the provider normally forwards that length. PostgreSQL documents that JSONB rejects malformed surrogate pairs; this database implication is source-backed, not a live database result. [PostgreSQL JSON input rules](https://www.postgresql.org/docs/current/datatype-json.html).

The fifth case confirms the factory's fifteen-second deadline prevents late writes but leaves no receipt; at that baseline, provider work receives no abort signal. Evidence is retained at `C:/Users/Daniel/AppData/Local/Temp/bubaly-sms-bounds-probe-20260912/{characterization.test.ts,results.log}`.

Subsequent application `a61804db` repairs new text bounds, preserves historical reply content, holds unsupported old queued replies and forwards cancellation. All 15,011 unit tests and the production build pass; the new full signed HTTP/PostgreSQL text path still requires hosted execution. See [the text repair cycle](contact-center-text-bounds-cycle.md). Durable capture before candidate completion remains open; these baseline characterizations are historical evidence rather than a claim that the repaired Unicode defects persist.
