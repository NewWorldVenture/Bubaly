# Phone OTP ownership

2026-09-19. Baseline published main d9542e256324e99d4e94f816266a35ed2de446b5. AUTH-001 and AUTH-002 remain IN PROGRESS. Existing COMPONENT-8E58E3039ED0 and ROLE-8E58E3039ED0 cover the phone surface. The component moves to IN PROGRESS after direct execution; ROLE remains NOT STARTED because role visibility/access is not fully assessed.

## Discovery recorded before repair

Actual PhoneAuth, real PhoneInput/OtpInput, browser storage/logout/password adapters and installed SDK execute with intercepted synthetic transport. Four desired failures and one normal control pass in 3.2 seconds in Temp/bubaly-phone-otp-red-20260919.log. All 21 loaded production files match baseline Git blobs; provenance is Temp/bubaly-phone-otp-evidence-20260919.json. Scratch fixture SHA256 is 9a18978cece98e650d02db4ec2dfa01a70b45dc8497ccecc77d9978bf1472526. No app server, real SMS, provider operation, SQL or dependency changes occurred.

Holding A's verification response across actual logout (confirmed empty session), actual newer password login B, component unmount, or number change lets the old installed SDK save A and navigate. Existing B is verified before releasing the latter cases. Every case changes session bytes; the normal control sends and verifies once, installs A and navigates once. The singleton refresh fence does not own this verifyOtp session write.

The new permanent fixture tests/e2e/phone-auth-boundaries.spec.ts is inventoried as SUPPORT-B98CAFA44FB9, initially NOT STARTED and now IN PROGRESS after actual execution. The new helper SERVICE-110F74726994 is IN PROGRESS after the same focused checks; implemented HTTP fixture SUPPORT-788038831C82 is IN PROGRESS with runtime acceptance pending. The permanent fixture observes settlement of the actual SDK promise and drains component work, so rejection/retirement does not need to navigate to complete the test. Original scratch RED evidence remains unchanged.

## Implemented repair and focused acceptance

The isolated verifySmsWithOwnedSession adapter captures a shared session-v1 reservation before verification and checks actual cookie writes/readback. SMS comparisons permit ordinary rotation of the same user/session while preserving exact logout-generation and pending-decision ownership. The component guards synchronous attempt, mount, number/code, phase and destination, plus send/resend retirement, duplicate submission and countdown cleanup. Provider verification remains authentication authority. Phone OTP does not fabricate PKCE proof. Default password/child/callback comparison contracts remain unchanged.

An intermediate review found another lifecycle boundary: after successful session adoption, the SDK disposal await could outlive a newer marker or logout whose session deletion was refused. Two desired actual-SDK cases failed by navigating after that newer decision in Temp/bubaly-phone-dispose-red-20260919.log. This belongs to the intermediate repair, not published d954. A final SMS-only ownership recheck after disposal now retires both results silently.

The final focused matrix passes **34/34** in 8.3 seconds in Temp/bubaly-phone-matrix-expanded-20260919.log, on helper blob a56288154a475d0900639417413f6c53fd444ce5 and component e94ab99ec4581ae7f255455c5e535910c2558561. It covers all original races at both provider-response and final-cookie-write boundaries, destination changes, ordinary success, provider expired/disabled-code rejection, empty/missing-refresh/identity-mismatched/expired sessions, duplicate automatic/manual handlers, refused session/reservation and ambiguous storage, newer recovery/marker decisions, same-session rotation, silent stale rejection, held send/resend retirement, countdown cleanup, thrown-send/retry and disposal. The scheduling seams delegate to the installed SDK; the deliberate thrown-send case injects transport failure before a subsequent real SDK retry. Scoped strict fixture types, lint and whitespace pass.

The final separate compatibility matrix passes **408/408** across fifteen files in 36.1 seconds on the same helper/component: Temp/bubaly-phone-auth-matrix-20260919.log. Its evidence JSON verifies 26 exact source/test hashes. It excludes the new phone matrix; earlier compatibility checkpoints are not summed again. Full UTC passes 16,694/16,694 across 1,305 files in 33.83 seconds; final DST also passes 16,694/16,694 across 1,305 files in 32.19 seconds. The phone-only 252-page production build, full lint (three existing warnings), localization and query audit pass; this checkpoint preceded the final combined verification below. Logs include Temp/bubaly-phone-production-build-20260919.log. These are a pre-signout-bridge checkpoint, not final combined-source acceptance. Private controlled tests do not prove SMS delivery or production provider configuration.

The actual-HTTP phone fixture and disposable-only CI GoTrue SMS test configuration are staged: workflow blob34a3fbf9293f0bb5da064ee6622b31e71dc8c1ca and fixture00b5040f8b75af81e6b91a6af15626bf3175fe8a. Three cases exercise wrong/correct code, reload/another-tab persistence and production logout, then held genuine verification after logout and after a newer password login. Discovery3/3 with authenticated/durable flags, scoped strict types/lint,66/66 workflow guards and exact config rewrite/dry-run all pass. Logs: Temp/bubaly-phone-http-{types,lint,discovery,workflow-guards,config}-20260919.log. Product Supabase config is unchanged; existing SMS configuration causes the disposable rewrite to refuse. Reserved numbers and reusable test OTP do not prove real delivery, randomness, expiry or one-time consumption. Runtime acceptance is hosted-pending and separate from intercepted component/SDK checks.

## Published baseline acceptance, separate from this repair

Exact d954 Vercel deployment dpl_5iYakaAKtZmWYNbFS1zvYAiroh4d (GitHub Production6545653641) passes public acceptance: five exact-deployment HTML renders; SSR disabled credential controls/native POST; hydrated login/signup/reset readiness; invalid nonce cookie-neutral307 with private headers; invalid-link UI and clean history; zero auth actions/cookies/page errors. Log Temp/bubaly-auth-initiation-production-acceptance-d9542e25-verified-20260919.log. Initial smoke assertion/cancellation-count corrections are retained in its historical logs. Exact d954 CI35468935790 is terminal FAILURE only in E2E: 1,251/1,252 cases pass in 9.1 minutes, zero skips/flakes. The sole failure is durable-session.spec.ts explicit local signout preserving another device; both attempts remain on /auth/signout/complete instead of /login. The six enabled callback HTTP/Mailpit cases therefore pass by exact-source discovery plus all other cases passing; the dot reporter does not print named success entries. Web passes both 16,694-test/1,305-file unit runs, build/types; Database and Mobile pass. Logs: Temp/bubaly-d954-e2e-105966247055.log and bubaly-d954-web-105966247014.log. This is partial hosted acceptance of the published baseline, not a phone/provider or whole-release PASS. The following local bridge repair addresses the reproduced mismatch; published d954 remains the failed baseline until new hosted acceptance.

## Hosted signout regression follow-up

The server-prepared session owner omitted the pending verifier/initiation pairs that the browser compared, so an otherwise current password login marker prevented completion. The repaired bridge captures those exact pairs; oversized encoded receipts choose explicit review within the existing 2,048-byte bound. Direct browser logout is unchanged. Two desired actual server-prepared receipt/installed-SDK completion cases first failed, with and without ordinary token renewal; the final browser suite passes17/17 and unit suite45/45. Scoped strict types/lint pass. Logs: Temp/bubaly-signout-bridge-red-20260919.log and bubaly-signout-bridge-{browser,units,types,lint}-final-20260919.log. Source blob2d9cc9f15e5cb7d0852387b2554ac9a56193be7b, browser fixturecb184e80393073f0e251ff29c2e9447a9cbb45d4 and unit fixturedd9dada5510cbd7973da90d07522c72b0ce15817 are frozen. This is an AUTH-002 continuation using existing files, not a new route/service. Final combined local gates pass as recorded below; new hosted durable-signout acceptance remains pending.

## Final combined source verification

Source/test/workflow tree `f75e7febdf01bf944fa35a505745001533c80028` freezes nine files: three application files, five tests and the CI workflow. TypeScript discovery adds one runtime helper and two test-file obligations, with no new routes or controls. All 14,035 prior IDs remain; only the exercised PhoneAuth component advances from NOT STARTED to IN PROGRESS. The three new obligations are IN PROGRESS. Total: 14,038 — 13,844 NOT STARTED, 190 IN PROGRESS, one FIXED + PASS and three FAIL. AUTH-001/002/003 remain open; ROLE-8E58E3039ED0 stays NOT STARTED.

The final combined controlled browser run passes **459/459** across seventeen files in 1.1 minutes: `Temp/bubaly-phone-final-matrix-20260919.log`. Its manifest verifies 31 staged source/test blobs and the executed spec list. It supersedes the overlapping 34/408/17 focused checkpoints; those are not added again. Actual HTTP, authenticated and durable-session hosted fixtures are excluded from this no-server local run.

Both complete unit runs pass **16,703/16,703** across 1,305 files, zero failures/skips: UTC 34.55 seconds and DST 40.30 seconds, in `Temp/bubaly-phone-bridge-full-unit-{UTC,DST}-20260919.log`. Final full lint passes with the same three existing warnings (document-capture generation ref and two messages-module toastError dependencies); localization passes all eight surfaces and query audit resolves 491 tables, 86 functions and 146 routes. Logs: `Temp/bubaly-phone-bridge-{full-lint,i18n,query}-20260919.log`. Final combined production build passes 252 pages in Temp/bubaly-phone-bridge-production-build-20260919.log. Synthetic optional marketing-provider timeout diagnostics are expected fallback evidence, not live-provider acceptance or a claim that no errors were logged. Final full strict types pass; no final full-gate claim yet. New hosted phone and repaired durable-signout acceptance remain pending.

Final release discovery lists **1,296 tests across 54 files** with authenticated and durable-session flags enabled: `Temp/bubaly-phone-bridge-release-discovery-20260919.log`. This includes the three new real-HTTP phone cases and repaired signout fixtures; discovery is not hosted runtime acceptance.
