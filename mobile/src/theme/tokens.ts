// The mobile app's design tokens ARE the web app's design tokens: one JSON file
// at the repo root (design/tokens.json), typed once (design/tokens.ts), consumed
// by app/globals.css + tailwind on the web and by src/theme/theme.tsx here.
export * from '../../../design/tokens';
